# 评审模型身份更正（2026-09-07）

核验 modelUsage 发现先前 Claude Code CLI 报告实际使用 deepseek-v4-pro，并非 Claude。已向用户披露、重开 W1 #446–#449 并更正 #445/#451/#452/#453。显式 Claude 探针返回 429，未产生有效模型审查。

用户随后明确回复：**改为 Grok + DeepSeek 独立复审，接受已核验的模型组合**。因此后续及用户接受的现有评审按 Grok 4.6 + DeepSeek V4 Pro 计入门禁。W1 可据已有两路最终通过、机器和 PR #458 合并证据重新关闭；#451 据 R3 两路通过继续提交/CI闭环。历史文件名保留以便追溯，但不能继续当作 Claude 模型身份的依据。

以后审计同时记录实际模型 ID、CLI 版本、冻结代码摘要与判定。真实 API 错误不是审批拒绝；不要求用户分享密钥。未更改用户现用模型配置。
