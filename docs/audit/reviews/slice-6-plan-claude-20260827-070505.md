Review complete. Here is my assessment of slice 6 plan r2.

## Verification summary

**Patch integrity** — The batch diff (base `bf03318` → HEAD `e5d86a5`) is docs-only (plan + T1 preflight + README/status/memory) and matches current `git status`; not stale. Plan r2 is committed at `e5d86a5`.

**All four r1 REJECT lanes are actually folded, verified against live code:**

1. **Honesty door (Product/Security REJECT)** — Plan pins `/技能` + 按需 → `manual`, forbids overlay `skill.activate` writing `skill_selection_mode`. Live: auto union exists at skill-engine.ts:594-598; `skill.activate` today writes only `active_skill_ids` (message-router.ts:2423-2433); 按需 writes mode via `thread.update` (ContextPanelHost.tsx:406). Pin matches reality.
2. **Seed field (Product/Impl REJECT)** — `thread.open_todos` exists nowhere; `runtime_context_budget.handoff` is real (runtime-context-budget.ts:23, sanitized on read at thread-manager.ts:747-750), `open_todos` capped 8×120 (context-handoff.ts:17). Plan Task 4 seeds the correct path.
3. **IDF test validity (Impl REJECT)** — I checked the math: with `idf = log((n+1)/(c+1))`, all-doc term → 0 < unseen default 1; rare term ≈ 0.92 < 1. Pre-impl the test throws (undefined `idfFromDocs`) — genuinely red. Post-impl, `cosineSimilarity`'s zero-magnitude guard (semantic-match.ts:94) makes `1 > 0` hold — genuinely green. `tokensToVec` assertion matches current normalized-TF behavior exactly (semantic-match.ts:64-76).
4. **Overlay ACL (Security REJECT)** — Toggle verb absent from `SUMMONER_ALLOW` (summoner-acl.ts:14-45) and `SUMMONER_WEB_DISPATCH_ALLOW` (summoner-web.ts:36-59); overlay `thread.update` stays alias-only (summoner-acl.ts:87-101); pin 4 forbids adding it anywhere, with a required denial test.
5. **Wiring point** — The sole success-path `tool.result` send is adapter.ts:1296 (line 863 is abort-healing; 1179/1214 are parse/validation error paths); both extension and companion tools converge there. Plan's "after tool.result ok" is implementable unambiguously.
6. **UI copy / docs (External AWN)** — 「本轮步骤」 pinned, EmptyState targets real (ChatView.tsx:508/1682, `empty-state-copy`, `tokens.emptyTitle`); GOAL.md:251 G17, architecture.md:131 TF-IDF claim, CLAUDE A5 all exist as doc targets; T1-done claim and outbound expansion are BLOCKed.

**ADR-020 checklist** — Capability declaration present (plan lines 15-22). Axes fit: L0 Surface + existing Compose, no new runtime, no bare “中层 Agent”. Trust: no new confirm dialect; overlay gets strictly less (denied verb) — monotonicity holds. Pack-first: not a new scenario surface; mounts inside existing ChatView, not a new bottom-bar tab (respects NEVER list). No new `securityConfirmations.request`, so originWs is n/a. Diff is docs-only; no security regression possible at this stage.

## Nits (non-blocking)

1. **`/技能` signal is not `rest.skill_ids` presence.** The extension always seeds `skill_ids` from `state.activeSkillIds` (App.tsx:1246 → background/index.ts:549), so companion-side detection of "this turn used /技能" must key off the leading `/name` token in `rest.message` (or an explicit flag) — otherwise every auto thread with any active skill would silently flip to manual. Task 3's test (leading `/browse`) pins the correct behavior, so this is guidance, not a hole.
2. **Three merge sites, one named.** `rest.skill_ids` merges at chat.create (message-router.ts:565), chat.steer (1127), and chat.regenerate (1516). The mode-flip covers all three via shared `resolveSkillIdsForThread`, but a cheap manual-mode assertion on steer/regenerate would lock it against future drift.
3. **Naming/precision in Task 4.** Adapter's success field is `toolResult.success` (plan writes `ok===true`); worth naming adapter.ts:1296 as the wiring point so the tick doesn't land on the error-path sends (1179/1214) or the abort-healing send (863).

All r2 pins hold and map to verified code; no blocking issues.

VERDICT: APPROVE_WITH_NITS
