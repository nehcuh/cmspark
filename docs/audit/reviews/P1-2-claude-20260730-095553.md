Reviewed the diff, real code, and tests; verified the binding genuinely gates behavior.

**Findings**

Correctness — all three call sites correctly bound:
- `companion/src/server.ts:1828-1830` — navigate URL L2
- `companion/src/server.ts:3933-3934` — MCP tool confirm
- `companion/src/server.ts:4115-4116` — MCP meta confirm
- Each passes `{ originWs: ws }` as the third arg, where `ws` is the requesting socket already in scope.

Manager enforcement intact:
- `security-confirmation.ts:411-416` returns `origin_mismatch` and leaves the entry pending — origin socket can still approve.
- `respond()` privileged path (`security-confirmation.ts:474-506`, used by tray at `server.ts:1483`) untouched.
- evaluate/shell conditional ternary at `server.ts:1445` untouched, as claimed.
- `rejectAll(ws)` filter at `security-confirmation.ts:532-533` still skips entries owned by other sockets — multi-peer disconnect isolation preserved.

Tests are real — verified by mutation:
- Reverted the navigate `{ originWs: ws }` bind → both P1-2 navigate integration tests fail (`actual: 'unknown', expected: 'resolved'`), proving they genuinely depend on the call-site fix. Restored afterward; `git diff --stat` shows 7 insertions in server.ts, matching the patch.
- 101/101 pass on the restored tree.
- `mcp-capability-gate.test.ts` new case uses `respondFrom(rogueWs)` → `origin_mismatch`, then `respondFrom(serverSideWs)` → `resolved`. Without MCP tool binding, the rogue would resolve.
- Unit cases in `security-confirmation-origin.test.ts` cover manager-level enforcement for `navigate` and `mcp__fs__write_file` / `mcp_read_resource` toolNames, plus privileged `respond()` bypass intact.

ADR-020 checklist:
- Capability declaration present (Surface L1 navigate + Composition mcp-server; Autonomy single; Channel community; Trust = originWs multi-peer bind).
- No new runtime, no new confirm dialect, no new primary UI. Pack-first / no-middle-agent: clean.
- Trust monotonicity: bind only tightens. `skipUrlConfirmation` (god-mode / auto_approve) at `server.ts:1799-1801` is pre-existing and unchanged.

**Nits (non-blocking)**

1. No MCP-meta multi-peer integration test — only unit-level manager coverage with `mcp_read_resource`. The call-site shape is identical to MCP tool and routes through the same manager code, so risk is low, but a direct integration assertion would close the gap.
2. Test-harness wire path at `security-gates.test.ts:84` still routes `security.confirmation.response` from the test client through privileged `respond()` (harness limitation). The new `handleSecurityConfirmationResponse(rogueWs, …)` case exercises the production `respondFrom` path directly, which is what matters — but future harness work could make the wire path origin-aware too.
3. `analyze_image_fetch` (`server.ts:1940`) still broadcasts without `originWs` — explicitly out-of-scope per the design notes; flagging for a future batch.
4. Original spec line anchors (~4101-4110 etc.) were slightly stale; implementer correctly anchored to live code. No issue, just noting the inventory copy could be refreshed.

VERDICT: APPROVE_WITH_NITS
