# P1 Security Batch Fix Report — P1-4

- both_external_approve: true
- build_ok: true
- rounds: 1
- design: {"acceptance":["policy=allowlist rejects prefix+metachar bypasses (;|&`$()<> newlines) with clear error","benign allowlisted single-line command still runs under shell:true","confirm_per_command + L2 forceConfirm semantics unchanged","god-mode / auto_approve_dangerous alone cannot silent-skip shell L2","no netsec / argv API / schema break","companion unit tests green for capability-shell-netsec"],"batch":"P1-4 shell_exec structure tighten (P1a allowlist metachar; enterprise L2)","capability_declaration":"Surface: L2 | L2-classes: shell | Composition: capability modules (shell) | Autonomy: single | Trust: L2 forceConfirm (confirm_per_command default; allowlist+metachar structure; god-mode must not silent-skip shell L2) | Channel: enterprise","files":["companion/src/capability/shell.ts","companion/tests/capability-shell-netsec.test.ts"],"plan":"P1a only: in companion/src/capability/shell.ts, when modules.shell.policy==='allowlist', reject commands matching shell metacharacters [;|&`$()<>\\n\\r] BEFORE existing prefix match (command===prefix || startsWith(prefix+' ')). Bugs: spawn(command,{shell:true}) at shell.ts:87-91; prefix-only allowlist at shell.ts:30-37 enables 'echo ok; rm …' under shell:true. Gate via exported hasShellAllowlistMetachar + commandAllowedByPolicy so checkShellScope (server.ts ~1105 SHELL_SCOPE_DENIED) and shellExec (~67-68) both enforce. Leave confirm_per_command and shell:true unchanged. Do not alter L2 forceConfirm / god-mode path (server.ts ~1064-1075 capabilityForceConfirm includes shell_exec; ~1133 still requires confirm unless enterpriseSkip). Do not change security_token execute gate (server.ts ~3007-3013). P1b (shell:false + file/args) note only—out of scope. No netsec redesign.","risks":"False rejects for legit pipe/redirect allowlist workflows—operators must use confirm_per_command until P1b argv. Residual $VAR expansion under shell:true remains until P1b. Windows shell metachar differences mitigated by conservative reject set. Avoid scope creep into enterpriseSkip or forceConfirm.","tests":["allowlist policy + allowlist_commands=['echo']: 'echo hi; rm -rf /tmp/p14' rejected (metachar/scope deny, no side effect)","same allowlist: 'echo hello-allowlist' succeeds with expected stdout","allowlist rejects &&, |, $(), backticks, redirects, newlines under prefix","allowlist still denies non-prefix e.g. 'uname -a' with not-in-allowlist error","confirm_per_command still allows 'echo a; echo b' at policy/exec unit layer (metachar ban is allowlist-only)","L2/god-mode: no server change; shell_exec still requires security_token on execute path (regression lock / document)"]}
- last_impl: {"batch":"P1-4 shell_exec structure tighten (P1a allowlist metachar; enterprise L2)","diff_stat":"companion/src/capability/shell.ts | 20 ++++++\n companion/tests/capability-shell-netsec.test.ts | 79 +++++++++++++++++++++++++\n 2 files changed, 99 insertions(+)","done":true,"files_changed":["companion/src/capability/shell.ts","companion/tests/capability-shell-netsec.test.ts"],"notes":"P1a only. No server.ts / forceConfirm / god-mode / security_token / netsec changes. P1b (shell:false + file/args) residual $VAR under shell:true remains. Operators needing pipes/redirects must use confirm_per_command until P1b.","summary":"When modules.shell.policy=allowlist, reject shell metacharacters (;|&`$()<> newlines) before prefix allowlist match so spawn(...,{shell:true}) cannot run chained bypasses. Shared via commandAllowedByPolicy → checkShellScope + shellExec. confirm_per_command and L2 forceConfirm paths untouched.","tests_added":["P1a allowlist: benign allowlisted command succeeds","P1a allowlist: prefix + '; rm' rejected (metachar, no side effect)","P1a allowlist: rejects && | $() backticks redirects newlines under prefix","P1a allowlist: non-prefix still denied with not-in-allowlist","P1a confirm_per_command: metachar ban is allowlist-only (policy layer)"],"tests_run":"cd companion && npx tsc -p tsconfig.test.json && node --test .test-dist/tests/capability-shell-netsec.test.js → 9 pass / 0 fail"}
- last_adv: {"blocking":[],"pass":true,"summary":"P1a allowlist metachar ban is correctly placed before prefix match in commandAllowedByPolicy, shared by checkShellScope and shellExec; classic prefix+';/&&/|/$\n chaining is closed. L2 forceConfirm + G1 god-mode non-skip and token binding are unchanged and still correct by inspection. No blocking issues for stated P1a scope. | P1-4 P1a passes adversarial test review: allowlist metachar ban runs before prefix match in commandAllowedByPolicy (shared by checkShellScope + shellExec); real shellExec side-effect test rejects echo hi; rm/touch and asserts /metacharacter/; benign allowlist still works; confirm_per_command policy layer still allows metachar. No mocks/false-green on the fixed bypass. L2/god-mode unchanged by inspection, not by new test. | "}
- last_ext: {"both_approve":true,"claude_path":"/Users/huchen/Projects/cmspark/docs/audit/reviews/P1-4-claude-20260730-140202.md","claude_verdict":"APPROVE_WITH_NITS","exit_code":0,"pi_path":"/Users/huchen/Projects/cmspark/docs/audit/reviews/P1-4-pi-20260730-140202.md","pi_verdict":"APPROVE","raw_tail":"=== CLAUDE (last 40) ===\nDiff is current (matches working tree; only `companion/src/capability/shell.ts` + test file changed; no `server.ts` / forceConfirm / netsec / token-gate touched). Tests build clean and all 9 pass; `message-router-config-security` still green (12/12).\n\n## Findings\n\n### Verified correct\n- **Funnel placement** (`shell.ts:42-50`): metachar gate lives inside `commandAllowedByPolicy`, the single chokepoint used by both `checkShellScope` (→ `server.ts:1105` `SHELL_SCOPE_DENIED`, runs *before* `enterpriseSkip` is even computed) and `shellExec` (`shell.ts:87`, execute path). No third route exists (`rg shellExec\\(` confirms).\n- **Ordering**: metachar check runs BEFORE the prefix match (`shell.ts:55`), so the canonical bypass `echo ok; rm …` is closed at both scope and execute layers.\n- **Trust monotonicity intact**: `capabilityForceConfirm` still includes `shell_exec` (`server.ts:1064-1075`); G1 gate `(!skipConfirmation || forceConfirm) && !enterpriseSkip` unchanged (`server.ts:1134`); L2 `security_token` + command-bound payload still required on the execute path (`server.ts:3007-3013`, `security-policy.ts:141` constant-time + codeHash binding). God-mode / `auto_approve_dangerous` alone still hits SHELL_SCOPE_DENIED before any skip logic — verified by inspection.\n- **Test \"prefix + '; rm' rejected (metachar, no side effect)\" is a genuine regression lock**: with the gate removed, `command.startsWith(\"echo \")` would pass and `/bin/sh -c` would `touch ${marker}`, failing `assert.equal(fs.existsSync(marker), false)`. Not a mock.\n- **Scope discipline**: P1a only. `confirm_per_command` policy layer explicitly exempted (5th test asserts `echo a; echo b` is `ok:true` there, metachar ban is allowlist-only) — matches inventory §P1-4 P1a/P1b split.\n- **ADR-020 declaration** present and accurate: Surface L2 / L2-class shell / Composition capability modules / Autonomy single / Trust L2 forceConfirm / Channel enterprise. No Pack-first / originWs / new-runtime concerns (no new tool, gate, or UI).\n\n### Nits (non-blocking)\n1. `companion/src/capability/shell.ts:31` — regex `[;|&\\`$()<>\\n\\r]` does not cover brace expansion `{a,b}`, glob `*`, tilde `~`, or `$VAR` (no parens). Acknowledged as P1b residual in code comment and risks field; consider a one-line test asserting `echo $HOME` still passes under allowlist to document the residual boundary explicitly.\n2. `companion/src/capability/shell.ts:34` — `hasShellAllowlistMetachar` exported essentially for tests; consider `// @internal` JSDoc to signal non-public surface.\n3. Tab/`\\v`/`\\f` not in regex — benign under `/bin/sh -c`, just noting.\n4. \"god-mode alone cannot silent-skip shell L2\" is verified by inspection of `server.ts` only; the implementer/adversarial notes acknowledge this. A small server-level integration test (`auto_approve_dangerous=true` + `auto_approve_enterprise_tools=false` + `echo a; rm b` → `SHELL_SCOPE_DENIED`) would harden the regression lock — out of P1a scope but worth tracking.\n5. `companion/tests/capability-shell-netsec.test.ts:91` — third test exercises policy + scope layers only (not `shellExec` side-effect); the second test already covers execution, so this is fine, just noting the layering.\n\nThe fix fully addresses inventory P1-4 P1a, tests are real (not rubber-stamped), no new holes, Trust monotonicity holds.\n\nVERDICT: APPROVE_WITH_NITS\n\n=== PI (last 40) ===\n|-------|--------|----------|\n| **Axes fit** | ✅ PASS | Surface L2, L2-classes: shell. Declared in implementer design. No \"中层 Agent\" language. |\n| **Pack-first** | ✅ N/A | No new side panel, UI entry, or chrome. Pure backend capability module change. |\n| **Confirm dialects** | ✅ PASS | No new confirmation family. Metachar gate is a policy-level check within `commandAllowedByPolicy`, not a new confirm UI. |\n| **Trust monotonicity** | ✅ PASS | L2 is made **more** restrictive (metachar ban under allowlist), not less. server.ts ~1064-1070 `capabilityForceConfirm` still includes `shell_exec`. ~1133 `forceConfirm` overrides skip even with `enterpriseSkip`. ~3007-3013 `security_token` gate unchanged. God-mode cannot silent-skip shell L2. |\n| **originWs** | ✅ N/A | No new `securityConfirmations.request` calls. |\n| **No new runtime** | ✅ PASS | No second agent framework. |\n| **Experimental layers** | ✅ N/A | |\n| **P1-4 watchlist** | ✅ PASS | This IS the P1-4 P1a fix. Scope boundary respected (P1b out of scope). |\n\n### Scope boundary verification\n\n- **In scope (P1a)**: Metachar rejection BEFORE prefix match in `commandAllowedByPolicy`, shared by both `checkShellScope` (server.ts ~1105 SHELL_SCOPE_DENIED) and `shellExec` (~67-68). `hasShellAllowlistMetachar` exported for testability.\n- **Out of scope (documented)**: `shell:false` + file/args (P1b), `$VAR` expansion under allowlist (residual until P1b), netsec redesign, enterpriseSkip/god-mode changes.\n- **No scope creep**: git diff confirms 0 changes to `server.ts`. Only `shell.ts` (+20) and test file (+79).\n\n### Code review\n\n- **Ordering correct**: Metachar check (lines 34-41) runs BEFORE prefix allowlist match (lines 42-50). Classic `echo ok; rm -rf /` bypass closed.\n- **Regex coverage**: `/[;|&\\`$()<>\\n\\r]/` covers `;` (separator), `|` (pipe), `&` (background/AND), `` ` `` (backtick substitution), `$` (`$VAR` + `$()`), `()` (subshell), `<>` (redirects), `\\n\\r` (newline injection). Confirmed working via runtime test above.\n- **`$` catches `$VAR`**: This is a known, documented scope limitation. Under allowlist, `echo $HOME` is rejected. Conservative/safe. P1b will address.\n- **`checkShellScope` → `commandAllowedByPolicy`**: Both server.ts SHELL_SCOPE_DENIED path and `shellExec` execute path get metachar protection. Dual-gate verified in tests.\n\n### Test review (9 pass / 0 fail)\n\n| Test | Type | Quality |\n|------|------|---------|\n| Benign allowlisted command succeeds | Real `shellExec` spawn | ✅ Real side effect, no mock |\n| `echo hi; rm -rf ...; touch ...` | Real `shellExec` spawn + fs check | ✅ Side-effect test — asserts marker file NOT created |\n| All metachar variants (&&, \\|, $(), backticks, redirects, newlines) | `commandAllowedByPolicy` + `checkShellScope` | ✅ Both gate paths verified |\n| Non-prefix still denied | `commandAllowedByPolicy` | ✅ Old allowlist logic intact |\n| confirm_per_command allows metachar | `commandAllowedByPolicy` + `hasShellAllowlistMetachar` | ✅ Regression lock — metachar ban is allowlist-only |\n\nNo mocks, no false-greens. The side-effect test (`echo hi; rm -rf ${marker}; touch ${marker}`) actually verifies the exploit payload does not execute.\n\n### Conclusion\n\nThe fix correctly addresses P1-4 P1a: the allowlist prefix bypass via shell metacharacters is closed. All server.ts L2 forceConfirm, god-mode, security_token, and enterpriseSkip paths are confirmed unchanged by inspection. Tests have real side-effect coverage. Trust monotonicity holds. No scope creep. No blocking issues.\n\nVERDICT: APPROVE","verdict_json":"/Users/huchen/Projects/cmspark/docs/audit/reviews/P1-4-verdict-20260730-140202.json"}
- build: {"commands":["git diff --stat; git status; git diff --name-only","cd companion && npx tsc --noEmit -p tsconfig.json","cd companion && node -e \"require('fs').rmSync('.test-dist',{recursive:true,force:true})\" && npx tsc -p tsconfig.test.json && node --test .test-dist/tests/capability-shell-netsec.test.js"],"log":"Branch fix/diagnosis-P1-4. Unstaged: companion/src/capability/shell.ts (+20), companion/tests/capability-shell-netsec.test.ts (+79). Extension not touched — chrome-extension tests skipped. companion tsc --noEmit exit 0. Targeted capability-shell-netsec: 9 pass, 0 fail (shell_exec disabled/enabled, P1a allowlist metachar bans, confirm_per_command policy, netsec allowlist/localhost). Full npm test started but killed on timeout; custom node --test runner ignores --testPathPattern. No commit.","ok":true}
