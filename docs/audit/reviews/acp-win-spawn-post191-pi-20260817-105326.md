I've now read the consensus in full, the prior R1–R14 baseline, the live source at file:line, both test files, and the actual diff. Re-ran the machine gate myself: `tsc` exit 0; `node --test` on the three suites → 83 pass / 0 fail / 3 skip (win32 live fixtures), matching the parent's claim.

## 1. Lock table

| ID | Lock | Agree? | Evidence |
|----|------|--------|----------|
| **F1** | `launchStart` uses wrapViaCmd contract: extra `"` on start line + verbatim on cmd host only | **AGREE** | `planWindowsStartSpawn` `open-local-terminal.ts:265-275` → `args: ["/d","/s","/c", "\"${line}\""]`, `windowsVerbatimArguments: true`; `launchStart :575-583` forwards verbatim; `launchWt :585-605` passes none; `spawnDetachedWin :485-488` forwards verbatim only when true. Test: `planWindowsStartSpawn (F1)` suite asserts `/d /s /c`, wrap quotes, `start "CMspark"`, `doesNotMatch /\\"/`, verbatim=true; F2 suite asserts `wtCall.options?.windowsVerbatimArguments !== true` |
| **F2** | Real PE wt exit 0 / still-running = handoff success; only spawn error / non-zero → start; no 80ms-without-error | **AGREE** | `spawnDetachedWin :497-503` (cliHandoff: code 0/null → resolve, non-zero → reject) + `:522-529` (300ms window: exitCode non-zero → reject else resolve); `launchWt :600` `cliHandoff: true`; `wantWt :622-635` catch → `launchStart`; `WT_OBSERVE_MS=300` (`:440`) — no 80ms path. Tests: exit0-fast→WT no start, error→start, exit1→start, hang→WT, never-bare-wt/alias |
| **F5** | Pin `System32\cmd.exe` + `System32\WindowsPowerShell\v1.0\powershell.exe`; no ComSpec / bare names; missing → ENOENT | **AGREE** | `windowsCmdExePath` `win-spawn.ts:337-340`, `windowsPowerShellExePath :342-350`; `wrapViaCmd :265-266` uses pin (no ComSpec anywhere in acp/); `buildWindowsStartCommandLine open-local-terminal.ts:253` + `launchWt :598` use pin; no `existsSync` fallback. Tests in both files pin System32 + reject bare names |
| **F7** | Success label uses actual `r.app`, not pref; `pref=wt` + Console fallback must not say `wt · 交互` | **AGREE** | `manager.ts:196` `const shown = r.app || …`; every win32/darwin `ok:true` path sets `app` (`open-local-terminal.ts:1078,1097,1117,1219,1265`); failure path keeps pref (allowed). Source-lock test in test file |
| **R1–R14** | No regression of prior locks | **AGREE** | `spawnAcpChild` at both stdio sites (`manager.ts:615`, `protocol-session.ts:50`); no new `shell:true` (only pre-existing non-ACP modules/comments); `isRealWindowsTerminalExe` still rejects bare/alias (`open-local-terminal.ts:387-400`, candidates :405-411); cmd-host Mode C still L0 (`modeCWindowsLevelForSpec` + `if (!l1Allowed)`); `wrapViaCmd` still strips prompt via `argvForCmdWrap`; R6 wx+unlink, R8 taskkill, R9/R10/R11/R12 untouched with green tests |

## 2. New blockers the adversary missed

None found. I specifically probed: (a) `launchWt` guard doesn't re-stat the injected path — production always passes through `findWindowsTerminalExe` (stat+size>0), and the guard still blocks bare/alias; (b) verbatim-off on wt argv with a spaced `-d` cwd — Node's own argv quoting handles it, matching the consensus's explicit "never verbatim on wt" instruction; (c) start timeout path (`START_CONFIRM_MS` 2500ms) rejects honestly instead of stamping; (d) no new exec surface — payload is still our generated PS1 via `-File`, prompt only via `Get-Content` file read.

## 3. Nits (non-blocking)

1. **Linux success path has no `app`** (`open-local-terminal.ts` linux L1/L0 returns omit `app`) — `r.app` falls back to pref, so a nonsense pref like `wt` on Linux would still label `（wt · 交互）`. Pre-existing, outside the Windows F7 scope (and the deferred dropdown filter is exactly the fix), but the F7 principle isn't uniformly applied across platforms.
2. **Two SystemRoot helpers**: `windowsTaskkillPath` (`win-spawn.ts:333-335`) still inlines `env.SystemRoot || env.SYSTEMROOT || "C:\\Windows"` while the new `windowsSystemRoot` adds `windir`. Cosmetic consolidation opportunity.
3. **`spawnDetachedWin` settle timer not unref'd** — harmless in a long-running server (early-settled promises leave a ≤2.5s timer alive), but trivially avoidable.
4. **F7 test is a regex source-lock** (`assert.doesNotMatch(src, /已打开本机终端（\$\{terminalApp/)`) — brittle to formatting; acceptable given no manager harness, per consensus, just fragile.

Trajectory: diff is confined to `win-spawn.ts` / `open-local-terminal.ts` / `manager.ts` + the two test files; no drive-by. Consensus was neither too soft nor too harsh — F1/F2/F5/F7 are genuinely landed, tested, and the deferred items (F3/F4/F6/dropdown) were correctly not reopened.

VERDICT: APPROVE_WITH_NITS
