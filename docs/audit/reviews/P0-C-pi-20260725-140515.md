Here is my independent review of the P0-C batch.

---

## Review Report: P0-C

### 1. Patch vs. Live State
The patch file at `docs/audit/reviews/P0-C-diff-20260725-140515.patch` matches the current working tree exactly (5 files, +148/−11). No staleness.

### 2. COMP-2: reL2 Session-Trust Carve-out (`executor.ts:616–624`)

**Correctness**: ✅

- The `FORCE_INTERACTIVE_DANGEROUS` Set contains exactly `computer.danger_detected` and `computer.experimental_suggestion` — exactly the two gates the design requires.
- The guard `!forceInteractive` gates the entire session-trust auto-approve block at line 624. If ANY dangerous item in the array is force-interactive, the entire re-L2 surfaces.
- All other reL2 call sites verified:
  - `computer.budget_exhausted` (line 752) → not in set → still auto-approves ✅
  - `computer.uncrossverified_exceeded` (lines 854, 984) → not in set → still auto-approves ✅
  - `computer.foreground_yielded` / `computer.task_induced_dialog` (line 1381) → not in set → still auto-approves ✅
  - `computer.danger_detected` (line 924) → in set → blocked ✅
  - `computer.experimental_suggestion` (line 958) → in set → blocked ✅
- Comments in both `executor.ts` (lines 108–110, 616–623) and `session-trust.ts` (lines 24–27) clearly document the carve-out, including which callers are unaffected.

### 3. COMP-1: Darwin cuInject Client→Screen (`host.swift:558–681`, `:979–1006`)

**Correctness**: ✅

- `cuClientOriginScreen()` mirrors the screenshot's AX↔CGWindow frame matcher exactly (same CGWindowList + AX-by-Manhattan-distance logic at `host.swift:760–820`). The math is the inverse: screenshot computes `client = AX.pos - CGWindow.frame.origin`; inject computes `screen = clientOrigin + (x, y)` where `clientOrigin = AX.pos`. Consistent coordinate space.
- Fail-closed: on unresolved windowId, returns `nil` → `cuInject` returns `HWND_DEAD` error. No silent wrong-click.
- JSON response now includes `screenX`/`screenY` alongside client `x`/`y` — good for diagnostics.
- `host-skylight.swift` is lockstep: identical `cuClientOriginScreen()` (line 633) and identical inject logic (line 1052), with its own SkyLight-specific posting path. No A/B regression risk.
- Scroll (line 1049) exempted correctly — `CGEvent(scrollWheelEvent2Source:…)` takes no cursor position; the `x`/`y` guards are for parameter validation only.

### 4. Tests

**All 96 tests pass.** The two new tests are authentic:

- **danger_detected test (line 1266)**: Uses real OCR word `"确认删除"` (in `CAUTION_WORDS`) → `regionLevel: "caution"` → `reL2(computer.danger_detected)`. `trust.grant("sess-1", "win.app.test")` is set. Asserts: `confirm.captured.length === 1` (dialog surfaced), `dangerousApis === ["computer.danger_detected"]`, `errorCode === "DANGER_DENIED_BY_USER"`, `injector.clicks.length === 0`. Would **fail** without the carve-out (confirm would capture 0, auto-approve would inject). ✅
- **G4 test (line 1293)**: `FakeLocator([])` → L1 miss → `fakeTinyClickLocator()` hit → `reL2(computer.experimental_suggestion)`. Trust set. Deny → `errorCode === "ELEMENT_NOT_FOUND"`, zero inject. Would also fail without carve-out. ✅
- **Existing UX-spike test (line 1209)**: FOREGROUND-YIELD still auto-approves under trust with 0 confirm captures. Still green. ✅

### 5. Out-of-Scope Items (confirmed unchanged)

- Host.swift right-click still uses `.leftMouseDown`/`.leftMouseUp` event types (pre-existing; host-skylight.swift correctly distinguishes right/left). **Not a regression.**
- Scroll still posts without client→screen (wheel events, no cursor position semantics). **Correctly documented.**
- No occlusion/Stop/packaging/server grant changes. **Confirmed.**
- `build-host.sh` binary not in tree — source changes are what count for review.

---

### Nits (non-blocking)

1. **`executor.ts:619–622`**: `FORCE_INTERACTIVE_DANGEROUS` Set is re-allocated on every `reL2` call inside the `runComputerTask` closure. Could be a module-level `const` to avoid per-call allocation. Trivial perf impact — each task calls `reL2` at most a handful of times.

2. **`host.swift:991`**: Right-click still posts `.leftMouseDown` / `.leftMouseUp` event types regardless of `btn`. This is a pre-existing issue (not introduced by this patch) but worth noting that `host-skylight.swift:1073–1074` has the correct `isRight ? .rightMouseDown : .leftMouseDown` distinction. The design notes "scroll/drag Darwin gaps remain" — this right-click gap is similar and should be tracked for a follow-up.

---

VERDICT: APPROVE_WITH_NITS
