No server.ts changes — confirmed clean. Regex behavior validated via Node.js runtime: all metachar cases correctly rejected, clean commands correctly accepted.

---

## Dual Review — P1-4 Batch P1a (shell_exec allowlist metachar structure tighten)

### ADR-020 Capability Checklist

| Check | Result | Evidence |
|-------|--------|----------|
| **Axes fit** | ✅ PASS | Surface L2, L2-classes: shell. Declared in implementer design. No "中层 Agent" language. |
| **Pack-first** | ✅ N/A | No new side panel, UI entry, or chrome. Pure backend capability module change. |
| **Confirm dialects** | ✅ PASS | No new confirmation family. Metachar gate is a policy-level check within `commandAllowedByPolicy`, not a new confirm UI. |
| **Trust monotonicity** | ✅ PASS | L2 is made **more** restrictive (metachar ban under allowlist), not less. server.ts ~1064-1070 `capabilityForceConfirm` still includes `shell_exec`. ~1133 `forceConfirm` overrides skip even with `enterpriseSkip`. ~3007-3013 `security_token` gate unchanged. God-mode cannot silent-skip shell L2. |
| **originWs** | ✅ N/A | No new `securityConfirmations.request` calls. |
| **No new runtime** | ✅ PASS | No second agent framework. |
| **Experimental layers** | ✅ N/A | |
| **P1-4 watchlist** | ✅ PASS | This IS the P1-4 P1a fix. Scope boundary respected (P1b out of scope). |

### Scope boundary verification

- **In scope (P1a)**: Metachar rejection BEFORE prefix match in `commandAllowedByPolicy`, shared by both `checkShellScope` (server.ts ~1105 SHELL_SCOPE_DENIED) and `shellExec` (~67-68). `hasShellAllowlistMetachar` exported for testability.
- **Out of scope (documented)**: `shell:false` + file/args (P1b), `$VAR` expansion under allowlist (residual until P1b), netsec redesign, enterpriseSkip/god-mode changes.
- **No scope creep**: git diff confirms 0 changes to `server.ts`. Only `shell.ts` (+20) and test file (+79).

### Code review

- **Ordering correct**: Metachar check (lines 34-41) runs BEFORE prefix allowlist match (lines 42-50). Classic `echo ok; rm -rf /` bypass closed.
- **Regex coverage**: `/[;|&`$()<>\n\r]/` covers `;` (separator), `|` (pipe), `&` (background/AND), `` ` `` (backtick substitution), `$` (`$VAR` + `$()`), `()` (subshell), `<>` (redirects), `\n\r` (newline injection). Confirmed working via runtime test above.
- **`$` catches `$VAR`**: This is a known, documented scope limitation. Under allowlist, `echo $HOME` is rejected. Conservative/safe. P1b will address.
- **`checkShellScope` → `commandAllowedByPolicy`**: Both server.ts SHELL_SCOPE_DENIED path and `shellExec` execute path get metachar protection. Dual-gate verified in tests.

### Test review (9 pass / 0 fail)

| Test | Type | Quality |
|------|------|---------|
| Benign allowlisted command succeeds | Real `shellExec` spawn | ✅ Real side effect, no mock |
| `echo hi; rm -rf ...; touch ...` | Real `shellExec` spawn + fs check | ✅ Side-effect test — asserts marker file NOT created |
| All metachar variants (&&, \|, $(), backticks, redirects, newlines) | `commandAllowedByPolicy` + `checkShellScope` | ✅ Both gate paths verified |
| Non-prefix still denied | `commandAllowedByPolicy` | ✅ Old allowlist logic intact |
| confirm_per_command allows metachar | `commandAllowedByPolicy` + `hasShellAllowlistMetachar` | ✅ Regression lock — metachar ban is allowlist-only |

No mocks, no false-greens. The side-effect test (`echo hi; rm -rf ${marker}; touch ${marker}`) actually verifies the exploit payload does not execute.

### Conclusion

The fix correctly addresses P1-4 P1a: the allowlist prefix bypass via shell metacharacters is closed. All server.ts L2 forceConfirm, god-mode, security_token, and enterpriseSkip paths are confirmed unchanged by inspection. Tests have real side-effect coverage. Trust monotonicity holds. No scope creep. No blocking issues.

VERDICT: APPROVE
