Spot-check complete — every factual premise in the synthesis verified against live code, all lane pins traced into the spec. Writing the review.

---

# Dual re-review — Batch C path (#247)

Reviewer: independent (claude). Did not write the strawman or four-lane synthesis. All evidence below is `[inspected]` — direct file reads at `fix-247-host-p1` (HEAD baseline `e36176a5`); nothing executed.

## Verdict rationale

The path is implementable as written and the five DoDs close the "Confirm Center lies" gaps they claim to close. I independently verified every code premise the pins rest on, then checked each lane's BLOCKs against spec §2 pins 1–15 / §4 file map / §5 NEVER / §6 KEEP — all folded. No REJECT trigger from rule 1 survives: each of the seven is explicitly excluded by a named pin. Calibration is respected (C4 framed as Darwin alignment, C5 residual disclosed in pin 15, no `php -r`/`node -p`/pack-freeze creep). What keeps this from APPROVE: two pins have a defensible-but-lazy reading that would leave a small residual hole, and one file-map row under-scopes a branch that keeps lying in its error text. One-line spec edits each; nothing structural.

## Confirmed pins (folded correctly)

All four lanes' BLOCKs are in the spec, and the underlying code claims are true:

- **C1 premises** `[inspected]`: cache insertion-order last-http fallback (companion-dispatch.ts:1056-1069); AppleScript `contains` (:1139); HMAC expression-only at both sites — dispatch :1095 and message-router :3917 both use raw `validateToken`; osascript preview shows only JS (l2-admission.ts:295-296). Pins 1–5 fold: fail only when no absolute http(s) URL **and** no resolvable tabId (pin 1); catalog `required=["expression"]` + tabId while zod stays optional (pin 2 — correctly avoids chat-kill: zod already passes `tabId` through at tool-schemas.ts:240, and adapter injects pinned tabId *after* parse at adapter.ts:1235→1269); exact-URL contract with keep-query/drop-hash and `is` not `contains` (pin 3); preview URL (pin 4); HMAC lockstep incl. the message-router branch, no file split (pin 5).
- **C2 premises** `[inspected]`: preview builds command/args/cwd/enabled, no env (handlers/mcp.ts:46-56); `buildMcpStdioEnv` copies `config.env` verbatim (transport.ts:221-223); `USER_ENV_DENYLIST` contains PATH/HOME/NODE_ENV (user-env.ts:68-93); and `configEnv.PATH` override is load-bearing at transport.ts:225 — wholesale reuse would break it. Pins 6–9 fold: keys-or-`(none)`, no values ever, whole-update rejection not stripping, dedicated `isUnsafeLoaderEnvKey` (no PATH/HOME/NODE_ENV; case-insensitive; `DYLD_`/`BASH_FUNC_` prefixes) with fail-closed transport as depth.
- **C3 premises** `[inspected]`: raw-suffix regex `(?:^|\s)(?:-[ce]|--command|--eval)(?:\s|=|$)` (shell.ts:161) misses `python3 '-c' 'code'` (quote before `-c`); `tryParseSimpleArgv` returns null on `ENV=` prefix tokens (shell.ts:367). Pins 10–11 fold: deny on parsed `argv[1:]` incl. attached `=`-forms and clustered shorts, bare-entry-only gating, parse-null must not fail-open, and the DoD negative is correctly `python3 '-c' 'code'` (parens die at metachar).
- **C4 premises** `[inspected]`: `CMSPARK_WIN_SCRIPTS` honored whenever `NODE_ENV !== "production"` — empty NODE_ENV = live (powershell.ts:62-68); Darwin dual opt-in shape to clone exists at host-bin.ts:68-76 (`ALLOW === "1"` else throw). Pin 12 folds, including `computer-uia-watch.test.ts` ALLOW fix and `npm run dev` unaffected.
- **C5 premises** `[inspected]`: spawn payload = `spawn|role|pack|alias` only (security-policy.ts:94-95); `roleAllow && length > 0` treats `[]` as missing → default browser set (spawn.ts:19); empty-after-HARD_DENY already fails with an error (spawn.ts:141-143), so pin 14's `[]` semantics land on an existing failure path; spawn preview has `allow=` but no deny/intent (l2-admission.ts:302-303). Pins 13–15 fold, incl. sort+unique canon (Skeptic's order-DoS correctly downgraded to "fix anyway") and pack residual named-not-fixed.
- **Process facts**: single issue point `issueTokenFor` (l2-admission.ts:1636) with `issueTokenFor`/`validateTokenFor` as the only binding pair; all spec-named test files exist, incl. `tests/security/security-policy.test.ts`; NEVER bars D/E, SUMMONER_ALLOW, overlay Allow/Deny, `do shell script`, NODE_ENV-as-gate, HMAC reverse-parsing.

## Missing / still BLOCK

Nothing BLOCKs. All four-lane pins are folded; no REJECT condition from rule 1 is met; the DoD set leaves no path for Confirm Center to lie within C1–C5 scope (pack composition residual stays honestly disclosed per pin 15).

## Nits

1. **Pin 11 wording (the real one):** "跳过 `ENV=` token 后仍扫 deny" should say explicitly *strip leading `ENV=` tokens → re-parse → apply the pin-10 argv scan*, raw regex only as final backup. The lazy reading (strip then raw regex) still misses `FOO=1 python3 '-c' …` because `'` precedes `-c`. Pin 10's definition of "扫 deny" as the argv scan makes the correct reading available — one sentence removes the ambiguity.
2. **Pin 5 mechanics:** `bindingPayloadFor` is static and params-only; add one sentence stating issue-side resolution writes the resolved absolute URL into `finalParams` before `issueTokenFor` (or lazy-requires the tab-cache getter — precedent exists in the same function for cli-manifest/computer-types), so issue and execute provably share pin 3's single precedence.
3. **message-router branch scope:** file map covers only "改 `validateTokenFor`" for :3897-3926, but that branch's own error copy still teaches fragments (`url = fragment… 'zhihu.com'`, :3909-3912) and it drops `tabId` when forwarding to `executeTool` (:3925). It fails closed, so no lie in *execution* — but fold its error text + tabId passthrough into the row, or "废除 fragment 合同" is only half-executed.
4. **C2 legacy-config UX:** masked-echo edit-save round-trip (handlers/mcp.ts:101/:318) on a pre-existing config containing a loader key will now fail whole-update; the rejection message should name the offending key, and the C2 DoD test could cover that round-trip.

## Recalibrated: implement now? YES only if APPROVE*

YES — after the four one-line spec touches above (nits 1–3 are wording/scope; nit 4 is a message string). Batch sizing, blast (T3 post-auth), one-PR-five-commits, and test-first shape are all sound; I would start TDD on C3 (most self-contained) once the spec edits land.

VERDICT: APPROVE_WITH_NITS
