Diff is current (matches working tree; only `companion/src/capability/shell.ts` + test file changed; no `server.ts` / forceConfirm / netsec / token-gate touched). Tests build clean and all 9 pass; `message-router-config-security` still green (12/12).

## Findings

### Verified correct
- **Funnel placement** (`shell.ts:42-50`): metachar gate lives inside `commandAllowedByPolicy`, the single chokepoint used by both `checkShellScope` (→ `server.ts:1105` `SHELL_SCOPE_DENIED`, runs *before* `enterpriseSkip` is even computed) and `shellExec` (`shell.ts:87`, execute path). No third route exists (`rg shellExec\(` confirms).
- **Ordering**: metachar check runs BEFORE the prefix match (`shell.ts:55`), so the canonical bypass `echo ok; rm …` is closed at both scope and execute layers.
- **Trust monotonicity intact**: `capabilityForceConfirm` still includes `shell_exec` (`server.ts:1064-1075`); G1 gate `(!skipConfirmation || forceConfirm) && !enterpriseSkip` unchanged (`server.ts:1134`); L2 `security_token` + command-bound payload still required on the execute path (`server.ts:3007-3013`, `security-policy.ts:141` constant-time + codeHash binding). God-mode / `auto_approve_dangerous` alone still hits SHELL_SCOPE_DENIED before any skip logic — verified by inspection.
- **Test "prefix + '; rm' rejected (metachar, no side effect)" is a genuine regression lock**: with the gate removed, `command.startsWith("echo ")` would pass and `/bin/sh -c` would `touch ${marker}`, failing `assert.equal(fs.existsSync(marker), false)`. Not a mock.
- **Scope discipline**: P1a only. `confirm_per_command` policy layer explicitly exempted (5th test asserts `echo a; echo b` is `ok:true` there, metachar ban is allowlist-only) — matches inventory §P1-4 P1a/P1b split.
- **ADR-020 declaration** present and accurate: Surface L2 / L2-class shell / Composition capability modules / Autonomy single / Trust L2 forceConfirm / Channel enterprise. No Pack-first / originWs / new-runtime concerns (no new tool, gate, or UI).

### Nits (non-blocking)
1. `companion/src/capability/shell.ts:31` — regex `[;|&\`$()<>\n\r]` does not cover brace expansion `{a,b}`, glob `*`, tilde `~`, or `$VAR` (no parens). Acknowledged as P1b residual in code comment and risks field; consider a one-line test asserting `echo $HOME` still passes under allowlist to document the residual boundary explicitly.
2. `companion/src/capability/shell.ts:34` — `hasShellAllowlistMetachar` exported essentially for tests; consider `// @internal` JSDoc to signal non-public surface.
3. Tab/`\v`/`\f` not in regex — benign under `/bin/sh -c`, just noting.
4. "god-mode alone cannot silent-skip shell L2" is verified by inspection of `server.ts` only; the implementer/adversarial notes acknowledge this. A small server-level integration test (`auto_approve_dangerous=true` + `auto_approve_enterprise_tools=false` + `echo a; rm b` → `SHELL_SCOPE_DENIED`) would harden the regression lock — out of P1a scope but worth tracking.
5. `companion/tests/capability-shell-netsec.test.ts:91` — third test exercises policy + scope layers only (not `shellExec` side-effect); the second test already covers execution, so this is fine, just noting the layering.

The fix fully addresses inventory P1-4 P1a, tests are real (not rubber-stamped), no new holes, Trust monotonicity holds.

VERDICT: APPROVE_WITH_NITS
