All verification complete. Here is my review.

---

## Review: post-merge-198-201 fix diff (working tree vs `2faaefa`)

**Method**: read the synthesis doc + full patch + every touched file in full (not just hunks), plus callers (`url-cookie-admission.ts`, `stt-handlers.ts`, `message-router.ts`, `apps/acp handlers`, `allow-dir-expand.ts`, `apps-utils.ts`); replayed original attacks with a live probe; re-ran targeted suites. Confirmed the patch reverse-applies cleanly (working tree == patch, 34 files).

### Original attacks replayed against fixed code

| Attack | Result |
|---|---|
| `file:///C:Windows/System32` (F1) | Hard-refused `kind:"invalid"` pre-`path.resolve`; POSIX shape caged downstream — test + code verified |
| Junction chain to outside home (M2) | Existing in-home junction → anchor realpath escapes → cage (test `junc-escape` passed) |
| `[::ffff:169.254.169.254]` / `[::ffff:a9fe:a9fe]` / `[fd00:ec2::254]` / `[fe80::1]` / `[fe90::1]` / `[febf::1]` | **All BLOCKED** (probe-verified); loopback/ULA/public v6 correctly ALLOWED — consistent with RFC1918 policy |
| settings-web DNS failure | Fail-closed restored (17/17 incl. new IPv6 + DNS-fail tests) |
| Classic STT `resource_conflict` → per-chunk `session_unknown` + abort ACK in 250ms window | No kill, no zombie; retry succeeds once, adapter usable after (protocol-faithful fake, macrotask-delivered) |
| Peer-level abort kill-another-peer | Impossible: `bound.peerId !== peerId → peer_mismatch`; `peerId` = server-assigned per-connection `randomUUID`, never client-known |

### Attacks on the fixes themselves
- **Retry-sid swap race**: swapped `sessionId = retrySid` *before* abort+backoff closes the V1 window (old-sid rejects + abort ACK dropped by the sid guard); `reset()` now bumps `loopGen` so no coroutine survives reset; `ensureSub()` before retry start is a no-op belt (unsub never null in the fixed flow). Classic checks `genAtStop !== loopGen` after sleep (abort/destroy bump it), so `!aborted` is redundant-but-safe there. No new race found.
- **Late `end()` vs retry binding (V2)**: `if (this.bound === bound) this.dropBound()` on both success and error unwind correctly preserves a newer retry binding (tested with a gated runner).
- **`normalizeIpLiteral`**: no throw paths on garbage (returns null); `net.isIPv6` pre-guard + per-group hex validation make `parseInt` safe; compressed/hex-mapped/dotted forms all canonicalize (probe-verified).
- **Two probe-cache normalizations**: byte-identical implementations; port/scheme/path-case matrix tests on both sides; browser vs Node both WHATWG URL → agree. Model case preserved on both sides (N1 closed).
- **L6 `isFile()` vs design §6.3**: see finding 2.

### Trust monotonicity
No new auto-approve path, no persisted grant, no whitelist write. Probe cache is module-memory only, never persisted; `config.test` echo adds key material, not grants; `SENSITIVE_HOME_PREFIXES` only *grows* (`.git-credentials/.npmrc/.netrc/.docker`); MinimalConfirm changes copy only; settings-web allowlist unchanged.

### Tests
- `chrome-extension` full: **769/769** (re-run, green).
- companion targeted: file-url-admission, llm-endpoint-url, native-vision-probe-cache, vision-pipeline, allow-dir-credentials, config-test-probe-keyed, security-thread, voice-stt-session-service (107/107), settings-web (17/17), security-gates integration (64/64). All green.
- New tests discriminate against pre-fix code (mid-backoff `errors`/`ends` asserts, IPv6 block asserts, `-r1` start order, case-preserved model miss, L4 non-mislabel).

### Findings

1. **[LOW]** `companion/src/tool/file-url-admission.ts:243-244` — dangling-junction anchor walk residual: `fs.existsSync` is false on a *dangling* link, so the walk anchors at the parent (in home) and OFFERs a missing leaf under the dangling link; a pre-planted dangling junction still escapes on open. Requires prior write access to home to plant; strict improvement over HEAD; documented in synthesis as out-of-threat-model. Non-blocking.
2. **[LOW]** `companion/src/tool/file-url-admission.ts:233` — L6 `lstat().isFile()` refuses **all** existing symlinks, even when realpath stays inside home, deviating from design §2.2 ("realpath 后再判笼子" — escape-only) and narrowing the §6.3 documented "home 内文件可开" flow for symlinked files; the cage copy also never mentions symlinks as a reason. Fail-closed/over-strict direction, documented follow-up. Non-blocking.
3. **[LOW]** `companion/src/security.ts:312` / `companion/src/settings-web.ts:128-134` — NAT64 `[64:ff9b::169.254.169.254]` and 6to4 `[2002:a9fe:a9fe::]` embedded-IMDS forms still ALLOW (probe-verified). Pre-existing residual, explicitly listed in synthesis follow-ups; requires a functional NAT64/6to4 route to exploit. Non-blocking.
4. **[LOW]** `chrome-extension/tests/voice-local-continuous.test.ts:272` — continuous-mode conflict-retry test still uses the weak fake (no per-chunk `session_unknown`, no abort error ACK), so the continuous V1 fix is verified by a weaker harness than the (now faithful) classic one. Code is structurally identical to classic. Documented follow-up. Non-blocking.
5. **[NIT]** `companion/src/message-router/handlers/config.ts:330-335` — WS `config.test` SSRF check is lexical-only, no DNS re-check (hostname resolving to 169.254.x passes until fetch). Pre-existing documented follow-up.
6. **[NIT]** `companion/src/llm/vision-pipeline.ts:113-114` — cache key scopes endpoint+model but not `customPrompt`; same image/model/endpoint with different prompts still shares a description entry. Pre-existing.
7. **[NIT]** `chrome-extension/src/sidepanel/components/vision-reuse-logic.ts:159` — `detected` remains in `resolveNativeVision` opts, now silently dead (misleading for future callers).
8. **[NIT]** `companion/src/settings-web.ts:146-156` — DNS fail-closed now blocks legitimate-but-unresolvable LAN LLM hosts (`.local`/split-DNS) on the settings test button. Intended trade-off, header comment updated.

All findings are LOW/NIT, non-blocking, and mostly already logged as follow-ups in the synthesis. V1, V2, S1, F1, M1, M2, M3-copy, L1–L8 (with the documented residuals) are closed; no new critical/high/medium found.

VERDICT: APPROVE_WITH_NITS
