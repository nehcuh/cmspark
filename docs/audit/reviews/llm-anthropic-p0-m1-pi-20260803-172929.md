## Review: llm-anthropic-p0-m1 (base 8c70c3a)

I inspected the full working-tree diff vs `8c70c3a` (the patch artifact only covers the 8 tracked modified files; the untracked `provider.ts`, `providers/`, and two new test files are part of the real change — confirmed current via `git status`/`git diff`, patch not stale). Ran the full suite: **2274 + 15 tests, 0 failures**; the 68 tests in the three new/modified files pass.

### Requirement verification

1. **createProvider wired; default openai unchanged** — ✓ `adapter.ts` (`createProvider(config)`), `generateThreadTitle`, `llm-extract.ts` (`toLlmConfig`) all consume the provider; default `protocol:"openai"` filled via `deepMerge` (`config.ts:465`); `model-probe` regression test asserts `/models` probe still fires for DeepSeek. `OpenAIProvider` preserves stream/tool_choice/usage/reasoning semantics.
2. **L7 first-party union deny** — ✓ `headers.ts`: exact + suffix with leading dot (`endsWith(".anthropic.com")`), trailing-FQDN-dot stripped (`api.anthropic.com.` can't bypass), `evilanthropic.com`/`myclaude.ai` correctly NOT matched; first-party + `profile≠none` **or** *any* `extra_headers` → `HeaderPolicyError` before fetch (tests assert `fetched=false`). CRLF/Host/Cookie/hop-by-hop rejection applies on all hosts.
3. **Deterministic convert/SSE/tool id/max_tokens + fixtures** — ✓ `computeMaxTokens = min(8192, max(256, floor(cw/8)))`; `sanitizeToolCallId` deterministic & pairing-aligned; SSE fixture tests (text/tool_use/thinking→reasoning per M6); multi-turn fetch mock (stream tool_use → complete with tool_result); cross-protocol resume both directions (M7, `reasoning_content` dropped on wire); no `/v1/v1` in URL resolution.
4. **No OAuth/Max/system-prompt CC injection** — ✓ grep clean.
5. **No UI scope creep** — ✓ settings-web only adds a protocol soft-skip on the existing test path; no new fields; side panel untouched.
6. **Header values never logged** — ✓ `headerNamesForLog` (names only); `llm.anthropic_request` logs only `base_host`/`profile`/`header_names`/`path`; test asserts api_key absent from body.
7. **Vision still OpenAI-only** — ✓ `vision-pipeline.ts` untouched.

### ADR-020 checklist
Capability declaration is **missing** from the implementer prompt → checklist says nit (no tools/gates/UI entry points added; this is Composition/infra transport — fits the synthesis's ADR-020 assessment; no new runtime, no confirm families, no originWs, no trust/gate changes, no god-mode/auto_approve surface).

### Nits (non-blocking)
- **Missing capability declaration** in the implementer prompt (ADR-020 checklist nit).
- `skill-craft.ts:124`, `skill-engine.ts:385`, `message-router.ts:312` still build their own `new OpenAI(...)` clients — design §7 lists "skill-craft/skill-engine → 统一经 llm-extract" (v1, not P0). A user on `protocol=anthropic` will get OpenAI-wire failures on skill-craft/skill-matching paths; flag for the v1 sweep.
- `message-router` `config.test` (`models.list`) is not protocol-aware while `settings-web.handleTestProxy` is — inconsistent "test connection" behavior across entry points (P1 per L10, but worth noting).
- Patch artifact omits the untracked provider/test files; reviewers must run `git diff`/read working tree (done here) — suggest a combined diff next batch.
- `assertHeaderPolicy` takes an unused `protocol` param; `parseAnthropicSseStream` ignores inline `input` in `content_block_start` (fine — streaming uses `input_json_delta`).
- settings-web soft-skip returns `ok:true` — mitigated by the explicit "skipped" message.

No REJECT criteria fire: DeepSeek path green (regression tests + full suite), L7 not bypassable (trailing-dot/port/union covered), P0 contract tests present and passing.

VERDICT: APPROVE_WITH_NITS
