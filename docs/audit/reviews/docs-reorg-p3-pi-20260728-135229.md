All 8 conditions verified against actual source code. Here is my complete analysis:

---

## Condition-by-condition verification

### 1. Guides match real modules ✓
- `computer-use-user-guide.md` → `companion/src/computer/` exists (session-trust.ts, estop.ts, policy.ts, executor, adapters, TinyClick)
- `host-and-apps.md` → `companion/src/host-use/` + `companion/src/apps/` exist (adapters, blacklist, nonce, biometric-gate, guards)
- `notebooklm-user-guide.md` → `chrome-extension/src/notebooklm/` exists (16 files: import-orchestrator, dom-automation, extractor, rss-parser, etc.)
- `multi-agent-user-guide.md` → `companion/src/board/` + `companion/src/orchestrator/` exist (schema, service, spawn, tab-lease, fleet, constants)
- No invented UX — every tool name and panel name maps to real code

### 2. Computer Use: session-trust G1 spot-check ✓
- `session-trust.ts` confirms: `g1InitialSkipEligible()` requires `thread:` key, `explicitOptIn`, corpus ⊆ prior, budget ≤ maxBudget, actions ≤ maxActions, non-expired, no credential latch, NOT experimental — exactly as guide §5 describes
- `PROMPT_ALWAYS_TAGS`: `danger_detected`, `experimental_suggestion`, `foreground_yielded` → "始终 prompt" — guide §5 matches
- `IDLE_EXPIRY_MS = 30 * 60 * 1000` → "约 30 分钟" — matches
- `policy.ts`: `assertCoordinateAllowed` double-switch (coordinateEnabled + coordinateAllowed) + structural vault/LOLBIN exclusion — guide §3 matches

### 3. Host/apps: no unrestricted shell claim ✓
- Guide §5 clearly separates `workspace_*` / `shell_exec` / `host_read` / `host_write` / `host_app` / `host_computer`
- "host_write 每次需生物识别" — `biometric-gate.ts` confirmed
- "opaque TargetId" / "validateTargetId" — `host-use/types.ts` pattern confirmed
- Blacklist/vault documented — `apps/guards.ts` `canEverCoordinate`, MAC_VAULT_BUNDLE_IDS confirmed

### 4. NotebookLM: user-facing ✓
- Guide covers entry points, permissions, preconditions, import results, failure modes — not engineering process
- ADR-011/012/013 files exist and links resolve
- NotebookLM module is extension-side (`chrome-extension/src/notebooklm/`), not a Companion LLM tool — guide correctly notes "不依赖 Companion 调 LLM"

### 5. Multi-agent ≠ sub_agent; tab locks + board accurate ✓
- Guide §1: "这与 Skill 类型里的实验性 sub_agent 不是同一机制" — explicitly distinguished
- `WORKER_HARD_DENY` in `orchestrator/constants.ts`: shell_exec, netsec_port_scan, osascript_eval, host_computer, host_write, host_read, host_app — guide §2, §6 match
- `ORCHESTRATOR_CAPS` (max 5 workers, 2 tabs per worker, 10 total, L2 caps) — guide §3 matches
- `TAB_LEASE_TOOLS` set confirmed in code — guide §4 matches
- Board tools `board_read` / `board_complete` in `ORCHESTRATOR_TOOL_ALLOWLIST` — guide §5 matches

### 6. ADR-017/018 status consistent ✓
- ADR-017: "Implemented / Accepted" — code map paths (`computer/`, `tool-definitions.ts`, Cockpit/SafetyStrip) all exist
- ADR-018: "Implemented / Accepted" — code map paths (`host-use/`, `apps/`, `server.ts`) all exist
- Both ADRs reference user guides; no contradictions

### 7. README + docs/README link all four guides ✓
- README capability matrix has explicit rows linking all four guides
- README "相关文档" table lists all four
- docs/README user guides table lists all four
- All relative paths (e.g., `computer-use-user-guide.md`, `adr/017-computer-use.md`, `adr/011-notebooklm-import.md`) resolve to existing files

### 8. No Phase 1 regression ✓
- FAQ: old "等待确认完成后开放" → replaced with "已交付 L2 确认：evaluate / osascript_eval 等强制走 SecurityConfirmationManager（约 45s 超时）→ Side Panel / Confirm Center（Cockpit）人机确认"
- ADR-016: "Implemented (P0)" with concrete code paths (`board/`, `board_read`, `board_complete`, `BoardPanel`) — no downgrade

---

**No blocking issues found.** Documentation is accurate, well cross-referenced, and code-verified across all 8 conditions. New guides are user-facing and honest about platform limitations. Session-trust G1 description matches the source code precisely. No regressions to Phase 1/2 facts.

VERDICT: APPROVE
