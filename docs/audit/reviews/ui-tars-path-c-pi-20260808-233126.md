All evidence gathered. Here is my review.

---

## Pi path review: UI-TARS absorption (Path C)

**Summary**

1. **Path selection is correct.** Path C (pattern absorption) is the right 0.5.x choice: cost 3 / risk 2, preserves the tool-loop + hybrid locate + trust-gate identity, and ships the highest learning density overnight.
2. **Path B REJECT is sound** (decision doc §2): a parallel GUIAgent runtime would bypass/dilute L2/corpus/hard-deny — risk 9 is honest; matches research §4 "do not absorb" list.
3. **Path A deferral is sound**: Qwen3-VL already occupies the experimental slot; weights/GPU/license UX don't fix identity. Correctly parked in Phase 2.
4. **Security posture verified intact**: the diff touches only the G4 experimental caption path. Dual-switch, A4 hard-deny, danger scan, A1.3 sub-budget, and X3 staleness are untouched. `experimentalRaw` is sliced to 500, documented "never inject authority", and flows only into `extractGuiThought` → `sanitizeComputerCaption` (the exact sanitizer the L2 caption mandates: Zl/Zp/Cc→space, Cf stripped) + 160-char cap + 「模型思考：」prefix. The §3 adversarial table mitigations hold in code.
5. **Research claims verified**: `outcome.raw` was indeed dropped before (no `experimentalRaw` in old `ChainLocateResult`), and the old caption hardcoded target only — confirmed via `git diff`.
6. **But the diff as submitted is not mergeable**: the S2 headline deliverable ("robust UI-TARS-like parse") is both **buggy** (failing test) and **unwired** (dead code). Test suite goes red.

**Blocking issues (must fix before S6/merge — they do NOT invalidate the C path itself)**

- `companion/src/computer/gui-action-parse.ts:45-56` — comma-separated box form `start_box='(10,20,30,40)'` (the real UI-TARS format) is parsed as the box **top-left corner** (10,20) instead of **center** (20,30): the optional `(?:\s+…\s+)` group only matches whitespace-separated numbers, so it silently drops the box semantics. Test `companion/tests/computer-gui-action-parse.test.ts:18-20` **fails** (12/13 pass) → `npm test` (tsc test build + `node --test .test-dist/tests`) is red. If wired as-is, this yields wrong-click suggestions.
- `companion/src/computer/gui-action-parse.ts:39` — `parseGuiClickPoint` is **dead code**: no caller in `src`; the production parse path is `qwen-vl-worker.py::_parse_point`, which was NOT updated for UI-TARS action DSL. The "parse robustness" scope item has zero production effect in this PR. Either wire it into the worker/locator path or cut it from 0.5.x (it only earns its keep when a model emitting that DSL — Path A — lands).

**Non-blocking nits**

- S4 (chat-LLM CU playbook, `companion/src/llm/adapter.ts`) and S5 (user guide + architecture §9 Operator map) are declared Path C scope (decision §2 item 4-5, stages S4-S5) but absent from this diff — must land before the S6 completion gate.
- `parseGuiClickPoint` last-resort heuristic `/(?:^|\s)([0-9]{1,5})\s+([0-9]{1,5})(?:\s|$)/` (line ~100) false-positives on prose ("meeting 10 30"); harmless while G4-gated, but tighten (require click/point context) if wired.
- No test for `formatExperimentalSuggestionCaption` truncating a >160-char thought passed directly (only `extractGuiThought` truncation is covered).
- The new caption drops "Qwen3-VL 本地模型" for generic "本地视觉定位" — fine, arguably more accurate for future model swaps; just confirm no product messaging depends on the old wording.

Verdict rationale: the path analysis (B reject, A defer, C select, D inferior) is fully confirmed and security holds; the REJECT is scoped to the reviewed diff state — red test + unwired headline feature — with a small, enumerable fix list. The multipath conclusion stands; do not re-open A/B/D.

VERDICT: REJECT
