## Review Summary: PR1 — StatusRail + offline hygiene + token connection colors

### Spec §12 scope checklist

| Requirement | Status | Evidence |
|---|---|---|
| Header → StatusRail extraction | ✅ | `StatusRail.tsx` created; `Header()` removed from App.tsx; imports cleaned |
| Mode badge + pin-on-badge | ✅ | `ModeBadge` with `togglePin` inside StatusRail (not buried in ⋯) |
| Connection dot: tokens only | ✅ | `connectionColor()` uses `tokens.success`/`tokens.warning`/`tokens.danger` |
| Thread switcher + ⋯ menu | ✅ | `ThreadList` + full ⋯ menu moved intact (Craft/export/NB/logs/settings) |
| No Material hexes in touched paths | ✅ | Zero `#4CAF50`/`#FF9800`/`#F44336` usage; only in prohibition comment |
| Remove `alert()` from reconnect | ✅ | `onRetry` now `showToast()`; `handleOpenLogs` now `setHint()` — zero `alert()` calls remain |
| `docs/DESIGN.md` radius SoT 6/8/12 | ✅ | Table updated: sm=6, md=8, lg=12, pill=999 |
| `docs/DESIGN.md` connection tokens | ✅ | Replaced Material hexes with `tokens.success`/`tokens.warning`/`tokens.danger` reference |
| Prefer `tokens.ts` helpers | ✅ | StatusRail imports `connectionColor`/`connectionLabel`/`connectionDotShadow` |

### DO NOT checks

| Constraint | Status |
|---|---|
| Delete BottomBar | ✅ **Not deleted** — `BottomBar` still imported (line 9) and rendered (line 181) |
| Add 装配 drawer | ✅ **Not added** — zero drawer code |
| Change FocusBand | ✅ **Not touched** |
| Touch companion daemon | ✅ **No daemon changes** |

### ADR-020 / cross-cutting

- Mode badge ontology (L0 `聊` / L1 `网页` / L2 `计算机`) preserved ✅
- Pin stays on badge, not buried in ⋯ ✅
- No scope creep (PR1-only deliverables, no PR2–PR7 artifacts leaked in) ✅

### Tests

- `chrome-extension/tests/tokens-helpers.test.ts`: new `connectionColor`/`connectionLabel`/`connectionDotShadow` tests
- **333/333 tests passing** (0 fail, 0 skip)

### Nits

None blocking. The PR is clean, focused, and exactly matches the PR1 scope.

---

VERDICT: APPROVE
