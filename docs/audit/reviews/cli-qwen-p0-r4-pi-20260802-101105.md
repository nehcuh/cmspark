All verification complete. Here is my review summary.

## Review: cli-qwen-p0-r4 (CLI Phase-2 + Qwen3-VL P0)

**Patch freshness**: Confirmed not stale — `git status` shows clean working tree at HEAD `905738d`, and `git diff 007130b…HEAD` matches the patch file exactly (base `007130b`, same stat).

### Re-verification items (all pass)

**1. LICENSE_DOOR_TEXT — no 永久跳过, advertises settings reset** ✅
- `companion/src/computer/model-license.ts:120,125` now say 跳过 + 「复位许可拒绝」/设置页复位. Grep confirms zero remaining 永久跳过 in the file.
- Mirrors updated consistently: `model-handlers.ts` LICENSE_DECLINED error + `license_required` notice, `model-state-messages.ts` `MODEL_SWITCH_COPY` (licenseDoorHint, licenseDeclinedNotice), `model-switch-logic.ts`, SettingsSlideout button text + new 复位许可拒绝 button.
- Test updated to assert `永久跳过` is absent and 复位/设置页 present — passes.

**2. validateWsMessage reset_decline** ✅
- `server.ts:4890-4896`: accepts `reset_decline:true | resetDecline:true` without `accepted`; still requires `source:"settings"`; handler belt re-checks source (model-handlers.ts:296-304 → `INVALID_SOURCE`).
- Handler reset path (model-handlers.ts:406-414) clears `modelLicenseDeclined`, broadcasts, returns `declineReset:true`; unit-tested. Extension sends `reset_decline:true, source:"settings"`. Tests pass.

**3. host_cli security gates intact** ✅
- L2 always: `capabilityForceConfirm` includes host_cli → `forceConfirm` unconditional (god-mode/auto-approve cannot skip, L-CLI-9); Q5 taint forces L2 on host_cli/host_app after CLI output until next real user turn (adapter injects `__thread_id` and `chatCreate` clears taint only on user turns).
- Executor three-place gate: security_token required → `validateTokenFor` binds `app|subcommand|canonical(flags,args)` via `hostCliBindingPayload` → token-namespace/`apps.enabled`/kind/enabled checks.
- `prepareCliExecution`/`resolveCliExePath`: realpath + lolbin + vault + `checkAddAllowed` dual door; manifest-only flags (undeclared rejected), charset/value_regex validation, option-injection guard (leading `-`/`/`/`@`), fixed-arity positionals, dangerous-risk ceiling caps; `execFile` with `shell:false`, env allowlist + secret deny-regex (no `buildChildEnv` reuse), timeout/maxBuffer, `windowsHide`; ANSI-strip + truncate; output routed through `PAGE_CONTENT_TOOLS` as untrusted.
- Policy cap ai (no auto) at add (`CLI_POLICY_CAP`), at `set_policy`, and add-flow belt; `WORKER_HARD_DENY` includes host_cli.
- Test suite green: companion 447 pass / 1 pre-existing win32 skip, extension model-switch-logic 12 pass.

**4. No new blockers** — ADR-020 declaration table exists in master plan (Surface F1=L2 Host / F2=L2 CU experimental, Autonomy, Trust, Channel, originWs); host_cli correctly on Surface axis (no bare 中层 Agent), reuses the existing L2 confirmation family (no new dialect), Pack-first honored, trust monotonicity via forceConfirm + Q5 + worker hard-deny. L-QW-1 `canEnableProbe` hard-refuses `set_enabled(true)` with zero config write (tested); `g1InitialSkipEligible` blocks on config-level `modelEnabled` (tested); pixel-only normalize (no 0–1000 rescale) in both TS and Python (tested).

### Nits (non-blocking)
1. `companion/src/apps/handlers.ts:364-366` — broken indentation on the CLI auto-cap check and `const cap = maxPolicyForEntry(entry)` (cosmetic only).
2. `companion/src/apps/add-flow.ts:240-241` — empty `if (!isWin && input.kind === "gui" && !hasBundleId) { /* comment only */ }` dead block; remove or implement.
3. `companion/src/server.ts:832-834` — host_cli L2 preview text shows only `app`+`subcommand`, not flags/args or manifest risk; the user confirms an invocation without seeing actual flag values. Not a security hole (token binds the full canonical payload; manifest constrains values; forceConfirm always), but a transparency gap — include canonical flags/args + risk.
4. Q5 fallback key mismatch: gate checks taint under raw `sessionId` while the executor marks under `ws:<sessionId>` (`resolveComputerTrustKey`) when `__thread_id` is absent. Dead in production (adapter always injects `__thread_id`), but unify the fallback for robustness.
5. host_cli doesn't fail-fast on unknown/disabled tokens before the L2 dialog (host_app does) — a malformed call burns a confirmation click before erroring in the executor.
6. All host_cli confirms render riskLevel "high" (forceConfirm) even for read-only subcommands — conservative but potentially desensitizing; could derive from manifest risk.

VERDICT: APPROVE_WITH_NITS
