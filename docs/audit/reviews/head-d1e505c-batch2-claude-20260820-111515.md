All verification complete. Here is my independent review.

---

# Dual review — d1e505c batch2 (residuals + fzbcro osascript)

## Patch freshness [executed]

Prompt patch (`-111515`) = header + diffstat + full diff; hunks **byte-identical** to live `git diff d1e505c`. Lane-frozen patch sha256 prefix `52730daf49e185d1` matches live exactly. Nothing staged; untracked files are review artifacts only. Not stale.

## Claims — all 12 verified independently

**SSRF/DNS (live `tsx` probes against `src/`, not tests):**
1. **S-XLAT** — `[::ffff:0:a9fe:a9fe]` hex/dotted/expanded all → IMDS-blocked; `64:ff9b::808:808`, `::1`, SIIT-of-public (`::ffff:0:808:808`) all `null` (allowed); v4-mapped/NAT64/6to4 IMDS still blocked by their own branches — new branch (security.ts:241-247) is prefix-disjoint, no misfire.
2. **S-GCPDOT** — `metadata.google.internal.` (both cases) → blocked; trailing-dot strip at security.ts:303.
3. **N1** — NXDOMAIN → `LLM_ENDPOINT_DNS_ERROR` (`!==` IMDS copy); IMDS-resolving name → IMDS copy; empty DNS answers → `unresolved` (fail-closed).
4. **N2** — `canonicalizeLlmHostname("LocalHost.")` → `localhost`; allowlist reached before `isIP`/DNS (settings-web.ts:144-155).
5. **N3** — all 10 request-path gates confirmed: openai.ts:37,112; anthropic.ts:64,104; vision-pipeline.ts:167; lifecycle.ts:613; connection-test.ts:89; config.ts:330,362. First await before fetch/SDK-create.
6. **C1** — `resolveNativeVision({foo-bar-7b, auto, detected:true})` → `false`; heuristic (`gpt-4o`) and `mode:"on"` intact.

**Voice** [executed]: 15/15 voice + 4/4 gate-error-copy pass. `git diff d1e505c --stat -- chrome-extension/src` shows **only** gate-error-copy.ts — production adapter untouched (hostile Q5 confirmed). Fake protocol strings byte-match production (`stt-session-service.ts:180` resource_conflict; `stt-session-core.ts:101/170/201`; `stt-handlers.ts:330`).

**L2 osascript (T3):**
9. **[executed mutation]** dispatch (companion-dispatch.ts:1087-1114): no-token → error; invalid token → error; post-token regex is log-only. I restored `if (safety.blocked) return` in the compiled artifact → the APPROVE test went **red with the exact fzbcro string**; restored artifact → green. Grep confirms no other `checkHighRiskExecution` hard-block caller remains (l2-admission.ts:943 is the pre-confirm preview, per A4 design).
10. Tokenless router path is token-check only; with a session it routes to `createToolExecutor(ws)` (lifecycle.ts:796) → L2 confirm; no-session returns "No session available" — no spoof.
11. `osascript_eval` still in `L2_GATE_TOOLS` (l2-admission.ts:51); forceConfirm waives only on three-flag cruise (pre-existing carve-out); god-mode deny test passed in my 65/65 security-gates run.
12. **[executed]** copy probe: 「若你已拒绝弹窗」 only on `User denied`; timeout/unavailable/disconnect/leftover each get distinct non-deny copy.

## Hostile focus

1. **`/api/testVision` = correctly P2.** Allowlist skip (settings-web.ts:147) → direct fetch (:481) with no second DNS gate, confirmed in code. Exploitation needs settings-API access (loopback + unguessable token + Origin check) **and** host-DNS control of an allowlisted public name; HTTPS names die on TLS; chat path and `/api/test` both re-gate. Defense-in-depth residual, not P1.
2. **Forged token: no.** HMAC (timing-safe) + toolName + threadId + 2-min TTL + sha256(code) binding + one-time delete (security-policy.ts:199-240); LLM-supplied tokens stripped at server.ts:471.
3. **Not a confirm skip.** Removing the post-approve regex veto restores the documented A4 model (“regex 仅作为风险预览，不再 gate WHETHER to confirm”); L2 admission itself is untouched and still forceConfirms.
4. **Restoring the re-block: red on Darwin [executed], green on Linux CI** — the test early-returns at the platform gate and CI is `ubuntu-latest` (ci.yml). Lane C N1 is accurate and slightly strengthened by the CI platform fact; still a coverage nit, not a product hole.
5. **Voice production unchanged** [executed].

## ADR-020 checklist

Declaration present and accurate: no new Surface (L1 tighter, osascript stays existing L2 — verified in `L2_GATE_TOOLS`), no Compose, single Autonomy, Trust monotonic (LLM/IMDS strictly tighter; osascript still HITL), community channel. No new confirm dialect, no new `securityConfirmations.request` site, no new runtime. Pack-first N/A.

## Machine [executed, mine]

companion: 165/165 (llm-endpoint-url, likely-multimodal, llm-connection-test, llm-provider-anthropic, settings-web, security-thread, single/files) + security-gates 65/65. chrome-extension: 19/19. All probes pass; mutation cycle green→red→green. (Consistent with lane numbers: A 73 ⊂ my set; C 157 = my 92 + 65.)

## Nits (non-blocking)

1. **P2-A1** — `/api/testVision` vision branch (settings-web.ts:476-491) should run `assertLlmEndpointAllowedAsync(validatedBaseUrl)` like `/api/test` does via `probeLlmConnection`.
2. **C-N1** — APPROVE regression is Darwin-only and CI is ubuntu-latest; add a platform-free unit (issueToken → dispatch, stub `execFile`) so Linux CI catches a restored re-block.
3. **B-N1** — double-stop test doesn't lock the `delayMs` precondition; guard is load-bearing today but silently stops being exercised if `delayMs` is stripped.
4. `hostnameResolvesToImds` (security.ts:371-373) — new export with no production caller; name conflates "unresolved" with "imds" (Lane A6).
5. Cosmetic: deny-path copy emits the re-issue guidance twice (user-gate-copy.ts:168-170).
6. Pre-existing, correctly deferred (Lane C N3): osascript `code`/`expression` bind/preview split.

The adversary synthesis's SHIP-with-nits conclusion is confirmed: no missed P0/P1, all closures replay-verified, residual list accurate (none over- or under-stated).

VERDICT: APPROVE_WITH_NITS
