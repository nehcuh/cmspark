Verification complete. Patch matches working tree; all 269 tests pass; TypeScript compiles cleanly.

**Findings:**

- ✅ Patch matches `git diff HEAD` exactly (5 files modified, 1 new test file).
- ✅ `npm --prefix chrome-extension test` → **269 pass / 0 fail** (includes 9 new `ui-mode-acceptance.test.ts` cases covering §8 P0/P1/P2 acceptance subset).
- ✅ `META_PANEL_SLASH` entries match `SkillMeta` shape (`sidepanel/types.ts:295` — `site?: string`, `tags?: string[]` both valid).
- ✅ BottomBar listener registers/unregisters cleanly with proper deps `[state.activeThreadId, dispatch]`; validates `id` against `ALL_TABS` before `setActivePanel` (no spoofing of arbitrary panel ids).
- ✅ McpPanel `STATUS_COLORS` token swap is sound — all five tokens (`success/warning/danger/textMuted/textSecondary`) exist in `tokens.ts`.
- ✅ CockpitApp allow button: `tokens.success` (#16a34a) + `tokens.darkMuted` (#9aa0a6) both exist; L2 early-return bypass works correctly when slash event fires `setActivePanel`.
- ✅ No security regression — `cmspark:open-context-panel` is an internal `window` CustomEvent; no external input crosses a trust boundary.
- ✅ Spec §8 checkboxes honestly reflect state (P0 4/5, P1 5/6, P2 3/3, with unchecked items accurately labeled "needs manual smoke").

**Nits (non-blocking):**

1. `App.tsx:21-47` — `META_PANEL_SLASH` const is declared *between* import statements. ES module imports hoist, so runtime is fine, but stylistically odd. Move it below all imports.
2. `BottomBar.tsx:108` — Comment says `/packs /board /more` but the actual third slash is `/mcp` (no `/more` command exists). Stale comment.
3. `SlashCommandPopover.tsx:179,201` — `key={m.skill.name}` would collide if a user-defined skill is named exactly `packs`/`board`/`mcp` (rare, but the META_PANEL_SLASH names overlap with plausible user skill names). Mitigation: prefix the meta keys, e.g. `key={`meta-${m.skill.name}`}`.
4. `CockpitApp.tsx:492` — Disabled-state color swap is a no-op: `tokens.darkMuted` is `#9aa0a6`, identical to the previous literal `#9aa0a6`. Token-wrap is still a marginal improvement, but the commit scope "Cockpit allow button → tokens" slightly overstates the visible change (only the active bg `#22c55e`→`#16a34a` actually changed).

VERDICT: APPROVE_WITH_NITS
