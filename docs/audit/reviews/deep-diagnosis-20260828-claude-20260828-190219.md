All required inputs read and every Critical/High claim traced to current code. Writing the dual re-review now.

---

## Verdict rationale

Independently re-verified all 14 High findings against current `feat/overlay-card-first-paint` HEAD; every one exists in code at the cited locations, with the two overlay-plane chains (XSS, lease/mcp-surface) confirmed end-to-end. Critical=0 is independently supported: pairing HMAC intact, overlay token is `randomBytes(32)` (summoner-web.ts:430), timing-safe compares throughout, `require_grant` defaults true (config.ts:404) with cmg_≠ws_secret enforced (stdio-server.ts:64-75). No missed High+ found on overlay-origin, grant-cli, outbound, same-face, RunProgress, or knowledge CRUD. One High is a calibration downgrade candidate; several evidence sentences are slightly overstated. Grade C+ and the P0 plan survive.

## Confirmed Critical / High

| id | title | file:line | why real |
|----|-------|-----------|----------|
| overlay-xss | Capture markdown href attribute injection | summoner-web.ts:1413 (`esc` only `/[&<>]/g`), overlay-md.ts:32-34 (capture `[^)\s]+` allows `"`; raw `href=""+u+""`), summoner-web.ts:525 (`script-src 'unsafe-inline'`), :1704/:2162/:2470 (`innerHTML=renderMd`) | Quote survives escaping → `href="https://evil/"onclick="…"` breaks out (HTML5 after-quoted-state treats any char as separator); unsafe-inline permits event handlers; token in `location.search` lets payload drive `/api/*` |
| bridge-tools-01 | osascript_eval wrong-tab execution | companion-dispatch.ts:1056-1068 (cache-fallback URL, last Map-inserted http), :1139 (`URL of t contains pageUrl` substring), security-policy.ts:50-53 (token binds expression only) | Approved JS can execute in a tab the preview never showed; target not HMAC-bound |
| mcp-01 | MCP stdio env hidden from L2 preview | handlers/mcp.ts:46-48 (preview = command/args/cwd only), transport.ts:220-224 (`config.env` copied verbatim, no loader-key deny) | `NODE_OPTIONS`/`LD_PRELOAD`/`DYLD_*` in `config.env` invisible to the confirming operator |
| computer-host-01 | estop liveness = anonymous /tmp socket | darwin-estop.ts:28 (`/tmp/cmspark-estop.sock`), :174-181 (CONNECT-first → ok), no peer cred/chmod/nonce | Any local process (any user on shared host) can pre-bind and satisfy proof-of-life; CU safety net silently disarmed |
| computer-host-03 | Shell allowlist quoted `-c` bypass | shell.ts:161 (`(?:^|\s)-[ce]` on raw suffix), :168-179, :318-371 (`tryParseSimpleArgv` dequotes), :501-503 (argv spawn) | `python3 '-c' '<code>'` passes regex (quote before `-`), parser strips quotes, argv executes code; defeats the anti-injection layer; l2-admission.ts:1596-1637 shows skip paths mint tokens without interactive confirm |
| orchestrator-packs-01 | spawn_worker token omits tool scope | security-policy.ts:94-95 (binds `role\|pack\|alias` only), companion-dispatch.ts:141-145 (execute reads `params.tool_allow/tool_deny/intent_id`) | Approved scope not enforced by HMAC; single-use within TTL but replayable with different scope than previewed |
| x-architecture-01 | Overlay chat gets full L1/L2 tool loop | llm/adapter.ts:127-133 (no `surface` in ChatCreateParams), :619-623 (`getToolDefinitions()` ∩ whitelist, null=full) | ADR-020 L0 exists only in docs/UI tables |
| overlay-summoner-03 | Overlay mutates MCP via tray client | menu-bar-agent.ts:1634-1637 (`mcp.toggle_server` → `companionClient`), :913-917 (`mcp.add` via tray), summoner-web.ts:242 (policy check hardcoded surface "summoner") | HTTP overlay actions execute with tray ACL; `SUMMONER_WEB_DISPATCH_ALLOW` cannot see the real egress surface |
| orchestrator-packs-02 | pack.apply/skill.activate unbound thread | message-router.ts:3052-3083 (overlay guards exist but `rest.thread_id` never bound to overlay session), :2475-2484 (`skill.activate` — no gate at all) | Overlay session can recompose any thread; skill.activate has zero overlay filtering |
| orchestrator-packs-03 | Stale ThreadManager snapshot overwrites index | skill-engine.ts:480, :652 (`new ThreadManager()` per lookup), thread-manager.ts:760-765 (`get()` seeds run_progress → `saveIndex()`), :461-476 (merge-back covers digests only) | Fresh snapshot's non-digest fields (alias, skills, board state) clobber live index.json |
| knowledge-loop-01 | Truncated knowledge Save destroys tail | KnowledgeSubPanel.tsx:676 (body ← truncated doc.body), :754-767 (Save sends `body`, `readOnly` only checks `builtin`), skill-engine.ts:1487-1503 (wholesale `writeRestrictedFile`, only 6MB cap) | 512KiB-capped body silently overwrites full doc |
| overlay-summoner-01 | HTML hide leaks composer lease | summoner-web.ts:358-361 (`hideSummonerWebShell` = SSE close + SIGTERM only), :570-572 (SSE close = delete), menu-bar-agent.ts:1020-1026 + :1531-1532 (`handleSummonerClosed` only on Swift `summoner.closed`) | Tray's summoner WS survives HTML hide, so the lifecycle.ts:1404-1411 survivor count never drops — no release path fires |
| x-correctness-abort | Abort paths incomplete | message-router.ts:789-792 (file.upload claims controller, never sets `llmLoopOwnerPanel` — contrast :563/:1500), lifecycle.ts:1418-1437 (close = abort + grace only) vs message-router.ts:1318-1336 (chat.abort adds worker-intent abandon + pending reject + pending-aware lease release) | Disconnect-terminated uploads stay unowned; close never drains leases/nextRun |

(All 14 confirmed; computer-host-04 listed under downgrades.)

## Refuted / downgraded

| id | original sev | new sev | why |
|----|--------------|---------|-----|
| computer-host-04 | High | Medium (defensible either way) | powershell.ts:63-68 confirmed: `CMSPARK_WIN_SCRIPTS` gated only by `NODE_ENV !== "production"`, and packaged SEA doesn't set NODE_ENV. But the only exploiter is a same-user env-var attacker who already owns the user (the file's own comment concedes this). No privilege boundary crossed → fails the stated High bar ("post-auth integrity / local RCE-equivalent / durable secret tape"). The consistency fix (match darwin's `ALLOW_HOST_BIN_OVERRIDE=1`) stays P1. |
| computer-host-02 | (already refuted by fanout) | — | Agree with the fanout's refutation; 2026-08-01 dual-review contract, not an open defect. |
| x-correctness-abort evidence wording | High (kept) | High | "close 只 abort" overstates: lifecycle.ts:1437 grace-kills pending tools owned by the socket (5s) and :1438 rejects confirmations. The upload-owner and lease/nextRun gaps are real; the pending-tool half is partially handled. Finding survives, evidence sentence imprecise. |

## Missed by fanout (High+ only)

None found. Swept: overlay HTTP origin/token (empty-Origin accept + argv token = Medium, correctly counted; token itself is strong), knowledge server-side gesture (covered by extension-ui-05), `outbound_mcp.set_require_grant` with no L2/surface gate (message-router.ts:3416-3426 — authenticated-peer-only, matches the one-click-off Medium), estop flag file spoof (local DoS, Low), ChatShell/RunProgress unicast (Medium, correctly placed), grant-cli (Medium, matches #230, code not worse than ticket).

## Nits

- 08-26 product snapshot records the live box once had `require_grant=false` + `auto_approve_dangerous=true` on disk (post-227-status.md §2 precheck BLOCK). Code defaults are honest, but that disk state multiplies exploitability of shell `-c` and enterpriseSkip paths — worth stating in the report's threat model.
- Fanout says tabUrlCache fallback is "Map 插入序" — directionally right; core risk (token doesn't bind target tab) holds regardless.
- overlay-md fence placeholder `%%MD<n>%%` round-trip is fine, but the unit-test claim "no quote-breakthrough test" (x-tests-cluster) is consistent with what I see — P2 test items are the right guard for the P0 XSS fix.
- Report's healthy list is accurate per my checks (HMAC pairing, evaluate/osascript L2, worker HARD_DENY incl. shell/host at companion-dispatch spawn gate, cookie trusted_domains).

## Recalibrated counts

Critical: 0  High: 13–14 (14 confirmed in code; 13 if computer-host-04 downgrade is accepted)  Medium: 36–37  Overall grade: C+ (unchanged)

P0 plan (XSS, hide→release, overlay L0 + MCP-surface removal, truncated-save, estop socket) is correctly prioritized and all five are real.

VERDICT: APPROVE_WITH_NITS
