I have verified all four re-verification points against the actual repo state and run the relevant tests.

## Findings

**Verified ✅**
1. **LICENSE_DOOR_TEXT** (`companion/src/computer/model-license.ts:97-125`) — no `永久跳过`; explicitly advertises settings-page `复位许可拒绝` reset (lines 120 and 125). The `无需手改 config.json` phrasing correctly steers users to the D9 UI reset path.
2. **`validateWsMessage` reset_decline** (`companion/src/server.ts:4890-4898`) — `isReset = m.reset_decline === true || m.resetDecline === true` short-circuits the `accepted:boolean` requirement; `source:"settings"` still enforced. Handler belt (`companion/src/computer/model-handlers.ts:404-410`) mirrors the gate.
3. **`host_cli` security gates intact**:
   - L2 token required at dispatch (`server.ts:3766-3777`) and at confirm-center gate (`server.ts:805,820`).
   - God-mode never skips — `forceConfirm` list includes `host_cli` (L-CLI-9, `server.ts:1187`).
   - Q5 post-output taint forces L2 (`server.ts:1153-1165`, `cli-q5.ts`).
   - Platform / token-pattern / `.cli.` substring / entry-kind=cli / entry-enabled all checked at `server.ts:3778-3798`.
   - `prepareCliExecution` + `runCliExecFile` = structured argv execFile (no shell).
4. **No new blockers.**

**Test execution** (`npx tsx --test`):
- `computer-model-license.test.ts` — 3/3 pass (incl. `LICENSE_DOOR_TEXT.includes("永久跳过"), false`).
- `apps-cli-phase2.test.ts` + `computer-model-handlers.test.ts` — 20/20 pass, including `license_response reset_decline clears permanent skip` and `validateWsMessage: license_response reset_decline without accepted is valid`.

**Nits (non-blocking)**
- **`companion/src/computer/model-handlers.ts:13`** — file-header comment still reads `已拒绝 → LICENSE_DECLINED（跳过，无 UI 复位）`. Stale: D9 added a UI reset path (the door text and the same file's line 336 advertise it). Code at `model-handlers.ts:406-410` is correct; only the header comment was missed in the r4 sweep alongside `config.ts:114` and `model-state-messages.ts:214` (both of which were updated).
- **Patch file pollution** — `cli-qwen-p0-r4-diff-…patch` is 11310 lines because it bundles the cumulative `007130b→HEAD` diff plus ~8200 lines of unrelated prior-batch artifacts (`browser-download-p1-*`, `scratch/_decode_git_obj.mjs`) that were untracked at base. Process/meta bug in patch generation; does not affect review outcome, but forces reviewers to grep rather than read.
- **ADR-020 capability declaration** — implementer prompt has no explicit `Surface/Compose/Autonomy/Trust/Channel` block. Acceptable here because r4 itself is text/comment/test only and the capability was declared in the already-reviewed antecedent commits (4097a0c/348e75b/45cf155); flagging only as a process reminder for future batches that touch tools/gates.

VERDICT: APPROVE_WITH_NITS
