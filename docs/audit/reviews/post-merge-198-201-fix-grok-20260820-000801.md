I'll follow the dual-review prompt exactly: read the review brief, synthesis, and patch first, then attack the fixes against the hostile focus list.The brief is loaded. Next I’ll read the synthesis, then the patch, then the full files in scope.The synthesis is in. I'll finish the patch and then read the full source files the prompt lists.Patch is complete. Next I’ll read the full source files and the design spec so I can replay the original attacks.Full files next: voice adapter, STT abort, design §6.3, and the probe/config callers.I’ll replay the original attacks in Node, then run the targeted test suites.GROK dual review of uncommitted post-merge-198-201 fixes vs `2faaefa`. Read synthesis + patch + full files. Replayed the listed attacks against live modules on this Windows host. Targeted tests: chrome-extension 46/46 pass (including rewritten voice retry); companion file-url / llm-endpoint / probe-cache / allow-dir / voice-stt / config.test-echo / security-thread all pass.

## Original attacks (replay)

| Attack | Result |
|---|---|
| `file:///C:Windows/…` and `file://localhost/C:Windows/…` | `[executed]` `parseLocalFileUrl` → `{ok:false, kind:"invalid"}`. Admission never reaches confirm. `file:///C:/Windows/System32` parses then `assertFileOpenOfferable` cages. |
| Junction chain `j1→j2→C:\Windows`, missing leaf + existing `notepad.exe` | `[executed]` both caged. Isolated-home junction to `C:\Windows\Temp\…` also caged. |
| `[::ffff:169.254.169.254]`, `[fd00:ec2::254]`, `[fe90::1]` | `[executed]` all `Cloud-metadata / link-local hosts are not allowed`. Hex-serialized mapped form, uncompressed `fd00:0ec2:…:0254`, and ECS `::ffff:169.254.170.2` also blocked. Zone-id URLs (`fe80::1%eth0`) are `Invalid URL` (fail-closed). |
| Classic `resource_conflict` → per-chunk `session_unknown` + abort ACK in the 250ms window | `[executed]` `classic: resource_conflict retries once with -r1 sessionId` + abort-during-backoff (faithful fake). Adapter does not lock; follow-up dictation works. Companion V2 tests: stale-sid abort frees slot; late `end()` does not drop the retry bind. |

## Attack the fixes

**Retry-sid swap** — does not open a new kill window. Abort ACK echoes the *request* sid (`stt-handlers.ts:319-330`); after swap `onWs` drops non-current sids (`local-stt-adapter.ts:227`). `reset()` bumps `loopGen` so a killed sleeper cannot upload. `ensureSub()` before retry start. `stopChainInFlight` is set synchronously before the async stop chain.

**Peer-level abort** — `peerId` is still the ownership boundary (`stt-session-service.ts:498-500`). Other-peer abort stays `peer_mismatch` (`[executed]` existing test). Same-peer reclaim of the max-1 slot is the intended V2 behavior, not cross-user kill. `dropBound` is identity-guarded (`this.bound === bound`) so a late aborted infer cannot drop a newer retry bind.

**`normalizeIpLiteral` throw/fail-open** — never threw on garbage / double-`::` / empty / zone-id (`[executed]` → `null`). settings-web treats `null` as blocked. LLM URL path fail-closes invalid URLs at `new URL()`.

**Probe-cache lock-step** — panel and companion `normalizeProbeUrl`/`normalizeProbeModel` are the same algorithm. `[executed]` companion matrix: `:80`/`:443` default-port, scheme/host case-fold, path case preserved (`/V1` ≠ `/v1`), IPv6 host, userinfo. Unkeyed `detected` is ignored on the panel (`vision-reuse-logic.ts:159-169`). `config.test` echoes tested `{base_url, model_name}` (`config.ts:345-349`). App.tsx both `resolveNativeVision` sites pass `baseUrl`.

**§6.3 / `lstat.isFile()`** — design doc has **no §6.3**. Residual §6.3 in the strawman is “home 内任意非敏感文件”. Regular files still offer; missing leaves still L2 (`[executed]`). Directories refused (L6, intended). That does **not** break D1 (`~/Downloads/*.pdf`). In-home *file* symlinks are extra-strict vs §2.2 realpath-then-cage (could not even create a file symlink here: EPERM).

**Tests vs pre-fix** — F1 / IPv6-literal / case-preserving probe / L4 `files.example.com` / faithful voice fake would fail on pre-fix code. They are not tautologies.

**Trust monotonicity** — no new auto-approve path, persisted grant, or whitelist write. Sensitive prefixes extended (more blocking). Probe cache is session-only, never `config.json`. `file:` still cannot skip L2 via `auto_approve_dangerous`.

---

## Findings

1. **`companion/src/security.ts:225-230` (and `parseIpv6Groups` dotted-tail at `:202-206`) — medium / residual**  
   The new parser extracts a dotted-quad tail, then `mappedV4FromGroups` only reduces `::ffff:`. `[executed]`:
   - `http://[::169.254.169.254]/v1` → **allowed** (`hostname` serializes to `[::a9fe:a9fe]`)
   - `http://[64:ff9b::169.254.169.254]/v1` → **allowed** (NAT64)
   On this Windows host `http.request` to `[::169.254.169.254]` produced `ECONNRESET` (stack did attempt a connection), not `Invalid URL`. Original S1 literals are closed; this is the same class synthesis parked as NAT64/6to4 follow-up. Not a new grant, but the new reducer is incomplete: it already has the IPv4 tail and then ignores it unless group[5] is `0xffff`.

2. **`companion/src/tool/file-url-admission.ts:219-247` — low**  
   Dangling junction: `existsSync` is false after the target is deleted, so the missing-file ancestor walk climbs to the in-home parent and **OFFERs** (`[executed]` `dangling\x.pdf` and the dangling node itself). `lstat` still sees a symlink. Chrome open fails unless the junction is repaired (TOCTOU). Matches the synthesis leftover; not a live-junction escape (live chains to `C:\Windows` caged).

3. **`companion/src/tool/file-url-admission.ts:233` — low / product**  
   `lstatSync(resolved).isFile()` refuses in-home file symlinks that §2.2 realpath-then-cage would allow. Fail-closed, copy still does not mention symlink. D1 regular files unaffected.

4. **`companion/src/llm/likely-multimodal.ts:43` — low / drift**  
   Companion `resolveNativeVision` still honors unkeyed `opts.detected === true`. Panel never does. Production callers (`message-router.ts:736`, `adapter.ts:527`, `visionConfigForAnalyze`) do not pass `detected`. Test-only today; a future caller wiring persisted `native_vision_detected` would re-open M1.

5. **`companion/src/settings-web.ts:154-158` — low / copy**  
   DNS failure is correctly fail-closed, but the thrown copy is still “cloud-metadata / link-local”. Unresolvable intranet names get SSRF wording.

---

Nits not blocking: duplicated probe normalizers (currently lock-step); `vision-pipeline.test.ts` proto-mock missed a live LM Studio on `:1234` under `tsx --test` (harness, not the L2 key change — key is `hashBase64(\`${base_url} ${model_name} ${image.base64}\`)` `[inspected]`).

VERDICT: APPROVE_WITH_NITS
