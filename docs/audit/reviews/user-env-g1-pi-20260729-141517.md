All verifications complete. Here is my final review:

---

## Review: ADR-019 Gate1 (Companion PR1) — Commit `aa8924e`

### 1. user-env.ts storage 0o600 atomic; denylist real; CMSPARK_* banned; empty≠delete

**PASS**. `atomicWriteJSON` in `io.ts` (line 16) uses temp-file write + rename + explicit `chmodSync(f, 0o600)`. Denylist contains real scanned keys: `DEEPSEEK_API_KEY` (read in `config.ts:188`), `APPDATA`/`LOCALAPPDATA`/`USERPROFILE`/`SystemRoot`/`windir` (used in `mcp/transport.ts`, `apps/`, `host-use/win/`, `crash-handlers.ts`), `NVM_DIR` (`transport.ts:92`), `WAYLAND_DISPLAY` (`tray/readline-tray.ts:21`), plus baseline dangerous loader keys. `CMSPARK_*` prefix-banned via `isCmsparkPrefixKey` (`user-env.ts:100`). Empty string is a legal value (not delete) — `setUserEnvVars` stores it, `deleteUserEnvKeys` deletes it. Both tested.

### 2. buildUserEnvPublic only outbound; no plaintext on list/updated

**PASS**. `buildUserEnvPublic` only returns `{name, masked: "***"}` — no `value` field. Both `user_env.list` (line 966) and `user_env.updated` broadcast (lines 983, 1002) go through `buildUserEnvPublic`. No plaintext values in any WS outbound path.

### 3. set path redacts before logger/audit

**PASS**. `message-router.ts:975-978` redacts values via `redactUserEnvVarsForLog()` BEFORE the `logger.info()` call. `shell.ts` audit log only records `type/thread_id/cmd_len/exit_code` — zero env fields.

### 4. shell.ts + mcp/transport.ts merge order correct

**PASS**. 

- **shell.ts** `buildChildEnv()`: `{...process.env, ...getUserEnvVars(), CMSPARK_SHELL: "1"}` → user overrides process, forced key wins. Tested with hand-edited file tampering (line 237-251).
- **mcp/transport.ts** `createTransport()`: `{...process.env, ...getUserEnvVars()}` → `env.PATH = buildSpawnPath()` → `Object.assign(env, config.env)` → `if (config.env.PATH) env.PATH = config.env.PATH`. Server.env wins over user_env; per-server PATH overrides buildSpawnPath. Tested with `SHARED` key (line 263-269).

### 5. tests cover denylist, redact, corrupt degrade, `***` ignore

**PASS**. All covered:
- Denylist: "rejects denylist keys", "rejects all CMSPARK_* prefix keys"
- Redact: "redactUserEnvVarsForLog masks all values (R1)"
- Corrupt degrade: "getUserEnvVars returns {} on corrupt JSON (S9)", "returns {} on invalid schema"
- `***` ignore: "value *** is ignored (unchanged)"
- 18/18 tests pass. Regression: 27 pass / 1 skip (Windows-only) / 0 fail.

### 6. no secrets in LLM path

**PASS**. `getUserEnvVars()` imported ONLY in `capability/shell.ts` (for `buildChildEnv` → spawn env) and `mcp/transport.ts` (for stdio env). Zero imports in `llm/`, `bridge/`, `skill-engine/`, `orchestrator/`, system-prompt generation, or tool-definition paths. User env vars never enter LLM context, system prompts, or tool descriptions.

### ADR §12.1 R1-R7 mandatory requirements: ALL SATISFIED

| ID | Requirement | Status |
|----|-------------|--------|
| R1 | set redacts before logger/audit | ✅ `redactUserEnvVarsForLog` before `logger.info` |
| R2 | Outbound ONLY via `buildUserEnvPublic()` | ✅ All WS paths use it; no raw `UserEnvFile` broadcast |
| R3 | Denylist from code scan, no fabricated entries | ✅ `DEEPSEEK_API_KEY` + Windows keys + baseline |
| R4 | osascript_eval invariant preserved | ✅ Not modified by this PR |
| R5 | argv leakage documented as accepted risk | ✅ In ADR §8 |
| R6 | Empty string ≠ delete | ✅ Implemented + tested |
| R7 | getUserEnvVars corrupt → {} degrade (S9) | ✅ Implemented + tested |

---

### Nits (non-blocking)

1. **`companion/src/user-env.ts:93`** — `DEEPSEEK_API_KEY` in denylist: the comment says "From companion process.env scan" but doesn't explain *why* blocking an LLM API key in child-process env is desirable. The implicit rationale (prevent leaking LLM keys to shell/MCP processes) is sound, but a one-line comment would aid future readers.

2. **`companion/src/message-router.ts:977,979`** — `vars as Record<string, unknown>` is cast twice in the `user_env.set` handler. A single type-narrowed binding (`const varsObj = vars as Record<string, unknown>`) after the validation guard at line 971 would be cleaner.

3. **`companion/src/mcp/transport.ts:144-146`** — Comment says "server.env highest among optional layers; PATH override remains verbatim." The actual merge chain is `process → user_env → buildSpawnPath → Object.assign(server.env) → config.env.PATH override`. The comment is correct but could clarify that `config.env.PATH` explicitly overrides `buildSpawnPath()`.

---

VERDICT: APPROVE_WITH_NITS
