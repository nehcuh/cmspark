All checks pass. Let me confirm my findings:

**Verified against ADR-019 §12.1 R1–R7 / S1–S9 and the actual code:**

1. **0o600 atomic storage** ✓ — `user-env.ts:220` calls `atomicWriteJSON(path, toWrite, 0o600)`; `io.ts:16-30` writes temp file with `mode`, renames, then re-chmods.
2. **Denylist is real (R3)** ✓ — Cross-checked `process.env.*` scan; every non-CMSPARK env var the codebase reads is in `USER_ENV_DENYLIST` (`DEEPSEEK_API_KEY`, `USERPROFILE`, `LOCALAPPDATA`, `APPDATA`, `SystemRoot`, `windir`, `ProgramFiles`, `ProgramData`, `NODE_ENV`, `NVM_DIR`, `WAYLAND_DISPLAY`, plus OS/loader baseline). `ProgramFiles(x86)` is unreachable anyway — its `(` fails the POSIX regex. `WS_SECRET` correctly omitted (it's a file, not env).
3. **CMSPARK_* prefix ban** ✓ — `isCmsparkPrefixKey` + `isReservedUserEnvKey` at lines 104–110.
4. **Empty ≠ delete (R6)** ✓ — `setUserEnvVars` happily stores `""`; deletion only via `deleteUserEnvKeys`.
5. **Outbound never leaks plaintext (R2/S1/S8)** ✓ — `buildUserEnvPublic` only emits `{name, masked:"***"}`; message-router `user_env.list` uses it; `user_env.updated` broadcast and direct response are both `{type, ...r.public}` with no values.
6. **Set path redacts before logger (R1/S3)** ✓ — `message-router.ts:975-978` calls `redactUserEnvVarsForLog(vars)` before `logger.info`. No `appendCapabilityAudit` call on this path (and none is needed — it'd be redundant with the redacted logger).
7. **shell.ts merge order** ✓ — `buildChildEnv()` = `{...process.env, ...getUserEnvVars(), CMSPARK_SHELL:"1"}`; CMSPARK_SHELL forced last so user cannot override even via hand-edited file (test at `user-env.test.ts:213-229` proves it).
8. **mcp/transport.ts merge order** ✓ — `process.env → user_env → buildSpawnPath (PATH) → config.env` (lines 146-155). Per-server env still wins, as ADR §6.2 specifies.
9. **Tests cover the matrix** ✓ — 18/18 pass; covers denylist (PATH/LD_PRELOAD/HOME/NODE_OPTIONS/PYTHONPATH), CMSPARK_* prefix, INVALID_KEY, VALUE_TOO_LONG, TOO_MANY_KEYS, empty≠delete, `***` ignore, redact, 0o600 atomic, corrupt-JSON → `{}` (S9), invalid-schema → `{}`, file-tamper force-override, MCP stdio precedence, live `printenv` integration.
10. **No secrets in LLM path** ✓ — grep confirms no `user-env` / `getUserEnvVars` / `loadUserEnv` references in `llm/adapter.ts`, `llm/llm-extract.ts`, `threads/thread-manager.ts`, `orchestrator/spawn.ts`.

**Nits (non-blocking):**

- `message-router.ts:983-989 / 1002-1008` — response to `user_env.set` / `user_env.delete` carries `type: "user_env.updated"` (same shape as the broadcast). This is intentional (single snapshot shape across response + broadcast) but slightly unusual for request/response; extension UI in PR-2 must dispatch on `user_env.updated` rather than expecting a distinct ack type.
- `user-env.ts:43-52` — `UserEnvPublic.keys[].masked` is always literal `"***"`. Field name `masked` suggests it could vary; could be inlined as a constant marker, but harmless.

No blocking issues. Tests green on real code; security requirements satisfied; no LLM/WS plaintext leak path found.

VERDICT: APPROVE_WITH_NITS
