I have enough evidence to finalize my review. Let me write it up now.

---

## Review summary

I verified the dual-review fixes and re-inspected the real code. The CLI Phase-2 / Qwen3-VL P0 implementation itself is correct on every gate listed in the prompt (verified `cli-exec.ts:94-105, 148-158`, `cli-manifest.ts:55-155, 163-191`, `security-policy.ts:61-65`, `server.ts:1153-1165, 1181-1187, 3764-3860`, `cli-q5.ts` + `adapter.ts:268-273`, `orchestrator/constants.ts:24`, `handlers.ts:219-223, 364-366`, `add-flow.ts:341-344`, `model-handlers.ts:347-376`, `session-trust.ts:91-96` + `server.ts:1003-1005`, `qwen-vl-coords.ts:8-23` + `qwen-vl-worker.py:65-76`). The companion test suite is green (2161/2161 pass). However the chrome-extension test suite is NOT green, and the previous REJECT pattern (changed copy + stale test) recurs in a third place the prior review did not inspect.

### Blocking issues

**1. `chrome-extension/tests/model-switch-logic.test.ts:130-136` — stale test, regressed by `4097a0c` and not fixed by `348e75b`**

The feat commit changed `MODEL_SWITCH_COPY.licenseDeclinedNotice` (chrome-extension/src/sidepanel/components/model-switch-logic.ts:229-231) from "你已拒绝实验层许可证——本层**永久跳过**…" to "…本层**已跳过**…可在设置页「实验层」重新打开许可流程以复位拒绝…". The fix commit (`348e75b`) propagated the same wording change into the companion `model-state-messages.ts:250-253`, but neither commit updated the test that asserts the old text:

```
test("禁用原因:declined → 永久跳过提示;其余 → null", () => {
  const r = modelSwitchDisabledReason(modelState({ modelLicenseDeclined: true }))
  assert.ok(r !== null)
  assert.ok(r!.includes("永久跳过"))   // ← stale
  …
})
```

`node --test .test-dist/tests/model-switch-logic.test.js` → 1 fail:
```
✖ 禁用原因:declined → 永久跳过提示;其余 → null
  AssertionError: ok(r.includes("永久跳过"))
```

Extension summary: 382 pass, **1 fail**. This violates the master-plan stop gate F2-K ("`npm --prefix companion test` + extension 相关测试绿") in the exact way the prior REJECT flagged for `apps-add-flow.test.ts:166` and `apps-config.test.ts:139` — production copy changed, the test verifying the old copy was not updated, the implementer ran only `npm --prefix companion test` and missed the extension half of F2-K.

**2. User-visible copy contradiction: pre-decline promise says "永久跳过", post-decline notice says "可复位"**

The fix commit fixed `LICENSE_DECLINED` (model-handlers.ts:336) and `licenseDeclinedNotice` (model-state-messages.ts:250-253 + extension mirror 229-231) to advertise the new settings reset path, but left the **pre-decline** user-facing copy still promising permanent skip:

- `companion/src/computer/model-handlers.ts:344` (the `license_required.notice` rendered by `SettingsSlideout.tsx:1049`):
  > "阅读并接受许可证与免责声明后可开启实验层；拒绝则本实验层**永久跳过**，其余定位层不受影响。"
- `companion/src/computer/model-state-messages.ts:239` and `chrome-extension/src/sidepanel/components/model-switch-logic.ts:218` (`licenseDoorHint`, rendered at `SettingsSlideout.tsx:1027`):
  > "首次开启需阅读并接受模型许可证与免责声明；拒绝则本实验层**永久跳过**，其余定位层不受影响。"

After the user clicks "拒绝(跳过本层)" they see `licenseDeclinedNotice` saying it's resettable — directly contradicting what they were just told. The fix is incomplete: the door copy still promises a permanence the implementation no longer honors. Also `model-handlers.ts:13, 19, 332` code comments still say "永久跳过 / 无 UI 复位".

### Non-blocking nits

- **Commit-message over-claiming**: `348e75b` body says "Add ADR-020 capability declaration to master plan" but `docs/superpowers/plans/2026-08-02-cli-phase2-qwen-vl-master-plan.md` contains no Surface / L2-classes / Compose / Autonomy / Trust / Channel block (`grep -ni "surface\|capabilit\|adr-020\|autonomy"` returns nothing). The capability content is present in spirit (F1-C/E/J, host-and-apps.md axis table), so this is process-level, but the commit message claims work that wasn't done.
- `companion/src/apps/handlers.ts:364-367` — indentation drift inside `apps.set_policy` (the `if (entry.kind === "cli"…)` and `const cap = …` lines are flush-left inside a case block). Carried over from the prior review.
- `companion/tests/apps-add-flow.test.ts:169` — `"C:\tools\run.bat"` (single backslashes) is malformed: JS parses it as `C:<TAB>ools<CR>un.bat`. Should be `"C:\\tools\\run.bat"`. Currently harmless because the Win branch only checks the `.exe$` tail, but the path content is wrong.
- No explicit test for the `apps.set_policy` L-CLI-1 downgrade-to-ai path (the `apps.add` path is tested; `set_policy` is not). Carried over from prior review.

### ADR-020 capability checklist

- Axes fit: ✅ `host_cli` is correctly placed on the Surface axis (L2 host-side, structured argv, no shell), not a "middle agent".
- Pack-first: ✅ uses Apps config + tool catalog + skill-style system-prompt index; no new Pack surface.
- Confirm dialects: ✅ reuses the existing `SecurityPolicy` L2 token (no new confirmation family); `capabilityForceConfirm` already includes `host_cli` so god-mode cannot skip.
- Trust monotonicity: ✅ `host_cli` L2 sits above `host_app` L0, and Q5 taint escalates `host_cli`/`host_app` to L2 after CLI output until the next real user message.
- originWs: existing SecurityPolicy binding is thread-bound (consistent with `host_app` precedent); no new `request(` was added in this diff that would regress origin tracking.
- Experimental layers: Qwen3-VL retains per-task re-L2 (`PROMPT_ALWAYS_TAGS` still has `computer.experimental_suggestion`); the config-level `modelEnabled` block in `g1InitialSkipEligible` is correctly applied at `server.ts:1003-1005`.

### Verdict

The blocking issues are the same shape as the previous REJECT (changed production behavior, stale test verifying the old behavior) plus an incomplete copy fix that surfaces a contradictory promise to the user. Both must be addressed before F2-K can pass.

VERDICT: REJECT
