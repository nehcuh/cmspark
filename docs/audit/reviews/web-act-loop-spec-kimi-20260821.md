kimi version 0.36.1
• UserPromptSubmit hook

  {}

• The user wants an independent re-review of the LOCKED spec before implementation. This is a code review of a spec document against the actual codebase. I should verify the claims in the spec against the actual code, and check whether the three REJECT must-locks were actually folded in correctly.

  This is a substantial review task. I need to check key claims against the codebase:
  - adapter.ts: MAX_SAME_TOOL_RECOVERABLE_FAILURES, recoverableFailureCounts
  - security.ts classifyError
  - browser-bridge.ts: ensureAttached, safeEvaluate, evaluate, click, pressKey
  - l2-admission.ts
  - find-element-by-text.ts
  - shell.ts spawn behavior
  - catalog definitions (press_key modifiers Meta=8 Shift=4?)
  - fill_form Ctrl behavior

  The spec claims MUST-LOCK items were folded in. My job: verify the spec is internally consistent, complete, and implementable; check that the folds actually address the REJECTs; check code claims (e.g., catalog Meta=8 Shift=4, fill_form ctrlKey:true without VK, find-element-by-text.ts exists, etc.).

  This is a big review. I should delegate exploration to subagents to verify specific code claims, or do it myself. Given the volume, I could spawn parallel explore agents to verify specific claims. But the spec is also self-contained for logical review. Let me verify the critical code-dependent claims with a few targeted greps/reads myself, or delegate.

  Key verifiable claims:
  1. `find-element-by-text.ts` exists (for the "one finder, parameterized" claim).
  2. Catalog press_key modifiers says Meta=8 Shift=4 (claim of inconsistency with CDP).
  3. fill_form Ctrl half: ctrlKey:true without windowsVirtualKeyCode.
  4. classifyError exists with default non_recoverable.
  5. a7ubt9 trace numbers (81 osascript, 78 unique hashes, max repeat 2) — trace file location?
  6. shell.ts spawn shell:true cmd on win32.
  7. l2-admission L2 tools list.

  I'll delegate to parallel explore agents to verify these claims, then do the logical review myself. Actually, let me do targeted checks myself — maybe 2-3 explore agents for speed. The review's core value is in logical analysis; code verification supports it.

  Let me think about what an independent rereview should assess:

  1. **Fold completeness**: Do the MUST-LOCKs from the three REJECTs actually appear in the spec?
     - Locator: 组合 C ✓ (§4.2), one finder ✓ (§4.1), SYNTAX_ERR only ✓ (§4.5), type focus exemption ✓ (§4.2), fill_form item schema ✓ (§4.2, §7), Trust freeze ✓ (§4.7). DoD items: trim-empty → SELECTOR_OR_TEXT_REQUIRED ✓ (DoD 2), type focus ✓ (DoD 3), fill_form field fail ✓ (DoD 11), text-exclusive ✓ (DoD 4), hitAttr-only diff ✓ (DoD 5), fallback attr ✓ (DoD 6), INVALID_SELECTOR ✓ (DoD 7), catalog ✓ (DoD 12). Nits: case-sensitive exact=false in catalog copy — is it in spec? The nits list says "non-blocking once MUST-LOCK lands". Spec §7 catalog descriptions — doesn't mention case sensitivity. Non-blocking, fine.
     - One nit item: "AMBIGUOUS includes ≤5 matches + user_hint_zh + suggested_action: disambiguate_selector_or_exact_text" — spec §4.3 says "≤5 matches" but does it include user_hint_zh and suggested_action? Spec §4.3 table only says "≤5 matches". The must-lock #9 said AMBIGUOUS includes ≤5 matches + user_hint_zh + suggested_action. Spec doesn't mention user_hint_zh or suggested_action for AMBIGUOUS. Hmm — partial fold. That's a potential nit or miss.
     - Also must-lock #9: `error` string `CODE: …` — spec §9 says "error 以 CODE: 开头" ✓.
     
  2. **Budget**: The budget REJECT's core falsification: 8-cap on identical hash never trips (78 unique hashes, max repeat 2). Spec changed to dual counters: A) same script 3, B) volume 24 per origin. Does volume cap address it? 81 osascript same origin (x.com presumably) → 24 caps it ✓. But wait — osascript without tabId: "osascript 无 tabId 时用其 url 解析 origin/tab" — the a7ubt9 osascripts targeted Chrome via AppleScript; do they have a url? The spec says "osascript url 解析出的 origin" — 5.3 B says origin = tabs.get origin or osascript url parsed origin. OK. But what if osascript has no url either (e.g., `tell application "Google Chrome" to execute javascript` — the url isn't in the script necessarily)? The payload heuristic includes `osascript` keyword — but the volume key needs an origin. If origin unparseable → what key? Under-specified. Potential hole: osascript targeting front document without URL in payload → origin unknown → volume cap can't key. Could fall back to a global family cap? Not specified. That's a real finding.
     - Also `exprHash = sha256[:12](规范化 command 或 expression)` — "规范化" (normalization) is undefined. What normalization? Whitespace? If undefined, implementers will differ. Nit/finding.
     - Budget A: 3 successes → LOOP_CAPPED on (family=dom_script, exprHash, tabId). Note a7ubt9 max repeat was 2, so cap A at 3 wouldn't fire there — spec acknowledges ("a7ubt9 单 hash 最多重复 2，这条打的是「同一句刷发布」"). Fine.
     - "3 次成功 → 该 key DOM_SCRIPT_LOOP_CAPPED" — does the 3rd succeed and the 4th get capped? DoD 13 says "同 hash+tab 成功 3 次后第 4 次 LOOP_CAPPED" — consistent.
     - Volume: 24 次成功 → cap. DoD 14: "成功 24 次后" — consistent-ish. But is 24 motivated? a7ubt9 had 81 osascript; 24 would stop at 24. Better than nothing but arbitrary — spec doesn't derive. Acceptable as machine gate.
     - Hard refusal after cap: "封顶后该家族对该 origin 硬拒成功路径（再调用仍 false）" ✓ addresses hop-bait concern. And suggested_action=stop_or_change_task, forbidden to suggest host_computer ✓.
     - Payload heuristic now includes document.querySelector, el.click(, chrome.debugger, --remote-debugging-port, Runtime.evaluate, osascript/osacript. Explicit non-hits: Start-Process chrome, Get-Process chrome, tasklist, opening browser ✓ (addresses budget MUST-FIX 2&3, win32 attack 2).
     - cscript: budget MUST-FIX 1 said "Restore direction's cscript". Spec doesn't name cscript explicitly, but payload-based heuristic covers `cscript //nologo inject.js` only if payload contains those strings. A .js file executed by cscript — the file contents aren't in the command line! `cscript //nologo inject.js` payload contains none of the hit strings. So cscript with file-backed JS still misses. Similarly `osascript /tmp/inject.scpt` — file-backed, no payload strings. Budget REJECT explicitly listed "file-backed osascript" as under-counted. Spec §5.3 doesn't address file-backed scripts. That's a real residual hole — worth flagging. Though DoD 15 tests cmd/cscript containing the strings — so spec tests the string case only. This is a genuine gap: file-backed script bypasses the family counter. Severity: this is a best-effort heuristic gate, not security boundary; but the budget REJECT named it and the spec didn't fold an answer. Finding.
     - linux: spec says shell heuristic — win32 REJECT MUST-FIX 4 said either name xdotool/ydotool or write "best-effort". Spec §5.4 linux: "同 win32。host_computer 在非 darwin/win32 硬拒；文案不得暗示有 CU". The payload heuristic is payload-based so xdotool typing into Chrome wouldn't contain DOM-inject strings → miss. Spec doesn't say "linux shell heuristic is best-effort". Minor.

  3. **Win32**: MUST-FIX 1: don't list evaluate as suggested_action for CDP_ATTACH_FAILED ✓ (§5.1 table + DoD 19). MUST-FIX 2: OS-split last-resort ✓ (§5.4). MUST-FIX 3: cap → hard-stop not recoverable hop ✓ （硬拒成功路径， stop_or_change_task). MUST-FIX 4: X.com CSP darwin-only ✓ (§5.4 "X.com CSP 退路仅 darwin"). Fill_form Ctrl真正生效 ✓ (§5.5, DoD 18).

  4. **Internal consistency / implementability issues to hunt**:
     - §5.1 classification applies to "click / type / hover / press_key / get_element_info / evaluate" — what about fill_form, select_option, drag_and_drop, browser_download? Presumably they share the path; not listed. Minor gap — attach failure for fill_form would be...? Also download isn't in the list.
     - §5.1: "scheme http(s) 且 debugger+scripting 都失败 → CDP_ATTACH_FAILED". But what if only debugger fails and scripting succeeds — which path is used? Under-specified but arguably fine (whichever transport works).
     - WRONG_ORIGIN for chrome-extension scheme: but browser_download on a PDF viewer (chrome-extension://) — download needs to work there? W1 doesn't cover download attach? browser_download uses CDP on the page; a PDF in chrome-extension:// PDF viewer would get WRONG_ORIGIN under this rule for click/type etc. Download not in the list — deliberate? Unclear. a7ubt9 was a PDF viewer case. The spec lists only click/type/hover/press_key/get_element_info/evaluate. Hmm, but §5.1's table is about attach/scripting failure classification for those tools. OK.
     - §5.2 evaluate honesty: probe `1+1===2` — adds an extra evaluate round-trip per empty-completion statement; fine. "语句有完成值" — detection of completion value: spec says if result is non-null? Actually "语句有完成值 | success:true, result 为该值" — what about completion value that is legitimately `null` or `undefined`? `document.title` returning null on dead world vs legit null (e.g., `localStorage.getItem('x')` returns null genuinely). Probe distinguishes: if probe 1+1===2 true and completion null → empty_completion? But spec's third row is "语言上无完成值（void 0 / 只有副作用）且探针为 true → empty_completion". What about a genuine `null` completion value (e.g. `localStorage.getItem`)? Is that "有完成值" (result null) → row 1 success:true result:null — which is exactly the a7ubt9 shape that misled. Hmm. Row 1 vs row 3 distinction: "有完成值" vs "无完成值" — a legit null completion is ambiguous. The probe helps: if probe succeeds and value is null → could be either genuine null or no completion value. Spec doesn't distinguish null completion value from no-completion. Actually `Runtime.evaluate` returns `result.subtype === "null"` for both `null` and `undefined`. CDP doesn't distinguish "no completion value" — a statement like `void 0` evaluates to undefined; `foo()` returning undefined also undefined. So spec's classification "语言上无完成值" can't be determined from CDP result alone. Implementability concern: how does implementation decide "语句有完成值" vs "无完成值"? Possibly: result null + probe true → treat as empty_completion regardless. Then a genuine `localStorage.getItem('missing')` → null → labeled empty_completion — misleading but recoverable? It's honesty issue in reverse. This is a real spec ambiguity worth flagging as nit/finding.
     - §5.5 press_key: "catalog 今日写 Meta=8 Shift=4 — 与 CDP 不一致" — need to verify against code. Wave-1: press_key 走 DOM key 布尔 — "已有 ctrlKey/metaKey" — verify. And补 windowsVirtualKeyCode "(scroll PageDown 已做）" — verify.
     - §4.2 fill_form interactiveSel expansion claim "否则 text 会点到 label" — verify current interactiveSel.
     - §4.6 hit fallback "同一次调用" — download existing behavior presumably.
     - §3 waves: W1+W3′+W4+W5 本波. Spec sections cover 4 (W1), 5 (W3′), 6 (W4), 7 (W5) ✓.
     - DoD 8: `https://zhihu.com` + attach 失败 → CDP_ATTACH_FAILED not ELEMENT_NOT_FOUND ✓ consistent with 5.1.
     - DoD 20: new codes in classifyError recoverable list — §7 lists 9 codes; §9 error code list has 11 codes including EVAL_THROWN and ELEMENT_NOT_FOUND. Are EVAL_THROWN / ELEMENT_NOT_FOUND already in classifyError? Spec §7's must-add list omits ELEMENT_NOT_FOUND and EVAL_THROWN — presumably existing. Verify.
     - §5.3: "不要提高 MAX_SAME_TOOL_RECOVERABLE_FAILURES=3" ✓.
     - Contradiction check: budget cap keys are "线程持久" — thread record, not chatCreate local Map ✓ addresses budget finding.
     - §5.3 A: key includes tabId; osascript no tabId → use url parsed "origin/tab" — circular? "用其 url 解析 origin/tab" — tab can't be parsed from url. Sloppy wording. For osascript targeting front tab of Chrome, tabId unknown → key A for osascript is (exprHash, ???). Under-specified. Actually the osascripts in a7ubt9 were `tell application "Google Chrome" to execute active tab of front window javascript ...` — no tabId. Key A then can't include tabId for osascript. Spec says "无 tabId 时用其 url 解析出的 origin/tab" — doesn't make sense for tab. Finding/nit.
     - Volume counter B keyed by origin; "换 origin 清零；换 tabId 但同 origin 不清" ✓ good.
     - 24 for volume: 81 stopped at 24 — fine.
     - §4.3: text poll 3s like selector ✓.
     - §4.2 drag_and_drop: from_text/to_text both empty on a side → SELECTOR_OR_TEXT_REQUIRED? Says "两侧独立 ELEMENT_*" — but missing-locator error for a side with both empty isn't specified. Minor.
     - §6 W4 type fallback: "禁止对非 INPUT/TEXTAREA 设 el.value"; contenteditable via insertText command or input event; failure TYPE_UNSUPPORTED_EDITOR. OK. What about select_option? In §4.4 liar-success list but not in W4 — select_option sets select value presumably fine (it IS a form element). OK.
     - §9 error codes list includes EVAL_THROWN — is EVAL_THROWN defined anywhere in the body? §5.2 doesn't mention thrown case explicitly ("语句有完成值" / dead world / empty completion). What about a statement that throws? Presumably EVAL_THROWN success:false. Not in §5.2 table! Gap: evaluate throwing case not specified in §5.2, though code exists in §9. Finding (minor).
     - DoD 16: "死世界探针失败 → 非 empty_completion" ✓.
     - §5.1: "file: HTML 不当 WRONG_ORIGIN" and PDF plugin residual recorded as known residual ✓ addresses budget's file: PDF concern honestly.
     - §7: architecture.md same PR ✓ (nit restated).
     - §8: tests parameterized platform win32 ✓ (win32 MUST-FIX 5-ish).
     - Trust: click stays off L2_GATE_TOOLS ✓.
     - One thing: §4.1 "Transport：现有 Runtime.evaluate / scriptingExecute。禁止走 evaluate/osascript_eval 工具" ✓ matches must-lock 6.
     - Must-lock 9 AMBIGUOUS: spec §4.3 says "≤5 matches" but omits `user_hint_zh` + `suggested_action: disambiguate_selector_or_exact_text`. Actually let me re-read §4.3: "≥2 | ELEMENT_AMBIGUOUS | 不点第一个；≤5 matches". The must-lock #9 explicitly required user_hint_zh and suggested_action in the AMBIGUOUS payload. Spec dropped those. Is that a blocking miss? The must-lock list was "supposed to be folded". §4.4 covers success:false+error_code generally. user_hint_zh — does the spec use user_hint_zh anywhere? Not mentioned. So the fold is incomplete on that point. I'd rate it a nit-to-minor: suggested_action for AMBIGUOUS is useful for LLM recovery; spec elsewhere uses suggested_action for other codes (§5.1, §5.3). Inconsistent: AMBIGUOUS lacks suggested_action while other codes have them. Worth flagging as a finding (probably nit-level or minor must).
     
  5. Also check: does spec address "AMBIGUOUS recoverable?" — §7 classifyError list includes ELEMENT_AMBIGUOUS ✓.

  6. a7ubt9 evaluate "26 calls, 26 unique" and returned {success:true, result:null} — §5.2 handles.

  Now, code verification: I should verify a handful of factual claims in the spec that implementation will rely on:
  - find-element-by-text.ts exists and is parameterizable (hitAttr).
  - catalog press_key modifiers Meta=8 Shift=4 (claim).
  - fill_form ctrlKey without VK.
  - interactiveSel current value.
  - pressKey bare attach, PageDown VK already done.
  - classifyError default non_recoverable and current list (do ELEMENT_NOT_FOUND / EVAL_THROWN exist?).
  - L2_GATE_TOOLS membership.
  - MAX_SAME_TOOL_RECOVERABLE_FAILURES=3.

  I can delegate this verification to an explore agent with a precise checklist. That keeps my context lean. Given the review quality depends on these, one thorough explore agent is right. Actually maybe two: one for chrome-extension side (browser-bridge, find-element-by-text, catalog), one for companion side (adapter, security classifyError, l2-admission, shell). They're independent — run in parallel.

  Also verify a7ubt9 trace claims? The trace location "轨迹： a7ubt9 darwin" — where? Maybe docs/audit. The budget REJECT already did that analysis; I can spot-check if the thread file is findable but it's less critical for the re-review since I'm reviewing the fold, not redoing the budget analysis. Skip; rely on budget REJECT's numbers which the spec itself cites.

  Let me launch two explore agents in parallel with precise checklists.

  Then produce the review with VERDICT. Based on my analysis so far, the spec folds the major must-locks; remaining issues are: (a) AMBIGUOUS payload missing user_hint_zh/suggested_action from must-lock #9; (b) file-backed script bypass of family heuristic (budget reject named it, spec silent); (c) §5.2 completion-value classification not implementable from CDP result alone (null vs undefined vs no-completion); (d) osascript tabId keying circular wording; (e) EVAL_THROWN case missing from §5.2 table; (f) drag_and_drop empty-locator side error unspecified; (g) linux heuristic best-effort note missing. These are mostly nits; (b) and (c) could be minor must-fix. I'd lean APPROVE_WITH_NITS if the code claims verify. If code claims fail (e.g., find-element-by-text.ts doesn't exist, or interactiveSel already expanded), that changes things.

  Let me dispatch the explore agents.

• I'll independently re-review the LOCKED spec. First, I'll verify the spec's factual claims against the actual codebase with two parallel explorations, then do the fold-completeness and consistency analysis.

• Both explorations are back. All spec factual claims verified as TRUE. Now let me do the actual independent review: fold completeness vs the three REJECTs, internal consistency, implementability, DoD coverage.

  Let me go through the must-locks systematically.

  ### LOCATOR REJECT must-locks (#6-10 + DoD additions):

  **#6 Trust freeze** — spec §4.1/§4.7: resolveLocator 只在扩展 IIFE; transport = existing Runtime.evaluate/scriptingExecute; needle JSON.stringify; no evaluate/osascript_eval tool; click stays off L2_GATE_TOOLS; IIFE may not click/innerHTML/eval user JS. ✓ Fully folded. Verified: L2_GATE_TOOLS doesn't contain click (l2-admission.ts:49-67). ✓

  **#7 Hit consumption** — spec §4.6: matches[0].{x,y} same-turn; fallback only `[data-cmspark-hit="1"]` same invocation; clear own namespace at IIFE start; LLM must not store hit attr. ✓ Folded. Note verified current behavior matches (download does exactly this with dl-hit).

  **#8 CSS first-match** — spec §4.3: "CSS querySelector 保持 first-match（遗产，写进 SoT）. W1 不把 CSS 升级成 querySelectorAll". ✓ Folded.

  **#9 Liar-success envelope** — spec §4.4: type/hover/fill_form/select_option/drag_and_drop/get_element_info → success:false + error_code. §9: error 以 CODE: 开头. BUT must-lock #9 also required: "AMBIGUOUS includes ≤5 matches + `user_hint_zh` + `suggested_action: disambiguate_selector_or_exact_text`". Spec §4.3 says only "≤5 matches" — **dropped `user_hint_zh` and `suggested_action`**. Partial fold. Is this blocking? The header says "locator / budget / win32 三路均为 REJECT → 下列 MUST-LOCK 已写入正文" — claiming all must-locks are in. This one is incompletely folded. The suggested_action matters for LLM self-recovery consistency (§5.1 and §5.3 give suggested_actions for other codes; AMBIGUOUS has none). I'd call this a nit-to-minor must-fix. Given DoD 20 requires all codes in classifyError recoverable, the LLM will get the error and retry — but without suggested_action it's weaker. I'd flag as a nit (small, one-line fix).

  **#10 DoD additions** — check each:
  - trim-empty both → `LOCATOR_REQUIRED` / chosen alias → DoD 2 with SELECTOR_OR_TEXT_REQUIRED ✓ (spec §4.2 explicitly chose the download name, good).
  - type with neither locator → focus success path → DoD 3 ✓.
  - fill_form field 2 fail → DoD 11 ✓.
  - both selector+text under C rule, "include a case that today's D would have silently clicked" → DoD 4 "构造「CSS 会点错、text 唯一」→ 不得点 CSS" ✓.
  - shared module: download IIFE and click IIFE differ only by hitAttr; test attribute names not equal → DoD 5 "download 与 click IIFE 仅 hitAttr 不同" — the "attribute names are not equal" test is implicit but DoD 6 covers the actual attribute strings. Mostly ✓.
  - fallback expression contains `data-cmspark-hit="1"` and not `dl-hit` → DoD 6 ✓.
  - invalid CSS `a[` → INVALID_SELECTOR; `a[href*="blog" i]` not asserted invalid → DoD 7 ✓.
  - catalog: click/type have `text`; required doesn't force selector; fill_form items don't force selector → DoD 12 ✓.

  All DoD additions folded. ✓

  Nits from locator review (non-blocking): case-sensitive exact=false in catalog copy — spec §7 says "每个 locator 消费者 description 含「text 或 selector」" but doesn't mention case-sensitivity copy. It was explicitly listed as a non-blocking nit, fine.

  One NEW issue from verification: the locator review's §12 answer #5 said "Fork is in §4.1. Parameterize hitAttr on one module" — spec §4.1 says "一个模块（现 find-element-by-text.ts 参数化）. hitAttr: click 族 data-cmspark-hit, download data-cmspark-dl-hit". Verified find-element-by-text.ts exists with `data-cmspark-dl-hit` hardcoded (line 13, 84, 86) — parameterization is a real but small change. ✓ consistent.

  ### BUDGET REJECT must-fixes:

  The budget REJECT's core falsifications and whether spec fixes them:

  1. **"MAX=8 on identical expr never trips"** → spec replaced with dual counters: A) same-hash 3, B) **volume 24 per origin**. The volume counter directly addresses the 78-unique-hash storm: 81 osascript on same origin → capped at 24. ✓ This is the correct shape change. The spec even documents the reasoning (§5.3 A note: "a7ubt9 单 hash 最多重复 2，这条打的是「同一句刷发布」"). Good.

  2. **Thread-persistent** → §5.3 "存线程记录（不是 chatCreate 局部 Map）。换线程清零" ✓. Verified: currently only per-chatCreate Map exists, so this is new work but clearly specified.

  3. **evaluate empty_completion falsified** → §5.2 with probe `1+1===2`: dead world → EVAL_DEAD_WORLD/CDP_ATTACH_FAILED; genuine empty completion + probe true → empty_completion. ✓ addresses "document.title returned {success:true, result:null}".
     - **Implementability concern**: how does implementation distinguish "语句有完成值" (row 1) from "语言上无完成值" (row 3) when the completion value is legitimately `null`/`undefined`? CDP `Runtime.evaluate` with returnByValue gives `result: {type:"undefined"}` for both `void 0` and a function returning undefined; `{type:"object",subtype:"null",value:null}` for both literal `null` and e.g. `localStorage.getItem('missing')`. You cannot distinguish "no completion value" from "completion value is undefined/null" from the CDP result alone. So row 1 vs row 3 for null/undefined values is not decidable as written. Practical implementation: probe first (or on null/undefined result), if probe dead → EVAL_DEAD_WORLD; if probe alive and result null/undefined → report `evaluate_kind: empty_completion` with result null. That means a genuine `localStorage.getItem('x') → null` would be labeled `empty_completion` — a small honesty blemish in the other direction. The spec doesn't resolve this. It's a spec ambiguity that an implementer will hit immediately. Worth flagging as a must-clarify nit. Not fatal — the safe reading (probe-gated) is obvious — but the spec's three-row table pretends a distinction CDP can't make.
     - Also missing row: statement **throws** → presumably `EVAL_THROWN` success:false (code exists in §9 but §5.2 table has no thrown row). safeEvaluate currently throws on exceptionDetails so the path exists; spec should say success:false + EVAL_THROWN. Minor gap.

  4. **WRONG_ORIGIN schemes** → §5.1 ✓ chrome-extension/chrome/edge/devtools. **file: PDF residual** → spec records as known residual explicitly ("W1 记录为已知残差，不在本波用子串猜 MIME") ✓ honest.

  5. **Shell heuristic** → budget MUST-FIX: restore cscript or DOM-inject predicate; fingerprint payload not interpreter brand; explicit allow for Start-Process/Get-Process/tasklist; linux best-effort note; DoD with parameterized win32.
     - Spec §5.3: payload-based hit list: `execute javascript`, `chrome.automation`, `Runtime.evaluate`, `document.querySelector`, `el.click(`, `chrome.debugger`, `--remote-debugging-port`, `osascript`/`osacript` (incl. `tell application "Google Chrome"`). Non-hits: `Start-Process chrome`, `Get-Process chrome`, `tasklist`, opening browser. ✓ Fixes the false-positive and the brand-matching inversion.
     - **Residual hole**: `cscript //nologo inject.js` / `wscript inject.js` / `mshta file.hta` / `osascript /tmp/x.scpt` — **file-backed scripts** where the payload strings live in a file, not the command line. Budget REJECT explicitly named "file-backed osascript" as under-counted. Spec's DoD 15 only tests "含 execute javascript / document.querySelector 的 cmd/cscript 计入" — inline strings. A cscript/osascript invocation pointing at a file carries no hit strings → misses family → unlimited. The spec neither closes this nor records it as a known residual (unlike the file: PDF case which IS recorded). This is an under-specification of a known-named attack. Severity: this is a best-effort anti-loop gate, not a security boundary — the L2 confirmation still gates each shell_exec — but under cruise (three-flag) confirmations are waived, and a7ubt9 was exactly a cruise storm. I'd flag this as a genuine finding: either add interpreter+file-extension heuristic (`cscript`/`wscript`/`mshta`/`osascript <file>` → count) or record as known residual like the PDF case. Lean: must-fix-lite or strong nit.
     - **linux**: budget MUST-FIX 4 said "either name xdotool/ydotool or write best-effort; do not write 同 win32". Spec §5.4 linux says "同 win32" for last-resort matrix (which is fine — that's about JS paths, and "同 win32" there means no third JS path). The shell payload heuristic is payload-based and platform-agnostic, so xdotool typing into Chrome contains no DOM-inject strings → miss. Spec doesn't say "linux shell heuristic best-effort". Minor nit. Actually — xdotool type into Chrome is keyboard injection, not DOM-script; the family is `dom_script` so arguably correctly excluded (it's not JS). The budget reviewer wanted it named or disclaimed. Weak nit.

  6. **exprHash normalization** — spec says `sha256[:12](规范化 command 或 expression)` — "规范化" undefined (whitespace? case? semicolons?). If normalization is unspecified, "same script 3次" is implementer-dependent. The redacted trace hashes were of the raw expression presumably. For a machine gate, specify: e.g., trim + collapse whitespace. Nit-level but real — cap A's entire behavior depends on it. Though honestly even raw-string hash would work for the "刷发布" case (identical reposts). Nit.

  7. **Cap A key includes tabId; osascript has no tabId** — spec: "osascript 无 tabId 时用其 url 解析 origin/tab" — you can't parse a tabId from a URL. For key A, osascript would key on... origin? url fragment? The sentence is sloppy/circular. An implementer can't build key A for osascript from this. Volume key B works (origin from url). Must-clarify: for osascript_eval, key A should be (family, exprHash, url-fragment) or (family, exprHash, origin). This is a real spec defect — one sentence fix. Given verified osascript_eval schema REQUIRES url param, keying on the url fragment is natural. I'd call this a must-fix nit (blocking? it's a one-line clarification; without it implementers diverge).

  8. **"不要提高 MAX_SAME_TOOL_RECOVERABLE_FAILURES=3"** ✓ §5.3.

  9. **Capped codes recoverable but stop_or_change_task, no host_computer hop, hard-refuse success path** → §5.3 ✓ ("封顶后该家族对该 origin 硬拒成功路径"). Addresses win32 MUST-FIX 3 (cap → hard stop, not recoverable hop). ✓

  10. **DoD**: 13 (hash cap), 14 (volume cap), 15 (shell heuristic both directions), 16 (dead world probe). ✓ Matches budget must-fix DoD. Volume cap DoD says "成功 24 次后" — 24th succeeds, 25th capped, consistent with "24 次成功 → capped". ✓

  ### WIN32 REJECT must-fixes:

  1. evaluate not suggested for CDP_ATTACH_FAILED → §5.1 table explicitly forbids suggested_action=evaluate; suggests list_tabs/retry_after_user_focus/stop_or_change_task. ✓ DoD 19 covers both evaluate and host_computer. ✓
  2. OS-split last resort → §5.4 matrix: darwin osascript allowed (http not banned, X.com CSP darwin-only), win32 no third JS path → stop, linux same + host_computer hard-refuse on non-darwin/win32. ✓ 
     - Verified: host_computer currently runtime-gated to darwin/win32 (companion-dispatch.ts:1641) and NOT in L2_GATE_TOOLS but separately hostComputerGated with always-confirm. Spec §5.4 says "host_computer 在非 darwin/win32 硬拒" — matches current behavior (linux already runtime-errors); spec wants the *copy* to not imply CU exists. ✓ consistent with code.
  3. Cap → hard stop not hop → covered above ✓.
  4. X.com CSP darwin-only recorded → §5.4 "X.com CSP 退路 仅 darwin" ✓ and §8.
  5. fill_form Ctrl truly effective → §5.5 + DoD 18 ✓. Verified current code: fill_form select-all sends ctrlKey:true without windowsVirtualKeyCode (browser-bridge.ts:856-861) — spec's "Windows 全选是演戏" claim verified TRUE.
  6. press_key catalog mask Meta=8 Shift=4 → verified TRUE (tool-definitions-catalog.json:362). Spec's fix: DOM key booleans + windowsVirtualKeyCode, catalog rewrite in human terms, don't publish wrong mask. ✓ 
     - Note subtlety: current press_key implementation *also* uses the wrong mask internally (browser-bridge.ts:1142-1149: `shiftKey: !!(modifiers & 4), metaKey: !!(modifiers & 8)` — matches the catalog's wrong mask, and does NOT pass CDP modifiers int). Spec says "press_key 走 DOM key 布尔（已有 ctrlKey/metaKey）并补 windowsVirtualKeyCode" — i.e., keep boolean approach but keyed how? If press_key keeps accepting a `modifiers` mask parameter, which mask? Spec says catalog 改成人话 (darwin Meta, win/linux Ctrl) — so the schema presumably moves away from publishing a numeric mask. But the wire/impl detail of what the mask bits mean internally is left implicit. Since impl and catalog currently agree (both wrong-vs-CDP but self-consistent), and spec wants DOM booleans + VK, the direction is coherent. Minor under-specification: does press_key schema change from mask to booleans (breaking) or keep mask with corrected bit meanings? "catalog 改成人话：darwin 全选用 Meta，win/linux 用 Ctrl；不要再公布错误掩码当 SoT" suggests descriptions, not schema change. Hmm — if the schema keeps `modifiers` as integer mask, and catalog no longer publishes the mask, how does LLM pass Ctrl? Impl maps mask→booleans with the *wrong* bit assignment today. If spec says "走 DOM key 布尔（已有 ctrlKey/metaKey）" maybe the schema already has boolean fields? Verified: press_key catalog at 362 has the mask description; explorer didn't report boolean fields in schema. The impl derives booleans from mask bits. So "已有 ctrlKey/metaKey" refers to the impl's derived booleans, not schema fields. The spec sentence is confusing but the intent (use booleans in impl, add VK, fix catalog copy) is implementable. There's a latent trap: if catalog stops documenting the numeric mask but schema still requires it, LLM can't call press_key with modifiers at all. The spec should say whether the mask parameter is replaced by booleans in the schema. Minor must-clarify nit.

  Also Rule 12: spec says "本波 W5 补上 NEVER host_computer for browser-DOM（三平台 Rule 12/12b）". Verified: Rule 12 win32 currently says NEVER host_read/host_write only; darwin mentions host_computer as last resort with 12b playbook. Adding "NEVER host_computer for browser-DOM" on darwin conflicts with darwin's existing "LAST RESORT pixel/OCR inject" positioning — but the spec's intent is browser-DOM tasks specifically, which is consistent with 12b's "prefer structure first". Fine, implementable. ✓

  ### Cross-cutting checks:

  - **§5.1 tool list**: classification applies to click/type/hover/press_key/get_element_info/evaluate. fill_form/select_option/drag_and_drop/browser_download not listed. fill_form calls click internally so it inherits? Not necessarily — fill_form does its own CDP. Under-specified: does the WRONG_ORIGIN/CDP_ATTACH_FAILED classification cover the other locator consumers? Presumably yes via shared ensureAttached error path, but spec should say "all L1 browser tools" or list them. Minor gap — one line.
  - **§4.2 drag_and_drop**: "两侧独立 ELEMENT_*" — what if a side has both from_selector and from_text? Text-exclusive presumably (composition C). And both empty on a side → SELECTOR_OR_TEXT_REQUIRED? Not stated. "两侧独立" implies same rules per side; readable by inference. Nit.
  - **§4.2 type**: "允许省略 locator（焦点路径）" — verified type schema today already makes selector optional ("不填则输入到当前焦点元素"). ✓ consistent.
  - **fill_form interactiveSel expansion**: verified the interactiveSel lives in find-element-by-text.ts:62 (the text finder), not fill_form. Spec §4.2 says "本 PR 必须把 interactiveSel 扩到 input,textarea,select,[contenteditable],[role=textbox]（否则 text 会点到 label）". Current value includes `label` — expanding to include input/textarea/select/contenteditable/textbox — should `label` be removed? Spec's replacement string doesn't include `label`. For fill_form field-by-text, matching a label and clicking it focuses the associated input — actually often works. But spec decided; replacement string is explicit. However note: this changes the *shared* finder for click-by-text too — for click, matching `label`/`a`/`button` is the point! The interactiveSel is in the shared finder module used by download (and soon click). If you replace the download/click candidate selector with `input,textarea,select,[contenteditable],[role=textbox]`, click-by-text on a button stops working! Wait — is interactiveSel the *candidate pool* for text matching? Explorer: `const interactiveSel='a,button,[role="button"],input[type="submit"],input[type="button"],[onclick],label,summary'` in find-element-by-text.ts:62. Need to know how it's used — probably as the pool of candidate elements scanned for text. If fill_form's text path uses the same finder with the same pool, then "expanding" the shared pool to form elements is right, but the spec's replacement value **drops** a/button/label — which would break click-by-text and download-by-text! Unless the expansion is additive ("扩到" = expand to include) rather than replace. "把 interactiveSel 扩到 `input,textarea,select,[contenteditable],[role=textbox]`" — "扩到" reads as "expand to [become]" which is ambiguous between union and replace. If replace: catastrophic for click (can't click buttons by text — the entire W1 JTBD "唯一可见字可点" for buttons/links dies). If union: fill_form text matching a label still possible ("否则 text 会点到 label" suggests label should NOT be matched for fill_form...). Hmm, the parenthetical "否则 text 会点到 label" implies the fix prevents label matching — so maybe the pool should be parameterized per-family too (like hitAttr): click-family pool keeps a/button/label; fill_form pool uses form elements. The spec says "一个 finder... 参数化" only for hitAttr. The interactiveSel expansion as written is genuinely ambiguous and a naive reading breaks the main JTBD. This is a real finding — I'd rank it the most significant one: **must-clarify**. Actually wait — let me re-read: "本 PR 必须把 interactiveSel 扩到 input,textarea,select,[contenteditable],[role=textbox]（否则 text 会点到 label）". Hmm, "扩到" + the reason given. If the current pool is `a,button,...,label,summary`, and the reason for expansion is "otherwise text will click a label" — that reason only makes sense if fill_form's text path uses this pool and a field's label text matches the `<label>` element instead of the input. Expanding the pool to include bare `input` doesn't stop label from matching — label is still in the pool and likely earlier in document order. So even the union reading doesn't fix the stated problem! To fix "text clicks label", fill_form needs a *different* pool or a label→input resolution. The spec's stated fix doesn't achieve its stated goal under either reading. This is a legitimate defect worth flagging as the top finding. Must-fix (one-line: parameterize candidate pool per family, or resolve label→control).

  Actually hold on — maybe I should double check how interactiveSel is used in find-element-by-text.ts before making this a headline finding. The explorer quoted the constant but not its usage. It's likely the candidate selector for elements to scan. Let me verify quickly with a read of that file section. Worth it — this could be my main finding.

  - **§5.2 row 1 vs row 3**: as discussed, CDP can't distinguish "has completion value null/undefined" from "no completion value". Also: probe on every evaluate, or only when result is null/undefined? Spec implies probe only for the ambiguous case ("探针 1+1===2 为 true" in row 3). And dead world detection: "1+1 / document.title 也是 null" — spec says probe failure → dead world. Implementable: if result.value == null → run probe → dead? EVAL_DEAD_WORLD : empty_completion. But then genuine-null results mislabeled empty_completion (honesty blemish, acceptable?). Also probe costs an extra round trip — fine. Flag as must-clarify nit: define when probe runs and what genuine-null maps to.
  - **EVAL_THROWN missing from §5.2 table**: code exists in §9. safeEvaluate throws on exceptionDetails → maps to EVAL_THROWN success:false presumably. One-row addition. Nit.
  - **classifyError**: verified default non_recoverable → chat.error aborts run (adapter.ts:1345-1355). Spec §7 lists 9 codes to add. §9 full list has 11 (adds ELEMENT_NOT_FOUND, EVAL_THROWN). DoD 20 says "新 error_code 均在 classifyError recoverable 列表". ELEMENT_NOT_FOUND — as substring "element not found" already recoverable via existing substring BUT the code token "ELEMENT_NOT_FOUND" lowercased is "element_not_found" which does NOT match "element not found" substring (underscores). The error string will be `ELEMENT_NOT_FOUND: no visible element...` — hmm, the rest of the message "no visible element matching text" — does it contain "not found"? "ELEMENT_NOT_FOUND: no visible element matching..." — no "not found" substring with spaces... wait "ELEMENT_NOT_FOUND" contains "NOT_FOUND" not "not found". So a bare CODE: message without lucky prose would classify non_recoverable → abort. DoD 20 says all new codes — ELEMENT_NOT_FOUND isn't "new" (exists in download handler) but for click it's new-ish. §7's must-add list omits ELEMENT_NOT_FOUND and EVAL_THROWN. If the implementation's error strings are `CODE: <prose>`, the prose might accidentally contain matching substrings, but that's fragile. §7 list should include ELEMENT_NOT_FOUND and EVAL_THROWN (or note they're covered). Given the underscore trap, this is a real gap: spec §7 says these 9 must be added "否则 default non_recoverable" but the full code set is 11 — EVAL_THROWN thrown from evaluate would abort the whole run as non_recoverable, contradicting the honesty design (a thrown page error should be recoverable). Finding: §7 classifyError list incomplete vs §9. Must-fix-lite. Actually wait — is ELEMENT_NOT_FOUND covered? classifyError checks lowercase error message for "element not found" substring. Message: "ELEMENT_NOT_FOUND: no visible element matching text "X"" → lowercase: "element_not_found: no visible element matching text..." — contains "element not found"? No (underscore). Contains "not found"? No. So non_recoverable → abort. But current download handler already emits ELEMENT_NOT_FOUND and presumably... would abort runs today? That's today's bug, but W1 makes it mainstream for click. So yes — must add ELEMENT_NOT_FOUND (and EVAL_THROWN) to §7's list. Good catch, rank as must-fix-lite (one-word fix, blocks honesty design otherwise).

  - **DoD 8**: `https://zhihu.com` + attach 失败 → CDP_ATTACH_FAILED — consistent with §5.1 (debugger+scripting both fail). ✓
  - **§5.1**: "scheme http(s) 且 debugger+scripting 都失败" — but what if attach succeeds and scripting succeeds but the page is a PDF plugin? Recorded residual ✓.
  - **a7ubt9 trace claims**: budget reviewer verified 81 osascript/78 unique/max 2; spec's design (24 volume cap) would have stopped it at 24. Sanity ✓. Also note osascript_eval requires url param (verified) → origin derivable for volume key ✓. Cap A for osascript: key needs tab substitute — the url fragment works. Still, spec's "用其 url 解析 origin/tab" wording is sloppy for tab. Nit confirmed.

  - **§3 waves**: W1+W3′+W4+W5 this wave; W2 forbidden. Spec sections match. ✓
  - **§11 non-goals** consistent with waves. ✓
  - **DoD count vs spec**: 20 items, all mapped to spec sections. DoD 9 (chrome-extension:// → WRONG_ORIGIN) — but wait: the extension's own PDF viewer is chrome-extension:// — and browser_download must work there (that's the a7ubt9 PDF case — download from PDF viewer). §5.1 classification list doesn't include browser_download, so download isn't WRONG_ORIGIN'd. Consistent, but fragile: someone "tidying up" could add download to the list and break PDF downloads. Not a spec defect per se. Skip or nit.

  - **§4.5**: INVALID_SELECTOR only SYNTAX_ERR; verified the i-flag caution consistent with Chrome behavior. ✓
  - **§4.3 poll 3s**: verified click already polls 3s (waitForSelector, non-fatal timeout). For text path, poll — but note: current waitForSelector timeout is non-fatal, falls through to canonical not-found. For text, poll then classify count=0 → ELEMENT_NOT_FOUND. ✓ coherent.
  - **§6 W4**: type fallback — verified current selector-branch fallback sets el.value unguarded (browser-bridge.ts:830) — spec forbids exactly this. ✓ And contenteditable via insertText command or input event; failure TYPE_UNSUPPORTED_EDITOR. Note: CDP Input.insertText actually works on contenteditable if focused (it inserts at caret like real typing). The fallback path is for when CDP fails; using execCommand('insertText') or input events. Spec says "insertText 命令或 input 事件" — dispatching an `input` event alone doesn't insert text into contenteditable; you'd need beforeinput/execCommand/range insertion. "insertText 命令" presumably = document.execCommand('insertText'). OK, readable. Fine.

  - **§5.3 volume counter scope**: "(family=dom_script, origin)" counts evaluate+osascript+shell-DOM-inject combined per origin. 24 across all three. a7ubt9: 81 osascript + 26 evaluate on... origins varied? Mostly zhihu/x.com presumably. 24 would cap. ✓. Edge: legit long sessions (form filling across 24+ script steps on one origin) get capped → stop_or_change_task with recoverable → user continues, thread record persists across "继续" ("巡航仍计，跨「继续」") — wait, "线程持久，巡航仍计，跨「继续」" means the counters survive user "continue". So a capped origin stays capped for the thread — after user says continue, the 25th call still hard-false? "封顶后该家族对该 origin 硬拒成功路径（再调用仍 false）" — yes, hard for the thread. Hmm — is that too hard? User explicitly says "continue" and the gate ignores it? That's the design intent (machine ceiling, not LLM-persuadable). It's a product decision, locked. Fine — but worth noting the user-visible consequence: user cannot override within thread; must start new thread. That's arguably the point (kill the storm). OK.

  - **Contradiction scan**: §5.3 B says origin = tabs.get origin **or** osascript url parsed origin. For evaluate, tabId known → tabs.get(tabId).url → origin. For shell_exec dom-inject via CDP (--remote-debugging-port), origin unknown → key B can't key → only cap A (exprHash, tabId=?) applies? Shell payload has no tabId either. So shell-path DOM scripts escape BOTH counters unless they embed a URL. E.g. `shell_exec: curl localhost:9222/json ... Runtime.evaluate` storm — keyed how? Under-specified for shell family. The a7ubt9 storm was osascript (has url) so the named case is covered, but the shell hole remains: no tabId, no url → no key. Nit/finding: define fallback key for shell family (e.g., global per-thread family counter). Hmm, this is the same class as the file-backed hole: spec covers the observed trace but leaves the adjacent evasion unkeyed. Worth one combined finding: "shell_exec dom_script entries without url/tabId have no volume key; file-backed scripts carry no payload signature — either define fallback keys/residuals".

  Let me quickly verify interactiveSel usage before finalizing. I'll read find-element-by-text.ts lines ~40-90.

  Also double-check the fill_form current flow (does fill_form even have a text path today? Explorer said fill_form takes field.selector and clicks directly, no text fallback — spec adds text option to fill_form items, and the interactiveSel concern is about the *new* fill_form text path using the shared finder). So the spec's parenthetical "否则 text 会点到 label" is about the new fill_form-by-text feature. If fill_form text path uses shared finder with pool including label/a/button, a field labeled "邮箱" would match the <label> element; clicking a label actually focuses the input in HTML... actually clicking a label DOES focus its associated control (native behavior). Hmm — so clicking the label isn't even wrong for focus purposes. But type path then uses insertText at focus — works. But if label isn't associated (no for/nesting), click does nothing. The real fix: prefer form controls. Whatever — the key question is whether "扩到" is replace or union, and whether the fix achieves the goal. Let me read the finder file to ground this.

  Let me also check: does the finder's candidate scan use interactiveSel as the pool, or is it a visibility filter? Read lines 32-92.

• Both verifications confirm the spec's factual claims about current code are accurate. Let me ground one open question — how `interactiveSel` is actually used in the shared finder — before finalizing the verdict.

• Confirmed: `interactiveSel` is the **preferred candidate pool** for text matching in the shared finder. If the pool is non-empty, the fallback `body *` never runs. So:

  - For click-by-text (W1's headline JTBD: "唯一可见字可点" — buttons, links, menu items like "员工管理"), the interactive pool (a/button/label/…) is exactly what makes it work.
  - Spec §4.2: "本 PR 必须把 interactiveSel 扩到 `input,textarea,select,[contenteditable],[role=textbox]`（否则 text 会点到 label）".

  Two readings:
  1. **Replace**: pool becomes form elements only → click-by-text on buttons/links dies (pool never matches a button; falls to `body *` scan, leaf-filtered — actually wait, if pool.length===0 it falls back to body* which WOULD find the button as a leaf. Hmm — so replace wouldn't fully kill click, it would degrade it: buttons found via fallback with ancestor filtering, and multiple matches more likely → more AMBIGUOUS. Still a regression of the "prefer interactive" design).
  2. **Union** (add form elements to existing list): pool = `a,button,...,label,summary,input,textarea,select,[contenteditable],[role=textbox]`. Then for fill_form field "邮箱", both the `<label>邮箱</label>` and possibly the input (if it has matching aria/value) match → AMBIGUOUS, or label matches alone → click label. The stated problem "text 会点到 label" is NOT fixed by the union — label stays in the pool.

  Actually, clicking a label natively focuses the associated control, so for fill_form/type it might work by accident — but the spec's own stated goal is to not point at the label. The fix as written doesn't achieve its stated purpose under the union reading, and harms click under the replace reading. The correct fix is a **per-family candidate pool** (like hitAttr parameterization): fill_form/type text path prefers form controls (with label→control resolution or label exclusion), click family keeps the interactive pool. The spec already has the parameterization mechanism (hitAttr) — the candidate pool needs the same treatment. This is my top finding: **must-fix**, small (one parameter), but as written the spec's instruction is self-defeating.

  Also note: the spec says fill_form text path at runtime one-of selector/text — new fill_form text path will use resolveLocator (shared finder). So this matters in-wave.

  Second-order: spec §4.2 says "fill_form 每个 field... 同一 L1 调用，0 次确认" — fill_form is one L1 tool call, fine.

  Now let me also double-check §5.2's decidability concern. CDP Runtime.evaluate with returnByValue: 
  - `1+1` → {type:"number", value:2}
  - `void 0` → {type:"undefined"}
  - `(()=>{})()` → {type:"undefined"}
  - `localStorage.getItem('x')` (missing) → {type:"object", subtype:"null", value:null}
  - `document.title` on dead world → in a7ubt9, result null. Dead world means evaluate returns... actually in a7ubt9 the extension returned {success:true, result:null} — the underlying CDP returned undefined/NULL because the execution context was destroyed or the evaluate silently failed? The spec's probe approach: if result is null/undefined, run probe `1+1===2`; probe true → empty_completion; probe also null/false or throws → EVAL_DEAD_WORLD. That's implementable. The spec table row 1 "语句有完成值 → result 为该值" vs row 3 — the implementable decision procedure is: non-null value → row 1; null/undefined → probe → row 2 or 3. Genuine null completion (localStorage.getItem) → row 3 mislabeled empty_completion. Minor honesty blemish; spec should acknowledge or define. Nit.

  Also EVAL_THROWN row missing from §5.2 table — nit (code in §9, classifyError list in §7 omits it and ELEMENT_NOT_FOUND).

  classifyError underscore trap: verified — "ELEMENT_NOT_FOUND".toLowerCase() = "element_not_found", doesn't match "element not found" substring. So bare `CODE: msg` strings classify non_recoverable → chat.error aborts run. §7's must-add list has 9 codes but omits ELEMENT_NOT_FOUND and EVAL_THROWN from the 11 in §9. DoD 20 says "新 error_code 均在 classifyError recoverable 列表" — ELEMENT_NOT_FOUND isn't strictly "new" but for click/type/hover it is newly on the path. This is a real gap: either add all 11 or justify. Must-fix-lite. Actually wait — existing classifyError has "element not found" — and current download emits "ELEMENT_NOT_FOUND: no visible element matching text ..." — the full message lowercase contains... "element_not_found: no visible element matching text" — no "element not found" substring, no "not found" substring. So today's download text-path failure is already non_recoverable → aborts run? That's a live bug today, and W1 spreads it to click. Definitely flag.

  osascript key-A wording: "osascript 无 tabId 时用其 url 解析 origin/tab" — tabId can't be parsed from url. For key A the natural substitute is the url fragment (which osascript_eval requires). One-line clarification. Nit+.

  Shell-family keying hole: shell_exec dom-inject entries have no tabId and possibly no URL (e.g., CDP via localhost:9222). Key B (origin) can't key them → only key A with... what tab? Both counters can fail to key shell entries. Combined with file-backed script hole (cscript inject.js, osascript /tmp/x.scpt — no payload strings). The spec covers the observed trace but leaves adjacent evasions unkeyed and doesn't record them as residuals (it did record the file: PDF residual — good practice it should repeat). Finding: either define fallback key (per-thread global family counter) or record as known residual. This is a genuine must-fix-lite/strong nit. Given §0's one-liner says "成功环要有机器封顶" and the whole point is the machine ceiling, leaving the shell family unkeyed is a hole in the ceiling's own terms. But practically: shell_exec is capabilityForceConfirm (always confirm unless 3-flag cruise), so a storm needs cruise. a7ubt9 was cruise. So the hole is real under exactly the conditions that matter. I'll rank it must-fix-lite: add "shell family without resolvable origin keys on a thread-global bucket" or record residual.

  Volume cap user-override: counters persist across "继续" — deliberate, locked. Fine, note as design consequence.

  §5.1 classification tool list omits fill_form/select_option/drag_and_drop/browser_download — fill_form does its own CDP attach; its failures need the same typed codes or §4.4's liar-success envelope catches them as generic success:false? §4.4 lists fill_form: "locator/focus/CDP/fallback false → success:false + error_code" — but which code for attach failure? CDP_ATTACH_FAILED presumably — but §5.1's list doesn't include fill_form. One-line fix: say "all L1 browser tools" or add them. Nit.

  press_key schema question: catalog currently documents numeric mask (Alt=1 Ctrl=2 Shift=4 Meta=8) and impl derives booleans from those bits. Spec: "press_key 走 DOM key 布尔（已有 ctrlKey/metaKey）并补 windowsVirtualKeyCode... catalog 改成人话... 不要再公布错误掩码当 SoT". If catalog copy stops publishing the mask but schema still takes `modifiers` int, LLM can't know what to pass. Does press_key schema have boolean fields? Explorer didn't report any — catalog:362 description is the mask; likely params are key + modifiers int. So the spec implies a schema change (mask → booleans or key names) without saying so. Under-specified: must state whether `modifiers` param becomes booleans/array of names, and migration of the wrong-bit mapping (impl currently maps bit4→shift, bit8→meta, matching the wrong catalog — if catalog is "fixed" to CDP order, impl mapping must flip too, and any stored prompts break). Since spec says "不要公布错误掩码" rather than "fix mask to CDP", the cleanest read: replace mask with explicit booleans in schema. Needs one sentence. Nit+.

  drag_and_drop empty-side: nit.

  linux heuristic note: weak nit (payload heuristic is platform-agnostic; xdotool isn't dom_script anyway). Mention in passing.

  Nits from locator review header: case-sensitive exact=false in catalog copy — still absent from §7 (it was explicitly non-blocking). Mention as carried nit.

  AMBIGUOUS payload: must-lock #9 required ≤5 matches + user_hint_zh + suggested_action: disambiguate_selector_or_exact_text. Spec §4.3 has ≤5 matches only. Fold incomplete. Nit+ (one line; matters for LLM recovery consistency).

  DoD coverage of the above: DoD doesn't test the AMBIGUOUS payload shape, doesn't test classifyError for ELEMENT_NOT_FOUND/EVAL_THROWN (DoD 20 says "新" codes), doesn't test per-family pool. If findings are accepted, DoD needs 2-3 lines.

  Structure of verdict: The spec is dramatically improved over the rejected versions — the core shapes are right (dual counter with volume cap, payload heuristic, OS-split last resort, composition C, trust freeze, honesty envelope). All factual claims about code verified true. The remaining issues are small, well-localized, one-line-to-one-paragraph fixes — none invalidate the architecture. That's APPROVE_WITH_NITS... but a few of my findings are arguably must-fix-before-implementation (interactiveSel self-defeat, classifyError underscore trap, shell-family keying hole, AMBIGUOUS payload). Are they blocking? The instruction: APPROVE | APPROVE_WITH_NITS | REJECT. REJECT = don't implement until fixed. These issues would cause implementation-time divergence or in-wave bugs (interactiveSel replace-reading breaks click; classifyError omission aborts runs — that's the exact "default non_recoverable → chat.error" failure the spec itself warns about in §7 and DoD 20, yet its own list is incomplete against its own §9 code list — an internal contradiction).

  Hmm, internal contradiction check: §7 says the 9 codes "必须" be added "否则 default non_recoverable". §9 lists 11 codes. DoD 20 says "新 error_code 均在 classifyError recoverable 列表". If ELEMENT_NOT_FOUND/EVAL_THROWN are deemed "existing" (not 新）, they still fail the underscore trap. So the spec's own honesty design is undercut. That's a must-fix but it's a one-line fix — does that make the whole spec REJECT? The verdict scale: previous rounds REJECTED for shape-level falsifications (cap that never fires, finder fork, third-path mirage). This round's issues are localizable wording/spec-completeness bugs with obvious fixes, not shape errors. I judge APPROVE_WITH_NITS is honest only if the "nits" can be fixed without re-running对抗. The spec itself says: "实现前若再改 5.3 阈值或 5.4 矩阵，必须重跑对应对抗路" — my findings don't touch thresholds or the OS matrix. They're clarifications within the locked shape. But two of them (interactiveSel, classifyError completeness) would produce wrong code if implemented literally. A literal implementation of a spec shouldn't be possible to do wrongly. I'll call it APPROVE_WITH_NITS but with the nits explicitly labeled "fix-in-spec-before-code, no re-adversarial needed" — hmm, that's arguably a weak REJECT.

  Let me weigh: the reviewer's job is to decide "is this spec ready to implement?" With 4-5 one-line spec defects that an implementer will hit in the first hour (interactiveSel reading, probe decidability, classifyError list, osascript key-A, shell keying), the spec is not quite "locked". But the verdict options are coarse. Precedent in this doc: REJECT was used for foundational falsifications. APPROVE_WITH_NITS exists precisely for "non-blocking once MUST-LOCK lands" style items. My findings are slightly above nits but far below the previous REJECTs. The most defensible verdict: **APPROVE_WITH_NITS**, with a short must-clarify list (fix in spec text before/at implementation start, no threshold/matrix changes, no re-adversarial required). I'll clearly separate "must-clarify (land before coding)" from "nits".

  Actually — let me reconsider the interactiveSel finding's severity once more. Spec §4.2 fill_form: "本 PR 必须把 interactiveSel 扩到 input,textarea,select,[contenteditable],[role=textbox]（否则 text 会点到 label）". The module is shared with click/download. Under the union reading, click behavior unchanged (pool grows), fill_form field text "邮箱" matches `<label>` (still in pool) → clicks label → native label-click focuses input → type via insertText works. So union reading: problem stated isn't "fixed" but outcome mostly works by native label semantics; edge case = unassociated label (no for/nested control) → click dead → liar-success envelope catches? Click succeeded (label was clicked), focus went nowhere, insertText goes to previous focus → wrong field typed → silent corruption, the exact class of bug this spec exists to kill. Under replace reading: click-by-text degrades to body* fallback (still finds buttons as leaves, but loses interactive-preference → more ambiguity on pages where both a button and a div contain the text... actually leaf-filter usually keeps the button; but e.g. `<button><span>保存</span></button>` — ownText of button includes child text? ownText uses el.innerText which includes descendants, plus leaf filter drops button because it contains span match → clicks the span → fine-ish). Either reading is survivable but neither achieves the stated goal cleanly. The right fix (per-family pool parameter, fill_form pool prefers controls + label→for-resolution) is small. Must-clarify, not reject-worthy.

  classifyError: DoD 20 would actually catch this if tests are written honestly ("新 error_code 均在 classifyError recoverable 列表" — tester checks each code string → ELEMENT_NOT_FOUND fails the test → fix). Hmm, but ELEMENT_NOT_FOUND arguably isn't "新". EVAL_THROWN is new (absent from src per verification) → DoD 20 does cover it. ELEMENT_NOT_FOUND is the gap. One word in §7. Must-clarify-lite.

  OK. Final verdict: APPROVE_WITH_NITS, with a "fix these 5 in spec text before first PR" list (clarifications, not shape changes) and carried nits. Let me also give credit: factual claims all verified; fold completeness high.

  Findings list:

  **Must-clarify before coding (spec-text edits, no shape change, no re-adversarial needed per §12's own rule):**

  F1. §4.2 interactiveSel: self-defeating as written. `interactiveSel` is the shared finder's *preferred candidate pool* (find-element-by-text.ts:62-64; if non-empty, body* fallback never runs). Replacing it with form elements degrades click-by-text/download (loses a/button prefer); union keeps `label` so fill_form text still matches labels — and clicking an unassociated label types into the wrong focused element (silent corruption, the liar-success class W1 exists to kill). Fix: parameterize the candidate pool per family like hitAttr — click/download keep current pool; fill_form/type text path prefers `input,textarea,select,[contenteditable],[role=textbox]` with label→control resolution (or label exclusion).

  F2. §7 vs §9 classifyError list incomplete. §9 has 11 codes; §7's must-add list has 9, omitting ELEMENT_NOT_FOUND and EVAL_THROWN. classifyError is lowercase-substring with default non_recoverable (security.ts:950-1045); "ELEMENT_NOT_FOUND" lowercases to "element_not_found" which matches neither "element not found" nor "not found" → a bare `ELEMENT_NOT_FOUND: …` from click aborts the whole run via chat.error (adapter.ts:1345-1355) — the exact failure §7 warns about. Add both (DoD 20 should say "§9 全部 11 码").

  F3. §5.3 shell-family keying hole + file-backed scripts. (a) shell_exec dom-inject via `curl localhost:9222/.../Runtime.evaluate` or `--remote-debugging-port` carries no tabId/url → neither key A (exprHash, tabId) nor key B (origin) can key it → uncounted under exactly the cruise conditions that produced a7ubt9. Define a thread-global fallback bucket for dom_script entries with no resolvable origin, or record as known residual. (b) file-backed scripts (`cscript //nologo inject.js`, `wscript`, `mshta x.hta`, `osascript /tmp/x.scpt`) carry no payload strings → family miss; budget REJECT named file-backed osascript explicitly. Either count interpreter+file invocations or record as known residual (spec already does this honestly for file: PDF — same treatment).

  F4. §5.2 three-row table is not decidable as written. CDP returnByValue cannot distinguish "completion value is null/undefined" from "no completion value" (`void 0`, `(()=>{})()`, and `localStorage.getItem('missing')` all collapse to undefined/null). State the decision procedure: non-null → row 1; null/undefined → probe 1+1===2 → dead ? EVAL_DEAD_WORLD : empty_completion (accepting that genuine-null results are labeled empty_completion — record that). Also add the missing thrown row: exceptionDetails → success:false EVAL_THROWN (code exists in §9 but no row).

  F5. §5.3 key-A for osascript: "用其 url 解析 origin/tab" — a tabId cannot be parsed from a URL. osascript_eval's schema already requires `url` fragment (tool-definitions-catalog.json:1023-1041) — say key A uses (family, exprHash, url-fragment) for osascript. Also define "规范化" for exprHash (e.g., trim + collapse whitespace) or say raw-string hash.

  F6. §5.5 press_key: if catalog stops publishing the numeric mask, the schema must stop requiring it — state that `modifiers` becomes booleans (or named keys) in the schema, and that the impl's current bit mapping (browser-bridge.ts:1142-1149, which mirrors the wrong catalog mask) is replaced by the DOM booleans + windowsVirtualKeyCode. One sentence; otherwise implementers don't know whether to flip bits or change schema.

  **Nits:**

  N1. §4.3 AMBIGUOUS payload: locator must-lock #9 required ≤5 matches + user_hint_zh + suggested_action: disambiguate_selector_or_exact_text; spec kept only "≤5 matches". §5.1/§5.3 give suggested_action for other codes — AMBIGUOUS should too, for LLM self-recovery consistency.

  N2. §5.1 classification list omits fill_form/select_option/drag_and_drop (fill_form does its own CDP attach; §4.4 demands typed error_code but §5.1 doesn't give it the codes). Say "all L1 browser tools" or add them.

  N3. §4.2 drag_and_drop: both-empty side → SELECTOR_OR_TEXT_REQUIRED not stated (only "两侧独立 ELEMENT_*"); text+selector both present on one side → text-exclusive by C, worth one clause.

  N4. linux shell heuristic: "同 win32" is fine for the JS-path matrix, but add the budget reviewer's sentence "linux shell heuristic is best-effort" (xdotool/wtype carry no dom_script signature).

  N5. Carried locator nits still unlanded (explicitly non-blocking): catalog copy must state case-sensitive exact=false; ownText uses getAttribute('value') not live el.value; same-tab click queued behind 60s download → TROUBLESHOOTING note.

  N6. Volume/loop caps persist across "继续" within a thread — deliberate and locked, but note the user-visible consequence somewhere (TROUBLESHOOTING): user cannot override a cap in-thread; new thread required.

  **Verified-true factual basis** (worth stating): all spec claims about current code check out — catalog mask wrong vs CDP (tool-definitions-catalog.json:362), fill_form Ctrl+A without VK (browser-bridge.ts:856-861), evaluate always success:true (:1270-1282), click polls 3s (:782-784), classifyError default non_recoverable (security.ts:1044-1045), click not in L2_GATE_TOOLS (l2-admission.ts:49-67), host_computer runtime-gated darwin/win32 (companion-dispatch.ts:1641), Rule 12 win32 lacks host_computer NEVER (adapter.ts:418-451), architecture.md:305 has click("员工管理").

  **Fold completeness**: locator must-locks #6/7/8/10 fully landed, #9 partially (N1); budget: dual-counter redesign correctly answers the unique-hash falsification (24-volume would stop a7ubt9's 81 at 24), thread-persistence, payload heuristic with explicit allow-list, cap→hard-stop not hop; win32: all 4 must-fixes landed (no evaluate suggestion, OS matrix, hard-stop, darwin-only CSP) + fill_form Ctrl + Rule 12 NEVER host_computer.

  Verdict: APPROVE_WITH_NITS — with the note that F1-F6 should be edited into the spec text before first PR (clarifications within the locked shape; none touch §5.3 thresholds or §5.4 matrix, so no re-run of adversarial lanes per the spec's own §12 rule).

  Write the review in Chinese (user's language — the spec is in Chinese, user message is mixed but predominantly Chinese context; the user's instruction line is English: "Independent Kimi rereview of LOCKED spec before implementation. End with VERDICT: ..." — the bulk is Chinese. Previous reviews in the thread are English. Hmm. Language rule: user's most recent message — the instruction wrapper is English, the quoted spec is Chinese. The embedded reviews are English. I'll write in English with Chinese terms where quoting, since the reviewer-role outputs in this thread (the two REJECTs) are English and the user instruction is English. Actually "Write in the user's language unless they explicitly ask" — the user's own words are English ("Independent Kimi rereview..."). Go English, keep Chinese quotes.

• # Independent Re-Review — Web Act-Loop SoT (LOCKED, 2026-08-21)

  ## Method

  Two parallel verification passes over the codebase (chrome-extension + companion), then a fold-completeness audit of the three REJECT must-lock lists against the spec text, then an internal-consistency / implementability pass. Every factual claim the spec makes about current code was checked against source.

  ## Factual basis — spec's claims about current code: all verified TRUE

  - Catalog `press_key` mask `Alt=1, Ctrl=2, Shift=4, Meta=8` (`tool-definitions-catalog.json:362`) — contradicts CDP Alt=1/Ctrl=2/Meta=4/Shift=8. Impl mirrors the wrong mask (`browser-bridge.ts:1142-1149`).
  - fill_form Ctrl+A sends `ctrlKey:true` with **no** `windowsVirtualKeyCode` (`browser-bridge.ts:856-861`) — "Windows 全选是演戏" is accurate.
  - `evaluate` returns `success:true` unconditionally, `result` undefined on dead world (`browser-bridge.ts:1270-1282`); companion does no probe/retry (`tool-forward.ts:237-313`).
  - `classifyError` is lowercase-substring with default `non_recoverable` (`security.ts:950-1045`); non_recoverable → `chat.error` + run abort (`adapter.ts:1345-1355`).
  - `recoverableFailureCounts` is per-`chatCreate`, per-toolName, deleted on success (`adapter.ts:834,1271-1274`). No thread-persistent outcome store exists.
  - `click` not in `L2_GATE_TOOLS` (`l2-admission.ts:49-67`); `osascript_eval` darwin-gated and **requires `url`**; `host_computer` runtime-gated darwin/win32 (`companion-dispatch.ts:1641`), not in L2_GATE_TOOLS.
  - Rule 12 win32 names only `host_read`/`host_write` NEVER (`adapter.ts:418-451`); `docs/architecture.md:305` has `click("员工管理")`.
  - `find-element-by-text.ts` exists, marks only/never clicks, JSON.stringify needle, clears own namespace, `data-cmspark-dl-hit` only — parameterizable as spec §4.1 assumes.

  ## Fold completeness — high

  - **locator REJECT**: must-locks #6 (Trust freeze), #7 (hit consumption), #8 (CSS first-match), #10 (all DoD additions) fully landed in §4/§10. #9 partially — see N1.
  - **budget REJECT**: the core falsification (8-cap on identical hash never fires; 78 unique hashes, max repeat 2) is answered with the right shape change — the **24-per-origin volume counter** would have stopped a7ubt9's 81 osascript at 24. Thread-persistence, payload-over-brand heuristic with explicit allow-list (`Start-Process`/`Get-Process`/`tasklist`), cap→hard-stop with `stop_or_change_task` (not recoverable hop), and the dead-world probe all land. Known residual for `file:`/https PDF plugins is recorded honestly.
  - **win32 REJECT**: all four must-fixes land — evaluate forbidden as `CDP_ATTACH_FAILED` suggestion (§5.1 + DoD 19), OS-split last-resort matrix (§5.4), cap→hard-stop (§5.3), X.com CSP darwin-only (§5.4/§8) — plus fill_form Ctrl真生效 (§5.5/DoD 18) and Rule 12 `NEVER host_computer for browser-DOM` (§5.4).

  The architecture is now sound. What remains is a set of spec-text defects an implementer will hit in the first hour — none touch §5.3 thresholds or the §5.4 matrix, so per the spec's own §12 rule none require re-running the adversarial lanes.

  ## Must-clarify before coding (spec-text edits, no shape change)

  **F1 — §4.2 `interactiveSel` is self-defeating as written.** `interactiveSel` is the shared finder's *preferred candidate pool* (`find-element-by-text.ts:62-64`): if it's non-empty, the `body *` fallback never runs. Replacing it with `input,textarea,select,[contenteditable],[role=textbox]` degrades click-by-text/download (loses the a/button preference — W1's headline JTBD). Widening it to a union keeps `label` in the pool, so fill_form text still matches the `<label>` — and clicking an *unassociated* label types into whatever held focus before: silent wrong-field corruption, exactly the liar-success class W1 exists to kill. Fix: parameterize the candidate pool per family, same mechanism as `hitAttr` — click/download keep the current pool; fill_form/type text path prefers form controls with label→control resolution (or label exclusion).

  **F2 — §7's `classifyError` list contradicts §9.** §9 defines 11 codes; §7's must-add list has 9, omitting `ELEMENT_NOT_FOUND` and `EVAL_THROWN`. `"ELEMENT_NOT_FOUND".toLowerCase()` is `element_not_found`, which matches neither the `"element not found"` nor `"not found"` substrings — so a bare `ELEMENT_NOT_FOUND: …` from click classifies **non_recoverable → `chat.error` aborts the whole run**, the precise failure §7 and DoD 20 exist to prevent (this already affects today's download text path; W1 mainstreams it). Add both codes; DoD 20 should read "§9 全部 11 码".

  **F3 — §5.3 shell-family keying hole + file-backed scripts.** (a) A `shell_exec` DOM-inject via `curl localhost:9222/...` / `--remote-debugging-port` carries no tabId and no URL — neither key A nor key B can key it, so it's uncounted under exactly the cruise conditions that produced a7ubt9. Define a thread-global fallback bucket for dom_script entries with no resolvable origin, or record as known residual. (b) File-backed scripts (`cscript //nologo inject.js`, `wscript`, `mshta x.hta`, `osascript /tmp/x.scpt`) carry no payload signature — the budget REJECT named file-backed osascript explicitly and the spec is silent. Count interpreter+file invocations or record as residual (the spec already models this honesty for `file:` PDFs — same treatment).

  **F4 — §5.2's three-row table is not decidable as written.** CDP `returnByValue` cannot distinguish "completion value is null/undefined" from "no completion value" (`void 0`, `(()=>{})()`, and `localStorage.getItem('missing')` all collapse). State the procedure: non-null → row 1; null/undefined → probe `1+1===2` → dead ? `EVAL_DEAD_WORLD` : `empty_completion` (accepting genuine-null results get labeled `empty_completion` — record that). Also add the missing thrown row: `exceptionDetails` → `success:false` `EVAL_THROWN` (the code is in §9 but has no row).

  **F5 — §5.3 key-A wording for osascript is circular.** "用其 url 解析 origin/tab" — a tabId cannot be parsed from a URL. `osascript_eval`'s schema already requires a `url` fragment; say key A is `(family, exprHash, url-fragment)` for osascript. Also define "规范化" for exprHash (trim + collapse whitespace), or say raw-string hash.

  **F6 — §5.5 press_key schema consequence unstated.** If the catalog stops publishing the numeric mask, the schema must stop requiring it. Say explicitly: `modifiers` becomes booleans (or named keys) in the schema, and the impl's current bit mapping is replaced by DOM booleans + `windowsVirtualKeyCode` — otherwise implementers won't know whether to flip bits or change the schema.

  ## Nits

  - **N1** — §4.3 AMBIGUOUS payload: locator must-lock #9 required ≤5 matches + `user_hint_zh` + `suggested_action: disambiguate_selector_or_exact_text`; only "≤5 matches" survived the fold. §5.1/§5.3 give `suggested_action` for peer codes — AMBIGUOUS should too.
  - **N2** — §5.1's classification list omits fill_form/select_option/drag_and_drop (fill_form does its own CDP attach; §4.4 demands a typed `error_code` for it but §5.1 never gives it the codes). Say "all L1 browser tools" or add them.
  - **N3** — §4.2 drag_and_drop: both-empty side → `SELECTOR_OR_TEXT_REQUIRED` unstated; selector+text on one side → text-exclusive by C, worth one clause.
  - **N4** — linux: "同 win32" is fine for the JS-path matrix, but add the budget reviewer's sentence "linux shell heuristic is best-effort" (xdotool/wtype carry no dom_script signature).
  - **N5** — carried locator nits still unlanded (explicitly non-blocking): catalog copy must state case-sensitive `exact=false`; `ownText` reads `getAttribute('value')` not live `el.value`; same-tab click queued behind a 60s download → TROUBLESHOOTING note.
  - **N6** — caps persist across 「继续」 within a thread (deliberate, locked) — write down the user-visible consequence: a cap cannot be overridden in-thread; new thread required.

  ## Summary

  The three REJECTs are substantively absorbed: the volume counter kills the unique-hash storm, the payload heuristic kills the brand-matching inversion, the OS matrix kills the third-path mirage, and the Trust freeze + composition C kill the finder fork and fail-open. Every claim about current code checked out. F1–F6 are one-line-to-one-paragraph spec edits with obvious correct answers; none alter thresholds, the OS matrix, or any locked shape — but implementing the spec literally without them would produce wrong code (F1, F2) or divergent code (F4–F6), so they should land in the spec text before the first PR.

  VERDICT: APPROVE_WITH_NITS

To resume this session: kimi -r session_f65e7c2c-4c3b-4e7b-8300-7b6ae0df4398
