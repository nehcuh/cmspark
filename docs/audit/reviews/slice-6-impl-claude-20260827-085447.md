All spot-checks complete. Review summary:

## Verification against real code `[executed]`

**Patch freshness**: `slice-6-impl-diff-20260827-085447.patch` matches live `git diff origin/main...HEAD` at bbfd687 (same 23 files/stat; patch only adds a review-context header). Not stale.

**Six spot-checks — all confirmed**:
1. `matchSkills` builds `idfFromDocs` once per call over skill name/desc/tags and compares `tfidfVec` (skill-engine.ts:565-590); comments updated to "IDF is live". `tokensToVec` remains only at knowledge-related.ts:52, obsidian/vault-index.ts:52,72, threads/related.ts:77 — untouched.
2. `pinSlashSkill` matches leading `/^\/(\S+)/` against `rest.message`, not `skill_ids` (skill-engine.ts:127-143); `skill.activate` handler (message-router.ts:2464-2474) writes only `active_skill_ids`, never mode — covered by tests in both files.test.ts and skills.test.ts:2002. Slash pin flips to manual with next-turn no-union proven in slash-skill-pin.test.ts:119-177.
3. thread-manager get() (~757) and update() (~865) both hydrate empty `run_progress` from `runtime_context_budget.handoff.open_todos`; no-clobber test present; this is exactly the bbfd687 Product-REJECT fold.
4. `applyToolResult` requires `success===true`, exact `it.tool === tool`, seed|user only, oldest-undone, never model_draft, never text substring (run-progress.ts:97-117). Wired at adapter.ts:1305-1327 on the success-path send; a source-scan test asserts absence from abort/parse/validation windows.
5. `thread.run_progress.toggle` absent from `SUMMONER_ALLOW` (summoner-acl.ts:14-45), asserted denied in two test files; also asserted absent from `SUMMONER_WEB_DISPATCH_ALLOW` and the `thread.update` key allowlist; overlay `thread.update` stays alias-only via `applySummonerPayloadPolicy`.
6. RunProgress.tsx:36 「本轮步骤」； extension test asserts copy is not 进行中； EmptyState/empty-state-copy/tokens.emptyTitle have zero diff.

**ADR-020 checklist**: capability declaration present in plan r2 (Surface L0 chat column / no overlay edit; Trust = tool_result or user gesture; Channel community). No new confirm dialect, no `securityConfirmations` in diff (no originWs surface), no new runtime/Python, no new primary chrome, no T1 claim in docs — G17/architecture/A5/DESIGN copy is accurate about what shipped.

**Tests `[executed]`**: companion — skills + slash-skill-pin + run-progress + summoner-acl (143 pass), single/files (74 pass); chrome-extension — 823 pass.

## Nits (non-blocking)

1. **Evidence-tick lane is dormant in v1**: H1 `open_todos` is `string[]` (context-handoff.ts:29), so seed rows never carry `tool`, and no v1 writer adds tool-declaring rows — `applyToolResult` can only fire on test-constructed items. Plan permits this (v1 seed-only), but DESIGN.md's "H1 seed + tool_result / click" slightly oversells the tool_result half for current behavior.
2. ChatView can render RunProgress directly above EmptyState simultaneously (items>0, messages=0) — cosmetic overlap after compaction.
3. thread-manager get() seeds and calls `saveIndex()` as a read side effect — convergent and tested, but a read-path write.
4. adapter.ts:1316 `!th.run_progress && next.items.length > 0` branch is dead in practice since `get()` already hydrated.

Four-lane synthesis confirmed: Product REJECT fold (bbfd687) is real and tested; overlay never gains Allow/Deny or a toggle verb; no T1 bake-off claim anywhere in the diff.

VERDICT: APPROVE_WITH_NITS
