## P1-2 Dual Review Findings

**Verified by inspecting real code + running the 101 tests (all pass, 0 fail).**

### Call-site binding (server.ts)
All three in-scope sites now pass `{ originWs: ws }` as the third arg to `securityConfirmations.request`:
- **navigate URL L2** — `companion/src/server.ts:1830` (within `createToolExecutor`)
- **MCP tool** — `companion/src/server.ts:3934` (within `executeMcpTool`)
- **MCP meta** — `companion/src/server.ts:4116` (within `executeMcpMetaTool`)

Origin enforcement lives in `SecurityConfirmationManager.respondFrom` at `companion/src/security-confirmation.ts:411-416` (returns `origin_mismatch`, pending left intact) and `rejectAll(ws)` at `:532-533` (spans entries whose `originWs !== ws`). Confirmed correct.

### Out-of-scope call sites left untouched (correctly)
- `server.ts:1389` L2 gate ternary `(winL2NonceChallenge || hostComputerGated) ? { originWs: ws } : undefined` — unchanged, evaluate/shell conditional bind preserved (Trust monotonicity holds).
- `server.ts:1483` tray privileged `securityConfirmations.respond(sharedConfirmId, ...)` — unchanged, single trusted local channel.
- `server.ts:1940` `analyze_image_fetch` — still unbound, **explicitly deferred** in plan and acknowledged in inventory.
- `server.ts:2017 / 5089 / 5216` host/biometric/history — already bind `{ originWs: ws }`.
- `server.ts:5147` HUD spike `evaluate` — elevated surface, no Side Panel push; `undefined` is correct.

No god-mode/shell expansion changes. Tray privileged `respond()` path intact. Trust monotonicity holds.

### Tests (real, not rubber-stamp)
Ran `node --test` on the three compiled files: **101 pass / 0 fail**. The P1-2 cases genuinely exercise the call-site binding:
- `security-confirmation-origin.test.ts:169` navigate rogue→mismatch, origin→resolved
- `security-confirmation-origin.test.ts:201` MCP tool rogue→mismatch, origin→resolved, then privileged `respond()` still resolves an origin-bound meta
- `security-gates.test.ts:317` navigate `respondFrom(rogueWs)` → `origin_mismatch`; `respondFrom(serverSideWs)` → `resolved`; tool then dispatches
- `security-gates.test.ts:351` navigate via production `handleSecurityConfirmationResponse(rogueWs, ...)` leaves pending; origin deny finishes
- `mcp-capability-gate.test.ts:426` MCP `manual` confirm via `respondFrom(rogue)` → mismatch, then `respondFrom(serverSideWs)` → resolved

The integration tests would **fail** if the call-site `{ originWs: ws }` were removed, because they deliberately avoid the privileged `respond()` bypass.

### ADR-020 capability checklist
- Declaration present (Surface L1 navigate + Composition mcp-server; L2-classes none; Compose mcp-server; Autonomy single; Trust originWs multi-peer; Channel community). ✓
- Axes fit: Trust-axis multi-peer bind, no new runtime/Pack/Skill. ✓
- Confirm dialects: no new family. ✓
- Trust monotonicity: deeper surfaces still bind; broadcast → bound is a strict tightening. ✓

### Nits (non-blocking)
1. **No MCP-meta multi-peer integration test.** The new `mcp-capability-gate.test.ts` case covers `executeMcpTool` (manual `read_file`) but not `executeMcpMetaTool` (mcp_read_resource / mcp_list_resources). The manager-level unit test does exercise `mcp_read_resource` via privileged `respond()`, which proves the surface binds, but a two-peer `respondFrom` flow on meta would be belt-and-suspenders. Adversarial summary already flagged this.
2. **Inventory body still references old line anchors** (`~3905-3918`, `~4089-4098`, `~1805-1816`) in narrative text; only the status table at lines 67-73 was updated. Cosmetic; the table is authoritative.
3. **navigate `handleSecurityConfirmationResponse` rogue test** relies on the subsequent origin deny succeeding as implicit proof that the pending entry survived. A direct `getNonceChallenge(cid)` / pending-exists assertion would be marginally stronger.

### Verdict
Core fix is correct, tightly scoped, security-positive (tightening only), and tested. No incomplete fix, no regression, no new hole. Nits are coverage polish, not blockers.

VERDICT: APPROVE_WITH_NITS
