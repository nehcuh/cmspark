Both blockers verified closed. Full analysis:

## C1 — Trust trash cookie double-restore: CLOSED

- **`pack-engine.ts:407-427`** — `releaseTrustBeforeThreadGone` now restores **then clears** `mission_pack_trust_snapshot` (via `threadManager.update` → `saveIndex()` persists; in-memory fallback on throw). Second call is a no-op (`isPackTrustSnapshot(null)` → false).
- **`message-router.ts:1190-1195, 1256-1259, 1352-1355`** — all release callers (single trash/hard, batch, cleanup_empty) pass `threadManager`, so the cookie is null before `trash()` sets `trashed_at`. Trashed rows from new code never hold a cookie.
- **`message-router.ts:1386-1398`** — `thread.list` sweeps trashed rows with `clearTrustCookieWithoutRestore` (no re-restore) **before** `purgeExpiredTrash` (line 1401). Pre-S51 leftover rows are neutralized the moment the trash view loads (`ThreadList.tsx:316-320` sends `thread.list` on open; hard-delete is only reachable from that view, `ThreadList.tsx:276`).
- **`message-router.ts:1207-1210`** — single hard-delete now broadcasts `thread.deleted`, closing the C5 desync nit; `useWebSocket.ts:750` handles it.
- Tests pass (36/36 packs-engine incl. both new S51 tests; trash→Settings flip→hard-delete does not re-restore; trash A→apply B→hard-delete A doesn't clobber B).

## C2 — mid_loop M2 request strip: CLOSED

- **`adapter.ts:570-579`** — when `phase === "mid_loop" && keepSummary && mode === "m1"`, the prior `prevMeta.rolling_summary` is re-attached via `attachRollingSummaryToMessages`, which **replaces the M1 omit notice in-place** (`context-budget.ts:328-339`, single notice, no duplication). `messages` is `let` (line 427) and consumed by the next LLM round after `runContextBudgetPass("mid_loop")` (line 1188).
- Meta persist (lines 589-594) keeps `rolling_summary`/`summary_sha256`/`summary_bytes` from the prior pass. New two-pass test passes.

## Nits (non-blocking)

1. **`adapter.ts:564-565`** — the keep-guard is `phase === "mid_loop"` only: a `pre_loop` M1-only pass (M2 disabled/failed) still overwrites the prior turn's `rolling_summary` with `undefined`, destroying it for「查看摘要」and leaving mid_loop nothing to re-attach. Pre-existing, outside the two blockers.
2. **`adapter.ts:585`** — persist `mode:` ternary is dead logic (the re-attach branch already set `mode = "m2"`); harmless redundancy.
3. **`message-router.ts:1390-1397`** — the leftover-cookie sweep runs on every `thread.list` (including the main list), not just trash-scoped responses; trivial cost, cosmetic.
4. **`message-router.ts:1192-1193`** — a pre-S51 trashed row hard-deleted before *any* `thread.list` sweep would still re-restore the stale cookie; unreachable via UI (trash view load always triggers the sweep), acceptable.

VERDICT: APPROVE_WITH_NITS
