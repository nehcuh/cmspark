Review complete. Here's my assessment:

## Verified (via tools)

**Architecture** — `chatCreate` (adapter.ts:444) now uses `createProvider(config)`; no `new OpenAI` in the chat path. `ChatCreateParams.config` is `LlmConfig` (adapter.ts:81-87); `effectiveLLMConfig = { ...config.llm }` (message-router.ts:414) preserves `protocol`/`auth_style`/`client_header_profile` from `getConfig().llm`. `generateThreadTitle` and `llmExtract` route through `provider.complete`. `rebuildMessagesFromHistory` returns canonical `CanonicalChatMessage[]` with identical pairing/compaction logic.

**OpenAI/DeepSeek parity** — OpenAIProvider keeps baseURL/apiKey(`sk-placeholder`)/timeout 120s/maxRetries 0, `include_usage`, `reasoning_content`, per-index tool delta accumulation, temperature always sent, no max_tokens. Adapter tests mock the OpenAI SDK at the prototype level, so tool rounds still exercise the full provider path on mocks — all green.

**Anthropic path** — fetch+SSE provider (no SDK), conversion hoists system, merges contiguous tool results into `user`/`tool_result`, sanitizes tool ids deterministically, drops `reasoning_content` on wire (M7), computes max_tokens. L7 first-party deny + header policy enforced before fetch; `llm.anthropic_request` logs only header *names* + host + path, never values.

**Scope** — vision untouched (vision-pipeline.ts, server.ts:5430 vision health check, config.testVision all still OpenAI-only as intended). No settings-web HTML/UI changes — only the `/api/test` soft-skip. Model-probe soft-skip + 2 regression tests added.

**Tests** — `tsc -p tsconfig.test.json` clean; ran llm-headers-policy, llm-provider-anthropic, model-probe, adapter, adapter-usage, adapter-recovery, llm-stream-jailbreak-scan, settings-web: **197 pass / 0 fail** (superset of claimed 102).

## Nits (non-blocking)

1. **`extra_headers` values leak unredacted to the extension** — `config.get` (message-router.ts:160) and `redactConfigForBroadcast` (server.ts:4765-4768) spread the whole `config.llm`, so `extra_headers` (documented in config.ts:190 as "values never logged") are broadcast to WS clients with full values, unlike `api_key` and MCP `env` (masked). Mask them like the other secrets.

2. **WS `settings.test` (message-router.ts:312) has no anthropic soft-skip** — it will call `client.models.list()` with Bearer against an anthropic base URL → false 401 failure. Declared P1 in the claim, but only the settings-web HTTP endpoint was patched; the WS-side test path (the one the extension actually uses) was missed and has no test.

3. **`skill-engine.ts:385` (rerank) and `skill-craft.ts:124` still construct OpenAI clients directly** — for `protocol=anthropic` users these send OpenAI wire format + Bearer auth to the Anthropic endpoint. Not listed in the claim's P1 note (only message-router config.test is).

4. **Cosmetic**: Anthropic streaming `llm.usage` loses `prompt_tokens` — `message_delta` carries only output_tokens and adapter's `finalUsage` is last-event-wins; `message_start` input tokens get overwritten.

No blockers: chat is not hardcoded to OpenAI, DeepSeek path preserved (regression-tested), tool rounds pass on mocks, secrets not logged by the provider.

VERDICT: APPROVE_WITH_NITS
