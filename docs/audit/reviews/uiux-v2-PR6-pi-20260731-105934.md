All checks pass. Summary:

| Check | Status | Evidence |
|-------|--------|----------|
| **Board NOT in 装配** | ✅ | `ComposeDrawer.tsx:65` defense guard `if (section.panelId === "board") return`; `COMPOSE_SECTIONS` has no board entry; footNote confirms |
| **Tokens not Material** | ✅ | Zero `#4CAF50` / `#FF9800` / `#F44336` in any changed file |
| **No BottomBar delete** | ✅ | `BottomBar.tsx` not touched |
| **No alert() added** | ✅ | No `alert(` in changed files |
| **Scope: drawer sections** | ✅ | `ComposeDrawer` → full group UI with `Modal` + icons + attach-line + surface chip |
| **Scope: visual density** | ✅ | `ChatView`: token radii, tighter padding, emoji→stroke icons, tool card density |
| **Section groups** | ✅ | capability → connect → record ordering; `COMPOSE_GROUP_LABELS` no 中层/Board |
| **Test coverage** | ✅ | `compose-drawer-pr6.test.ts` covers ontology, groups, attach lines, anti-board |

VERDICT: APPROVE
