**Review Summary — UI S1 acceptance batch**

### Verified

| Check | Result |
|---|---|
| `npm --prefix chrome-extension test` | **269 pass, 0 fail** (includes 12 new §8 tests) |
| Patch vs. live git state | Match confirmed — no stale diff |
| `McpPanel.tsx` STATUS_COLORS → tokens | All 5 hardcoded hex colors replaced with `tokens.success`, `tokens.warning`, `tokens.textMuted`, `tokens.danger`, `tokens.textSecondary` |
| `CockpitApp.tsx` token sweep | `"#22c55e"` → `tokens.success`, `"#9aa0a6"` → `tokens.darkMuted`. Disabled-state colors (`"#374151"`, `"#052e16"`) remain hardcoded (no corresponding token exists — acceptable per spec's "residual nits ok" clause) |
| `App.tsx` meta slash (`/packs` `/board` `/mcp`) | `META_PANEL_SLASH` defined, prepended to `slashSkills`, dispatches `cmspark:open-context-panel` on select; clears the `/` token from input |
| `BottomBar.tsx` event listener | `useEffect` listens for `cmspark:open-context-panel`, validates panel ID against `ALL_TABS`, loads panel data, cleans up on unmount/dep change |
| `ui-mode-acceptance.test.ts` imports | All imports resolve: `deriveCapabilityLevel`, `contextBarTabsForLevel`, `contextBarOverflowTabsForLevel`, `levelBadgeLabel`, `levelEscalateToast`, `ModeInput`, `isCockpitTabUrl`, `COCKPIT_SESSION_KEY`, `tokens`, `riskLabel` |
| Spec §8 checkbox honesty | 12/14 checked (P0–P2), 2 left unchecked for manual smoke / dual-surface — no over-claiming |
| Security | Custom event fires on extension-sidepanel `window`, not page-origin — no XSS vector |
| Pin/queue/cockpit persist | No changes to those code paths; no regression possible |

### Nits

None blocking. The partial hardcoded hexes in `CockpitApp.tsx:493-494` (`"#374151"`, `"#052e16"`) are dark-surface disabled-state variants without token equivalents — the spec explicitly allows "residual nits ok" for the P2 token sweep.

VERDICT: APPROVE
