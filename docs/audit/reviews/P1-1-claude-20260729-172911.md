I've completed my independent review. Here are my findings.

## P1-1 Review — god-mode companion step-up

### Verification performed
- Read diff file vs live `git diff HEAD` — patch is current, not stale.
- Read `companion/src/security-arm.ts` (new) and `message-router.ts:170-285` config.set handler.
- Ran `npx tsc -p tsconfig.test.json` + `node --test .test-dist/tests/message-router-config-security.test.js` → **21/21 pass [executed]**.
- Searched all `saveConfig(` callers in `companion/src/` — confirmed no alternate WS-reachable writer for the three flags.
- Confirmed `companion/src/server.ts` has **zero diff**; CU L2 / shell·netsec `forceConfirm` invariants untouched.

### Strengths
- **Real companion gate, not theater**: `findArmingSecurityFlags` runs in the config.set handler after `normalized.security` is built (both flat path at message-router.ts:204-218 and nested spread at :201-203 feed the same `normalized.security`), and BEFORE `saveConfig` at :270. Missing/wrong phrase → `return { type: "error" }` with no persist.
- **Transition-only**: `isTruthyFlag(proposed) && !isTruthyFlag(current)` correctly exempts disarm (true→false) and already-true Save-snapshot resend. Test `already-true resend without phrase → success` confirms.
- **Downstream strict equality insulates against type-coercion bypass**: every consumer of the three flags uses `=== true` (server.ts:1032-1033, 1104, 1763, 1787-1788, 1409, etc.). Even if a non-boolean truthy slipped through the nested spread path (no `!!` normalization there), downstream gates stay shut.
- **Extension pass-through is real**: `SettingsSlideout.sendSecurityFlagConfig` attaches `confirmation_phrase` on arm; `background/index.ts:517-528` forwards it; companion reads `rest.confirmation_phrase`. End-to-end verified by code path.
- **Tests are integration, not gate mocks**: drive `handleMessage` → `saveConfig` → `getConfig` → real log-file reads asserting `security.arm_rejected` / `security.flag_armed` strings. The P1-1 matrix covers all three flags × {no phrase, wrong phrase, correct phrase, disarm, already-true resend, two-flag one-message, nested}.
- **ADR-020 compliance**: implementer prompt carries full Surface/Compose/Autonomy/Trust/Channel declaration (L0|L1|L2 no expansion; Trust step-up; community). Trust axis is correct; no new runtime/Pack/agent. Trust monotonicity strengthened (UI theater → real gate), not weakened.

### Non-blocking nits
1. **No automated phrase lock-step test**: `"我了解风险"` is duplicated across `companion/src/security-arm.ts:14`, `SettingsSlideout.tsx` `GODMODE_CONFIRM_PHRASE` and `ENT_B_PHRASE`. Comments document lock-step; nothing enforces it. Drift would cause arm failures (fail-closed), not silent bypass — bounded but worth a follow-up assertions test importing both sides.
2. **`logger.warn` for `security.flag_armed`** (message-router.ts:272): successful arm audit at warn level — convention is `info`. Cosmetic.
3. **UI optimistic dispatch precedes WS ack**: `dispatch(SET_CONFIG …)` runs before `sendSecurityFlagConfig` in all three arm handlers. If companion rejects (e.g., phrase drift), UI shows armed while companion stays disarmed until next `config.get`. Companion is ground truth so not a security hole; minor UX desync.
4. **Whole-message reject on missing phrase**: co-bundled LLM fields (e.g., `model_name`) are dropped along with the arm attempt. Documented in plan's risks section; mitigation is focused arm config.set first. Not a regression vs. pre-P1-1 silent-arm.
5. **`let armingFlags`** at message-router.ts:224 could be `const` (never reassigned). Trivial.

### Conclusion
Fix fully addresses P1-1 inventory item. Tests are real integration. No new holes found — strict equality downstream + single WS-reachable writer closes the type-coercion surface. Trust monotonicity holds (gate strictly tightens; CU L2 / shell·netsec forceConfirm unchanged). No commit/push in the diff. ADR-020 capability declaration present and accurate.

Nits listed above are all non-blocking documentation/UX/style items.

VERDICT: APPROVE_WITH_NITS
