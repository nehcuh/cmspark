All verification complete. Here is my independent review.

## Review: day-20260810-11-full (multi-adv deep Wave0–2 + C10 god-file split A→H)

Verified against live tree at `cd3aa9c` (`git diff origin/main...HEAD`, patch file confirmed fresh — 117/117 files identical to live).

### Part 1 — Multi-adv deep fixes (C1–C16): all confirmed at tip
- **C1** ✅ Pre-arm `captureCruiseSnapshot` (message-router.ts:2932) → durable file (0o600, atomic rename) → boot `reconcileUnattendedCruiseOnBoot` (ws/lifecycle.ts:416–437) → disarm gated by `shouldRestoreCruiseOnDisarm` (bare disarm without `clear_cruise` doesn't clobber) → TTL expire restores once. Tested incl. simulated-restart durable-file reconcile (computer-unattended-grant.test.ts:374).
- **C2/C3** ✅ 「任务已停 · 值守仍开 · 点解除」 in SafetyStrip.tsx:91–118, 213–215; Confirm Center permanent 值守 banner (CockpitApp.tsx:212–220).
- **C4** ✅ `forceConfirm = (capabilityForceConfirm || hostComputerGated) && !userFullAutonomy`; `isFullAutonomyCruise` = three-flag only (l2-admission.ts:65–72, 738–758). Default 值守 dual-writes only dangerous+enterprise → evaluate still forceConfirms. autopilot-tier matrix honest.
- **C5** ✅ Server-side phrase gate (pack-engine.ts:1618) + PacksPanel client gate; tests cover rejection path.
- **C6** ✅ WORKER_HARD_DENY re-enforced in `isToolAllowed` (thread-manager.ts:854+) with fail-closed fallback; `thread.update` rejects `tool_whitelist:null` for workers + filters HARD_DENY tools (message-router.ts:1490–1510).
- **C7/C8** ✅ `normalizeShellCwd` / `normalizeNetsecPorts` applied pre-L2 (server.ts:561–571) and in execute (companion-dispatch.ts:611, scan.ts:132).
- **C9/C12** ✅ Lockstep test passes with arm/disarm in CORE_REQUIRED; security-gates asserts confirm arrival + `critical_apis` array — no `force_confirm` false-green.
- **C13–C15** ✅ SUPERSEDED banners; mcp.md `require_grant=true` default; CU guide + ADR-017 + AppsPanel real `computer.set_enabled` toggle (AppsPanel.tsx:192–207).

### Part 2 — C10 split: mechanically sound
- **No circular imports** — no extracted module imports server.ts; lifecycle→message-router is one-directional.
- **Executor order preserved** exactly: normalize → tool.start → pregate → cookie → browser_download → L2 → URL → image → companion/MCP → forward (server.ts:561–985).
- **Algebra moved, not rewritten**: L2 forceConfirm/three-flag logic identical (modulo `isFullAutonomyCruise` extraction); image-fetch gate byte-identical to base; URL gate originWs `isOutboundMcpCall ? {} : { originWs: ws }` identical; MCP critical forceConfirm + originWs (mcp/dispatch.ts:184, 346, 522) intact; evaluate token revalidation intact (l2-admission.ts:1468–1497); config.set phrase step-up intact (handlers/config.ts:141–157).
- **Re-exports intact**: `startServer`, `handleSecurityConfirmationResponse`, `pendingToolCalls`, `enhanceMcpError`, `validateWsMessage`, `broadcastToClients`, `runL2ToolAdmission`, etc.
- **bind\* runtime**: bound at module load (server.ts:915–916), re-bound in `initServices` and `seedThreadManagerForTests`; each module has explicit "not bound" guards.

### Part 3 — Tests (re-ran)
tsc clean; security-gates 63/63; lockstep+unattended 33/33; worker/pregate/tool-forward 19/19; image-fetch/url-cookie/ws-origin/healthz 25/25; integration dir 169 pass/0 fail; broad ws/router/security/mcp/packs batch 609/611 pass/0 fail; packs+shell+download 50/50; origin-ws suites 22/22; computer-executor passes (no pre-existing failure seen).

### ADR-020 checklist
Declaration present and accurate (Surface L2 / Compose Pack+MCP / Autonomy / Trust restore / Channel). Axes fit; no 中层 Agent framing; Pack-first; no new confirm dialect; trust monotonicity holds; originWs bound on new/changed requests; no new runtime.

### Nits (non-blocking)
1. image-fetch confirm (tool/image-fetch-admission.ts:193) still lacks `originWs` — byte-identical to base, so moved-not-regressed, but remains a documented P1-2 residual.
2. Deferred `tabUrlCache` colocation (server-owned, injected via bind — documented in phase-G reviews).
3. C9 lockstep test uses regex extraction of case labels (best-effort; flagged by the adversarial reviewer).
4. Lazy `require()` for shell/netsec normalizers in the hot path (server.ts:561/567) — minor, consistent with existing patterns.

VERDICT: APPROVE_WITH_NITS
