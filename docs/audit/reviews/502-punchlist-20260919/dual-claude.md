All ten findings verified. Writing the report.

# Dual re-review (claude)
HEAD: 022b2f60

## Per-finding
- **X1: TRIGGERED** 鈥?Mid-loop narration is the *norm* (adapter.ts:1585-1592 echoes every pre-tool assistant row with content+tool_calls 鈫?committed by useWebSocket.ts:648-687), and `consolidateRunToolTurns` relocates the last narration row to `trailing` after the block (tool-history-view.ts:386-391), so the live block loses `itemIsLast` and ToolHistoryBlock gets `threadBusy=false`/EMPTY confirms (ChatView.tsx:484,491-492) 鈫?`live` false at ChatView.tsx:1153 鈫?collapsed done-chip only (ChatView.tsx:1235-1259) while a tool is running/L2-pending; violates the slice's own NEVER 銆屾妸 L2 纭涓殑宸ュ叿鎶樿繘鑺€?and AC 銆屽綋鍓嶅崱鍙銆?(plan lines 15, 182). BLOCK stands. (FocusBand MinimalConfirm still shows the confirm itself 鈥?the burial is transcript-side, exactly what the spec forbids.)
- **X2: TRIGGERED** 鈥?`archiveToolPayload` deliberately reuses the SEC-C envelope (tool-persistence-redact.ts:272-306, comment admits it), default tier is on (`persist_full_tool_history: false`, config.ts:495), and the reload UI renders that envelope as 銆屽嚭浜庡畨鍏ㄦ湭鎸佷箙鍖栤€︺€?(ChatView.tsx:1724-1735) 鈥?while the design spec line 124 demands the stub be honestly labeled 銆屾鏂囨湭淇濆瓨銆? Security attribution on every benign navigate/click stub = spec-prohibited copy. BLOCK stands.
- **X3: TRIGGERED** 鈥?L2 preview omits goal (tool/l2-admission.ts:329-330, whereas `spawn_expert_team` includes `goal=` at :333) and the HMAC binding is `spawn|role|pack|alias|allow|deny|intent` with no goal (security-policy.ts:95-101); approval is immediately followed by `persistWorkerBrief`+`kickWorkerChat` (companion-dispatch.ts:328-347). Token minted for goal A validates a dispatch with goal B within TTL; HARD_DENY still at pregate 鈫?MAJOR, not BLOCK.
- **X4: TRIGGERED** 鈥?`kickWorkerChat` calls `adapter.chatCreate` with no `signal` and never registers an AbortController (server.ts:765-794); `abortControllers` is module-private SoT for both `listLlmActiveThreadIds` (message-router.ts:186,196-198) and `abortThreadChat` (230-252, no controller 鈫?`stopped:false`, and `releaseMultiAgentLlmLoop` is skipped inside the `if (controller)` branch); `fleet.stop_all` relies on exactly that (message-router.ts:4436-4438); Glance `llm_active` derives from the same map (fleet.ts:70-73,107). Result: kicked worker streams with Glance showing idle, and 鍏ㄥ仠/chat.abort cannot interrupt the in-flight HTTP run (adapter has no mid-run paused gate 鈥?zero `paused` in adapter.ts). Stop-that-doesn't-stop = BLOCK.
- **X5: TRIGGERED** 鈥?FleetStrip polls `fleet.status` every 4s unconditionally while mounted (FleetStrip.tsx:66-71); each snapshot stamps a fresh `at` (fleet.ts:156) which SET_FLEET writes (useWebSocket.ts:949-960); FocusBand's freshness gate reads that same `at` (FocusBand.tsx:66-75; 10min at focus-band-priority.ts:223; idle+fresh鈫抋ctive at :239). FleetStrip's only mount point is the FocusBand fleet slot (FocusBand.tsx:165) 鈥?to expire you must unmount, to unmount you must first expire. 銆屼笉鏄?forever squat銆?fails in any quiet session. MAJOR.
- **X6: TRIGGERED** 鈥?Archive toggle only dispatches SET_CONFIG and flips its label to 銆屽凡寮€鍚€?(SettingsSlideout.tsx:3449-3465); sibling toggles in the same file send immediate `config.set` (3238-3270), this one doesn't; persistence rides only `handleSave`'s full snapshot (426-433), and the archive decision is made companion-side at write time, so until 淇濆瓨骞跺叧闂?the toggle affects nothing. MAJOR.
- **X7: TRIGGERED** 鈥?Kernel admits `round_limit` into the unarmed suggest branch (loop-kernel.ts:359-378) while `shouldShowRoundLimitHint` checks only `!loopView && terminal==="round_limit"` (LoopStatusRow.tsx:35-40); RoundLimitHint and LoopSuggestCard are independent blocks (ChatView.tsx:558-580) 鈫?both render; the card's click is `task_loop.arm` (LoopStatusRow.tsx:135-143,343-345). #505's 銆屾棤鍙屾彁绀恒€?was fixed only for LoopStatusRow. MAJOR.
- **X8: TRIGGERED** 鈥?Live mirror remembers the pre-archive result (adapter.ts:1250-1256); `quarantinePersistedToolRow` rewrites only persisted rows (adapter.ts:283-331, called at :2344) and never touches the mirror; every same-process run rebuilds with `getLiveMirror` (adapter.ts:836) and live wins over the quarantined stub (adapter.ts:436-441) 鈫?the triggering body is re-sent and content-risk 400s again 鈥?the exact regression the :2342-2343 comment claims the persisted-row sync prevents. live-rebuild-continuity.test.ts covers stub/restart but no content-risk case. MAJOR.
- **X9: TRIGGERED** 鈥?[executed] I recompiled (`tsc -p tsconfig.test.json`, exit 0) and ran both files via `scripts/run-tests.mjs`: **12 tests, 7 pass, 5 fail**, failures all `INVALID_ARGS`/`spawn_worker requires goal` vs expected `SPAWN_PACK_FAILED`/`SPAWN_INTENT_FAILED`/success. Fixtures omit goal: p2-deep-diagnosis-batch.test.ts:98-102,129-132; spawn-rollback-demote.test.ts:63-68 (`mintedSpawnParams`), exercised at :75,:90,:184. Production gate is correct (companion-dispatch.ts:179-187); the tests are stale. The runner discovers all compiled tests (run-tests.mjs walk) 鈫?Linux CI red identically (platform-independent dispatch failure). MACHINE gate FAIL.
- **X10: DOWNGRADED** 鈥?Real [inspected]: UI sends `{embedded_terminal:{enabled}}` (SettingsSlideout.tsx:3262-3271) and the explicit allow-list handler has no `embedded_terminal` branch (handlers/config.ts 鈥?zero hits; only persist_full at :126-128 and coding_handoff at :267). But this is pre-existing at base `93923c1d` (handler 0 hits, toggle already present there) and fail-closed in the dangerous direction (default `enabled:false` config.ts:597-599; all consumers deny on `!==true`: pty/handler.ts:74, open-local-terminal.ts:1225, acp/manager.ts:253). Not introduced by this diff 鈫?NIT as carry-back, not MAJOR.

## New findings (only if grounded)
### N-1 鈥?MAJOR
- File: companion/src/orchestrator/llm-loop-gate.ts:47-56, 80-145; companion/src/message-router.ts:4436-4438
- Evidence: [inspected]
- Failure mode: The deferred-kick queue has no cancel path 鈥?`fleet.stop_all`/`worker.pause`/`chat.abort` clear the loop `nextRun` and in-flight controller, but a kick that was *queued* because the multi-agent cap was full stays in `deferredKickQueue` (only `_resetMultiAgentLlmLoopsForTests` clears it; no production remove API). When any slot releases, `drainDeferredLlmRuns` starts the queued worker's `chatCreate` 鈥?adapter has no paused gate 鈥?so a worker the user just stopped (or never saw start) begins an autonomous LLM run *after* the stop gesture. Narrow window (cap-full at kick time) and X4 already blocks independently, hence MAJOR not BLOCK. Fix belongs in the same X4 rework: stop paths must drop the thread's deferred kick (and drained runs must honor `paused`).

## ADR-020
- Surface: pass 鈥?Side Panel stream + Settings + existing terminal tab; no new primary chrome (embed is click+L2, `darwin 鈭?enabled`, fail-closed at pty/handler.ts:74).
- L2-classes: pass as declared 鈥?none new; X3 is a binding gap on the existing spawn L2 (implementer declared it).
- Compose: pass 鈥?fleet_suggest_propose is advisory-only (catalog: never spawns/arms); Fleet stays ADR-015 threads; no second runtime.
- Autonomy: **fail** 鈥?declared invariant 銆宬ick must not bypass abort map銆?is violated by the implementation (X4, + N-1); 銆宎ccept 涓?arm銆?holds (buildFleetAcceptMessage = chat.send, LoopStatusRow.tsx:188-199).
- Trust: pass 鈥?archive redaction runs unconditionally before stubbing (tool-persistence-redact.ts:313-315); persist_full default off; X2 is copy dialect, not redact relaxation.
- Declaration/channel/blast: present; community; T2 鈥?but the locked gate order requires MACHINE green first, and MACHINE is FAIL (X9, [executed]).

## Notes
- All four BLOCKs reproduce independently at HEAD; I found no synthesis error worth killing. X1/X2 are default-path spec violations (both cite the branch's own NEVER/prohibition lines), X4 is a stop-integrity failure, X9 is a measured red suite.
- X10 and N-1 do not change the outcome; fold X10's allowlist branch and N-1's queue-drop into the same X4/X6 fix commits.
- The four-BLOCK + MACHINE-FAIL state matches the synthesis; nothing here softens the recommendation: fix X1 X2 X4 X9 (+X3/X6/X7) before re-review. Sources of executed evidence: `tsc -p tsconfig.test.json` exit 0; `node scripts/run-tests.mjs .test-dist/tests/p2-deep-diagnosis-batch.test.js .test-dist/tests/spawn-rollback-demote.test.js` 鈫?tests 12 / pass 7 / fail 5.

Blocking issues: X1 (ChatView.tsx:484,491-492 + tool-history-view.ts:386-391), X2 (tool-persistence-redact.ts:272-306 + ChatView.tsx:1734), X4 (server.ts:765-794 + message-router.ts:236-246,4438), X9 (companion-dispatch.ts:179-187 vs p2-deep-diagnosis-batch.test.ts:98-132, spawn-rollback-demote.test.ts:63-90,184 鈥?5 red, [executed]).

VERDICT: REJECT
