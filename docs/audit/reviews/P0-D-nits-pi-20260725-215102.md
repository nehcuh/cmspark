No eslint config to flag it. All evidence collected. Here is the final review:

---

## Review: P0-D nits

### Verification results

| Check | Result |
|---|---|
| `bash scripts/tests/test-package-gates.sh` | **46 passed, 0 failed** |
| `companion/` builds (`tsc` + `npm run build`) | **Clean** |
| `FORCE_INTERACTIVE_DANGEROUS` hoisted to module scope | **Correct — only 2 occurrences, no stale inline declaration** |
| release.yml ORT hard-fail platform-qualified | **Correct — windows-x64 only; macOS/Linux marked "optional soft stage"** |
| package.sh windows-x64 GATE-ONLY prechecks tinyclick + ORT | **Correct — exits 1 on missing** |
| package.sh macOS/Linux ORT is soft (`\|\| echo "NOTE:"`) | **Correct — no abort** |
| models.manifest.json staged in build-windows-exe.ps1 | **Correct — copies when present, warns when absent** |
| ci.yml wires test-package-gates.sh | **Correct** |
| No soft-ship regression of host/tray hard gates | **Confirmed — release.yml body explicit per-platform** |
| No overclaim that mac/linux ORT aborts package | **Confirmed** |

### Nits (non-blocking)

1. **`companion/src/computer/executor.ts:33`** — `FORCE_INTERACTIVE_DANGEROUS` is placed *before* the `import` block (imports start at line 38). While ES module hoisting makes this technically valid and the build passes, it's an unconventional ordering. Move the constant to after the import block.

VERDICT: APPROVE_WITH_NITS
