Reviewed the patch against the working tree (HEAD = 8b70fb8). All 6 scope items land cleanly:

**Verification performed**
- `git diff --stat` matches patch claims (6 files, +166/-9)
- `bash scripts/tests/test-package-gates.sh` → **46 passed, 0 failed** (incl. new dynamic missing-tray / missing-scpt / missing-tinyclick + static ORT-qualification / CI-wire / ps1 manifest asserts)
- `tsc --noEmit` → clean (FORCE_INTERACTIVE_DANGEROUS hoist compiles, no TDZ)
- Companion tests: 1728 pass / 3 fail — re-ran with diff stashed, identical 3 failures (`isUserWritablePath`, `apps.add lolbin`, `log-rotation`), so pre-existing and untouched by this batch
- Verified `companion/models.manifest.json` exists (so the PS1 guard is a defensive Test-Path, not papering over a missing file)

**Faithfulness**
- `release.yml` body now qualifies hard-fail to windows-x64 + macos-arm64, and explicitly documents macOS/Linux ORT as soft stage — matches `package.sh:319/321/372/374` (`stage_onnxruntime ... || echo "  NOTE: ORT … not staged"`)
- `package.sh` GATE-ONLY windows-x64 branch prechecks tinyclick-worker (3 candidate paths) + ORT dir before exit 0 — additive gate, no soft-ship regression of host/tray/TinyClick
- `ci.yml` wires `bash -n` + run of `test-package-gates.sh`
- `executor.ts` hoist is a pure refactor; `forceInteractive` semantics unchanged
- `build-windows-exe.ps1` stages manifest when present, warns otherwise

**Non-blocking nits**
- `companion/src/computer/executor.ts:34-39` — `const FORCE_INTERACTIVE_DANGEROUS` is declared textually *before* the `import` block (line 41+). ESM import hoisting makes this safe at runtime and tsc accepts it, but it is unusual style; no project lint enforces `import/first` today, so non-blocking.
- `scripts/build-windows-exe.ps1:230-237` — missing `models.manifest.json` is a `Warn` (continue), whereas `scripts/package.sh:234` does an unconditional `cp` that under `set -euo pipefail` will hard-fail. Asymmetric, but the PS1 is an auxiliary SEA sidecar path and the scope explicitly says "stage when present" — acceptable.
- `scripts/package.sh:160-168` — the tinyclick precondition accepts the raw `.ts` source as evidence of presence. If `npm run build` later silently failed to emit `dist/computer/tinyclick-worker.js`, the gate-only path would still pass; the later full-package staging at `package.sh:354-358` would still catch it. Acceptable since the gate is documented as a precondition, not a full-build substitute.

No incomplete fixes, no security regressions, no overclaiming, no wrong file:line references.

VERDICT: APPROVE_WITH_NITS
