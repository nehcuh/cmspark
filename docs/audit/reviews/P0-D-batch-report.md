# P0 Batch Fix Report — P0-D

- both_external_approve: true
- build_ok: true
- rounds: 1
- design: {"acceptance":["macOS release zip never ships without cmspark-host and host-scripts/*.scpt","macos-arm64 zip always includes cmspark-tray","Makefile package-macos builds host before package","windows package.sh zip includes host-scripts-win","TinyClick/ORT staged for windows-x64 or explicitly feature-cut in release notes","release notes no longer claim C1/WS shared-secret deferred","package job fails closed on missing host/tray rather than WARNING","scope limited to packaging/release files only"],"batch":"P0-D","files":["scripts/package.sh","Makefile",".github/workflows/release.yml","scripts/build-windows-exe.ps1"],"plan":"Hard-gate macOS packaging: package.sh invokes build:host, hard-fails if cmspark-host/host-scripts/*.scpt (and macos-arm64 cmspark-tray) missing; Makefile package-macos depends on build-host; release.yml asserts zip contents. Stage windows host-scripts-win always (fail if empty); stage tinyclick-worker + platform-trimmed ORT on windows-x64 (mirror build-windows-exe.ps1) or document explicit feature-cut in release body; add --external:onnxruntime-node to package.sh esbuild. Rewrite release.yml body to remove false 'WS auth deferred' claim (C1 HMAC is FIXED_VERIFY) and document packaging honesty/signing limits. No certs, no runtime TS, no P0-A/B/C scope.","risks":"Swift/osacompile failure fails release (desired). ORT ~60MB zip growth if staged. Cross-OS package.sh macos-* must hard-error. Externalizing ORT requires staged node_modules layout matching SEA. Optional ps1 Warn→Fail is symmetry-only.","tests":["package.sh macos-arm64 with cmspark-host deleted → exit 1 (negative)","make package-macos / package.sh → zip lists cmspark-host, cmspark-tray, host-scripts/*.scpt","package.sh windows-x64 staging has host-scripts-win/*.ps1 non-empty","windows staging has tinyclick-worker.js + onnxruntime-node win32/x64 OR release body states TinyClick/ORT feature-cut","release.yml macos assert step: unzip -l greps host+tray+scpt","rg shows no 'WS auth' deferred / shared-secret deferred text in release.yml body"]}
- last_impl: {"batch":"P0-D","diff_stat":"4 files changed, 280 insertions(+), 51 deletions(-) (+ new test-package-gates.sh)","done":true,"files_changed":["scripts/package.sh","Makefile",".github/workflows/release.yml","scripts/build-windows-exe.ps1","scripts/tests/test-package-gates.sh"],"notes":"33/33 package gate tests passed [executed]. ORT win32/x64 staging smoke ~61MB [executed]. Full package.sh zip build not run (downloads Node + full build); CI assert steps cover zip listing. Test-only env: CMSPARK_PACKAGE_GATE_ONLY / CMSPARK_SKIP_HOST_BUILD.","summary":"Hard-gated macOS host/tray/scpt and Windows host-scripts-win + TinyClick/ORT packaging; Makefile package-macos depends on build-host; release.yml asserts zip contents and marks C1 WS auth FIXED (no deferred claim).","tests_added":["scripts/tests/test-package-gates.sh"]}
- last_adv: {"blocking":[],"pass":true,"summary":"P0-D packaging gates hold: macOS host/scpt/tray hard-fail + build:host; Windows host-scripts-win + tinyclick-worker + ORT hard-fail in package.sh and build-windows-exe.ps1; release.yml zip asserts + C1 FIXED (no deferred WS auth). Dynamic tests can fail on missing host/empty win scripts. Residual: soft ORT on mac/linux vs slightly overbroad fail-closed prose; SEA path still lacks models.manifest.json. | P0-D packaging fail-closed is implemented correctly: package.sh hard-requires build:host + cmspark-host/scpt/tray(arm64); Makefile package-macos depends on build-host; windows host-scripts-win + TinyClick/ORT are hard-gated in package.sh and build-windows-exe.ps1; release.yml asserts zip contents and marks WS auth FIXED (no deferred claim). Tests include real dynamic negatives for missing host and missing win scripts; most other checks are static greps and GATE_ONLY never exercises ORT/tinyclick staging. Residual: gate suite not in CI, no dynamic tray/scpt/ORT fail tests, release body slightly overclaims universal ORT fail-closed vs soft mac/linux ORT. | "}
- last_ext: {"both_approve":true,"claude_path":"/Users/huchen/Projects/cmspark/docs/audit/reviews/P0-D-claude-20260725-214108.md","claude_verdict":"APPROVE_WITH_NITS","exit_code":0,"pi_path":"/Users/huchen/Projects/cmspark/docs/audit/reviews/P0-D-pi-20260725-214108.md","pi_verdict":"APPROVE_WITH_NITS","raw_tail":"=== CLAUDE (last 40) ===\nI've inspected the actual code, ran the test suite (33/33 pass), verified the patch matches `git diff`, and confirmed key claims:\n\n- `companion/dist/cmspark-host` (259k), `cmspark-tray` (150k), `host-scripts/*.scpt` (4 files via osacompile) all exist\n- `companion/src/host-use/win/scripts/*.ps1` (18 files) all exist\n- `companion/node_modules/onnxruntime-node/bin/napi-v6/win32/x64/` = 61MB (under 70MB budget)\n- `companion/src/computer/tinyclick-worker.ts` exists and compiles to `dist/computer/tinyclick-worker.js`\n- `build:host` npm script → `bash src/host-use/darwin/build-host.sh`\n- PS1 `Fail` function = `Write-Error; exit 1` (real hard-fail, not Warn)\n- Dynamic test moves `cmspark-host` out then back; verified exit 1 with \"cmspark-host missing\" message\n- Dynamic test renames `host-use/win/scripts` → `.bak`; verified exit 1\n\nScope is clean — only `scripts/package.sh`, `Makefile`, `.github/workflows/release.yml`, `scripts/build-windows-exe.ps1` + new test. No TS/runtime/security code touched. No P0-A/B/C scope creep.\n\n**Nits (non-blocking)**:\n\n1. **Release body overclaims ORT fail-closed scope** (.github/workflows/release.yml:147-149). Body says \"a missing host binary, tray (macos-arm64), Windows host scripts, or TinyClick/ORT payload aborts the release job\". Reality: TinyClick/ORT hard-fails **only on windows-x64** (scripts/package.sh:331-339); macos/linux use `stage_onnxruntime ... || echo \"NOTE: ... not staged\"` (lines 296, 298, 349, 351). The per-platform assets table (line 141-145) is accurate, but the unqualified \"TinyClick/ORT payload aborts\" sentence is slightly overbroad. Adversarial summary flagged this same gap.\n\n2. **Gate test suite not wired into CI** (.github/workflows/release.yml). `scripts/tests/test-package-gates.sh` exists and passes 33/33 locally, but no workflow step invokes it. The release.yml zip-content asserts (lines 66-97) backstop on tag, so this is a missed pre-tag regression catch, not a hole.\n\n3. **GATE_ONLY never exercises ORT/tinyclick staging** (scripts/tests/test-package-gates.sh). The dynamic negatives cover `cmspark-host` missing and `win scripts` dir missing, but not: missing `cmspark-tray` (macos-arm64), missing `*.scpt`, missing `tinyclick-worker.js` (win-x64), or ORT staging failure (win-x64). Those gates are static-grep-only here; CI zip asserts exercise them end-to-end on tag.\n\n4. **Cross-arch macOS packaging unverified** (scripts/package.sh:112-116). Requires `uname -s = Darwin` but doesn't check arch — building on Apple Silicon for `macos-x64` would emit an arm64 `cmspark-host` into an x64 zip. Not in CI matrix (only `macos-arm64`), pre-existing, and out of P0-D scope, but worth noting if macos-x64 is ever shipped.\n\nAll acceptance criteria from the design are met: macOS host/scpt/tray hard-fail, Windows host-scripts-win hard-fail, TinyClick/ORT hard-fail on windows-x64, Makefile `package-macos: build build-tray build-host`, release.yml zip asserts, C1 marked FIXED (no \"deferred\" language). Tests are real and execute actual mutations (move/rename). No new security holes introduced.\n\nVERDICT: APPROVE_WITH_NITS\n\n=== PI (last 40) ===\nNow I have a complete picture. Here's my review:\n\n---\n\n## P0-D Batch Review — Independent Verdict\n\n### What was checked\n- Read all 4 modified files + new test file in full\n- Ran `scripts/tests/test-package-gates.sh` → **33/33 PASS**\n- Verified `rg` for old `WARNING` patterns in `package.sh` → clean (soft warnings replaced with hard `exit 1`)\n- Verified `rg` for `deferred` / `shared-secret.*deferred` in `release.yml` → clean\n- Verified `--external:onnxruntime-node` in both `package.sh` (×2) and `build-windows-exe.ps1` (×2)\n- Verified Makefile `package-macos: build build-tray build-host` dependency\n- Verified `models.manifest.json` staged in `package.sh:211` but **not** in `build-windows-exe.ps1`\n\n### Findings\n\n**Hard gates — all correct:**\n1. **macOS host/scpt/tray**: `package.sh` hard-fails via `exit 1` if `cmspark-host` / `*.scpt` / `cmspark-tray` (arm64) missing. Cross-OS macOS packaging also hard-errors. Double-checked: there's a second staging-time assert after the copy. ✅\n2. **Makefile dependency**: `package-macos` now depends on `build-host`. ✅\n3. **Windows host-scripts-win**: Both `package.sh` and `build-windows-exe.ps1` hard-fail if scripts missing or empty. ✅\n4. **Windows TinyClick/ORT**: `package.sh` windows-x64 case hard-fails on missing `tinyclick-worker.js` or failed `stage_onnxruntime win32 x64`. `build-windows-exe.ps1` hard-fails on missing ORT. ✅\n5. **release.yml zip asserts**: macOS assert greps for `cmspark-host$`, `cmspark-tray$`, `host-scripts/.*\\.scpt$`. Windows assert greps for `host-scripts-win/.*\\.ps1`, `tinyclick-worker\\.js`, `onnxruntime-node`. ✅\n6. **WS auth claim fixed**: Old \"shared-secret handshake is deferred\" text removed; replaced with \"**FIXED** — origin allowlist + shared-secret HMAC handshake\". ✅\n\n### Non-blocking nits\n\n1. **Release body slightly overclaims ORT fail-closed** (`release.yml:148`): The body says \"TinyClick/ORT payload aborts the release job\" without qualification. In reality, `stage_onnxruntime` failure only **hard-aborts** on `windows-x64`; on macOS and Linux it is **soft** (`|| echo \"NOTE: ... not staged\"`). A reader could misinterpret this as meaning ORT is mandatory for all platforms — it isn't. Consider qualifying: \"…or TinyClick/ORT payload (windows-x64) aborts…\"\n\n2. **`build-windows-exe.ps1` doesn't stage `models.manifest.json`**: `package.sh:211` copies it, but the SEA exe builder doesn't. Without it, `defaultManifestPath()` candidates all miss → download/license gate can't read provenance hashes. Pre-existing (not introduced by this patch), but relevant since P0-D touches ORT/TinyClick staging completeness.\n\n3. **Step numbering cosmetic gap**: `package.sh` macOS path uses `[1/9]`, `[2b/9]`, `[3/9]` — the `[2/9]` step is skipped in the numbering (this is cosmetic only).\n\n4. **`GATE_ONLY` mode never exercises `stage_onnxruntime` or TinyClick bundling**: The adversarial reviewer correctly flagged this — the gate tests exit before reaching the esbuild bundle + ORT staging sections. No test covers the `stage_onnxruntime` path end-to-end. Track for follow-up CI integration.\n\nVERDICT: APPROVE_WITH_NITS","verdict_json":"/Users/huchen/Projects/cmspark/docs/audit/reviews/P0-D-verdict-20260725-214108.json"}
- build: {"commands":["git diff --stat && git status -sb && git diff --name-only","bash scripts/tests/test-package-gates.sh"],"log":"Tracked P0-D diff: release.yml, Makefile, build-windows-exe.ps1, package.sh (4 files, +280/-51). Extension/companion not in tracked diff → npm tests skipped. scripts/tests/test-package-gates.sh: 33 passed, 0 failed, EXIT=0. ok=true.","ok":true}
