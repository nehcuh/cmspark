Verified the diff matches the working tree, all 24 tests across the 4 test files pass, and the production code at server.ts:243-250, adapter.ts:565-571 + 903-913, and useWebSocket.ts:139-241 matches what the implementer claims. The P0-B acceptance items hold:

- `chat.abort` silently calls `flipAllComputerTaskAborts()` at server.ts:3477-3479 before falling through to `handleMessage` for the AbortController/chat.aborted path
- `shouldApplyStreamEvent` pure helper gates `chat.token`/`done`/`aborted`/`error` by `msg.thread_id === activeThreadRef.current` (useWebSocket.ts:54-60)
- `streamingRef` cleared via `useEffect(..., [state.activeThreadId])` (useWebSocket.ts:139-142) and on error/abort
- Inter-tool abort throws `AbortError` (adapter.ts:567-571) → caught by the M10 rollback path at adapter.ts:918-934 → `deleteMessagesFrom` → re-throw → message-router emits `chat.aborted`
- `shouldStop` rolls back via `deleteMessagesFrom(savedAssistantId)` with no follow-up `chat.done` (adapter.ts:903-913)
- `rebuildMessagesFromHistory` tracks `openToolCallIds` and skips unpaired tool rows (adapter.ts:154-211); verified the assistant branch clears the open set in BOTH the valid and stripped paths, so a stray text-only assistant between a tool_call and its result can't leak the open id forward

Non-blocking nits:

1. `companion/src/server.ts:226-242` — Two consecutive JSDoc blocks. The old "WP2 (§E.6) F1: computer.task.abort WS handler" block (lines 226-236) now visually documents `flipAllComputerTaskAborts` rather than `handleComputerTaskAbort`, leaving the latter without any doc. Move the old block back above `handleComputerTaskAbort` (line 252) and keep the new P0-B block above `flipAllComputerTaskAborts`.

2. `companion/tests/integration/computer-task-abort.test.ts:133-153` — The "chat.abort silent flip" test calls `flipAllComputerTaskAborts()` directly rather than driving a `chat.abort` WS message through the seam. The acceptance reads "chat.abort flips all computerTaskAborts registry entries" — the test proves the helper, not the wiring (server.ts:3477-3479 `if (msg.type === "chat.abort")` branch is not exercised). The implementer/adversarial summary already flags this; leaving it as a follow-up is fine but it should land before claiming the chat.abort integration is verified end-to-end.

3. `chrome-extension/src/sidepanel/hooks/useWebSocket.ts:255+` — `tool.start` and `tool.result` events are still ungated by `thread_id`. A late `tool.result` from the previous thread will `ADD_MESSAGE` with `msg.tool_call_id` as the UI id and no `thread_id` field. Out of P0-B scope per plan ("tool.cancel deferred", "tool.start/result ungated" residuals), but worth a tracking TODO so it doesn't become the next thread-bleed bug.

4. `companion/src/llm/adapter.ts:198-202` — `rebuildMessagesFromHistory` emits tool rows whose `tc.id` is in `openToolCallIds` and silently drops others. If a future persisted format ever stores multiple tool_call_ids in a single tool row that span different assistant turns, the ordering assumption (each tool row corresponds to the most recent assistant with tool_calls) could mis-pair. Low risk given current storage layout; documenting the assumption inline would help.

5. `chrome-extension/src/sidepanel/hooks/useWebSocket.ts:139-142` — The `useEffect` clears `streamingRef` after render, but `activeThreadRef` updates synchronously during render. There's a sub-frame window where a legacy `chat.done` (no `thread_id`) arriving between render and useEffect commit could `ADD_MESSAGE` the stale buffer. The gate is the primary defense (correct), so this is defense-in-depth; an integration test asserting the clear fires before any late `chat.done` would close the residual the adversarial summary names.

All in-scope SEC/SRV/integration items are addressed, tests are real (not auto-pass mocks), no new holes introduced.

VERDICT: APPROVE_WITH_NITS
