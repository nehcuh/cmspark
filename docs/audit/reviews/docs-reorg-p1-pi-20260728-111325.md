All verifications pass. Here's the summary:

---

**Claim-by-claim verification against actual files on disk (branch `docs/reorg-phase12-0.3.0`):**

| # | Claim | Result | Evidence |
|---|-------|--------|----------|
| 1 | README free of `等待用户确认机制完成后开放` | **PASS** | `grep` returns no matches |
| 2 | README FAQ L2 + SecurityConfirmationManager + Confirm Center/Cockpit | **PASS** | Line 697: `SecurityConfirmationManager` → Side Panel / **Confirm Center（Cockpit）** + HMAC `security_token` |
| 3 | README stage 0.3.0 MVP stable + delivered + opt-in | **PASS** | Line 726: `当前阶段（0.3.0）` — MVP 已稳定 + 已交付扩展 + 进阶/opt-in |
| 4 | README Node >=20 and package examples 0.3.0 | **PASS** | Line 79: `Node.js ≥ 20`; Lines 604-605: `CMspark-v0.3.0-macOS.dmg` / `cmspark-v0.3.0-macos-arm64.zip` |
| 5 | README Swift NSStatusBar tray + pairing window | **PASS** | Line 368: `Swift NSStatusBar 原生托盘 + 配对码窗口`; Lines 410-414: pairing window details |
| 6 | GOAL G8 multi-gate; not live risk-engine/privilege-manager | **PASS** | Line 117: `多门独立门禁（非已删除的 risk-engine / privilege-manager 三层设计）`; dirs absent on disk |
| 7 | ADR-016 status Implemented (P0) | **PASS** | Line 4: `Implemented (P0)`; `companion/src/board/` exists with schema/service/intent-claim |
| 8 | architecture no ConnectionStatus.tsx / live security-token.ts / cmspark/ phantom | **PASS** | Line 493 explicitly documents they don't exist; `cmspark/` is doc tree-root label, not phantom |
| 9 | architecture tree spot-check paths exist on disk | **PASS** | 37/37 key paths verified present (companion + extension + docs) |
| 10 | TESTING no live server.test.ts; domain map matches real tests/ | **PASS** | `server.test.ts` ABSENT; 117 test files in `companion/tests/`; 13/13 domain-mapped files all present |
| — | Phase2 (docs/README.md nav + capability matrix) still pending | **CONFIRMED** | `docs/README.md` does not exist |

**Independent findings:**
- The diff shows 16 files modified (654 insertions, 240 deletions) across allowed docs paths — these are Phase1 implementation changes already on the branch, being verified here. No new writes needed.
- The untracked `chrome-extension/src/background/active-tab-hostname.ts` and related files are the unrelated site-knowledge hostname WIP correctly excluded from scope.
- No security regressions, missing tests, or over-claiming detected.

VERDICT: APPROVE
