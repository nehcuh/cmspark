Independent verification complete. Findings:

**COMP-2 (reL2 force-interactive carve-out) — verified correct**
- `executor.ts:620-625` correctly adds `FORCE_INTERACTIVE_DANGEROUS = {computer.danger_detected, computer.experimental_suggestion}` and gates the trust auto-approve with `!forceInteractive`.
- All six `reL2` call sites inspected (lines 752/854/924/958/984/1381); only `danger_detected` (924) and `experimental_suggestion` (958) are in the carve-out set. Budget / uncross / foreground_yielded / task_induced_dialog remain trust-eligible as designed.
- `session-trust.ts` diff is comment-only (no API change).
- Both new tests are authentic: FakeLocator with `确认删除` (a `CAUTION_WORD`) triggers `regionLevel==="caution"` → `reL2(["computer.danger_detected"])`. Without the carve-out, `trust.isTrusted` would return true and `confirm.captured.length` would be 0, failing the assertion. Same shape for the G4 test using `fakeTinyClickLocator` + `FakeLocator([])`. Both verify `dangerousApis` tag, the right deny errorCode, and zero injects.
- Full suite run via `node --test .test-dist/tests/computer-executor.test.js`: **96/96 pass** including the two new tests.

**COMP-1 (Darwin client→screen) — verified correct**
- `host.swift:563-616` adds `cuClientOriginScreen(windowId:)` mirroring the screenshot AX-by-frame matcher (`host.swift:783-819`); `screen = clientOrigin + (x, y)` in logical CG points.
- `host.swift:982-991` (cuInject click family) fail-closes with `HWND_DEAD` when origin can't be resolved, posts CGEvent at `screenPt`, and JSON returns both client (`x/y`) and screen (`screenX/screenY`) coords.
- `host-skylight.swift:633-681` and `:1048-1083` are lockstep with host.swift (same matcher, same JSON shape, SkyLight SPI variant only differs in `slPostToPid` vs `.post(tap:)`).
- Windows `computer-input.ps1:286-369` unchanged (still uses `ClientToScreen`, still reports `screenX/screenY`); schema parity confirmed.
- Production binary `companion/dist/cmspark-host` exists, mtime `Jul 25 13:55:08` (after source `13:54:34`), SHA256 `29ee68d6…` matches implementer claim. `otool -s __TEXT __cstring` confirms the literal `cannot resolve client origin for windowId` is embedded — rebuild is real, not stale.

**Out-of-scope items** (occlusion, scroll cursor, drag stub, Stop/abort, server grant) confirmed untouched — diff stat matches (4 tracked files, +143/-11).

**Nits (non-blocking)**
- `cuClientOriginScreen` `as? UInt32` filter on `kCGWindowNumber` would silently fall back to `info = raw` if the cast type ever drifts; same pattern as `cuScreenshot`, so consistent, but a one-line `as? Int` fallback would harden it.
- No pure-math `--dry-run` fixture for the screen-mapping math (design marked it optional; math is inline and trivial).
- No regression test asserting `budget_exhausted` still auto-approves under trust (design marked optional; FOREGROUND-YIELD coverage at line 1207 already exercises the trust path).

VERDICT: APPROVE_WITH_NITS
