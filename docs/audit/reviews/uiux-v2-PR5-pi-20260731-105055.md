**Review: PR5 — Hide/remove BottomBar strip behind flag**

### Changes analyzed (6 files, +32/-14)

| File | Change | Verdict |
|------|--------|---------|
| `ui/flags.ts` (new) | Compile-time flag `ui.bottomBarStrip: false` | ✅ Matches spec §4.7 M3 |
| `App.tsx` | `{ui.bottomBarStrip ? <BottomBar /> : null}` | ✅ Clean gate, comment cites M3/PR5 |
| `BottomBar.tsx` | Early `return null` when flag off; inner → `BottomBarStrip` (un-exported) | ✅ Strip chrome preserved for smoke/rollback; no deletion |
| `ContextPanelHost.tsx` | Comment-only: Host SoT clarified | ✅ No logic change |
| `mode-controller.ts` | Comment-only: demotion docs updated | ✅ No logic change |
| `DESIGN.md` | BottomBar tab row doc → flag-gated, default `false` | ✅ SoT updated |

### Spec conformance

| Check | Result |
|-------|--------|
| **ADR-020 fidelity** | ✅ No new capability axes, no Surface/Composition/Autonomy changes |
| **Tokens not Material** | ✅ Zero hex colors in diff; `flags.ts` has no styling |
| **No BottomBar delete** | ✅ `BottomBarStrip` preserved internally; gate only hides render, Host remains SoT |
| **Offline no `alert()`** | ✅ PR1 concern; not touched here |
| **Default off after smoke** | ✅ `bottomBarStrip: false` |
| **Host SoT maintained** | ✅ `CONTEXT_PANEL_TABS` unchanged; test confirms all 8 panel ids present |
| **Chips+装配+slash parity** | ✅ Test file (`bottom-bar-strip-flag.test.ts`) validates L0/L1/L2 chips, `COMPOSE_SECTIONS` has packs/MCP but not board, slash names cover all paths |
| **No scope creep** | ✅ Only strip visibility gating — no new components, no layout changes, no API changes |

### Nits

- None. Clean, minimal, exactly scoped to PR5.

---

VERDICT: APPROVE
