# P0 Batch Fix Report — P0-B

- both_external_approve: true
- build_ok: true
- rounds: 1
- design: {"acceptance":["Stop during host_computer sets computerTaskAbort and stops further injects without separate 急停","chat.token/done/error/aborted for non-active thread_id do not mutate active UI","SET_ACTIVE_THREAD / activeThreadId change clears streamingRef so late tokens cannot reappear","shouldStop multi-tool leaves no unpaired tool_calls on disk","inter-tool abort rolls back via deleteMessagesFrom like M10 AbortError path","legacy corrupt history rebuild skips orphan tool rows (no next-turn 400)","terminal chat.error is not followed by adapter chat.done committing partial stream as complete","UI chat.error clears streamingRef so residual tokens are not ADD_MESSAGE'd as completed assistant"],"batch":"P0-B Stop/stream/thread lifecycle","files":["companion/src/server.ts","companion/src/llm/adapter.ts","chrome-extension/src/sidepanel/hooks/useWebSocket.ts","companion/tests/m10-abort-orphans.test.ts","companion/tests/integration/computer-task-abort.test.ts","companion/tests/adapter.test.ts","chrome-extension/tests/stream-thread-gate.test.ts"],"plan":"1) server.ts: on chat.abort (before handleMessage) flip all computerTaskAbort flags (global *, same registry as handleComputerTaskAbort; avoid message-router→server import cycle). 2) useWebSocket.ts: ignore chat.token/done/error/aborted when msg.thread_id set and !== activeThreadRef; clear streamingRef on chat.error/aborted and on activeThreadId change; chat.done prefer msg.thread_id. 3) adapter.ts: inter-tool signal.aborted rethrow AbortError (not quiet break); shouldStop path deleteMessagesFrom(savedAssistantId) and do NOT send chat.done after chat.error; history rebuild track openToolCallIds and skip unpaired role=tool rows. Optional only: App.tsx dual-send computer.task.abort *; tool.cancel stretch deferred.","risks":"Global * abort is correct for single-task registry but aborts any concurrent task if invariant ever weakens. Full round deleteMessagesFrom drops partial tool tape (intentional). Quiet-break→throw may surface abort banner after partial tool UI events. Prefer silent flag flip on chat.abort to avoid double computer.task.abort.ack. Skip tool.cancel until extension has a consumer.","tests":["chat.abort flips all computerTaskAbort registry entries (reuse getComputerTaskRegistryForTests)","m10: multi-tool shouldStop/non_recoverable → deleteMessagesFrom; no dangling tool_calls on disk","m10: inter-tool abort between tools rolls back round (no partial push into next create)","adapter history rebuild: unpaired tool rows skipped so OpenAI schema is valid","shouldStop wire: chat.error without subsequent chat.done","pure helper: stream events rejected when thread_id !== active","regression: existing m10 abort + computer.task.abort F1 tests stay green"]}
- last_impl: {"batch":"P0-B Stop/stream/thread lifecycle","diff_stat":"7 files changed, 334 insertions(+), 58 deletions(-) (incl. stream-thread-gate.test.ts untracked)","done":true,"files_changed":["companion/src/server.ts","companion/src/llm/adapter.ts","chrome-extension/src/sidepanel/hooks/useWebSocket.ts","companion/tests/m10-abort-orphans.test.ts","companion/tests/integration/computer-task-abort.test.ts","companion/tests/adapter.test.ts","chrome-extension/tests/stream-thread-gate.test.ts"],"notes":"tool.cancel deferred per plan. Existing m10 abort + F1 computer.task.abort tests remain green. No commit/push.","summary":"chat.abort silently flips all computerTaskAbort flags; useWebSocket gates stream events by thread_id and clears streamingRef on error/abort/thread switch; adapter shouldStop/inter-tool abort rolls back via deleteMessagesFrom without chat.done after chat.error; history rebuild skips unpaired tool rows.","tests_added":["companion/tests/m10-abort-orphans.test.ts (inter-tool abort + shouldStop no chat.done)","companion/tests/integration/computer-task-abort.test.ts (chat.abort silent flip)","companion/tests/adapter.test.ts (rebuildMessagesFromHistory orphans)","chrome-extension/tests/stream-thread-gate.test.ts"]}
- last_adv: {"blocking":[],"pass":true,"summary":"P0-B acceptance holds under adversarial inspection: chat.abort silently flips all computerTaskAbort flags then falls through to AbortController/chat.aborted; useWebSocket gates chat.token/done/error/aborted by thread_id and clears streamingRef on thread switch and on error/abort; adapter inter-tool abort throws AbortError and shouldStop rolls back via deleteMessagesFrom without chat.done after terminal chat.error; rebuildMessagesFromHistory strips incomplete assistants and skips orphan tool rows. Residual: deferred tool.cancel for extension tools, concurrent multi-thread streamingRef race, ungated tool.start/result, weak WS wiring tests for silent flip. | P0-B production paths match claims: chat.abort silently calls flipAllComputerTaskAborts then falls through to AbortController/chat.aborted; useWebSocket gates token/done/aborted/error via shouldApplyStreamEvent and clears streamingRef on error/abort/activeThreadId change; adapter inter-tool abort throws AbortError for deleteMessagesFrom rollback, shouldStop rolls back without chat.done, rebuildMessagesFromHistory skips unpaired tool rows. m10/adapter tests assert real disk+wire behavior (not auto-pass mocks). No blocking defects; residual: chat.abort WS seam and streamingRef clear untested at integration level; tool.cancel deferred; tool.start/result still ungated. | "}
- last_ext: {"both_approve":true,"claude_path":"/Users/huchen/Projects/cmspark/docs/audit/reviews/P0-B-claude-20260725-133741.md","claude_verdict":"APPROVE_WITH_NITS","exit_code":0,"pi_path":"/Users/huchen/Projects/cmspark/docs/audit/reviews/P0-B-pi-20260725-133741.md","pi_verdict":"APPROVE","raw_tail":"===== CLAUDE (last 40 / full 24 lines) =====\nVerified the diff matches the working tree, all 24 tests across the 4 test files pass, and the production code at server.ts:243-250, adapter.ts:565-571 + 903-913, and useWebSocket.ts:139-241 matches what the implementer claims. The P0-B acceptance items hold:\n\n- `chat.abort` silently calls `flipAllComputerTaskAborts()` at server.ts:3477-3479 before falling through to `handleMessage` for the AbortController/chat.aborted path\n- `shouldApplyStreamEvent` pure helper gates `chat.token`/`done`/`aborted`/`error` by `msg.thread_id === activeThreadRef.current` (useWebSocket.ts:54-60)\n- `streamingRef` cleared via `useEffect(..., [state.activeThreadId])` (useWebSocket.ts:139-142) and on error/abort\n- Inter-tool abort throws `AbortError` (adapter.ts:567-571) → caught by the M10 rollback path at adapter.ts:918-934 → `deleteMessagesFrom` → re-throw → message-router emits `chat.aborted`\n- `shouldStop` rolls back via `deleteMessagesFrom(savedAssistantId)` with no follow-up `chat.done` (adapter.ts:903-913)\n- `rebuildMessagesFromHistory` tracks `openToolCallIds` and skips unpaired tool rows (adapter.ts:154-211); verified the assistant branch clears the open set in BOTH the valid and stripped paths, so a stray text-only assistant between a tool_call and its result can't leak the open id forward\n\nNon-blocking nits:\n\n1. `companion/src/server.ts:226-242` — Two consecutive JSDoc blocks. The old \"WP2 (§E.6) F1: computer.task.abort WS handler\" block (lines 226-236) now visually documents `flipAllComputerTaskAborts` rather than `handleComputerTaskAbort`, leaving the latter without any doc. Move the old block back above `handleComputerTaskAbort` (line 252) and keep the new P0-B block above `flipAllComputerTaskAborts`.\n\n2. `companion/tests/integration/computer-task-abort.test.ts:133-153` — The \"chat.abort silent flip\" test calls `flipAllComputerTaskAborts()` directly rather than driving a `chat.abort` WS message through the seam. The acceptance reads \"chat.abort flips all computerTaskAborts registry entries\" — the test proves the helper, not the wiring (server.ts:3477-3479 `if (msg.type === \"chat.abort\")` branch is not exercised). The implementer/adversarial summary already flags this; leaving it as a follow-up is fine but it should land before claiming the chat.abort integration is verified end-to-end.\n\n3. `chrome-extension/src/sidepanel/hooks/useWebSocket.ts:255+` — `tool.start` and `tool.result` events are still ungated by `thread_id`. A late `tool.result` from the previous thread will `ADD_MESSAGE` with `msg.tool_call_id` as the UI id and no `thread_id` field. Out of P0-B scope per plan (\"tool.cancel deferred\", \"tool.start/result ungated\" residuals), but worth a tracking TODO so it doesn't become the next thread-bleed bug.\n\n4. `companion/src/llm/adapter.ts:198-202` — `rebuildMessagesFromHistory` emits tool rows whose `tc.id` is in `openToolCallIds` and silently drops others. If a future persisted format ever stores multiple tool_call_ids in a single tool row that span different assistant turns, the ordering assumption (each tool row corresponds to the most recent assistant with tool_calls) could mis-pair. Low risk given current storage layout; documenting the assumption inline would help.\n\n5. `chrome-extension/src/sidepanel/hooks/useWebSocket.ts:139-142` — The `useEffect` clears `streamingRef` after render, but `activeThreadRef` updates synchronously during render. There's a sub-frame window where a legacy `chat.done` (no `thread_id`) arriving between render and useEffect commit could `ADD_MESSAGE` the stale buffer. The gate is the primary defense (correct), so this is defense-in-depth; an integration test asserting the clear fires before any late `chat.done` would close the residual the adversarial summary names.\n\nAll in-scope SEC/SRV/integration items are addressed, tests are real (not auto-pass mocks), no new holes introduced.\n\nVERDICT: APPROVE_WITH_NITS\n\n===== PI (last 40 / full 30 lines) =====\nAll 8 P0-B tests pass. The 3 pre-existing failures (`apps-config NTFS`, `apps-handlers lolbin`, `log-rotation`) are unrelated to P0-B. Here is my complete review:\n\n---\n\n## Review Summary: P0-B Stop/stream/thread lifecycle\n\n### Scope verification (claims vs. reality)\n\n| Claim | Verdict | Evidence |\n|---|---|---|\n| `chat.abort` silently flips all `computerTaskAbort` flags | ✅ CORRECT | `server.ts:3477` calls `flipAllComputerTaskAborts()` before fall-through to AbortController/chat.aborted. Refactored `handleComputerTaskAbort` to delegate `*` wildcard to same function. No double-ack. |\n| `useWebSocket` gates `chat.token/done/error/aborted` on `thread_id` | ✅ CORRECT | `shouldApplyStreamEvent()` pure helper at L43-55. All 4 event types gated. Missing/empty `thread_id` → apply (legacy compat). Tested in `stream-thread-gate.test.ts`. |\n| `streamingRef` cleared on `SET_ACTIVE_THREAD` change | ✅ CORRECT | `useEffect` at L141-143 clears `streamingRef` whenever `state.activeThreadId` changes. |\n| `chat.error` clears `streamingRef` to prevent late `chat.done` committing partial stream | ✅ CORRECT | L237-239: `streamingRef.current = \"\"` + `SET_STREAMING(\"\")` before error dispatch. |\n| Inter-tool abort throws `AbortError` instead of quiet-break | ✅ CORRECT | `adapter.ts:565-569`: `if (signal?.aborted) { throw AbortError }`. Caught in outer catch at L919, rolls back via `deleteMessagesFrom`. |\n| `shouldStop` path: `deleteMessagesFrom` + no `chat.done` after terminal `chat.error` | ✅ CORRECT | L904-912: `deleteMessagesFrom(threadId, savedAssistantId)` then `return` — no `chat.done` emitted. `chat.error` was already sent in the error-classification branches. |\n| `rebuildMessagesFromHistory` skips unpaired tool rows | ✅ CORRECT | L172-191: `openToolCallIds` set tracks valid assistant `tool_calls`; orphan `role=tool` rows with no matching id in the set are skipped. Stripped assistant rounds also clear `openToolCallIds`. |\n| Tests are real (no auto-pass mocks) | ✅ CONFIRMED | `m10-abort-orphans.test.ts` uses real HTTP server + real `chatCreate` + real `ThreadManager` on temp disk. `adapter.test.ts` tests pure `rebuildMessagesFromHistory`. `computer-task-abort.test.ts` tests real registry mutations via WS server. `stream-thread-gate.test.ts` tests pure helper. |\n\n### Non-blocking nits\n\n1. **`messages.pop()` dead-code fallback** (`adapter.ts:910`): In the `shouldStop` path, `savedAssistantId` is always set (assistant message is persisted at L527 before tool-execution for-loop begins). The `else { messages.pop() }` branch is unreachable defensive code. Harmless but misleading.\n\n2. **`chat.aborted` handler uses `activeThreadRef.current` directly** (`useWebSocket.ts:200-212`): Unlike `chat.done` which prefers `msg.thread_id` via `doneThreadId`, the `chat.aborted` ADD_MESSAGE uses `activeThreadRef.current` directly for `id` and `thread_id`. Since `shouldApplyStreamEvent` already gates, the values are equivalent in practice, but the inconsistency with the `chat.done` pattern is a minor style nit.\n\n3. **Ungated `tool.start`/`tool.result`** (residual, out of scope): As noted in the design doc and adversarial summary, tool events are not gated by `thread_id`. This is explicitly deferred. Not a P0-B regression.\n\n4. **`rebuildMessagesFromHistory` dual `openToolCallIds.clear()`** (L167, L178): The set is cleared in both branches of the `validToolCalls` if/else. Could be hoisted once before the branch.\n\nVERDICT: APPROVE","verdict_json":"/Users/huchen/Projects/cmspark/docs/audit/reviews/P0-B-verdict-20260725-133741.json"}
- build: {"commands":["git diff --stat","cd chrome-extension && npm test","cd companion && npm test -- tests/adapter.test.ts tests/m10-abort-orphans.test.ts tests/integration/computer-task-abort.test.ts","cd chrome-extension && tsc -p tsconfig.test.json && node --test .test-dist/tests/stream-thread-gate.test.js","cd companion && tsc -p tsconfig.test.json && node --test .test-dist/tests/adapter.test.js .test-dist/tests/m10-abort-orphans.test.js .test-dist/tests/integration/computer-task-abort.test.js"],"log":"git diff --stat: 7 files (extension useWebSocket, companion adapter/server, 3 companion test files, session.md) + untracked stream-thread-gate.test.ts.\nP0-B targeted extension stream-thread-gate: 5/5 pass.\nP0-B targeted companion (adapter + m10-abort-orphans + computer-task-abort): 24/24 pass (orphan tool skip, silent chat.abort, inter-tool abort, multi-tool shouldStop).\nFull extension suite: 218/219; 1 unrelated fail appsPlatformSupported (apps-panel-logic).\nFull companion suite: 1726/1745; 3 unrelated fails (apps-config NTFS case, apps-handlers lolbin path, log-rotation retention).\nNo commit.","ok":true}
