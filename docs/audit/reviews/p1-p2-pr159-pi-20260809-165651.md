## Independent Review — PR #159 health-fanout P1/P2 (+ P0 stack)

### What I verified (real code, live tree at `5ba41f0`, patch confirmed non-stale vs `git diff 0de1760..HEAD`)

**P0 re-spot-check — all hold:**
- **SEC-A** `thread-manager.ts` — `isSafeThreadId` + `path.resolve` containment in `threadFilePath`; `addMessage` throws, readers/writers skip unsafe ids.
- **SEC-B** `message-router.ts` — `requireMcpStdioSpawnConfirm` (riskLevel high, `autoConfirmEligible:false`, `mcp.stdio.spawn` criticalApi, fail-closed without session); re-confirm on spawn-surface change incl. `toggle_server` enable; `***` secret restore via `mergeMcpServerPreservingSecrets`.
- **SEC-C** `tool-persistence-redact.ts` + `adapter.ts` — `createToolResultMessage` redacts before durable thread JSON.
- **SEC-D** generation CAS — `chat.create` ordering (abort → drain → acquire → bump gen) is correct; re-entrant acquire makes chat.create→chat.create supersede leak-free; `abortThreadChat` releases gate + bumps gen (tested).
- **SEC-E** `pendingToolCalls.originWs` bound in both executor paths; `handleToolResult(msg, ws)` rejects foreign peers; `applyConnectionCloseGracePeriod(ws)` scoped.
- **SEC-F** both `host.swift` and `host-skylight.swift` match `kCGWindowNumber` instead of `windows.first`.
- **VOICE-01** `resolveWhisperCliOnPath` only reachable under `CMSPARK_WHISPER_PATH_FALLBACK=1` (2 call sites, grep-verified).
- **MCPO-01** `require_grant: true` default; e2e asserts `ws_secret` → 401 `GRANT_REQUIRED`; opt-out is explicit+documented.

**P1 security gates — real, server-side, not client-only:**
- `isChromeExtensionOrigin` fence on all `voice.model.*` + `voice.stt.*`, wired from the WS `Origin` header (`server.ts:7108 → message-router.ts:2004/2014`), enforced in-handler.
- `privacy_ack_v2` + `sttEngine===local` enforced in `handleVoiceSttMessage` AND `validateWsMessage`; `set_engine local` requires wire ack + ready-model probe.
- VOICE-02 pin fail-closed: `allowUnpinned: false` when pin missing (test updated to assert refusal).
- Meeting GC: `gcExpiredMeetingAudio` at boot + 6h `setInterval`, containment via `resolveContained`, purge tested with real file deletion.
- CU UI: AppsPanel toggle routes to existing `computer.set_enabled` → biometric gate; disable free / enable gated.
- Release: preflight test+audit job (gating), SHA256SUMS, `package.sh` version lock-step. Deferred items (god-file, full pin matrix, codesign, full startServer) are honestly deferred, not overclaimed.

**Tests:** 132 targeted tests pass when invoked directly — origin refusal, privacy-ack refusal, engine gate, L2 approve/deny, grant e2e, strict WS, GC purge all assert *security behavior*; nothing loosened. ADR-020 declaration present and accurate (no new confirm family, no new runtime, originWs bound, trust monotonicity intact).

---

### BLOCKING ISSUE

**`companion/scripts/run-tests.mjs:5` — new file this PR, `npm test` hard-crashes.**
```
 * Runs all .test-dist/tests/**/*.test.js except:
```
The `*/` inside `**/` prematurely closes the JSDoc block comment, leaving `*.test.js except:` as code → `SyntaxError: Unexpected token '*'`. Reproduced minimally (`node t.mjs`), and `cd companion && npm test` fails identically (Node 24 here; comment tokenization is identical on Node 22 CI).

Consequences:
- **CI breaks** — `ci.yml` "Run companion tests" step (`cd companion && npm test`) goes red on this PR.
- **Release preflight breaks** — `release.yml` preflight job runs the same command.
- **Machine evidence is overstated** — "132 targeted tests pass" only holds via direct `node --test` invocation, not the official entrypoint the closeout doc itself says to re-run (`cd companion && npm test`). The claimed-FIXED P2 runner item is broken, and it regresses a previously working (Unix) test command.

This is a claimed-complete deliverable that fails on first execution and breaks the test gate. One-line fix (drop the `*` before `/` in the comment), but the PR as submitted is not mergeable.

---

### Non-blocking nits (for the follow-up)
1. `message-router.ts` file.upload (~L941) / chat.regenerate (~L1174) supersede paths bump the generation without releasing a predecessor chat.create's acquired multi-agent cap → permanent cap-slot leak on multi-agent threads (availability only; cap 5 exhaustion → `MULTI_AGENT_LLM_CAP`). Not reachable from the guarded UI; fix by releasing in the supersede branch.
2. `host-skylight.swift` `cuWindowInfoDict` fallback accepts a lone result with `kCGWindowNumber == 0` even when `windowId ≠ 0`.
3. `isChromeExtensionOrigin` accepts any `chrome-extension://<id>` — consistent with the pre-existing WS policy; the HMAC handshake remains the real boundary (documented).

VERDICT: REJECT
