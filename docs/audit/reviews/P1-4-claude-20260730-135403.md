Review complete. The fix is correct, tests are real (re-ran: 9/9 + 63 related regression tests green), patch is not stale, and ADR-020 trust monotonicity holds.

## Summary

**Fix correctness** (companion/src/capability/shell.ts:31-50): The `SHELL_ALLOWLIST_METACHAR_RE = /[;|&`$()<>\n\r]/` ban is placed at line 44, *before* the prefix match at line 55 — closing the `echo ok; rm …` chaining vector under `spawn(command, {shell:true})` (shell.ts:107). Shared gate via `commandAllowedByPolicy` means both `checkShellScope` (server.ts:1105 `SHELL_SCOPE_DENIED`) and `shellExec` (shell.ts:87) enforce. Command is not mutated between policy check and spawn.

**Scope discipline (P1a only)**: server.ts `capabilityForceConfirm` (1064-1075), `enterpriseSkip` branch (1078-1131), the `(!skipConfirmation || forceConfirm) && !enterpriseSkip` L2 gate (1134), and the `shell_exec` security_token gate (3007-3013) are all untouched by inspection. `enterpriseSkip` still requires either `auto_approve_enterprise_tools` (a separate flag) or active `enterpriseSessionTrust` — god-mode / `auto_approve_dangerous` alone still cannot silent-skip shell L2. Trust monotonicity holds.

**Tests are real**: Re-ran `node --test .test-dist/tests/capability-shell-netsec.test.js` → 9/9 pass. The "no side effect" test (line 80-89) genuinely spawns would-be `rm -rf`/`touch` of a marker under `tempHome` and asserts non-existence — no mocking of the bypass path. The 8-case metachar sweep (line 91-111) covers `;`, `&&`, `|`, `$()`, backticks, `>`, `<`, `\n`, `\r` via both `commandAllowedByPolicy` and `checkShellScope`. `confirm_per_command` metachar-allowed property also locked (line 120-133). 63 related tests in enterprise-session-trust / orchestrator-l2-flight / user-env all green.

**ADR-020 checklist**: Surface L2 / shell, Composition capability module, Autonomy single, Trust L2 forceConfirm, Channel enterprise — declared in prompt, matches diff. No Pack/confirm-dialect/runtime regressions. No new originWs surface (none introduced).

**Patch integrity**: `docs/audit/reviews/P1-4-diff-20260730-135403.patch` body matches live `git diff` (not stale).

## Non-blocking nits

- **N1 (test gap, accepted by design)**: The prompt's acceptance list item "god-mode / auto_approve_dangerous alone cannot silent-skip shell L2" is verified only by inspection of `server.ts:1134`, not by a new integration test. Implementer explicitly noted this as "regression lock / document." Acceptable for P1a scope, but a future server-level test asserting `skipConfirmation=true && enterpriseSkip=false` still routes shell_exec through the confirm branch would harden the invariant.
- **N2 (docstring)**: The comment at shell.ts:58 references `confirm_session` policy, but no such policy exists in the codebase — only `allowlist` and `confirm_per_command`. Slightly inclusive wording; harmless.
- **N3 (residual, acknowledged)**: `$VAR` expansion under `shell:true` remains for `confirm_per_command` (and even allowlisted benign commands like `echo $HOME`). Out of scope for P1a; deferred to P1b `shell:false + file/args`. Implementer's notes call this out.

VERDICT: APPROVE_WITH_NITS
