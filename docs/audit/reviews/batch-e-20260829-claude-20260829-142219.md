Spot-checks complete — all pins traced to code. Writing the verdict.

---

# Dual re-review — Batch E path (#251) — independent reviewer

All evidence below is `[inspected]` against `6e17d2df` in this worktree (no execution; static review as specified for a pre-implementation gate).

## Verdict rationale

The path is sound and the fold's central engineering claim survives adversarial tracing. The linchpin: **the lease gate consumes the stamped binary surface, not `wsAuth.surface`.** Chain verified: `lifecycle.ts:1092` stamps → `message-router.ts:437` strips → `:1399` passes `stampedSurface` → `composer-lease.ts:194` holder check. Because `stampCmsparkSurface` (`composer-lease.ts:118`) collapses non-summoner → `"tray"`, a panel-handshake extension carries stamp `"tray"` → expected holder `incomingHolderFromSurface("tray")` = `"panel"` (`composer-lease.ts:96-98`) → overlay claim denied with `LEASE_HOLDER_SURFACE_MISMATCH`. So pin 4's claim “stamp 二元则 lease `if` 不用为 panel 放宽” is correct, and pin 6's “composer-lease.ts 默认不碰” is correct.

The E1 target hole is real today: `lifecycle.ts:1023-1024` stamps any claimed `"summoner"` as summoner regardless of Origin, and `assertSummonerAllowed` (`summoner-acl.ts:59`) only *restricts* the summoner surface — it gates nothing for non-summoners — so an extension claiming `summoner` passes `composer-lease.ts:194` with expected holder `"overlay"` and steals the overlay lease (which also `releaseAllOverlay`s the real overlay's siblings). Terminate-post-proof (pin 2) closes it, and terminating after `verifyProof` is the right order: the pre-auth early gate (`lifecycle.ts:928`) already kills non-handshake probes, so no oracle is added.

Rule-1 REJECT triggers — none fire: no overlay HTTP in `isAllowedWsOrigin` (`lifecycle.ts:202-215`, exactly chrome-extension scheme + `cmspark-tray://local`); `SUMMONER_ALLOW` (`summoner-acl.ts:14-53`) in NEVER; router split banned (union + coerce comment only); claimed wire stays omit/`tray`/`summoner` with `"panel"` rejected (`validate.ts:782-784`) — panel exists only server-side; omit-deny calibrated T2 among HMAC peers, not unauth RCE; ext+summoner terminated.

Desync sweep for the ternary `wsAuth.surface`: every consumer I found keys `!== "summoner"` (confirm fan-out `confirm-fanout.ts:25-27`; chat mirror `lifecycle.ts:1342,1349`; disconnect `composer-lease.ts:363`) or `=== "summoner"` (`lifecycle.ts:1432`) or `=== "tray"` (`OVERLAY_SHELL_` relay `lifecycle.ts:941` — panel exclusion is a *tightening*: only the true HUD emits overlay-spawn errors). `pickAuthenticatedClientWs` (`lifecycle.ts:263-273`) is Origin-keyed, `waitForExtensionPeer` binds to the same picker (`:275`). STT wiring passes origin class + surface (`lifecycle.ts:1385-1387`), so extension stays allowed via Origin [wiring inspected, handler internals assumed]. No site misbehaves for `panel`.

E2/E3 claims verified: `protocol.ts` VERSION=MIN=MAX=1, omit→MIN, 99>MAX → `auth.failed` + terminate already exists (`lifecycle.ts:1002-1020`); all three literals are `1` today (`protocol.ts:10`, `ws-client.ts:161`, `companion-client.ts:611`); neither client consumes `auth.ok` fields yet (`companion-client.ts:515`, `ws-client.ts:106`) so consume-ok is purely additive. E3: `companion/package.json` has no esbuild entry; lock shows tsx 4.22.3 → esbuild ~0.28.0 → hoisted 0.28.1, and `run-esbuild-bundle.mjs:41` resolves `companion/node_modules/esbuild` — direct pin is additive with zero version churn. engines currently `>=20` at both root and companion while CI pins node 22 (`release.yml:30,69`) — the `>=22` bump is real and cheap.

Rule 2 calibration agreed: T2 among HMAC peers, and “HMAC is the real gate” does not delete E1 — the extension is a browser-context peer whose lease theft causes real cross-surface damage (silent overlay compose hijack), not security theater.

## Confirmed pins

- **E1-ORIGIN-CLASS** — `isAllowedWsOrigin` set unchanged; `http://127.0.0.1` → `false` today and untouched.
- **E1-NO-SUMMONER-FROM-EXTENSION** — terminate post-verifyProof is implementable exactly as written; extension omit → `panel` stamp via binary collapse.
- **E1-OMIT-DENY-TRAY** — `companion-client.ts:612` always sends `surface ?? "tray"`; same-repo lockstep holds; only hypothetical stale tray clients break, accepted.
- **E1-BINARY-STAMP** — see rationale; `message-router.ts:286-293, 345` consistently re-collapse; no desync site found.
- **E1-FANOUT-PREDICATES** — all four predicate families verified at cited lines.
- **E2-LOCKSTEP / CONSUME-OK** — literals verified; omit=MIN already tested semantics; missing auth.ok field = MIN preserves mock compatibility.
- **E3-ESBUILD / ENGINES** — verified; pin matches lock 0.28.1.
- **Red-list accuracy** — `ws-auth-handshake.test.ts:181-182` confirms the replica carries no Origin, so “new tests must carry Origin” is a genuine requirement, not pedantry.

## Missing / still BLOCK

Nothing BLOCKs. Rule-3 probe for a concrete lease hole under binary stamp came up empty: client-supplied `__cmspark_surface` is overwritten (`composer-lease.ts:115-118`), session surface re-collapses at `message-router.ts:345`, and the only remaining cross-class path (tray-origin HMAC holder claiming summoner) is the explicitly retained trust boundary, correctly calibrated.

## Nits

1. Name the matrix case explicitly in the red-list: ext claims `tray` → coerced `panel` → stamp `tray` → holder `"overlay"` still denied (`LEASE_HOLDER_SURFACE_MISMATCH`). The fold implies it; make it a test so the coercion path can't silently regress into a passthrough.
2. Emit distinct log events for terminate (ext+`summoner`) vs coerce (ext+`tray`) so a misbuilt extension is observable in `logs/`.
3. Pre-existing, out of #251, worth an issue: `composer.lease.release_overlay` (`composer-lease.ts:241-244`) is ungated by surface — any authenticated peer can force all overlays back to panel. Not fold-introduced; panel is trusted; don't fix here.
4. DoD E1 "tray Origin 无 surface → 未 authenticated" — the test should assert socket termination, not merely absence of authenticated state (terminate post-proof means it never authenticates, but assert the close).
5. Widen `wsAuth.surface` as a type union (`lifecycle.ts:99-100`), not `string` — let TS exhaustiveness catch any consumer the desync sweep and I both missed.
6. E2 lockstep test: assert the three literals are mutually equal *and* equal to `PROTOCOL_VERSION`, not just all `=== 1`.

## Recalibrated: implement now? YES

VERDICT: APPROVE_WITH_NITS
