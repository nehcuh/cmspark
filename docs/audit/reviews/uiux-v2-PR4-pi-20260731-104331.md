All checks pass. Here's my review summary:

---

## PR4 Review: Composer chips + slash parity + 装配 P0 entry

### Scope delivery (§4.4, §4.8, §4.5)

| Deliverable | Status | Evidence |
|---|---|---|
| **ComposerDock chips** (mode-aware, ≤3) | ✅ | `ComposerChips.tsx` + `composerChipsForLevel()` — L0: 装配/Skills/Know, L1: 装配/Tabs/工作区, L2: 确认台/装配 |
| **Slash parity matrix** all 11 commands | ✅ | `META_PANEL_SLASH` covers skills/knowledge/history/tabs/packs/mcp/apps/board/settings/cockpit/装配; `resolveMetaSlash()` handles all `metaKind` discriminators |
| **装配 P0 bottom-sheet** section list → Host | ✅ | `ComposeDrawer.tsx` with 6 Composition sections; Cmd+K shortcut; empty-state "装配" chip; Esc peel; landfill one-surface rule |
| **Unit tests** | ✅ | `composer-slash-parity.test.ts` locks matrix, section ontology, chip-level mapping, placeholder strings |

### ADR-020 fidelity

- **Board is Autonomy, NOT in 装配** ✅ — `COMPOSE_SECTIONS` excludes `board`; footnote in drawer: "任务板 / 编排不在装配内 — 使用 /board 或 ⋯「编排」"; `composeSectionsExcludeBoard()` test guard
- **No "中层 Agent" copy** ✅ — copy uses "组合能力 / 装配" consistently
- **Composition attaches to any Surface** ✅ — drawer subtitle "挂到当前线程 · Surface"

### Tokens

- **No Material hexes** in any new file ✅ — all styling uses `tokens.*` (border, bgElevated, textSecondary, accentSoft, radiusSm/Md/Lg, etc.)
- **Radius 6/8/12** ✅ — matches spec §5.2

### BottomBar / PR1 boundaries

- **BottomBar NOT deleted** ✅ — file exists; PR4 uses `ContextPanelHost` via `useContextPanelHost()` as intended; BottomBar header comment explicitly states "Strip delete is PR5 (not here)"
- **No new `alert()`** ✅ — no `alert()` calls in any new code

### Spec copy alignment

- "确认台" replaces remaining "操控台" references ✅ (K7)
- L0 empty: "今天想聊什么？" matches §5.4 wireframe ✅
- Empty-state chips: 总结本页·装配·/packs matches §4.8 ✅
- Placeholder: "问点什么，或 / 装配" matches §4.4 ✅

### Nits (non-blocking)

1. **`ComposeDrawer.tsx:82`** — subtitle reads "组合能力 · 挂到当前线程 · **Surface**" without showing the actual L0/L1/L2 level; spec §4.5 says "Surface Lx". Acceptable for P0 (full sections in P1).
2. **`meta-slash.ts:256`** — `composeSectionsExcludeBoard()` is production-dead code (only called in tests). Harmless guardrail; consider runtime call if desired.
3. **`ComposeDrawer.tsx` close button** is text "关闭" — icon would be better polish (P1).

---

**VERDICT: APPROVE**
