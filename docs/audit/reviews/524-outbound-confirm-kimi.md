kimi version 2.0.2
• UserPromptSubmit hook

  {"systemMessage": "🎯 VibeSOP routed: builtin/adversarial-panel (88% confidence)\nALTERNATIVE SKILLS (if 'builtin/adversarial-panel' doesn't fit, load one of these):\n  1. custom/main-64d301b8 (30%)\n  2. builtin/deep-diagnosis-optimization (24%)\n  3. custom/main-64d301b8 (43%)\n  4. builtin/deep-diagnosis-optimization (41%)\n  5. builtin/test-generation (37%)\n\n\nNEXT STEP (MANDATORY): read /Users/huchen/.local/share/uv/tools/vibesop/lib/python3.13/site-packages/vibesop/builtin_skills/adversarial-panel/SKILL.md\nDo NOT proceed without reading this file.\nIf the skill doesn't match, load an alternative skill above.", "hookSpecificOutput": {"additionalContext": "[ACTIVE SKILL: builtin/adversarial-panel]\nYou MUST follow this skill's workflow. Do not skip steps.\n\n请先读取 /Users/huchen/.local/share/uv/tools/vibesop/lib/python3.13/site-packages/vibesop/builtin_skills/adversarial-panel/SKILL.md ，然后严格按照该 skill 的工作流程执行「builtin/adversarial-panel」。不得跳过任何步骤。", "hookEventName": "UserPromptSubmit"}}

• The hook demands reading the adversarial-panel SKILL.md first. Let me read it, then do the review. The task is read-only review of a diff. Let me read the skill file and the diff.This is a review task — 双路复审 (dual review), read-only. The skill loaded (adversarial-panel) describes a pull-latest + multi-lane panel workflow, but the actual user prompt is a single independent reviewer task on an uncommitted diff. The user prompt is very specific: review the diff, 10 files, answer 8 questions with file:line evidence, output blocking items then NITs, last line a VERDICT. The skill's workflow (ff-only pull, five lenses, synthesizer) doesn't match — I'm one lane of a panel presumably, and the task is read-only review of a frozen diff already provided. I should follow the user's explicit instructions (which override skill routing), while respecting the skill's discipline: frozen-diff review, refute-first, evidence from the current tree, no edits.

  Now, the 10 files in the diff:
  1. CHANGELOG.md
  2. chrome-extension/src/background/cockpit-focus-policy.ts
  3. chrome-extension/src/sidepanel/components/OutboundMcpSettingsSection.tsx
  4. chrome-extension/tests/cockpit-focus-policy.test.ts
  5. companion/src/outbound-mcp/companion-http.ts
  6. companion/src/outbound-mcp/grant-cli.ts
  7. companion/tests/outbound-mcp-companion-http.test.ts
  8. docs/TROUBLESHOOTING.md
  9. docs/confirm-center-user-guide.md
  10. docs/mcp.md

  Key questions to verify:

  1. `[Outbound]` prefix coverage: does the L2 confirm path (navigate etc.) also use `[Outbound]` tool names? Check where outbound L2 confirmations are generated — the diff only shows `waitFirstExfilOperatorConfirm` in companion-http.ts. Need to find other places outbound MCP requests confirmations (e.g., in the extension or outbound invoke path) and what tool_name they use.

  2. `open_focus` actually calls `openOrFocusCockpit()` — check the background code that consumes decideCockpitFocus result. Also whether `security.confirmation.request` reaches the extension background when sidepanel closed.

  3. `fullPreview` semantics: in companion-http.ts, they set `fullPreview: preview` — does full_preview in the confirmation payload trigger "heavy preview" behavior in the sidepanel that changes approval capability? Also check nonce implications. Note: cockpit-focus-policy's `hasHeavyPreview` also becomes true now (full_preview non-empty), which would make it open_focus anyway even without outboundChannel. But more importantly question 3: does fullPreview change whether sidebar can click allow? Need to read the confirmation handling code in the sidepanel / MinimalConfirm.

  4. Focus exception scope: outboundChannel flag is set only by tool_name prefix; auto_approve/god-mode — check whether outbound confirmations can be auto-approved. `autoConfirmEligible: false` set in companion-http. Need to check L2 path too.

  5. `originWs` binding: check the `request()` function signature in companion-http.ts and how it was called before — did adding fullPreview change the call? Diff shows same call structure; but check current source.

  6. Windows docs and sidebar copy snippet: check grant-cli.ts printIssue — does the CLI print expanded paths on Windows? On non-Windows it still prints `%LOCALAPPDATA%`? Wait — the claim: "非 Windows 上 CLI 仍印 `%LOCALAPPDATA%` 时，文档是否诚实". Need to check how printIssue computes command/args — does it depend on host OS of the CLI? Check grant-cli.ts source.

  7. caller input: was there an input before? From the diff, previously there were inputs with placeholder "caller_id（与 MCP 调用 body 绑定）" — so an input existed but unlabeled. The claim says "加了标签" — diff confirms: input existed, now has label. Permission entry: "页面外泄勾选始终可见；上下文出口只在工具档为 outbound_context_v1 时出现" — the diff shows the "权限" label added, page-exfil checkbox always visible, context origins only when profile === OUTBOUND_CONTEXT_PROFILE. Check OUTBOUND_CONTEXT_PROFILE value.

  8. Tests only lock the policy function, not "background really opens window" — check cockpit-focus-policy.test.ts and whether there's any test for the message handler that calls openOrFocusCockpit.

  Let me explore the actual source. I should do this myself or delegate to explore agents. Given the size, I could delegate parts. But I need precise file:line evidence and my own reading. Let me do targeted reads/greps myself — it's a review, accuracy matters. The codebase areas:

  - companion/src/outbound-mcp/companion-http.ts — full read relevant parts
  - companion/src/outbound-mcp/ — other files (invoke path, L2 confirm)
  - chrome-extension/src/background/ — the consumer of decideCockpitFocus (search for openOrFocusCockpit, decideCockpitFocus usage)
  - sidepanel confirmation components — full_preview handling, nonce
  - grant-cli.ts — printIssue command/args computation
  - auto_approve / god-mode — search for autoApprove in confirm path

  Let me start with parallel greps/reads.L2-admission line 1270 is for tray toolName. I need to find where the WS confirmation request for L2 is sent — what's the tool_name there. Let me look further in l2-admission.ts for `security.confirmation.request` or the request() call with toolName.Critical finding for question 1: in l2-admission.ts, the WS confirmation request `securityConfirmations.request(..., { toolName, ... })` at line 1488 passes raw `toolName` — NOT `[Outbound] ${toolName}`. Only the tray path (line 1270) prefixes `[Outbound]`. So outbound L2 confirmations (navigate, etc.) sent to the sidepanel/background via WS have tool_name without the `[Outbound]` prefix → cockpitFocusEventFromMessage won't set outboundChannel → decideCockpitFocus returns stay_background (unless heavy preview/nonce). So the L2 outbound confirms still do NOT pull the cockpit forward on Windows/Linux. That's a potential blocker against user requirement 4 / implementer claim? The implementer claim says: "tool_name 以 [Outbound] 开头的确认，decideCockpitFocus 返回 open_focus" — that is technically true, but the question asks: "[Outbound] 前缀是否覆盖首次外泄和租手 L2（navigate 等）？有没有租手确认用了别的 tool_name，因而仍然不弹窗？" Answer: L2 outbound confirms via WS use raw toolName → not covered → still stay_background → window doesn't appear → 45s timeout fail persists for those. That's a real gap. Unless... maybe toolName itself is already prefixed upstream for outbound calls? Check where toolName comes from in l2-admission and whether outbound invocation passes `[Outbound]` name. Line 1444 logs `tool_name: toolName` raw, and 1457 message `Outbound MCP 请求: ${toolName}` — implies toolName is raw. Also line 1270 explicitly prefixes for tray, implying toolName raw. And companion-http.ts:208 prefixes explicitly too. So WS L2 confirm tool_name lacks prefix. Let me verify how sendConfirm builds the frame and whether tool_name is taken from req.toolName. Check security confirmations manager.

  Also check url-cookie-admission.ts:395 — that's another confirm path, prefixes `[Outbound]`. Does it send via WS with toolName? Let me check.

  Question 5: originWs binding — confirmBinding.originWs for outbound is unbound (comment at 1466: "outbound stays unbound"). The diff didn't touch l2-admission.ts. In companion-http.ts the request() call — need to check its signature and confirmOriginOpts there. Let me read companion-http.ts around the confirm function.

  Question 3: fullPreview — check how sidepanel handles full_preview: does it change MinimalConfirm → heavy confirm, blocking approve button? hasHeavyPreview in focus policy would now be true for first-exfil confirms too (full_preview non-empty), but outboundChannel already forces open_focus. But question: does full_preview make sidepanel treat it as "重预览" that disables quick allow? Check sidepanel confirmation components.

  Also check nonce: no nonceChallenge set in companion-http — ok.

  Question 4: auto_approve / god-mode: check where outbound calls skip confirm — in l2-admission there may be auto_approve logic; does isOutboundMcpCall force confirm? Check around line 276 comment "input injection is NEVER thread-trusted. God-mode..." Read that region.

  Question 2: openOrFocusCockpit — background/index.ts:582-584 calls it. Also does the background receive security.confirmation.request when sidepanel closed? The message comes from companion over WS to extension background service worker. Check that security.confirmation.request frames are broadcast to extension regardless of sidepanel open — the sendConfirm in companion: confirmOriginOpts originWs — for outbound, unbound, so broadcast to all authenticated clients including extension background? Need to check security-confirmations manager request() — how sendConfirm works and whether background service worker is a WS client that receives broadcast. The extension background connects to companion WS as a client; broadcast goes to it. Check.

  Also check cockpit-window.ts openOrFocusCockpitImpl — does it actually open window.

  Question 6: grant-cli printIssue — does it print expanded paths? Read grant-cli.ts printIssue and how command/args are computed (probably os-specific based on process.platform). The claim: "优先复制 Windows 本机 outbound-grant issue 印出的 command/args". Check whether the CLI prints expanded absolute path on Windows. Also on non-Windows it prints `%LOCALAPPDATA%`? Check.

  Question 8: tests — cockpit-focus-policy.test.ts only tests policy function; no test that background actually opens window. companion test locks tool_name/full_preview/code_preview on the wire frame. Whether that's blocking — my judgment.

  Let me read companion-http.ts confirm function, url-cookie-admission.ts, security confirmations manager request, sidepanel confirm component handling of full_preview, and grant-cli printIssue.Good — url-cookie-admission.ts:395 does prefix `[Outbound]` for WS confirm (navigate URL gate). So navigate URL-gate confirmations ARE covered.

  But l2-admission.ts:1488 passes raw `toolName` for the WS request. Which confirmations go through l2-admission for outbound? L2 admissions for dangerous tools (e.g., navigate? click? type?). The question mentions "租手 L2（navigate 等）". Navigate URL gating is in url-cookie-admission; but the L2 admission path handles dangerous APIs. For outbound MCP calls, do they flow through l2-admission? Line 1270 and 1441 reference isOutboundMcpCall, so yes outbound calls reach l2-admission. There, WS confirm tool_name is raw (line 1488), only tray name prefixed (1270). So L2 outbound confirmations (e.g. tools with dangerous APIs, forceConfirm) broadcast to the sidepanel/background with raw tool_name → no `[Outbound]` prefix → outboundChannel false → stay_background → on Windows/Linux with no tray, window won't appear; sidepanel red bar still allows if open. This is a genuine gap for question 1.

  Wait — but do outbound MCP calls actually hit l2-admission? The outbound tools run via extension as CDP runner. isOutboundMcpCall flag in l2-admission suggests yes. Line 1099 "security.confirmation.requested" — check context. Also check whether l2-admission L2 confirm is used for outbound e.g. `evaluate` etc. It's plausible. So the gap: any outbound confirmation that goes through l2-admission (dangerous tools L2) still uses raw toolName on WS → focus exception doesn't apply → claim "[Outbound] 前缀覆盖租手 L2" partially false.

  Hmm, but which tools? "navigate 等" — navigate's URL confirm is covered by url-cookie-admission prefix. But l2-admission covers other L2 (dangerous APIs, e.g. keyboard input?). Need to know whether outbound tools with L2 confirmation exist. PROFILE mentions `scroll`/`evaluate` forbidden by default but possible with other profiles. If an outbound tool hits l2-admission confirm, tool_name on the wire is raw. That's evidence enough: l2-admission.ts:1488 `toolName,` raw.

  Also check: does the frame that reaches background actually use toolName as `tool_name`? Check security-confirmations manager request() building the frame. Let me find SecurityConfirmationManager.

  Also question 5: originWs binding in companion-http: confirmBinding with isOutboundMcpCall: true — need resolveConfirmBinding semantics: outbound stays unbound per comment. fanOutConfirmRequest fans out to all authenticated non-summoner peers. So originWs binding preserved (unbound → broadcast). Diff didn't change this; confirm current source matches — yes lines 168-188.

  Question 4: auto_approve / god-mode skipping outbound: url-cookie-admission line 366: outboundTrack ? false — outbound never skips. Check l2-admission for outbound force-confirm behavior — search isOutboundMcpCall in l2-admission.I need to find the SecurityConfirmationManager.request — where the `security.confirmation.request` frame is built with tool_name. Search for the manager file.Confirmed: frame `tool_name: details.toolName` (line 312). So for l2-admission WS confirms, tool_name is raw toolName (no `[Outbound]` prefix). Only url-cookie-admission (395) and companion-http (208) prefix. So question 1: outbound L2 confirmations that go through l2-admission (e.g. tools requiring L2 confirm like dangerous APIs) still send raw tool_name → no open_focus. Which outbound tools hit l2-admission? Check l2-admission line 930 context (isOutboundMcpCall) to see what forces confirm for outbound.Now question 1 needs to know: which outbound tools actually flow through l2-admission. Outbound MCP tools are browser tools like navigate, click, type, get_page_text, screenshot, scroll, evaluate. Do browser tools hit l2-admission? Browser L2 gating happens via url-cookie-admission (navigate URL gate) and possibly l2-admission for dangerous APIs (evaluate etc.). If outbound profile includes evaluate or similar dangerous tools, the L2 confirm via l2-admission would send raw tool_name → no `[Outbound]` prefix → no open_focus. Also interactive tools like click/type may be "轻确认" minimal confirms? Actually browser click/type L2 — where do they get confirmed? Let me check whether click/type go through l2-admission or through a different confirm path (maybe extension-side overlay confirm). Outbound tools run on extension as CDP runner; the confirm may happen in extension? But l2-admission has isOutboundMcpCall handling, so outbound calls do reach it. Which tools? L2_GATE_TOOLS list. Let me check what tools are L2-gated and whether outbound browser tools (click/type) are among them.L2_GATE_TOOLS includes evaluate — an outbound profile may include evaluate (docs mention `cmspark__scroll` / `evaluate` forbidden by default but possible in other profiles). So an outbound `evaluate` L2 confirm would go through l2-admission → WS frame tool_name = "evaluate" (raw) → background focus policy: no outboundChannel, hasNonce false, hasHeavyPreview false (no full_preview for evaluate) → stay_background. So租手 L2 via l2-admission NOT covered by open_focus. But wait — is outbound `evaluate` even reachable? PROFILE_FORBIDDEN default excludes evaluate. Other profiles might allow. The question explicitly asks "有没有租手确认用了别的 tool_name，因而仍然不弹窗？" — evidence: l2-admission.ts:1488 passes raw toolName; only tray path (1270) prefixes. So yes, there is a gap for outbound L2 tools that route through l2-admission (evaluate etc.).

  Also what about browser interaction tools (click, type, scroll) for outbound? Do they go through any confirm? They may be "轻确认" MinimalConfirm... Actually browser tools' L2 for navigate is URL-gate (covered, prefixed). What about cookie admission? url-cookie-admission covers cookies too (same file, line 395 within URL confirm; cookie path may also prefix — check the cookie confirm path in the same file. Line 395 was the only prefix occurrence in that file, but cookie confirms might share that request call. Let me check the cookie confirm section — does it use the same securityConfirmations.request at 392? The grep showed only one `toolName: isOutboundMcpCall ? ...` in that file at 395. Cookie admission may also request confirmation elsewhere in that file... The file is 437 lines; the request at 392 is in the URL section. Cookie confirm might be at another request. Let me check lines 240-360 for cookie confirm.

  Also need to check:
  - menu-bar-agent.ts:2093 handles security.confirmation.request — that's macOS tray (Swift). Not Windows.
  - Background receives frames: the background WS client — does the companion broadcast security.confirmation.request to the extension background even when sidepanel closed? fanOutConfirmRequest sends to authenticated clients. The extension background is itself the WS client (the extension connects from background service worker). So yes, background receives all frames over its WS connection; sidepanel is just a UI listener via chrome runtime messaging. So question 2: yes, background receives and calls openOrFocusCockpit at index.ts:582-584. Check cockpit-window.ts impl quickly to confirm it opens a window.

  - Question 3: fullPreview → hasHeavyPreview true → also open_focus (redundant but fine). Does full_preview change sidepanel behavior re: allow button? Check sidepanel confirm dialog component for full_preview usage. Search in chrome-extension/src/sidepanel.

  - Question 6: grant-cli printIssue — check how command/args printed, platform dependent.

  - Question 8: test coverage — check chrome-extension tests for background index open behavior. Probably none.

  Let me read the cookie section, sidepanel full_preview handling, grant-cli printIssue, cockpit-window impl.grant-cli outboundMcpLaunchSpec: on actual win32 with LOCALAPPDATA env, prints expanded path (line 132-138). On non-Windows (or win32 without env), prints `%LOCALAPPDATA%\CMspark\node.exe` literal. Docs say "在 Windows 本机跑 outbound-grant issue 时，stdout 会印出已经展开的 command/args，优先复制那两行" — that's honest: on Windows it expands. On non-Windows, CLI prints `%LOCALAPPDATA%` template — docs tell user not to copy %LOCALAPPDATA%... The doc says "优先复制 Windows 本机印出的" — implies run it on Windows. TROUBLESHOOTING says same. Reasonably honest; minor nit: nothing warns that running the CLI on macOS to prepare a Windows config prints the unexpanded template. Actually the sidebar copy snippet — hostOs === "win" check in the extension: the sidebar shows winDir with `<你的用户名>` placeholder when hostOs is win. That's the extension's host OS detection. Fine.

  Question 3: full_preview usage in sidepanel — useWebSocket.ts:908 passes full_preview into the confirmation request object; check how the sidepanel confirm dialog uses full_preview — does it change allow behavior? Check MinimalConfirm vs heavy. Search for where confirmation request full_preview is consumed in sidepanel components.MinimalConfirm: `needsCockpit = needsNonce || expert_team` — full_preview does NOT disable the allow button in the sidebar. So answer to question 3: full_preview doesn't block sidebar approval; nonce is not set for outbound first-exfil (companion-http passes no nonceChallenge). Good — no mis-opening of nonce.

  However note: full_preview now makes hasHeavyPreview true in the focus policy, so even without outboundChannel the first-exfil confirm would open_focus. That's redundant, fine.

  But wait — the first-exfil confirmation: MinimalConfirm would allow it in the sidebar ("侧栏红条仍可批") — matches claim.

  Question 4: auto_approve / god-mode for outbound: l2-admission line 930-936 forces skipConfirmation=false for outbound. url-cookie-admission line 366-370: outboundTrack → skipUrlConfirmation=false. So god-mode doesn't skip outbound confirms. Also focus exception scope: outboundChannel only set when tool_name starts with "[Outbound]" — only outbound paths set that prefix (companion-http, url-cookie-admission:395, l2-admission tray:1270). Any non-outbound tool named with literal "[Outbound]" prefix? Tool names are internal names; a tool literally named "[Outbound] x" doesn't exist in registry — fine. But what about the light-confirm widening: the exception only applies to confirmations whose tool_name has the prefix, i.e., outbound channel. Non-outbound light confirms unaffected — test "普通 navigate 轻确认仍 stay_background" locks that.

  Question 2: Does background receive security.confirmation.request when sidepanel closed? The extension background maintains the WS connection to companion (background/index.ts wsClient). fanOutConfirmRequest sends to authenticated clients — the extension background is the client. So yes. And openOrFocusCockpit at index.ts:582-584 → cockpit-window.ts:198 opens window. Let me quickly verify cockpit-window impl opens a window even when sidepanel closed (obviously it's a separate window). Read cockpit-window.ts:139-210 briefly.

  Also check fanOutConfirmRequest to confirm outbound confirms actually reach the extension (broadcast to authenticated non-summoner peers). Read companion/src/mcp/confirm-fanout.ts.

  Question 8: tests — cockpit-focus-policy.test.ts tests decideCockpitFocus and cockpitFocusEventFromMessage only. No test that background/index.ts calls openOrFocusCockpit. Whether blocking: the wiring at index.ts:581-584 is pre-existing and simple; the policy change is the new logic and is tested. The L2 gap (question 1) is the real issue: outbound L2 confirms via l2-admission (e.g. evaluate) still stay_background — but wait, do outbound browser tools like navigate go through l2-admission? Navigate's URL gate is url-cookie-admission (prefixed). What other outbound tools could hit l2-admission? L2_GATE_TOOLS: evaluate (possible in interact profile? docs say evaluate forbidden by default; PROFILE_FORBIDDEN mentions scroll/evaluate not in default L1). The interact profile adds scroll/element inspect/keys/forms — do those require L2 confirm? Probably not L2_GATE_TOOLS. So the l2-admission path for outbound is mostly evaluate/osascript/etc., which are typically not in outbound profiles. But if a profile includes evaluate, its confirm would be raw tool_name → stay_background. Also — important: does the l2-admission confirm for outbound include relevant_domains? For evaluate via outbound with URL, maybe. If relevant_domains present, MinimalConfirm hint shows whitelist text but hasHeavyPreview still false → stay_background.

  Hmm, but is that a blocker? The user requirement 4 is about the confirmation experience; implementer's claim says "`tool_name` 以 `[Outbound]` 开头的确认 decideCockpitFocus 返回 open_focus" — literally true. The gap: l2-admission WS confirms for outbound don't carry the prefix. Whether this is blocking depends on how reachable. Outbound profiles: default L1 (8 tools), interact profile, context profile. Does any include tools in L2_GATE_TOOLS? evaluate is in L2_GATE_TOOLS. Docs said default excludes evaluate; interact profile adds scroll/element/keys/forms/tabs — none in L2_GATE_TOOLS. But can a profile include evaluate? TROUBLESHOOTING PROFILE_FORBIDDEN says "默认 outbound L1 不含 scroll/evaluate/cookies/shell 等" implying other profiles could. Let me check outbound profiles definitions.

  Also — what about the URL-gate confirm for navigate (prefixed [Outbound]): it has relevant_domains → light confirm, now open_focus via outboundChannel. Good.

  Cookie admission: does cookie confirm use prefix? Only one prefixed request in that file at 395. Are cookies reachable outbound? "cookies" forbidden by default. Let me check the cookie path in url-cookie-admission for its own request call.

  Let me also check confirm-fanout and cockpit-window, and outbound profiles. Then check OutboundMcpSettingsSection for question 7 (already in diff — caller input existed before with placeholder "caller_id（与 MCP 调用 body 绑定）", now labeled; permission section: 页面外泄 checkbox always visible, context export only when profile === OUTBOUND_CONTEXT_PROFILE — matches claim; "权限入口名不副实?" — the section labels two gates; the context export requires switching profile first. That's accurate to claim; mild nit at most).

  Also check hostOs detection in the sidebar component for the win snippet — read the top of OutboundMcpSettingsSection.

• Now checking the fan-out path, cockpit window implementation, outbound profiles (which tools can reach l2-admission), and the sidebar component's host-OS detection:

• Question 2 confirmed: outbound confirms fan out to all authenticated non-summoner peers (confirm-fanout.ts:74-87), extension background is the WS client so it receives frames regardless of sidepanel open; index.ts:581-584 calls openOrFocusCockpit which creates/focuses a real popup window (cockpit-window.ts:176-186). Docs say "确认台会到前面" with caveats "Chrome 要开着、扩展要已连接" — accurate enough.

  Question 5 confirmed: outbound binding stays unbound (confirm-fanout.ts:113-119), and companion-http.ts:168-176 uses resolveConfirmBinding with isOutboundMcpCall: true — unchanged semantics, originWs undefined. Not lost.

  Question 4 confirmed: l2-admission.ts:930-936 forces skipConfirmation=false for outbound; url-cookie-admission.ts:366-370 outbound never skips URL confirm. autoConfirmEligible: false in companion-http.ts:213. Focus exception only triggers on tool_name prefix "[Outbound]", only set on outbound paths (companion-http.ts:208, url-cookie-admission.ts:395, l2-admission.ts:1270 tray-only).

  Question 1 — the gap: l2-admission.ts:1488 passes raw `toolName` to the WS request; security-confirmation.ts:312 emits tool_name verbatim. So outbound calls that confirm via l2-admission (L2_GATE_TOOLS: evaluate, etc.) emit tool_name WITHOUT "[Outbound]" → cockpitFocusEventFromMessage won't set outboundChannel → stay_background → no window on Windows/Linux. Which outbound profiles include L2_GATE_TOOLS? Need to check profile definitions. Let me check outbound profiles to see whether evaluate or other L2-gated tools are grantable.Outbound profiles contain only browser tools: navigate, click, type, etc. None are in L2_GATE_TOOLS (evaluate etc.). So outbound calls do NOT reach l2-admission's confirm path? Wait — but l2-admission has isOutboundMcpCall handling, so outbound calls do pass through l2-admission (it's the general admission for all tools). Do navigate/click/type require L2 confirmation in l2-admission? They aren't in L2_GATE_TOOLS. Where do browser tools like click/type get confirmed? Maybe they don't confirm at all (L1). Navigate's URL gate confirms via url-cookie-admission (prefixed ✓). So what other outbound confirms exist?

  1. First-exfil (get_page_text/screenshot/get_page_html/analyze_image with allow_page_export) → companion-http waitFirstExfilOperatorConfirm → prefixed ✓ (this diff).
  2. Navigate URL gate → url-cookie-admission:395 → prefixed ✓.
  3. Cookie admission? Check whether outbound cookies reachable — not in profiles. Also the cookie path in url-cookie-admission — does it prefix? There's only one prefix in that file. Let me check if there's a separate cookie confirm request. Actually earlier grep showed line 395 is the only `toolName: isOutboundMcpCall` in that file. The cookie gate may share the same request call or have its own. Let me check quickly — cookies aren't in outbound profiles anyway, so out of scope.

  But wait — do browser tools like click/type go through l2-admission confirmation when domain not trusted? Browser L2 for click/type might be handled elsewhere. But regardless of l2-admission, since outbound tools are limited to the allowlist and none are L2_GATE_TOOLS, the WS confirm paths reachable by outbound are: exfil confirm (prefixed), URL gate (prefixed). What about `wait_for`, `downloads_find` — no confirm.

  Hmm, but is there another confirmation path for outbound — e.g. TAB_LOCKED, or domain trust for click/type? Click/type on untrusted domain? The URL gate covers navigate/create_tab/set_tab_url. Click/type probably execute without confirm. So coverage question 1: the two reachable outbound confirm types both carry `[Outbound]` prefix. The l2-admission raw-toolName path is NOT reachable by current outbound profiles (no evaluate). However the claim "[Outbound] 前缀覆盖首次外泄和租手 L2（navigate 等）" — navigate URL-gate covered ✓. The residual gap: l2-admission.ts:1488 sends raw toolName — if any outbound tool ever hits L2 (e.g. future profile with evaluate, or forced confirm via cli_q5/acp taint — those apply to host_cli/evaluate only), it would silently stay_background. That's a latent inconsistency worth a NIT, not a blocker, since tray name is prefixed but WS name isn't — inconsistent within the same function. Actually wait — is that pre-existing? Yes, line 1488 is pre-existing (not in diff). The diff didn't touch l2-admission. The review question asks "有没有租手确认用了别的 tool_name，因而仍然不弹窗？" — answer: within current shippable profiles, no; but l2-admission WS path uses raw toolName, a latent gap if outbound ever gates through L2 (e.g. evaluate in a future profile). NIT.

  Also menu-bar-agent.ts:2093 — macOS tray mirrors confirmations; does it filter by tool_name? Not needed for Windows focus.

  Now question 7 — check the sidebar component more fully: hostOs detection, whether caller input existed before (yes, per diff), permission entry naming. Also verify OUTBOUND_CONTEXT_PROFILE import in the tsx and the checkbox visibility. Let me read the relevant parts of OutboundMcpSettingsSection.tsx current source to verify the rendered result (labels, helpText, hostOs).

  Also verify grant-cli "printIssue" header line 195 prints CMSPARK_OUTBOUND_CALLER_ID matching caller — yes line 195.

  Question 6: On non-Windows CLI, launch spec prints `%LOCALAPPDATA%\CMspark\node.exe` (line 133 fallback when not on win32). Docs say "在 Windows 本机跑 outbound-grant issue 时 stdout 会印出已经展开的 command/args，优先复制那两行" — honest because it says "Windows 本机". But a user running CLI on macOS to configure a Windows machine would get the %LOCALAPPDATA% template printed with no warning. The docs (mcp.md line ~366) explicitly tell users not to write %LOCALAPPDATA% and to prefer copying from Windows-native CLI output. So docs are honest; minor nit: CLI on non-Windows still prints the unexpanded template without a warning comment. Could be a NIT.

  Also sidebar snippet for win: `C:\Users\<你的用户名>\AppData\Local\CMspark\node.exe` — user must replace placeholder; help text explains via Explorer. OK.

  One more check for question 2: "文档「确认台会到前面」有没有说满？" — docs mcp.md: "外部助手发起的确认会把 Chrome 确认台拉到前面（扩展需已连接；侧栏红条也可以批）" plus "若窗口没出现，确认 Chrome 已开且扩展已连接，或自己打开 Chrome 确认台" (mcp.md §3 用起来). TROUBLESHOOTING DISCLOSURE_HITL_REQUIRED also mentions window-not-appearing conditions. Reasonably complete. But note: open_focus depends on the extension service worker being alive to receive the WS frame and call chrome.windows.create. MV3 service worker — the WS connection keeps it alive? There's a wsClient in background; presumably persistent. Also chrome.windows.create with focused:true from a background worker without user gesture — allowed for extensions. Fine.

  Another subtlety for question 2: cockpit window open requires the Cockpit route; but also the confirm only appears in cockpit if cockpit receives the frame — cockpit is a separate window that gets frames via background relay (chrome.runtime messaging). When cockpit window is created by background, it hydrates via noteSecurityConfirmationRequest mirror (index.ts:554-556). OK.

  Question 8: tests — cockpit-focus-policy.test.ts locks policy + message mapping; companion-http test locks the wire frame (tool_name, full_preview, code_preview). No test locks that background actually invokes openOrFocusCockpit on outboundChannel — the wiring at index.ts:581-584 is generic (pre-existing, covers any open_focus). Risk of regression is low; I'd call it a NIT, not blocking. But is there any existing test covering index.ts focus wiring? Let me check tests dir for cockpit focus/open tests.

  Also need to double check the OutboundMcpSettingsSection current file for question 7 details and hostOs source. Let me read the top of the file.ui-command.test.ts:58 asserts a source slice matches /openOrFocusCockpit/ — let me check what that test does; it might be a source-grep test locking the wiring in background/index.ts. Read around it.The ui-command test locks a different wiring (ui.command), not the confirmation focus wiring. So question 8: no test locks "background really opens window on outbound confirm" — the wiring at index.ts:581-584 is generic and pre-existing; the new logic (policy) is tested. Is that blocking? The wiring is one shared code path for all open_focus events; the new branch only changes the policy decision, which is tested including the message-mapping (`cockpitFocusEventFromMessage` with tool_name prefix). I'd call it non-blocking but worth a NIT: an integration-style test (frame in → openOrFocusCockpit called) would lock the full chain; also the companion-side test locks tool_name prefix on the wire for first-exfil, and url-cookie prefix is pre-existing... is there a test locking url-cookie-admission `[Outbound]` prefix? Not in this diff. The chain depends on tool_name prefix being present on wire — for navigate URL gate it's pre-existing code, presumably tested before.

  One more check for question 1 completeness: are there other outbound confirm paths — e.g. `cmspark__accept_data_disclosure`? Or the context export path (site_context)? Context export confirm — check companion-http around line 507 (OUTBOUND_CONTEXT_PROFILE) — does context export require confirm, and with what tool_name? Let me check. Also `bindOverlayConfirmChannel` in url-cookie-admission — used for URL confirm; fine.

  Also check the cookie-admission confirm path — is there a second securityConfirmations.request in url-cookie-admission.ts without prefix? Cookies aren't in outbound profiles, so even if unprefixed, unreachable outbound. Skip.

  Check context export confirm path in companion-http.ts near 507.Context session doesn't add a confirmation here (site_context session establishment may confirm elsewhere — but not changed by this diff; skip unless it emits confirms with different tool_name. site_context export — does it go through waitFirstExfilOperatorConfirm? It's "上下文出口" separate permission. Not part of the diff's claims; skip).

  Now I have enough for most questions. Let me also verify the CockpitApp handling of the outbound confirm — cockpit ConfirmElevated with full_preview shows text and allows approve (CockpitApp.tsx:609 slices full_preview||code_preview to 1200 — fine, preview is short). Does cockpit allow approving high-risk confirm without nonce? Presumably yes — that's the normal path.

  One more: CHANGELOG entry claims match implementation — mostly verified.

  Question 7 details: caller input existed before (placeholder "caller_id（与 MCP 调用 body 绑定）"), now has visible label "调用方 caller_id" + helpText + htmlFor/aria-label. So claim "只是加了标签" — accurate; the input existed. The permission entry: "权限" label added above the page-export checkbox (always visible); "上下文出口" label only rendered when profile === OUTBOUND_CONTEXT_PROFILE (diff line 189-192: `{profile === OUTBOUND_CONTEXT_PROFILE && ...}`). Matches claim. Is it 名不副实？ The claim itself says context export only appears with the context profile — the user asked "签发时要有权限配置入口" — the page-export checkbox is the main permission and is always visible; context export inherently belongs to the context profile. I'd say acceptable; minor NIT: the "权限" heading may read as though context export were hidden rather than nonexistent for other profiles — but the helpText explains two gates. Fine.

  Question 6 recheck: sidebar snippet for win uses placeholder `<你的用户名>` — copy-paste would fail if not replaced, but there's explicit help text (win-only) telling the user to replace it via Explorer. The snippet also contains `#` comments in the copied text — the snippet format: "command: ...\nargs: [...]\n# ..." — it's not raw JSON, it's a human snippet. Fine.

  CLI on non-Windows printing `%LOCALAPPDATA%`: grant-cli.ts:132-138 — on win32 with LOCALAPPDATA env prints expanded; otherwise template. Docs say to prefer copying from Windows-native run — honest. NIT: CLI could annotate when it prints the unexpanded template.

  Also check the TROUBLESHOOTING/mcp.md statement "Claude Code、Grok 启动 MCP 时不会展开 %LOCALAPPDATA%（还会把 % 转义）" — a claim I can't verify from repo, but plausible; not blocking.

  Now question 2 "文档「确认台会到前面」有没有说满": docs say "扩展需已连接", "若窗口没出现，确认 Chrome 已开且扩展已连接，或自己打开 Chrome 确认台". Also note: MV3 service worker must be running to receive WS; the persistent WS connection implies worker alive. Adequate.

  One remaining check — question 1 asks specifically about "租手 L2（navigate 等）". Navigate URL-gate: prefixed ✓. But what about navigate when the domain IS auto-approved? outboundTrack → never skips (line 366-368), so outbound navigate always confirms via prefixed path. Good.

  But wait — one more subtle thing: in url-cookie-admission, is the confirm's sendConfirm fan-out? It uses bound.sendConfirm from bindOverlayConfirmChannel — for outbound it fans out (per comment line 387-389). OK.

  Also verify: cockpitFocusEventFromMessage checks msg.tool_name startsWith "[Outbound]" — frames for URL-gate confirm have tool_name "[Outbound] navigate" ✓.

  Now the l2-admission latent gap: outbound tools currently never reach l2-admission confirm (no L2_GATE_TOOLS in profiles). Actually wait — do outbound navigate/click calls pass through l2-admission at all, and could `forceConfirm` be triggered for them another way (e.g. critical APIs in code)? For click/type, code is selector/text — detectCriticalApis on code? That path only matters for evaluate-like tools. Browser click/type don't have code previews with dangerous APIs... they could hit the `safety.dangerousApis.length > 0` confirm? Let me quickly check whether click/type go through a confirm in l2-admission. Actually simpler: is there any existing confirm for outbound click/type? The pre-#524 behavior for outbound confirms was "fan-out to Side Panel; Windows must open cockpit". The docs mention OUTBOUND_CONFIRM_REQUIRED for "危险工具等 L2 确认". For outbound, L2 confirms arise from URL gate mainly. I'll note the l2-admission raw-toolName as a latent inconsistency NIT (WS frame unprefixed while tray frame prefixed at line 1270), recommending a one-line fix; not blocking since unreachable with current profiles.

  Hmm, wait — actually let me double-check whether outbound navigate ALSO goes through l2-admission in addition to url-cookie-admission. If navigate is not in L2_GATE_TOOLS, the l2-admission confirm path at 1485 only runs when skipConfirmation is false AND tool needs L2 (needsL2 flag at 1202). Let me check what sets needsL2 / the confirm decision for non-L2_GATE tools. Line 1071: `(L2_GATE_TOOLS).includes(toolName) || ...` — check context.Line 1083: `if ((!skipConfirmation || forceConfirm) ...)` — since line 930-931 forces skipConfirmation=false for outbound, ANY outbound tool passing through l2-admission reaches the WS confirm at 1485... wait, does l2-admission run for all tools or only gated ones? Let me check where this function is entered — is there an early return for tools not needing L2? Line 311: `if ((L2_GATE_TOOLS.includes(toolName) || hostAppGated || hostCliGated || hostComputerGated) && !finalParams.security_token)` — that's for token issuance. Need to find the top-level entry condition: does companion call l2-admission only for certain tools? Let me check how it's invoked — maybe checkHighRiskExecution decide. Actually check where the admission function early-returns for tools with no dangerous APIs. Let me look for the entry and the condition around line 311 and what happens for a plain "click".This file is large; I need the entry condition — where does the confirm block trigger for tools NOT in L2_GATE_TOOLS? Line 1083: `if ((!skipConfirmation || forceConfirm) && ...)` — is that inside a block that only runs for certain tools? Let me look at what wraps lines ~860-1083 — line 889 `let skipConfirmation = securityConfig.auto_approve_dangerous === true`. Need the enclosing condition — maybe `if (safety triggers || forceConfirm...)`. Let me search backward for the condition guarding that section, e.g. around line 850-890.I need the outer condition that encloses this whole block — what determines this function even runs the L2 confirm for a given tool. Let me look above line 820 — where `hostComputerGated` block ends and where this section starts. Actually, the key question: is this L2 confirm section reached for browser tools like click/type/navigate? l2-admission is presumably invoked from companion-dispatch for all tools? Let me check the caller. Search for where this admission function is called (companion-dispatch.ts or server.ts) and any tool filter.runL2ToolAdmission runs for ALL tools (including outbound browser tools). So the confirm block at line 1083 `if ((!skipConfirmation || forceConfirm) ...)` — is it guarded by some tool filter above? Since outbound forces skipConfirmation=false (line 931), `!skipConfirmation` is true for ALL outbound tools → every outbound tool would hit the WS confirm at 1485?? That can't be right — outbound click/type would then always confirm with raw tool_name. Wait, but checkHighRiskExecution(toolName, code) at 1089 — and the confirm block may be guarded earlier by something like "only if tool is in some confirm-requiring set". Let me check the structure between lines 700-820 and find the enclosing `if` for the confirmation section. Let me search for where the section starts — look for a condition like `if (needsL2Confirmation...` or the big `if` wrapping. Look at lines 700-820.This file is deep. Instead of reading everything, find the guard that wraps the confirm section — look between line 420 and 851 for the start of the confirm block, likely something like `if (requiresL2 || dangerousApis.length > 0 || forceConfirm...)`. Search for "checkHighRiskExecution" definition usage and the enclosing if. Let me grep for the section start around 560-660.The confirm block at 1083 runs when `!skipConfirmation || forceConfirm`. For outbound, skipConfirmation is forced false at 931. So `!skipConfirmation` = true → every outbound tool that reaches line 1083 hits the confirm path?! But that would mean outbound click/type always confirm via WS with raw tool_name — contradicting the design where only URL-gate/exfil confirms exist. Unless... the whole section from ~311 to ~1600 is inside the `if ((L2_GATE_TOOLS.includes(toolName) || hostAppGated || hostCliGated || hostComputerGated) && !finalParams.security_token)` block starting at line 311! Line 311 opens a block; the confirm section at 1083 is probably still inside it (the file is 1922 lines; block could span 311-1600). That would mean the L2 confirm only happens for L2_GATE_TOOLS/host gates. Browser tools (navigate/click/type) skip this entirely and only face url-cookie-admission. That matches the design: outbound confirms = URL gate (prefixed) + first-exfil (prefixed).

  Let me verify the block structure: find the closing of the line-311 if. Search for matching dedent. The block at 311 is indented 2 spaces (`  if (...) {`). Its body at 4 spaces. The confirm block at 1083 is indented 4 spaces (`    if (...`), consistent with being inside. And line 1884 `    if (!tokenValid)` also 4-space. Line 1910 `  if (earlyResult...` 2-space — outside. So likely everything 311→~1905 is inside the big if. I'm fairly confident: the l2-admission WS confirm only fires for L2_GATE_TOOLS / host gates — none of which are outbound-grantable. So the unprefixed toolName at 1488 is unreachable from outbound profiles today → NIT-level latent gap.

  Quick sanity: check line ~1600-1900 closing brace. Not crucial.

  Also verify line 930's isOutboundMcpCall block is inside the 311 if — yes (indented 4).

  Now let me also confirm the test file claims: cockpit-focus-policy.test.ts 36 tests — can't rerun per instructions ("不要重跑全库" but I could run just this test file? The prompt says "已跑：36 通过…请核对测试是否真锁住行为，不要重跑全库" — I may run the single test file to verify, it's cheap and doesn't touch .test-dist? Actually running tests builds to .test-dist — the instruction says don't run full `npm test` because it grabs `.test-dist`. Running a single file might also use .test-dist. The instruction explicitly says don't rerun; just verify tests lock behavior by reading. I'll read the test file to check the two new tests (already seen in diff) — they lock decideCockpitFocus + cockpitFocusEventFromMessage mapping, including the negative case (navigate stay_background). Good.

  Also the companion test (outbound-mcp-companion-http.test.ts) locks wire frame tool_name/full_preview/code_preview for first-exfil screenshot — good, that locks the prefix on the wire for exfil. Is there a test locking the URL-gate `[Outbound]` prefix on wire? Search companion tests.The integration test asserts `/navigate/` matches tool_name — which passes for both "navigate" and "[Outbound] navigate". So the URL-gate prefix is only weakly locked (regex /navigate/ would pass even without prefix). NIT: test doesn't strictly lock the `[Outbound] ` prefix for URL-gate; the focus feature depends on that prefix. Comment says it labels `[Outbound] navigate` but assertion is loose.

  Now I have everything. Let me also double-check the confirm-center-user-guide claim and CHANGELOG — fine.

  Question 6 sidebar snippet check once more: snippet includes `command: C:\Users\<你的用户名>\...` when hostOs==="win", with comment lines. If a user copies snippet wholesale into JSON it won't parse (it has `command:`/`args:` lines + `#` comments — but that was pre-existing format). Not new.

  One more possible issue for question 2: "open_focus 是否真的会 openOrFocusCockpit" — yes index.ts:582-584. Also does the focus policy arm gate matter? decideCockpitFocus confirmation branch ignores arm entirely (line: `if (event.outboundChannel) return "open_focus"` before arm checks) — arm only affects computer.task. Good.

  Also check `cockpitFocusEventFromMessage` reads msg.tool_name — the frame field is tool_name ✓ (security-confirmation.ts:312).

  Potential subtle issue: In cockpit, when the window opens, does it show the confirmation? CockpitApp receives frames relayed via background; noteSecurityConfirmationRequest mirrors for hydrate (index.ts:554-556). OK.

  Another check for question 4: does `auto_approve` in the cockpit/sidepanel auto-respond to confirmations with auto_confirm_eligible? companion-http sets autoConfirmEligible: false. URL-gate request doesn't set autoConfirmEligible — check SecurityConfirmationDetails default; auto_confirm_eligible undefined. Is there client-side auto-approve that acts when auto_confirm_eligible !== false? Let me check quickly where auto_confirm_eligible is consumed in the extension.auto_confirm_eligible only stored, no auto-approve consumer in extension. And companion-side auto-approve skips are disabled for outbound (l2-admission:930, url-cookie:366). Good.

  Now check whether god-mode on companion could resolve outbound confirms via "privileged respond" — that's operator-side, fine.

  I have enough to write the review. Let me also verify the exact line numbers I'll cite:

  1. cockpit-focus-policy.ts — read current file to confirm line numbers for outboundChannel logic.All verified. One last check on question 3: does fullPreview on the first-exfil confirm make the Cockpit treat it differently (e.g., requiring more steps)? CockpitApp.tsx:609 just renders text. ConfirmElevated — check quickly whether full_preview gates approve in Cockpit. Probably not. Also check that first-exfil confirm with risk_level "high" can be approved in Cockpit without nonce — yes, normal.

  Also should confirm the CockpitApp confirm rendering allows approve for this confirm — it's the standard confirm center; fine.

  Now, is there anything about question 2's "文档『确认台会到前面』有没有说满"? One caveat: on macOS with tray, both tray dialog and cockpit window may appear — fine. Another caveat: `openOrFocusCockpit().catch(() => {})` — failures swallowed; docs cover "窗口没出现时" fallback. Adequate.

  Also the docs/mcp.md says "外部助手发起的确认会把 Chrome 确认台拉到前面" — but this only holds for confirmations whose tool_name carries the `[Outbound]` prefix (URL gate + first exfil). As established, those are the only outbound confirms reachable with current profiles, so the doc statement is accurate today. The latent l2-admission gap (raw toolName at l2-admission.ts:1488 vs prefixed tray name at 1270) is unreachable via outbound profiles (profile.ts:21-67 contain no L2_GATE_TOOLS), so NIT not blocker.

  NIT list:
  1. l2-admission.ts:1488 — WS confirm passes raw `toolName` while tray path (1270) prefixes `[Outbound]`; latent inconsistency: if an outbound-capable tool ever lands in L2_GATE_TOOLS (e.g. evaluate in a future profile), its confirm would stay_background on Win/Linux and silently 45s-fail — exactly the bug #524 fixes. One-line fix: same prefix expression as 1270.
  2. companion/tests/integration/outbound-mcp-executor.test.ts:418 — assertion `/navigate/` passes with or without the `[Outbound] ` prefix; the focus feature hinges on that prefix. Tighten to `assert.equal(confirmation.tool_name, "[Outbound] navigate")` or startsWith.
  3. Tests lock the policy function and wire frame, but nothing locks the background wiring `focusEvent → openOrFocusCockpit` (index.ts:581-584). ui-command.test.ts:50-63 shows the repo already does source-slice tests for background wiring; a similar slice test could lock it. Non-blocking: the wiring is pre-existing and shared by all open_focus events.
  4. grant-cli.ts:132-138 — on non-Windows the CLI still prints the literal `%LOCALAPPDATA%\CMspark\node.exe` template with no inline warning; docs are honest ("优先复制 Windows 本机印出的"), but a one-line stderr note when printing the unexpanded template would close the last copy-paste-fail path. NIT.
  5. Maybe: the "权限" heading in the sidebar — context export entry only appears after switching profile to outbound_context_v1 (OutboundMcpSettingsSection.tsx ~line 505 in new file / diff line 189). The helpText does explain two gates. Borderline; I'll mention as observation, not a nit? The implementer claim matches exactly, so fine. Could mention as NIT that 上下文出口 is discoverable only after profile switch — but the profile hint text explains it. Skip or small.

  Blocking: none found. Question-by-question answers:

  1. Coverage: first-exfil prefixed (companion-http.ts:208), URL-gate navigate prefixed (url-cookie-admission.ts:395). Outbound profiles (profile.ts:21-67) contain no L2_GATE_TOOLS, so l2-admission's unprefixed WS path (l2-admission.ts:1488) is unreachable today — latent gap only (NIT-1).
  2. open_focus → openOrFocusCockpit at background/index.ts:581-584; creates/focuses real popup window (cockpit-window.ts:176-186). Outbound confirms fan out to all authenticated non-summoner peers (confirm-fanout.ts:74-87); the extension background is the WS client, so it receives the frame with sidepanel closed. Docs caveat (Chrome open + extension connected, fallback open cockpit manually) present in mcp.md §3 and TROUBLESHOOTING DISCLOSURE_HITL_REQUIRED — adequate.
  3. full_preview doesn't gate sidebar approval: MinimalConfirm needsCockpit = nonce || expert_team only (MinimalConfirm.tsx:147-148, 239, 381). No nonce set for first-exfil (companion-http.ts:207-214 has no nonceChallenge). full_preview also flips hasHeavyPreview (cockpit-focus-policy.ts:106) → open_focus doubly ensured; no behavior regression.
  4. Exception scoped to tool_name prefix (cockpit-focus-policy.ts:101-102); only outbound paths emit that prefix. Outbound never auto-approves: l2-admission.ts:930-936 forces skipConfirmation=false; url-cookie-admission.ts:366-370 outboundTrack → never skip; first-exfil sets autoConfirmEligible:false (companion-http.ts:213); extension has no auto-approve consumer of auto_confirm_eligible (only stored, useWebSocket.ts:899). Negative test locks navigate stay_background (cockpit-focus-policy.test.ts:233-244 new test).
  5. originWs binding intact: companion-http.ts:168-176 resolveConfirmBinding with isOutboundMcpCall:true → originWs undefined (confirm-fanout.ts:113-119) — unchanged by this diff; fan-out broadcast preserved.
  6. Sidebar win snippet uses expandable placeholder path + Explorer instructions (OutboundMcpSettingsSection.tsx:42-49, and win-only help block). CLI prints expanded path when actually run on win32 (grant-cli.ts:132-138); docs say to prefer Windows-native output — honest. NIT-4 for non-Windows template printing.
  7. caller input existed before (diff shows old placeholder "caller_id（与 MCP 调用 body 绑定）"), now has visible label + helpText + htmlFor (diff; current file). Permission entry: 页面外泄 checkbox always visible; 上下文出口 only under OUTBOUND_CONTEXT_PROFILE — matches claim, profile hint explains. Not 名不副实 since the two gates are explained in helpText.
  8. Tests lock policy + wire frame for exfil; background wiring untested (NIT-3) — not blocking since wiring pre-exists and is generic.

  Verdict: APPROVE_WITH_NITS.

  Write final answer in Chinese, blocking items first ("无阻塞"), then NITs with file:line, then verdict last line exactly.

• 评审完成。以下结论均来自我直接读到的当前工作区代码与补丁。

  ## 阻塞项

  无阻塞。

  ## 逐问核对（事实摘录）

  **1. `[Outbound]` 前缀覆盖**：首次外泄确认在 `companion/src/outbound-mcp/companion-http.ts:208` 以 `` `[Outbound] ${internal}` `` 发出；navigate URL 门在 `companion/src/tool/url-cookie-admission.ts:395` 同样带前缀。租手三个 profile（`companion/src/outbound-mcp/profile.ts:21-67`）只含浏览器工具，无一在 `L2_GATE_TOOLS`（`companion/src/tool/l2-admission.ts:64-83`），所以 l2-admission 的 WS 确认路径对租手不可达——但注意 NIT-1。

  **2. open_focus 真开窗**：`chrome-extension/src/background/index.ts:581-584` 对 `open_focus` 调 `openOrFocusCockpit()`，其实现真建/聚焦 popup 窗（`cockpit-window.ts:176-186`）。租手确认 fan-out 到所有已鉴权非 summoner peer（`companion/src/mcp/confirm-fanout.ts:74-87`），扩展后台本身就是 WS 客户端，侧栏关着也收得到帧。文档「确认台会到前面」附了「Chrome 要开着、扩展要已连接，或自己打开确认台」的兜底（docs/mcp.md §3、docs/TROUBLESHOOTING.md DISCLOSURE_HITL_REQUIRED 条），没说满但兜底诚实。

  **3. fullPreview 副作用**：侧栏 `MinimalConfirm` 禁批条件只有 `nonce_challenge || expert_team`（`chrome-extension/src/sidepanel/components/MinimalConfirm.tsx:147-148,239,381`），`full_preview` 不改变侧栏可批性；首次外泄未设 nonce（`companion-http.ts:207-214`）。副作用仅是让 `hasHeavyPreview` 也为 true（`cockpit-focus-policy.ts:106`），与 outboundChannel 同向，属于双保险，无误开 nonce。

  **4. 例外未放宽**：前缀只由 `tool_name.startsWith("[Outbound]")` 置位（`cockpit-focus-policy.ts:101-102`），仅租手路径产出该前缀；普通 navigate 轻确认仍 `stay_background`（新测试 `cockpit-focus-policy.test.ts` 负向用例锁住）。租手永不自动过：`l2-admission.ts:930-936` 强制 `skipConfirmation=false`，`url-cookie-admission.ts:366-370` outboundTrack 永不跳 URL 门，首次外泄 `autoConfirmEligible: false`（`companion-http.ts:213`）；扩展侧 `auto_confirm_eligible` 只存不用（`useWebSocket.ts:899`）。god-mode 不跳过租手确认。

  **5. originWs 绑定未丢**：`companion-http.ts:168-176` 仍走 `resolveConfirmBinding({isOutboundMcpCall: true})` → `originWs: undefined` 不绑定（`confirm-fanout.ts:113-119`），本 diff 未动此语义。

  **6. Windows 路径**：CLI 在真 win32 上印展开路径（`grant-cli.ts:132-138`）；侧栏 win 片段用 `C:\Users\<你的用户名>\...` 占位 + Explorer 说明（`OutboundMcpSettingsSection.tsx:42-49` 及 win-only 帮助块）；mcp.md/TROUBLESHOOTING 均明确「优先复制 Windows 本机印出的两行」——诚实。残留见 NIT-4。

  **7. caller 与权限入口**：caller 输入框**以前就有**（旧 placeholder「caller_id（与 MCP 调用 body 绑定）」），本次是加可见标签 + 必填说明 + `htmlFor`，实现者声称与 diff 一致。「权限」区：页面外泄勾选始终可见；「上下文出口」只在 `profile === OUTBOUND_CONTEXT_PROFILE`（`outbound_context_v1`）时渲染——与声称逐字相符，且 profile hint 已解释交互档/上下文档差异，不算名不副实。

  **8. 测试锁定面**：新测试锁住策略函数与消息映射（含负向），companion 测试锁住首次外泄线上帧的 `tool_name`/`full_preview`/`code_preview`（`companion/tests/outbound-mcp-companion-http.test.ts:311` 区域）。「后台真的开窗」这条线无测试，见 NIT-3；鉴于该接线是全 open_focus 共用的存量代码，不阻塞。

  ## 非阻塞 NIT

  1. **`companion/src/tool/l2-admission.ts:1488`**：WS 确认帧 `toolName` 用裸名，而同函数 tray 路径（:1270）有 `[Outbound]` 前缀——同一确认两个通道名字不一致。今天不可达（租手 profile 不含 L2_GATE_TOOLS），但若未来某 profile 放进 `evaluate` 之类，该确认会 `stay_background` 并在 Win/Linux 静默 45 秒失败——正是 #524 修的那个病。建议与 :1270 用同一前缀表达式，一行封堵。
  2. **`companion/tests/integration/outbound-mcp-executor.test.ts:418`**：注释说锁 `[Outbound] navigate`，断言却只有 `/navigate/`——有无前缀都通过。开窗功能恰恰押在这个前缀上，建议收紧为 `assert.equal(confirmation.tool_name, "[Outbound] navigate")`。
  3. **后台开窗接线无测试**：`index.ts:581-584` 的 `focusEvent → openOrFocusCockpit` 无锁定；仓库已有 source-slice 测试先例（`chrome-extension/tests/ui-command.test.ts:50-63`），可照样子锁一行。不阻塞：接线是存量共用路径，新逻辑（策略 + 帧字段）均有测试。
  4. **`companion/src/outbound-mcp/grant-cli.ts:132-138`**：非 Windows 上跑 CLI 仍印字面 `%LOCALAPPDATA%\CMspark\node.exe` 模板且无行内警告。文档已正确引导「Windows 本机跑」，但在 mac 上替 Windows 配的用户会原样复制到失败。印模板时加一行 stderr 提示即可。

  VERDICT: APPROVE_WITH_NITS

To resume this session: kimi -r session_6b52657a-c394-4248-88fb-65f359f8ab09
