All verification complete. Here's my review summary:

## R2 Review — PR #159 Pi REJECT fix

### Pi blocker: FIXED ✅
1. **Comment hygiene** — `companion/scripts/run-tests.mjs` no longer contains any `*/` sequence in the JSDoc block; the `**/` glob text was rewritten to "matching *.test.js" (verified by read + `git show 22688b2`).
2. **`node --check companion/scripts/run-tests.mjs`** → exit 0.
3. **`cd companion && npm test`** — tsc compiles clean, runner loads and executes the full suite (2623 tests, 2544 pass, 70 fail). **No SyntaxError.** Exit 1 comes only from dev-box environmental failures: `EPERM: rename` lock contention on the live `~/.cmspark-agent` dir (a running agent on this machine holds `threads/index.json`/`config.json`), Windows symlink privilege (vault-templates/index), backslash path-separator assertions (vault-profiler), chmod no-op on Windows, and darwin-only tests — all pre-existing, none in files changed by this PR, and CI runs `ubuntu-latest` so they don't hit CI.

### No security regression from the fix ✅
- Fix commit `22688b2` is **comment-only** (6 lines in run-tests.mjs) + review artifacts — zero security code touched.
- PR's own security test files re-run directly: **86/86 pass** (voice-privacy-origin-p1, ws-validate-strict, meeting-audio-gc, mcp-stdio-l2-gate, outbound-mcp-grants + http-e2e, thread-path-sanitize, tool-persistence-redact, pending-tool-origin-ws, llm-supersede-generation, voice-binary-resolve, message-router-config-security). Security gates (SEC-A–F, VOICE-01/02, MCPO-01, privacy_ack_v2, pin fail-closed) verified green.

### Deferred items — not overclaimed ✅
God-file split, full multi-arch pin matrix, codesign, full startServer remain honestly deferred (`memory/session.md` S59 "刻意未关"; r1 review confirms).

### ADR-020 checklist
Declaration present in R2 prompt (Surface L0 voice gates + L2 CU toggle reusing existing `computer.set_enabled` gate; Trust privacy_ack_v2/pin fail-closed/meeting GC/WS strict; Channel community). Axes fit; no new runtime; no new confirm family; originWs bound per r1; trust monotonicity intact; no bare "中层 Agent". The comment-only fix doesn't perturb any of this.

### Nits (non-blocking)
1. `p1-p2-pr159-r2-diff-20260809-170412.patch` is metadata-only (11 lines, no diff body) — readers must use git history to see the actual fix.
2. Full `npm test` is not green on Windows dev hosts (70 environmental failures as above); the closeout's "132 targeted" claim is per-targeted-suite, which I reproduced (security subset 86/86). CI is ubuntu and unaffected.
3. r1 nits carried forward per instructions: file.upload/regenerate multi-agent cap leak, host-skylight `kCGWindowNumber == 0` leniency, any-id chrome-extension origin — none escalated to High.

VERDICT: APPROVE_WITH_NITS
