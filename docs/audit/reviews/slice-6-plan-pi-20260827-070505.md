Review complete. Here is my independent re-review of **slice 6 PLAN r2** (T2 plan dual after r1 3×REJECT + External AWN).

## What I verified (live code, not rubber-stamp)

**Patch freshness**: HEAD `e5d86a5` is the r2 fold commit; working tree clean (only untracked review artifacts); patch content matches the committed plan byte-for-byte (251 lines both). Not stale.

**Pin 1 — Matching honesty door** ✅ Pinned. Task 3: `/技能` while auto → thread flips `manual` + activates the skill; next `resolveSkillIdsForThread` does not union `matchSkills`. Live: `resolveSkillIdsForThread` auto unions `active ∪ matchSkills ∪ getBySite` (skill-engine.ts:573–608) — real. `skill.activate` handler today only touches `active_skill_ids`, never `skill_selection_mode` (message-router.ts:2423–2432) — so the "don't grow overlay" freeze is consistent. 按需 writes mode via `thread.update` (ContextPanelHost.tsx:406) — plan's claim confirmed.

**Pin 2 — Seed source** ✅ Pinned. Plan seeds from `thread.runtime_context_budget?.handoff?.open_todos`, cap 8×120. Live: `context-handoff.ts:17` `open_todos: { max: 8, len: 120 }` — exact H1 match. `thread-manager.ts` has no `thread.open_todos` — the r1 Impl hole is closed.

**Pin 3 — applyToolResult** ✅ Pinned. Only `source:"seed"|"user"` with `tool`/id binding; never `model_draft`; never `text.includes(toolName)`; wired in `adapter.ts` after tool success (tool loop exists at adapter.ts:1149–1295 with `toolResult.success`).

**Pin 4 — WS toggle** ✅ Pinned. New `thread.run_progress.toggle` type: not in `SUMMONER_ALLOW` (deny-by-default, summoner-acl.ts:52), not in `SUMMONER_WEB_DISPATCH_ALLOW` (summoner-web.ts:36), `thread.update` overlay policy is alias-only. validate.ts + background/index.ts + summoner-acl.test.ts all exist; lockstep test specified.

**Pin 5 — UI** ✅ Pinned. ChatView-only, 「本轮步骤」 not 「进行中」; EmptyState untouched (ChatView.tsx:508/1682 confirms slice 5 in main).

**Pin 6 — IDF test honesty** ✅ Pinned **and computationally checked**: rareterm is in corpus; with `log((n+1)/(c+1))`, "web" (all 4 docs) → idf 0, rareterm → 0.916; the assertion `cos(q, rareOnly) > cos(q, commonOnly)` evaluates 1.0 > 0 — a real red-green, not fake red. `tokensToVec(["a","a","b"]) = {a:2/3, b:1/3}` matches current implementation exactly (Obsidian/related stay pure TF; verified users: related.ts, knowledge-related.ts, obsidian/vault-index.ts).

**Pin 7 — Docs** ✅ Pinned, and genuinely needed: GOAL.md G17, architecture.md:131, CLAUDE.md A5 all currently over-claim "TF-IDF" over a TF-only implementation. Plan fixes them, leaves `docs/mcp.md` T1 banner ("尚未跑") alone, never claims T1 done, and explicitly refuses outbound expansion.

**ADR-020**: Full Surface/L2/Compose/Autonomy/Trust/Channel declaration present. Trust monotonicity holds (overlay no write verbs, no new confirm dialect); originWs n/a (no new `securityConfirmations.request`); Pack-first addressed by locked SoT (summoner-strategy-rethink §9 = L0 chat chrome, not a Pack).

## Nits (non-blocking)

1. **`RunProgressItem` schema omits `tool?: string`** (plan line ~193) while pin #3 / Task 4 rules require `item.tool === tool` matching — the type should declare the field so it type-checks.
2. **`model_draft` "named companion ingest" hook is unpinned** — which component extracts the model's proposed list is unspecified, yet self-review claims "No TBD"; if no source exists at implement time, draft-row UI is dead code. Pin the ingest point or defer `model_draft` to seed-only.
3. Naming drift: plan says `tool.result ok===true`; actual field is `toolResult.success` (adapter.ts:1286) — trivial, implementer will map.
4. Extension-side one-send slash union (`App.tsx:1246–1253`) isn't explicitly decommissioned; it becomes harmless once companion flips to `manual`, but a lockstep test at the extension level would pin it.

No r1 BLOCK remains open; the plan is implementable as sequenced PR-A/B.

VERDICT: APPROVE_WITH_NITS
