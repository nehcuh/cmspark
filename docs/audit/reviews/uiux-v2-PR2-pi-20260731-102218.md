Here is the complete review:

---

## PR2 — ContextPanelHost extract — Review

### Scope check (§4.7 M1)

| M1 requirement | Status |
|---|---|
| Extract `ContextPanelHost` (registry + open/close + loaders + event listener) | ✅ Done |
| `activePanel` state moved from BottomBar → Host | ✅ Done |
| `loadPanelData` moved to Host | ✅ Done |
| `cmspark:open-context-panel` listener moved to Host | ✅ Done |
| Panel body mounts (`<TabsPanel/>`, `<SkillsPanel/>`, etc.) moved to Host | ✅ Done |
| BottomBar becomes thin chrome calling Host API | ✅ Done |
| Tab strip **not** deleted | ✅ Done |
| Deps: PR1 (StatusRail + offline hygiene) | N/A (offline `alert()` not in this diff) |

### No scope creep

- No StatusRail, FocusBand, chips, `/` palette, or 装配 drawer code introduced.
- No BottomBar tab strip removal.
- No Cockpit or dark-mode work.
- `#fff` on accent mode-button (line 498) is the only hardcoded hex remaining in the moved code — carried over verbatim from the original BottomBar. Not Material. Not new.

### Token compliance

- Zero Material hexes (`#4CAF50`, `#FF9800`, `#F44336`) in new/touched files.
- Moved code improved: `#bfdbfe` → `tokens.accentSoft`, `#e8f0fe` → `tokens.bgActive`, `#999` → `tokens.textMuted`, `#ddd` → `tokens.border`, raw `4px` → `tokens.radiusSm`, `boxShadow` → `tokens.shadowMd`, `transition` → `tokens.transitionFast`.
- All token keys used (`accentSoft`, `accentText`, `radiusSm`, `radiusMd`, `shadowMd`, `transitionFast`) exist in `tokens.ts`.

### Architecture

- **Provider pattern** clean: `ContextPanelHostProvider` wraps `AppContent`, exposes `activePanel`/`openPanel`/`openPanelForce`/`closePanel` via React context.
- **Shared registry**: `CONTEXT_PANEL_TABS` exported from Host, consumed by both `BottomBar` (tab strip) and `ContextPanelHost` (panel body). Single source of truth.
- **Esc handling** moved to Host (§4.9 layer 2), with input/tag guard preserved.
- **Level-filter auto-close**: Host closes panel if its id is no longer allowed for the current capability level.

### Tests

- Unit tests validate registry completeness against `mode-controller` output, `isContextPanelId` guard, Chinese labels. No React mount needed — appropriate for M1 scope.

### BottomBar residuals cleaned up

- Removed unused `base64ToBlob` helper (dead code).
- Removed stale inline comments.
- Removed unused imports (`IconTabs`, `IconHistory`, etc. — now only `IconMore`).

### Risk assessment

- **Behavioral**: Panel open/close, data loading, and event wiring are a 1:1 move. No logic changed.
- **Layout**: Panel body is now a sibling `<ContextPanelHost />` below `<BottomBar />` instead of nested inside it. Same vertical position, `flexShrink: 0` added.

---

VERDICT: APPROVE
