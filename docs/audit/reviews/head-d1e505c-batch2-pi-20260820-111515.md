All verification complete. The patch is byte-identical to live `git diff d1e505c` (sha256 `52730daf49e185d1` — matches the lanes' frozen hash), so it is not stale.

## Independent verification results

**Lane A (SSRF/DNS) — all claims HOLD, re-verified live:**

1. **S-XLAT** (`security.ts:240-247`): SIIT branch `[0,0,0,0,ffff,0,hi,lo]` is prefix-disjoint from v4-mapped (`groups[5]===0xffff`), NAT64, 6to4, v4-compatible. `::ffff:0:a9fe:a9fe` → 169.254.169.254 blocked; `::1` / public NAT64 `64:ff9b::808:808` allowed. Confirmed in code + `llm-endpoint-url` tests (19/19 pass on this host).
2. **S-GCPDOT** (`security.ts:300`): trailing-dot strip in `isCloudMetadataIp` + `canonicalizeLlmHostname`.
3. **N1**: `LLM_ENDPOINT_DNS_ERROR` ≠ `LLM_ENDPOINT_IMDS_ERROR` (`security.ts:336-337`); `classifyLlmHostnameDns` fail-closed → `unresolved` on NXDOMAIN/empty.
4. **N2**: `canonicalizeLlmHostname` runs before `LLM_HOST_ALLOWLIST.has` in `settings-web.ts:144-155` — `localhost.` hits allowlist.
5. **N3**: all listed request-path sites gate before fetch (openai/anthropic `streamChat`/`complete`, `vision-pipeline.ts:166-175`, `ws/lifecycle.ts:613`, `probeLlmConnection`, `config.test`/`config.testVision`). Verified in live code.

**Hostile Q1 — `/api/testVision` (P2-A1): correctly P2, not P1.** `settings-web.ts:477-485` fetches after `validateTestBaseUrl` allowlist-skip without a second DNS gate. But: server is loopback + 32-byte random token; upstream body is never reflected (status+short msg only); allowlisted entries are public HTTPS providers (TLS fails against IMDS) plus localhost/127.0.0.1 (loopback is a *permitted* LAN LLM host — no new exposure); exploitation requires DNS poisoning of an allowlisted name, i.e. resolver ownership. Chat/`/api/test`/providers all double-gate. P2 with a trivial fix (call `assertLlmEndpointAllowedAsync` in the vision branch).

**Lane C (osascript) — all claims HOLD, re-verified:**

- Dispatch no longer returns `safety.error` post-token (`companion-dispatch.ts:1101-1110`) — log-only preview. Token still mandatory; invalid/stale/forged tokens fail (HMAC + one-time + TTL + code-hash + threadId binding, `security-policy.ts:199-240`). No `checkHighRiskExecution` hard-block return remains in production (grep confirmed: only l2-admission preview + dispatch log).
- **Hostile Q2 (forged token)**: no execute path. `validateToken` is constant-time HMAC, one-time delete, expression-bound; LLM-supplied tokens stripped before L2 (`server.ts:464-478`).
- **Hostile Q3 (trust monotonicity)**: removing the regex veto is *not* a confirm skip — osascript stays in `L2_GATE_TOOLS` (`l2-admission.ts:51`), god-mode alone still forceConfirms (M3' test), and the APPROVE test ran 194ms on this Darwin host, proving it reached the real `execFile` path after an actual `security.confirmation.request` with `dangerous_apis:["fetch"]`.
- **Hostile Q4**: restoring the re-block would indeed stay green on Linux CI — the APPROVE regression test early-returns on non-darwin (`security-gates.test.ts:1119-1127`). Caught on Darwin today; a platform-free unit test would close it. Nit only.
- Copy: live probe confirms 「若你已拒绝弹窗」 appears **only** on `User denied`; timeout/unavailable/disconnect/leftover all avoid it via the `这不是确认弹窗` short-circuit (`user-gate-copy.ts:161-165`). Lane C N2 (`/你拒绝了/` ⊃ `不是你拒绝了`) is latent, not currently triggerable. Panel `gate-error-copy.ts` 4/4.

**Lane B (voice) — production untouched.** `local-stt-adapter.ts` diff vs `d1e505c` is empty (verified); `stopChainInFlight` (:951/:959) and sid-swap (:784/:982) are pre-existing. Tests-only batch.

**ADR-020 checklist:** declaration present and accurate — Surface L1(LLM, tighter)+existing L2 osascript (no new Surface); L2-classes none new; Compose none; Autonomy single; Trust monotonic (LLM/IMDS strictly tighter; osascript still HITL, regex veto removal ≠ confirm skip); Channel community. No Pack-first violation, no new confirm family, originWs binding unchanged and intact (`l2-admission.ts:1248-1254`). P1 watchlist items untouched.

**Missed P0/P1: none.** All 12 synthesis closures re-confirmed; the two residual "hostile" items (testVision P2, Linux-CI test gap) are correctly scored P2/nit.

## Non-blocking nits

- `settings-web.ts:477-485` — `/api/testVision` allowlist-skip then fetch without second `assertLlmEndpointAllowedAsync` (P2-A1); trivial fix available.
- `connection-test.ts:135-177` — `probeNativeVision` has no internal DNS gate (sole caller double-gated today, P2-A2).
- `security.ts:353-395` — lookup→fetch TOCTOU / redirect-follow without address pinning (pre-existing SSRF class, P2-A3).
- `security-gates.test.ts:1119-1127` — APPROVE regression is Darwin-only; Linux CI would miss a re-block (C-N1). Add a platform-free dispatch unit with stubbed `execFile`.
- `user-gate-copy.ts:168` / `gate-error-copy.ts:96` — `/你拒绝了/` matches `不是你拒绝了` (latent; today neutralized by `这不是确认弹窗` short-circuit).
- `security-policy.ts:51` vs `companion-dispatch.ts:1043-1046` vs `l2-admission.ts:267-268` — osascript `code`/`expression` bind/preview split (pre-existing; fail-closed for only-`code`).
- `voice-local-continuous.test.ts:430-432` — retry sid assert accepts any `*-r1`; `voice-local-stt-adapter-ws.test.ts` — `delayMs` not locked by an assertion (currently load-bearing).
- `security.ts:371-373` — `hostnameResolvesToImds` returns `true` on DNS failure (misleading name, no production caller).

VERDICT: APPROVE_WITH_NITS
