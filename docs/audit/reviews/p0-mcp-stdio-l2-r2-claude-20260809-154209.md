## Review Complete

**Pi REJECT #1 (enabled bypass)**: ✓ FIXED
- `mcpStdioSpawnSurfaceChanged` (lines 3542-3545) correctly catches `enabled: false → true` transition
- Test passes: `mcp.update enabled-only on disabled stdio requires L2`
- `toggle_server` path also re-confirms (lines 1910-1918)

**Pi REJECT #2 (*** corruption)**: ✓ FIXED  
- `mergeMcpServerPreservingSecrets` + `restoreMaskedRecord` (lines 3565-3595) restore real secrets when `***` is echoed
- Test passes: `mcp.update preserves secrets when client sends ***`
- `restoreMaskedRecord` drops `***` keys with no existing value (line 3577) — never persists mask literal

**Tests**: 7/7 pass ✓

**ADR-020 checklist**: 
- originWs binding verified at server.ts:7047 ✓
- All 9 broadcast paths redacted (list/add/update/delete/toggle responses + 3 event emitters) ✓
- Fail-closed without `requestConfirmation` maintained ✓

**Nits** (non-blocking):
- `mcp.update` command/args/cwd/env change re-confirm and `status_changed` redaction lack explicit tests (core L2 gate is tested)
- This was already noted in r1; acceptable for fix-focused diff

VERDICT: APPROVE
