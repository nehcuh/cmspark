All 21 tests pass (21 pass / 0 fail). Let me now complete the cross-check analysis.

---

## ADR-020 Capability Checklist Review

**Declaration** (from implementer prompt):
- Surface: L0|L1|L2 (no expansion) — matches the diff (no new tools/gates/surface classes)
- L2-classes: none new — confirmed (`server.ts` untouched)
- Compose: none — confirmed
- Autonomy: single — confirmed
- Trust: companion `confirmation_phrase` step-up on security flag arm
- Channel: community — confirmed

### Reviewer Checks (per checklist)

| Check | Result | Evidence |
|-------|--------|----------|
| 1. Axes fit | ✓ | Pure Trust-axis change — companion gate on `config.set` arm |
| 2. Pack-first | N/A | No new Side Panel chrome or scenario |
| 3. Confirm dialects | ✓ | No new confirmation family — reuses existing `config.set` + `confirmation_phrase` field. No new `securityConfirmations.request` |
| 4. Trust monotonicity | ✓ | God-mode still does **not** skip CU L2 / shell·netsec `forceConfirm` (verified: `server.ts` zero-diff, lines 1063/1122/1406 intact). Gate is additive (more restrictive on arm) |
| 5. originWs | N/A | No new/changed `securityConfirmations.request` calls |
| 6. No new runtime | ✓ | No agent framework |
| 7. Experimental layers | N/A | Not applicable |

### Gate coverage verification

- **Flat path** gated: lines 217–244 of `message-router.ts` — `normalized.security` is populated from flat fields (lines 205–215), then `findArmingSecurityFlags` runs against it ✓
- **Nested path** gated: same gate block checks `normalized.security` which is also populated from `cfg.security` object (line 204) ✓
- **Disarm bypass**: `findArmingSecurityFlags` only returns flags transitioning false→true; true→false returns `[]` → gate skipped ✓
- **Already-true resend**: same logic — proposed=true, current=true → not an arm transition → gate skipped ✓
- **Phrase check**: `isValidSecurityArmPhrase` uses strict `typeof === "string" && trim() === literal` — null/undefined/number/object all fail ✓
- **Wrong phrase rejection**: returns `"error"` type before `saveConfig` is called → no persist ✓
- **Audit on reject**: `logger.warn("security.arm_rejected", ...)` before return ✓
- **Audit on success**: `logger.warn("security.flag_armed", ...)` after `saveConfig` ✓

### UI end-to-end verification

- `handleGodmodeConfirm` / `handleAutoApproveDangerousConfirm` / `handleEnterpriseAutoApproveConfirm` all send focused `config.set` + `confirmation_phrase` → background pass-through (unchanged, line 524: spread only when `typeof === "string"`) → companion gate ✓
- Disarm paths send focused `config.set` without phrase → passes ✓
- `handleSave` sends full config snapshot without phrase → companion finds no arming transitions → passes ✓
- Confirmation panels reset on slideout open, disarm, and cancel ✓

### Document consistency

- `docs/audit/p1-security-open-items-2026-07-29.md`: P1-1 status updated to **FIXED** with accurate summary of Design A ✓
- `docs/adr/010-tiered-privilege-godmode.md`: paragraph added documenting the companion step-up, lock-step phrase, and scope boundaries ✓

### Nits

1. `securityArmRejectedError()` returns the literal phrase `"我了解风险"` in the error string sent to clients (`companion/src/security-arm.ts:58`). The phrase is static/fixed (not a credential), so this is a minor info-leak pattern, not a real security regression.
2. Background `config.set` handler forwards empty-string `confirmation_phrase` (`background/index.ts:519` — `typeof "" === "string"` is true). Companion correctly rejects it, but the unnecessary field could be filtered out to keep the wire contract tighter.

VERDICT: APPROVE
