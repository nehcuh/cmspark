# Dual external review — post220-nits-hud (Pi lane)

## Patch integrity
- `git status` confirms branch `fix/post220-head-p1-fold` @ `8f5c94c6`, diff `d4cbbfae..8f5c94c6` = 21 files / 381+/139− — matches patch stat exactly.
- Frozen patch SHA256 `ab1d1a12...e6825` matches the declared value; the two `.patch` files are content-identical (213602 copy only prepends a header). Not stale.

## Machine (re-ran, not trusting the adversary)
- companion `tsc --noEmit` → clean
- summoner-acl + doc-identity + distill + history → 76/76 pass (incl. `overlay pack.apply strips Trust extras (A-N2)`, `tray pack.apply is not rewritten`, `COM0`, `DSA PRIVATE KEY`, `short-eval-secret` collapse)
- summoner-web → 27/27 (incl. `on:!on` toggle, `ids:next` attach, `mcp.toggle_server && companionClient`)
- chrome-extension markdown-breaks → 2/2 (imports `CHAT_MARKED_OPTIONS`)

## Security / ADR-020 replays (live code, not prose)
1. **Overlay WS cannot mcp.add / knowledge.import / config.set** — none are in `SUMMONER_ALLOW` (summoner-acl.ts:26-45); lifecycle gate blocks pre-router (lifecycle.ts:1038); `knowledge.import`/`import_directory` additionally denied by `stampedSurface === "summoner"` at router (message-router.ts:2638, 2661). Allowlist unchanged vs base — additive-only diff.
2. **HTML HTTP path** — `SUMMONER_WEB_DISPATCH_ALLOW` excludes the same three; 403 at `dispatchAllowed` (summoner-web.ts:166-171). Every REST route maps through it.
3. **pack.apply strip** — ACL deletes `allowTrust/workspace_path/force_takeover/confirmation_phrase`, requires pack_id+thread_id, stamps `user_gesture:true` (summoner-acl.ts:125-146); router re-gates: overlay-eligible only, no trust cookie, `allowTrust:false` forced (message-router.ts:2978-3013). Belt + suspenders.
4. **No new confirm dialect** — HTML `mcp.confirm.pending` handler only sets a status string ("需在 Chrome 侧栏批准", summoner-web.ts:1099); `mcp.toggle_server` rides tray `companionClient` (menu-bar-agent.ts:1628) = Swift HUD precedent; zero `securityConfirmations.request` additions → originWs N/A.
5. **Trust monotonicity** — strictly tighter: evaluate/osascript history `result_summary` now full sha256 collapse instead of 200-char cap (history/store.ts:158-163, matches distill posture); COM0/LPT0 added to reserved names; DSA added to PEM redaction; junction skip fail-closed. 
6. **XSS** — all dynamic innerHTML in the new HUD passes through `esc()` (summoner-web.ts:779,794,992,1010,1025,1042); message bodies via `textContent`.
7. **Skill/knowledge compose fixes are real toggles** — `on:!on` (was activate-only), `ids:next` (was replace-all `[id]`); pin cap 32 + `knowledge_selection_mode:"manual"`.
8. **Import modal** — confirm disabled during 解析/抓取/失败; error dispatch omits `payload` and the reducer preserves prior payload (agentStore.tsx:921-936) — "error 不 wipe payload" holds.

## Nits (non-blocking; independently confirmed)
1. **Focus event lost when panel closed** — `ContextPanelHost.tsx:206-209` dispatches `cmspark:focus-knowledge` synchronously in the same tick as `setActivePanel("knowledge")`, but `KnowledgeSubPanel` mounts conditionally (:274), so when the panel was closed the listener isn't registered yet → row highlight/scroll and query-clear are lost (only works when panel already open). Adversary residual nit C, confirmed in code.
2. **Selector sanitization** — `KnowledgeSubPanel.tsx:58-59` strips only `"`/`\` from the id before `querySelector`; other CSS metacharacters would throw inside rAF (harmless, but unhandled).
3. **Tray-client fallback** — `menu-bar-agent.ts:1628` falls back to the summoner client when `companionClient` is absent → overlay can't answer L2, possible 8s timeout on mcp.toggle (adversary residual nit A).
4. `pack.apply` ACL rewrite is delete-then-rebuild rather than whitelist-strip of unknowns; router ignores strays — cosmetic only.

## Checklist verdict
Surface/Compose/Autonomy/Trust/Channel declaration present and matches code. No L2-classes on HUD, no middle-agent framing, no new runtime, no new confirm family, overlay never Allow/Deny, Pack-first intact. Blast T2 holds: no overlay WS reach into mcp.add/knowledge.import/config.set, no new confirm dialect.

Non-blocking nits listed above (focus-event timing is the only one that's a genuinely unfinished piece of the new feature; impact is cosmetic scroll/highlight only).

VERDICT: APPROVE_WITH_NITS
