All verification complete. Summary of the second-gate review:

**Frozen patch integrity**: SHA256 `26252380…74b51` matches; `092457.patch` byte-equals live `git diff 1d16b0e..9deff00 -- companion`.

**Machine (independently run)**: Full mandated suite **167/167 pass** (`run-queues`, `tool-batch-heal`, `adapter-steer-overflow`, `message-router-nextrun-drain`, `tool-persistence-redact`, `history`, `summoner-overlay`, `overlay-session`); `tsc --noEmit` exit 0.

**Claim-by-claim confirmation (live source, not just reports)**:
- S-A1: `NextRunItem {text, clientMessageId?}` at `run-queues.ts:16-21`; leftover keeps first cmid (`:71-79`); drain echoes it (`message-router.ts:335-338`, `:261-279`); occupied enqueue passes id (`:441-450`). All `takeNextRun` consumers use the object shape — no `.text`-on-string hazard.
- S-A2: skip scoped to contiguous assistant tool block via `blockUntil` walk (`tool-batch-heal.ts:180-190`), not global `now.some`.
- S-A3: `adapter.ts` imports only `convertLeftoverSteerToNextRun, takeSteer` — zero `dropSteer` occurrences; production clean. Unit-pin gap (mutation stays green because `takeSteer` empties the map first) is the known nit class.
- S-B1: pause/trash/not-found pre-checks run before `takeNextRun` (`message-router.ts:312-329`); pause pin green under delete-pause mutation per lane B. Trash unpinned in-tree (nit).
- S-B2: regen-overlay + conductor keep queue; both tests pass.
- S-B3: upload pushes any truthy drain frame, always returns `file.uploaded`, no `return drainedAfterUpload` (`message-router.ts:1177-1181`).
- S-C1: `setSummonerThreadId` zero hits in `companion/src/**`.
- S-C2: submit-ok bind conjunct requires `overlaySessionIsLive(token)` (`menu-bar-agent.ts:914-917`); reclaim routes through `claimOverlayIfLive` with bound token (`:687-710`); helper post-await generation check intact.
- S-D1/D2/D3: `SENSITIVE_KEY_RE` + `redactSensitiveLeaf` + `redactSensitiveKeysDeep` **byte-identical** across `tool-persistence-redact.ts` and `history/store.ts` (563/526 chars each); `passwd`/`authorization`/`bearer` added; non-string leaves redacted; `value` not blanket-redacted; history cookie/generic-tool call-site gaps are pre-existing (D-N1/N2).

**ADR-020**: Capability declaration present (Surface L0, L2 none, Compose none, Autonomy nextRun plumbing, Trust redaction, Channel overlay live-gate). Channel uses a non-template dialect — form nit flagged by lanes B/C, not a missing declaration. No new tools/gates/primary UI, no confirm dialect, no originWs change, Trust monotonic (tightened), overlay stays L0 non-Allow/Deny with conductor still blocking. Blast T2 confirmed; no T3 trigger.

**Over-claim scan**: The synthesis honestly downgraded S-A3's unit-test mutation-kill to a residual nit; no lane BLOCK is live; no claimed fold remains unverified; no security regression introduced.

Non-blocking nits (all carried from the lane reports): S-A3 unit test doesn't occupy the take→drop window; trash pre-check unpinned in-tree; S-B3 overlay-reject test doesn't uniquely pin "never return drained" vs the old gate-push branch; menu-bar S-C pins are grep-only + `bindSummonerThread` optional token default + reclaim omits `onStaleClaim`; history S-D3 test omits array-shape pin; D-N1/N2/N3 call-site lock-step gaps (pre-existing, out of slice).

VERDICT: APPROVE_WITH_NITS
