I have now thoroughly inspected all the relevant files and the diff. Here is my review:

---

## Batch B (G4–G6) Dual External Review

### G4 — Notes Create `posted+verified` via list-notes re-read

**✅ PASS with nit.** The `evaluateNotesCreateVerify` helper (`companion/src/host-use/darwin/notes-verify.ts:12-58`) correctly:
- Returns `posted: true` always (the write was posted)
- Sets `verified: true` only when target_id is found in listedIds (exact match or note-NNN suffix fallback)
- Fails closed: missing target_id, empty list, or empty body → `verified: false`
- Has test coverage in `companion/tests/notes-verify.test.ts` (6 cases)

The server integration (`companion/src/server.ts:2170-2195`) does writeOne → listReadTargets("note") → evaluateNotesCreateVerify → returns `{posted, verified, verify_note}`. Error in the verify step is caught and returned as `verified: false` — fail-closed.

**Nit**: The `needle` variable (body first-line) is extracted at `notes-verify.ts:22` but only used for the empty-body guard at line 25. It is never compared against listed note content. The implementation record honestly documents this as "list-notes has no body field" limitation. A one-line comment in the function body explaining this scope would prevent future readers from assuming body-content matching is active.

### G5 — Mail `host_read` verified + summary

**✅ PASS.** `evaluateMailReadVerify` (`notes-verify.ts:62-73`) enforces all four fields (`sender`, `subject`, `date_received`, `body_preview`) non-empty → `verified: true`. Server (`server.ts:1992-2005`) wraps the result with `posted: true`, `verified`, and a golden-path `summary` line. Tests cover both paths (all fields present, empty subject). Fail-closed.

### G6 — macOS Rule 12: semantic-first, single-task, `posted≠verified`

**✅ PASS.** The macOS Rule 12 (`companion/src/llm/adapter.ts:317-323`) properly:
- Labels `host_computer` as "LAST RESORT"
- Instructs: "Prefer host_read/host_write for Mail/Notes"
- Enforces `posted≠verified`: "NEVER say '已发送/已完成' unless verified===true"
- Mandates single-task aggregation: "Aggregate ALL same-app actions in ONE host_computer call"

The `host_computer` tool description (`tool-definitions.ts:557`) also reinforces aggregation. The executor's `posted_steps`/`verified_steps` return contract (`executor.ts:1534-1536`) and the server's `note` fallback text (`server.ts:2528`) provide honest signaling to the LLM.

### Cross-cutting correctness

| Check | Result |
|-------|--------|
| Trust key Q1=C (thread-first) | ✅ `resolveComputerTrustKey`, `trustKeyAllowsInitialSkip` |
| `explicitOptIn` only when checkbox checked (Q2=B) | ✅ `hasExplicitOptIn` guard, `addToSessionTrust` origin-gated via `pending.relevantApps.length > 0` |
| `maxActionsSeen` (Q3=B) | ✅ `recordActions`/`maxActionsSeen`, checked in skip gate |
| Experimental flag blocks initial skip | ✅ `experimentalFlag` scanned from actions, `!experimentalFlag` in gate |
| Corpus exact-match only (Q4=A) | ✅ `corpusContains` uses `Set.has()` — no NFC/NFKC leniency |
| Self-UI detection handles macOS bundle ids | ✅ `isCompanionUiOwner()` matches `com.google.Chrome` etc. |
| Swift data race fix (CGImage + Task) | ✅ `CUBox` with NSLock replacing unsynchronized `var` |
| SCK config size fix | ✅ Explicit `config.width/height` from frame×backingScaleFactor |
| `[host]` diagnostic fputs filtered from error surfaces | ✅ Gated behind `CMSPARK_HOST_DEBUG`, filtered in `rethrowDarwinExecError` |
| `respondFrom` backward-compatible parameter addition | ✅ Optional `extras?` parameter |

---

### Nits (non-blocking)

1. **`notes-verify.ts:22`** — The `needle` variable extracted from body is only used for the empty-body guard, not for content matching. Add a brief comment: `// Only used for empty-body guard; actual verify is target_id presence in list-notes (list-notes has no body field — see batch B impl record).`

2. **`host.swift` & `host-skylight.swift`** — `cuWindowNumber()` is duplicated identically in both files. Consolidate into a shared location or accept the duplication with a comment linking the two copies.

3. **`server.ts:1400`** — The `require("./computer/session-trust")` in the IIFE depends on `tsconfig` `module: "commonjs"`. Add a comment: `// Uses require() — tsconfig module: commonjs. Must migrate to dynamic import() on ESM switch.`

4. **`security-confirmation.ts:359`** — The privileged `respond()` method (tray adapter path) does not accept `addToSessionTrust`. This is correct (tray has no session-trust checkbox UI), but a brief comment would help: `// Tray adapter path — no session-trust UI, so addToSessionTrust is not supported here.`

---

VERDICT: APPROVE_WITH_NITS
