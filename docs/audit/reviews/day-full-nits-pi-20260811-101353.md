All verification complete. Summary of findings:

**Verified fixes (all real, all green):**
1. **originWs on image confirm** — `image-fetch-admission.ts:205` passes `{ originWs: ws }` with the same ws used as the send channel; matches L2/URL (`url-cookie-admission.ts:250`), MCP (`mcp/dispatch.ts:198`), and computer (`computer/confirm.ts:26`) patterns. Manager enforces originWs on respond (`security-confirmation.ts:411,533`). Unit test asserts the binding on the deny path. P1-2 closed.
2. **tabUrlCache colocation** — new `ws/tab-url-cache.ts` imports only `../logger`; no cycle with server or lifecycle (`lifecycle.ts:7` contract intact). `getDomainFromUrl` semantics preserved exactly (both return `URL.hostname`, `""` on error). All consumers (tool-forward:234-273, companion-dispatch:797, lifecycle:1140, security-gates tests) resolve to the single shared SoT.
3. **C9 lockstep** — brace-scoped `extractValidatorKeys` with loose fallback; the real regression guard (router cases ⊆ validators ∪ ALLOWLIST) still passes, proving no keys are missed; anti-false-positive test confirms no generic `type` key.
4. **Static imports** — `normalizeShellCwd`/`normalizeNetsecPorts` hoisted out of the hot path; remaining `require()` at `server.ts:735` (`resolveComputerTrustKey`) is pre-existing, unchanged by this diff, try/catch-guarded.
5. **Tests** — targeted units 16/16; security-gates 63/63 (M4 image variants included); full suite 2718+15, 0 failures. Trust-monotonicity explicitly tested (three-flag god-mode and ADR-007 cookie trust do NOT waive IMAGE_FETCH confirm).

**Nits (non-blocking):**
- **ADR-020 declaration missing**: the implementer prompt omits the Surface/Compose/Autonomy/Trust/Channel block. Non-blocking since the diff is refactor + test + tightening of an *existing* gate (no new tools/gates/UI entry points), but the declaration should be added to future prompts.
- **Dead optional dep**: `applyTabNavigated?:` in `WsLifecycleDeps` (`lifecycle.ts:133-134`) is marked "kept optional for bind compat" but the only `bindWsLifecycle` call site (`server.ts:980`) no longer passes it — the dep could be removed outright.
- **Style**: import block after an export block at the top of `server.ts` (lines 60-75) is unusual; harmless (ES import hoisting) but worth reordering.

VERDICT: APPROVE_WITH_NITS
