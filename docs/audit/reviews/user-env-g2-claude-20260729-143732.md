I've completed my review. Let me finalize my findings.

## Review Findings — ADR-019 Gate2 (PR-1 + PR-2)

**Inspection performed**: read `companion/src/user-env.ts`, `companion/src/message-router.ts`, `companion/src/capability/shell.ts`, `companion/src/mcp/transport.ts`, `companion/src/io.ts`, `companion/src/server.ts` (auth + broadcast paths), all extension diff files, ADR-019. Re-ran companion tests (18/18 pass), extension tests (283/283 pass), and capability-shell-netsec + mcp regression (83 pass, 1 skip, 0 fail). Verified denylist completeness against full `process.env.*` scan in `companion/src`.

**Verification of ship checklist**:
- Companion user-env + inject + tests green ✓
- Extension UI + WS routing; no secret reaches LLM context (grep confirms user_env references limited to 4 companion files: user-env, message-router, mcp/transport, capability/shell — zero leakage into llm/, threads/, skills/) ✓
- ADR §12.1 R1–R7 + §8 S1–S9 all addressed (redact-on-log, single outbound constructor, scanned denylist with no fabricated entries, empty-string≠delete, "***"=unchanged, CMSPARK_SHELL force-last, S9 corrupt-file degrade, atomic 0o600 write) ✓
- No regression on shell L2 (server.ts:2995-3001 still requires security_token) or MCP PATH hardening (buildSpawnPath still applied; config.env.PATH still wins) ✓
- Auth gating inherited from server.ts:4947 (only `auth.handshake` allowed pre-auth); broadcasts filtered to authenticated clients only (server.ts:4205) ✓

**Non-blocking nits**:

1. `UserEnvSection.tsx:192-209` (`handleAdd`): doesn't reject the literal value `"***"` for a *new* key. Companion treats `value === "***"` as "unchanged" and silently no-ops (`anyChange=false`), but the success broadcast still fires with the unchanged snapshot so the UI shows "✓ 已保存" — user believes a key was added when it wasn't. `handleUpdate` (line 219-222) explicitly guards this; `handleAdd` should mirror that guard.

2. `user-env-utils.ts:59-69` (`validateUserEnvKeyName`): extension only pre-checks `CMSPARK_*` prefix locally, not the full denylist (PATH/LD_PRELOAD/etc.). Companion rejects with `RESERVED_KEY` and routes back correctly via `isUserEnvErrorMessage`, but it costs a round-trip. Acceptable since companion is source of truth; documenting as a UX nit.

3. `message-router.ts:979-982`: logger records `keys: Object.keys(varsObj)` and `vars: {KEY: "***", ...}` separately — the keys are already implicit in the redacted `vars` object. Slight redundancy in logs, no correctness issue.

4. `UserEnvSection.tsx:233` (`handleDelete`): uses browser native `confirm()`. Functional but inconsistent with the modal-driven settings slideout. Nit only.

5. No integration test asserting an unauthenticated WS peer is rejected on `user_env.set` directly — protection is implicit via the existing auth.handshake gate (tested in `ws-auth-handshake.test.ts`). Non-blocking given the well-tested upstream gate.

VERDICT: APPROVE_WITH_NITS
