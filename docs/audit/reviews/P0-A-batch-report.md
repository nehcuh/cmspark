# P0 Batch Fix Report — P0-A

- both_external_approve: true
- build_ok: true
- rounds: 2
- design: {"acceptance":["getPageHTML/waitForSelector/getElementCenter never raw-interpolate selectors into Runtime.evaluate","SEC-1 regression test green in chrome-extension/tests","config.updated never sent to unauthenticated OPEN sockets","config.updated payload redacts mcp.servers env and headers values","background security.confirmation.response includes nonce_response when string","background forwards add_to_thread_whitelist boolean true only when true","background forwards stop_thread when true; keeps add_to_whitelist array","companion handleSecurityConfirmationResponse unchanged but now receives full fields end-to-end from Side Panel","npm --prefix chrome-extension test and companion targeted tests pass","no P0-B/C/D files modified"],"batch":"P0-A Security same-day (SEC-1, SRV-1, XC-Integration-1)","files":["chrome-extension/src/background/browser-bridge.ts","chrome-extension/src/background/index.ts","companion/src/server.ts","chrome-extension/tests/selector-js-literal.test.ts","chrome-extension/tests/security-confirmation-forward.test.ts","companion/tests/config-broadcast-redact.test.ts","companion/tests/integration/computer-broadcast-auth.test.ts"],"plan":"Three atomic patches only. (1) SEC-1 browser-bridge.ts:605-606,965-966,983-987 — replace raw/weak-escaped selector interpolation into Runtime.evaluate with JSON.stringify (export selectorJsLiteral for tests); leave already-safe sinks and DOM.querySelector fallback alone. (2) SRV-1 server.ts:3214-3232 — replace clients OPEN-only fanout with broadcastToClients(authenticated); export redactConfigForBroadcast masking llm/vision api_key plus mcp.servers env/headers values; keep applyConfig on unredacted config. (3) XC-Integration-1 background/index.ts:446-457 — forward nonce_response (string), add_to_thread_whitelist===true, stop_thread===true; keep add_to_whitelist; no companion changes (handler already at server.ts:1484/1515).","risks":"getPageHTML shape must keep document.querySelector('html') prefix for scriptingExecute bodyHtmlExpr special-case (:189). MCP value redaction must not drop keys if UI lists env key names. broadcastToClients uses wss.clients+wsAuth (correct auth gate) not module clients Set. stop_thread is forwarded but unused on companion today (UI already chat.aborts). Prefer pure exported helpers so tests avoid startServer/CDP.","tests":["chrome-extension: selectorJsLiteral / assembled querySelector expr rejects quote+backslash breakout payloads used against old replace(/'/g) pattern","chrome-extension: buildSecurityConfirmationWsPayload forwards nonce_response, add_to_thread_whitelist, stop_thread; coerces types; keeps add_to_whitelist","companion: redactConfigForBroadcast masks mcp env/headers values and llm api_key; preserves server names/command/url","companion integration: setupBroadcastAuthForTests — config.updated via broadcastToClients reaches auth client only; unauth recv length 0"]}
- last_impl: {"batch":"P0-A Security same-day (SEC-1, SRV-1, XC-Integration-1)","diff_stat":"5 tracked modified (+117/-33) + 5 new helpers/tests; chrome-ext 14/14, companion 8/8 P0-A tests pass","done":true,"files_changed":["chrome-extension/src/background/browser-bridge.ts","chrome-extension/src/background/index.ts","chrome-extension/src/background/selector-js-literal.ts","chrome-extension/src/background/security-confirmation-payload.ts","chrome-extension/tsconfig.test.json","chrome-extension/tests/selector-js-literal.test.ts","chrome-extension/tests/security-confirmation-forward.test.ts","companion/src/server.ts","companion/tests/config-broadcast-redact.test.ts","companion/tests/integration/computer-broadcast-auth.test.ts"],"notes":"Routing override: implementer batch not request-refactor-plan. Claude dual-review file was incomplete (UNKNOWN); Pi APPROVE_WITH_NITS — nits only, all applied. Pre-existing unrelated test failures (apps-panel, log-rotation) not in scope. stop_thread still unused on companion (documented). No git commit/push.","summary":"SEC-1: all Runtime.evaluate selectors via JSON.stringify (selectorJsLiteral). SRV-1: config.updated via broadcastToClients + redactConfigForBroadcast (api_key + mcp env/headers values); applyConfig unredacted. XC-Integration-1: confirmation WS payload forwards nonce_response, add_to_thread_whitelist, stop_thread, keeps add_to_whitelist. Dual-review nits applied. Targeted tests 22/22 green.","tests_added":["chrome-extension/tests/selector-js-literal.test.ts","chrome-extension/tests/security-confirmation-forward.test.ts","companion/tests/config-broadcast-redact.test.ts","companion/tests/integration/computer-broadcast-auth.test.ts (SRV-1 case)"]}
- last_adv: {"blocking":[],"pass":true,"summary":"P0-A SEC-1/SRV-1/XC-Integration-1 production paths are correct: selectors use JSON.stringify/selectorJsLiteral in getPageHTML/waitForSelector/getElementCenter; CONFIG_CHANGE uses authenticated broadcastToClients + redactConfigForBroadcast (mcp env/headers + api_keys) with unredacted applyConfig; security.confirmation.response forwards nonce_response, add_to_thread_whitelist, stop_thread, add_to_whitelist. Zero blocking issues after independent code+diff review. | P0-A SEC-1/SRV-1/XC-Integration-1 production fixes match claims: Runtime.evaluate selectors use JSON.stringify via selectorJsLiteral at getPageHTML/waitForSelector/getElementCenter; CONFIG_CHANGE uses broadcastToClients + redactConfigForBroadcast (api_key + mcp env/headers) with unredacted applyConfig; confirmation WS path forwards nonce_response/add_to_thread_whitelist/stop_thread/add_to_whitelist. Tests assert real helper properties and real auth fan-out (not empty mocks); wire-up of helpers into browser-bridge/index verified by inspection. No blocking authenticity failures; residual risks are untested production call-site drift and out-of-scope paths. | "}
- last_ext: {"both_approve":true,"claude_path":"/Users/huchen/Projects/cmspark/docs/audit/reviews/P0-A-claude-20260725-130727.md","claude_verdict":"APPROVE_WITH_NITS","exit_code":0,"pi_path":"/Users/huchen/Projects/cmspark/docs/audit/reviews/P0-A-pi-20260725-130727.md","pi_verdict":"APPROVE","raw_tail":"===== CLAUDE (last 40) =====\n## Independent Review — P0-A Round 2\n\nI verified every claim against the real code, ran the targeted tests, and confirmed scope is bounded.\n\n### SEC-1 (selector injection) — COMPLETE\nAll three in-scope sites now use `selectorJsLiteral` (= `JSON.stringify`):\n- `browser-bridge.ts:611` getPageHTML\n- `browser-bridge.ts:971` waitForSelector\n- `browser-bridge.ts:991` getElementCenter\n\n`grep` confirms no raw `'${selector}'` interpolation into `Runtime.evaluate` remains. Other pre-existing sites (click/type/scrollIntoView/etc.) already use `JSON.stringify` and were correctly left alone. `getPageHTML` keeps the `document.querySelector('html')` prefix required by the `scriptingExecute` `bodyHtmlExpr` special-case (`browser-bridge.ts:193`). 6 regression tests cover quote-breakout, backslash-breakout, newlines, and assembled-expression structure.\n\n### SRV-1 (config.updated fanout) — COMPLETE\n- `server.ts:3285–3290` uses `broadcastToClients({...config: redactConfigForBroadcast(updatedConfig)})`. The module-level `clients` Set fan-out is gone.\n- `broadcastToClients` (`server.ts:2716`) properly auth-gates via `wsAuth.get(client)?.authenticated === true` (X3 hardening already in production — now reused for config.updated).\n- `redactConfigForBroadcast` (`server.ts:2739`) masks `llm.api_key`, `vision.api_key`, and all `mcp.servers[*].env`/`headers` values while preserving key names. Does not mutate input (verified by test). `applyConfig(updatedConfig.mcp)` at :3294 still receives the unredacted original.\n- No other `config.updated` fan-out path exists (`type:\"config.updated\"` returns only the one site at :3287).\n\n### XC-Integration-1 (confirmation field forwarding) — COMPLETE\n- `buildSecurityConfirmationWsPayload` is a pure helper. Forwards `confirmation_id`, `approved` (strict `=== true`), `add_to_whitelist` (array or `[]`), and conditionally includes `nonce_response`/`add_to_thread_whitelist`/`stop_thread` only when properly typed.\n- `background/index.ts:452` calls it; `App.tsx:202-214` already supplies all four fields from the Side Panel.\n- Companion handler at `server.ts:1446+` consumes `add_to_whitelist` (validated against `relevantDomains`), `add_to_thread_whitelist===true` (validated against `relevantApps`), `nonce_response` (string, fed to `respondFrom`). `stop_thread` is forwarded-but-unused on companion, documented at `server.ts:1449` — not a silent regression.\n\n### Test run (executed, not just inspected)\n- chrome-extension: **213/214 pass**. The single failure is `apps-panel-logic.test.js` (`appsPlatformSupported: win32 + unknown`), pre-existing and out of P0-A scope. All 14 P0-A tests green (6 selectorJsLiteral + 8 security-confirmation-forward).\n- companion P0-A targeted: **8/8 pass** (4 redactConfigForBroadcast unit + 4 broadcast-auth integration including the new SRV-1 case).\n\n### Scope\nDiff bounded to the 3 in-scope production files + 4 new test/helper files + `tsconfig.test.json` + `memory/session.md`. No P0-B/C/D files touched.\n\n### Non-blocking nits\n1. `stop_thread` is forwarded on the wire but has no companion consumer today — documented inline, UI uses `chat.abort` instead. Future companion-side abort would require wiring this to `threadManager`.\n2. `redactConfigForBroadcast` masks the currently-known secret fields. Future config additions bearing secrets (e.g., URL-embedded credentials in `mcp.servers.*.url`, or new top-level secret fields) would require updating the redactor — out of P0-A scope.\n3. `selectorJsLiteral` is re-exported through `browser-bridge.ts` purely for test imports — minor indirection, acceptable for a service-worker module.\n\nNo incomplete fixes, no security regressions, no over-claiming, no missing tests, no wrong file:line references.\n\nVERDICT: APPROVE_WITH_NITS\n\n===== PI (last 40) =====\nAll verification complete. Here's my final assessment:\n\n---\n\n## P0-A Round 2 Review\n\n### SEC-1 (selector injection)\n**VERIFIED.** Three Runtime.evaluate sink sites all use `selectorJsLiteral` = `JSON.stringify`:\n- `getPageHTML` (line 611): `selectorJsLiteral(params.selector)` with `document.querySelector('html')` prefix preserved\n- `waitForSelector` (line 971): `selectorJsLiteral(selector)`\n- `getElementCenter` (line 991): `selectorJsLiteral(selector)`\n\nAlready-safe sinks at lines 397, 646, 689, 717, 781, 794, 838 use `JSON.stringify` directly — left alone per design. `DOM.querySelector` CDP API calls pass selectors out-of-band — safe. No other selector-in-Runtime.evaluate paths exist. Tests (6/6) cover quote breakout, backslash+quote breakout, newlines, and reject the old `replace(/'/g)` weak-escape form.\n\n### SRV-1 (config fanout)\n**VERIFIED.** Old `for (const client of clients)` with `readyState===OPEN` only removed from `CONFIG_CHANGE` handler. New code uses `broadcastToClients()` which gates on `wsAuth.get(client)?.authenticated === true`. `redactConfigForBroadcast` masks `llm.api_key`, `vision.api_key`, `mcp.servers.*.env.*`, `mcp.servers.*.headers.*` while preserving key names (UI can list configured vars). `applyConfig` receives unredacted `updatedConfig.mcp`. Does not mutate original. Integration test confirms unauth client receives 0 messages; redact unit tests (4/4) confirm masking and non-mutation.\n\n### XC-Integration-1 (confirmation payload)\n**VERIFIED.** `buildSecurityConfirmationWsPayload` forwards `nonce_response` (string only), `add_to_thread_whitelist` (true only), `stop_thread` (true only), `add_to_whitelist` (array, default `[]`), `approved` (strict boolean). Companion `handleSecurityConfirmationResponse` at ~1446-1515 already consumes `msg.nonce_response`, `msg.add_to_whitelist`, `msg.add_to_thread_whitelist`. No companion code changes needed. Tests (8/8) cover all field combinations and type coercions.\n\n### Test results\n- `chrome-extension/tests/selector-js-literal.test.ts`: **6/6 pass**\n- `chrome-extension/tests/security-confirmation-forward.test.ts`: **8/8 pass**\n- `companion/tests/config-broadcast-redact.test.ts`: **4/4 pass**\n- `companion/tests/integration/computer-broadcast-auth.test.ts`: **4/4 pass** (3 existing X3 + 1 new SRV-1)\n- Chrome extension `npm test`: 213 pass, 1 pre-existing fail (`apps-panel-logic.test.ts` — out of scope)\n\n### Scope check\nOnly P0-A files modified. No P0-B/C/D bleed. No new regressions.\n\n---\n\nVERDICT: APPROVE","verdict_json":"/Users/huchen/Projects/cmspark/docs/audit/reviews/P0-A-verdict-20260725-130727.json"}
- build: {"commands":["git diff --stat","cd chrome-extension && npm test","cd chrome-extension && node --test .test-dist/tests/security-confirmation-forward.test.js .test-dist/tests/selector-js-literal.test.js","cd companion && npx tsc -p tsconfig.test.json && node --test .test-dist/tests/config-broadcast-redact.test.js .test-dist/tests/integration/computer-broadcast-auth.test.js","cd companion && npm test"],"log":"git diff: extension (browser-bridge, index, tsconfig.test) + companion (server, computer-broadcast-auth.test) touched; untracked P0-A helpers/tests present.\nExtension full: 213 pass / 1 fail (appsPlatformSupported platform/pre-existing, not P0-A).\nExtension P0-A targeted: 14/14 pass (security-confirmation-forward + selector-js-literal).\nCompanion P0-A targeted: 8/8 pass (config-broadcast-redact 4 + computer-broadcast-auth X3/SRV-1 4).\nCompanion full (earlier): 1719 pass / 3 fail unrelated (isUserWritablePath NTFS case, apps.add lolbin path order, log-rotation retention).\nP0-A verification scope: PASS. No commit.","ok":true}
