## PR7 Review — Cockpit StatusRail alignment + DESIGN.md semantic roles

### Scope Check

| Area | Status |
|------|--------|
| Cockpit shell (`CockpitApp.tsx`) | ✅ In scope — StatusRail grammar, token migration, `prefers-reduced-motion` |
| New `cockpit-status.ts` | ✅ In scope — pure helper for Cockpit mode badge labels |
| `tokens.ts` additions | ✅ In scope — `connectionColorDark`, `connectionDotShadowDark` for dark surfaces |
| `DESIGN.md` semantic roles | ✅ In scope — full §5.2 role table, StatusRail grammar, PR7 note |
| Tests | ✅ In scope — cockpit-status-rail + tokens-helpers |
| **No BottomBar changes** | ✅ |
| **No App.tsx / Panel changes** | ✅ |
| **No new files outside cockpit+DESIGN+test** | ✅ |

### ADR-020 Fidelity

- Mode badge copy: `L2 · LIVE` / `L2` / `确认` / `工作区` — Surface axis only, no "中层 Agent" ✅
- "确认台" branding preserved in title bar and labels ✅
- Content-split (D10′) untouched — ConfirmElevated, dual-track all intact ✅
- `aria-live="polite"` on mode badge ✅

### Tokens: No Material Hexes

- `connectionColorDark` uses `tokens.darkLive` / `tokens.darkWarning` / `tokens.darkDanger` ✅
- All hardcoded hexes in CockpitApp.tsx (`#9aa0a6`, `#fca5a5`, `#5b8def`, `#4ade80`, `#d1d5db`, `#232833`, etc.) migrated to `tokens.*` ✅
- Tests explicitly assert no `#4CAF50` / `#FF9800` / `#F44336` ✅

### DESIGN.md Semantic Roles

- Full semantic role table matching spec §5.2 (surface/text/border/accent/status/risk/mode) ✅
- Light → Dark token mappings explicit ✅
- Legacy aliases documented (Accent, Error, Success, Warning, Background) ✅
- StatusRail grammar section added for Panel + Cockpit ✅
- Radius SoT updated: 6/8/12/pill — "Do not invent other radii" ✅
- PR7 Cockpit note added with `prefers-reduced-motion` ✅

### `prefers-reduced-motion`

- CSS injected with `@media (prefers-reduced-motion: reduce)` disabling animations/transitions ✅
- Button/link transitions use `tokens.transitionFast` (150ms ≤ 200ms) ✅
- LIVE pulse on Cockpit chip only (not Panel) ✅

### Radius Migration

- Ad-hoc `4px` → `tokens.radiusSm` (6), `6px` → `tokens.radiusSm` (6), `10px` → `tokens.radiusLg` (12), `999` → `tokens.radiusPill` (999) ✅
- All Cockpit radii now use token constants ✅

### Tests

- 371/371 pass, 0 fail ✅
- New: `cockpitModeBadgeLabel` 4 variations ✅
- New: `connectionColorDark` / `connectionLabel` cross-check ✅
- New: radius SoT assertion (6/8/12) ✅
- New: semantic role hexes match DESIGN.md §5.2 ✅

### Nits (non-blocking)

- `ConfirmElevated` header removed the `⚠` emoji — correct per DESIGN.md "emoji 仅用于消息内容", but the old `⚠ ` prefix provided at-a-glance severity. The `tokens.darkDanger` color alone carries that now. Fine.

---

**VERDICT: APPROVE**
