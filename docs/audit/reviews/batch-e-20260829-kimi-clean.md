# Dual re-review — CMspark 0.5.3 Batch E path (#251)\\n\\nYou are an **independent** senior reviewer. You did **not** write the strawman or the four-lane synthesis. Confirm, refute, or block the **path**.\\n\\nWork in: `/Users/huchen/.grok/worktrees/projects-cmspark/fix-251-p2`\\n\\n## Capability declaration\\n\\n```text\\nSurface:      Operate WS handshake ; Capture overlay HTTP 不动\\nL2-classes:   n/a\\nCompose:      n/a\\nAutonomy:     n/a\\nTrust:        chrome-extension Origin never summoner ; tray Origin omit deny\\nChannel:      community\\nBlast:        T2\\n```\\n\\n## Inputs\\n\\n1. Spec (folded): `docs/superpowers/specs/2026-08-29-post-diagnosis-batch-e.md`\\n2. Synthesis: `docs/audit/reviews/batch-e-adversary-synthesis-2026-08-29.md`\\n3. Spot-check: `lifecycle.ts` handshake + `isAllowedWsOrigin`; `composer-lease.ts` stamp/lease if; `validate.ts` surface enum; `ws-client.ts` / `companion-client.ts` handshake; `protocol.ts`; `run-esbuild-bundle.mjs`; `summoner-acl.ts` ALLOW (must be untouched)\\n\\n## Rules\\n\\n1. REJECT if implementing as written would: add overlay HTTP to WS Origin; change SUMMONER_ALLOW / overlay Allow/Deny; split `message-router.ts`; introduce claimed wire `surface:\\\"panel\\\"`; treat omit-deny as unauth RCE; leave extension+`surface:\\\"summoner\\\"` able to claim overlay lease.\\n2. Calibration: T2 mis-label among HMAC peers, not Critical. Skeptic “HMAC is the real gate” does **not** delete E1 terminate-summoner-from-extension.\\n3. `__cmspark_surface` stays binary unless you BLOCK that choice with a concrete lease hole the fold missed.\\n4. Final line exactly:\\n\\nVERDICT: APPROVE\\nVERDICT: APPROVE_WITH_NITS\\nVERDICT: REJECT\\n\\n## Required sections\\n## Verdict rationale\\n## Confirmed pins\\n## Missing / still BLOCK\\n## Nits\\n## Recalibrated: implement now? YES only if APPROVE*\\n\\n## Embedded spec (folded)\\n\\n# 0.5.3 体检批 E — P2 handshake / esbuild（#251）\\n\\n> **GitHub:** [#251](https://github.com/nehcuh/cmspark/issues/251)  \\n> **状态:** 四路已折 · **尚未** dual · **禁止实现**  \\n> **对抗合成:** [batch-e-adversary-synthesis-2026-08-29.md](../../audit/reviews/batch-e-adversary-synthesis-2026-08-29.md)  \\n> **前序:** #245/#246 A+B、#247/#248 C、#249/#250 D 已合 `6e17d2df`  \\n> **HEAD 基线:** `origin/main` `6e17d2df`\\n\\n```text\\nSurface:      Operate WS handshake ; Capture overlay HTTP 不动\\nL2-classes:   n/a\\nCompose:      n/a\\nAutonomy:     n/a\\nTrust:        chrome-extension Origin never summoner ; tray Origin omit deny\\nChannel:      community\\nBlast:        T2 (handshake mis-label) ; T1 (protocol lockstep / esbuild)\\n```\\n\\n**产品句：** 扩展不能靠握手字段自称召唤器去抢 overlay 租约。托盘进程必须显式说自己是 tray 还是 summoner。HMAC 仍是 WS 门。打包不再靠 tsx 偷来的 esbuild。\\n\\n**实现是否允许开工：否。** 须 kimi+claude dual 都 APPROVE*。\\n\\n## 1. 四路表\\n\\n见合成文档。overall = PASS_WITH_CHANGES。\\n\\n## 2. 必须折进路径的针（folded pins）\\n\\n1. **E1-ORIGIN-CLASS：** `chrome-extension://[A-Za-z0-9_-]+` → handshake `panel`。`cmspark-tray://local` → 仅显式 `\\\"tray\\\"` | `\\\"summoner\\\"`。其它 Origin 仍 `isAllowedWsOrigin` 拒。**禁止**加 overlay HTTP / loopback。\\n2. **E1-NO-SUMMONER-FROM-EXTENSION：** 扩展 Origin + 自称 `summoner` → **terminate**（verifyProof 成功之后，不是 validator linger）。扩展 Origin + 自称 `tray` → **coerce `panel`**（不砖 reconnect）。扩展 **省略** surface → stamp `panel`。\\n3. **E1-OMIT-DENY-TRAY：** tray Origin 省略 / `\\\"panel\\\"` / 其它 → **terminate**。生产 `CompanionClient` 已发 `surface: this.options.surface ?? \\\"tray\\\"`。旧无 surface 的 tray 与 companion 同发，升级一起走。\\n4. **E1-BINARY-STAMP：** `wsAuth.surface` 三元 `panel | tray | summoner`。`__cmspark_surface` **保持** `summoner | tray`（`stampCmsparkSurface`：非 summoner → `tray`）。`incomingHolderFromSurface` 已把非 summoner 映射成 lease holder `\\\"panel\\\"`。**不要**把 `\\\"panel\\\"` 放上握手 wire（`validate.ts` 继续拒 claimed `\\\"panel\\\"`）。\\n5. **E1-FANOUT-PREDICATES：** 确认/聊天镜像保持 `surface !== \\\"summoner\\\"`（含 `panel`）。`pickAuthenticatedClientWs` / `waitForExtensionPeer` 仍看 **chrome-extension Origin**。`OVERLAY_SHELL_` 中继保持 `auth.surface === \\\"tray\\\"`（真 HUD）。STT：扩展 Origin 总是允许；tray Origin 仅 `summoner`。\\n6. **E1-FILEMAP：** 必碰 `lifecycle.ts`、`validate.ts`（claimed 枚举不变）、`composer-lease.ts` 仅当 stamp 政策被打破时（默认 stamp 二元则 lease `if` 不用为 panel 放宽）。`message-router.ts` **只许** session union 加 `\\\"panel\\\"`（若 `wsAuth.surface` 传入 handleMessage）；**禁止拆文件**。\\n7. **E2-LOCKSTEP：** `PROTOCOL_VERSION` 仍为 1。测：`protocol.ts` / `ws-client.ts` / `companion-client.ts` 三处相等（读文件，**不要** extension import companion）。越界 `auth.failed` + terminate 已有。omit version = MIN。\\n8. **E2-CONSUME-OK：** 客户端读 `auth.ok.protocol_version` / `negotiated_protocol_version`；与本地常量不等则 close。**缺字段 = 当 MIN**（现有 mock 不集体红）。不 bump。\\n9. **E3-ESBUILD：** `companion/package.json` **直接** `devDependency` pin（与 lock 的 `0.28.1` 对齐）。保持 `run-esbuild-bundle.mjs` 路径 spawn。禁止 `npx esbuild`。tsx hoist 不算 vendored。\\n10. **E3-ENGINES：** root + companion `engines.node` = `>=22`（允许 nvm 24）。CONTRIBUTING/TESTING 一句跟上。**不**挂 GitHub Release DMG；不签名/公证。\\n11. **E4/E5：** 本票不实现测试簇翻案、不拆 router、不做 ConfirmSink。privacy_ack HTTP 伪造记 overlay-privacy-ack，**不在 #251**。\\n\\n## 3. 三项 DoD（机核）\\n\\n| ID | DoD |\\n|----|-----|\\n| **E1** | Origin chrome-extension + `surface:\\\"summoner\\\"` → 连接关，不得 stamp summoner、不得 overlay lease。tray Origin 无 surface → 未 authenticated。tray+`tray` / tray+`summoner` / 扩展省略 → 绿。`isAllowedWsOrigin(\\\"http://127.0.0.1\\\") === false`。 |\\n| **E2** | `protocol_version: 99` → `auth.failed`。三处字面量 === `PROTOCOL_VERSION`。 |\\n| **E3** | `companion/package.json` 直接依赖能解析 esbuild；engines 主版本与 CI `node-version: '22'` 一致。 |\\n\\n## 4. 文件地图\\n\\n| 文件 | ID | 做什么 |\\n|------|----|--------|\\n| `companion/src/ws/lifecycle.ts` | E1 | `surfaceFromOrigin` 在 verifyProof 后盖章；mismatch terminate |\\n| `companion/src/ws/validate.ts` | E1 | claimed 仍 omit/`tray`/`summoner`；拒 `\\\"panel\\\"` |\\n| `companion/src/tray/companion-client.ts` | E1, E2 | 已发 surface；读 auth.ok（缺字段=MIN） |\\n| `chrome-extension/src/background/ws-client.ts` | E1, E2 | **继续省略** surface；读 auth.ok |\\n| `companion/src/protocol.ts` | E2 | 常量不动 |\\n| `companion/src/message-router.ts` | E1 | 仅 union `\\\"panel\\\"`（若 session.surface 传入）；**不拆** |\\n| `companion/package.json` + lock | E3 | esbuild pin + engines |\\n| 测 | E1–E3 | Origin×surface 矩阵（不要只改无 Origin 的 replica）；lockstep 读文件；protocol 99 |\\n\\n**不改：** `summoner-acl.ts` ALLOW；`isAllowedWsOrigin` 集合；overlay HTML；`stampCmsparkSurface` 二元（除非 dual 改选）。\\n\\n## 5. NEVER / KEEP\\n\\n**NEVER：** `SUMMONER_ALLOW` / overlay Allow/Deny / #228；拆 `message-router.ts`；overlay HTTP 进 WS Origin；代码签名；live config；宣称 Capture/CU/F-S-10 闭合；protocol bump；Release DMG。\\n\\n**KEEP：** HMAC 配对；#245 L0；#247 HMAC；#249 cookie first-paint；Confirm origin-bound；D5 `!== \\\"summoner\\\"`。\\n\\n## 6. 测试红名单（更新，不要保 omit→tray）\\n\\n- `summoner-acl.test.ts`：omit 在 **schema** 仍 valid；claimed `\\\"panel\\\"` 仍拒。\\n- `composer-lease.test.ts`：stamp 二元仍 `tray`（panel 握手不进 stamp）。\\n- `l2-summoner-confirm-origin.test.ts`：fixture 可改 `panel` **或** 保持 tray 只要 `shouldReceiveConfirmRequest` 仍 `!== \\\"summoner\\\"`。\\n- `companion-client-auth.test.ts`：mock `auth.ok` 缺 protocol 字段 → 当 MIN，不 close。\\n- **新测** 必须带 Origin 头；`ws-auth-handshake.test.ts` replica 无 Origin，不能单独当 E1 DoD。\\n\\n## Embedded synthesis\\n\\n# 0.5.3 体检批 E — 四路合成 2026-08-29\\n\\n> **GitHub:** [#251](https://github.com/nehcuh/cmspark/issues/251)  \\n> **状态:** 四路合成 PASS_WITH_CHANGES — **尚未** kimi+claude dual，**禁止实现**  \\n> **基线 strawman:** `docs/superpowers/specs/2026-08-29-post-diagnosis-batch-e.md`  \\n> **HEAD:** `origin/main` `6e17d2df`\\n\\n```text\\nSurface:      Operate WS handshake ; Capture overlay HTTP 不动\\nL2-classes:   n/a\\nCompose:      n/a\\nAutonomy:     n/a\\nTrust:        chrome-extension Origin never summoner ; tray Origin omit ≠ ungated tray\\nChannel:      community\\nBlast:        T2 (handshake mis-label) ; T1 (protocol lockstep / esbuild)\\n```\\n\\n**产品句（修订）：** 扩展不能靠握手字段自称召唤器去抢 overlay 租约。托盘进程必须显式说自己是 tray 还是 summoner。HMAC 仍是 WS 门；本票不是未授权 RCE。CI 打包不再靠 tsx 偷来的 esbuild。\\n\\n**实现是否允许开工：否。** 须 kimi+claude dual 都 APPROVE*。\\n\\n## 1. 四路表\\n\\n| 路 | verdict | 抽查后站得住的 BLOCK | 与 SoT 冲突需折 |\\n|----|---------|----------------------|----------------|\\n| Security | PWC | 引入 `panel` 却不改 `stampCmsparkSurface`/lease `if` = overlay 租约洞；扩展自称 summoner = 租约盗窃；tray Origin omit 逃 SUMMONER_ALLOW | HMAC 仍是真门；omit-deny 不是 unauth RCE |\\n| Product | PWC | omit-deny **只**打 tray Origin；扩展继续省略 surface；fan-out 保持 `!== \\\"summoner\\\"`；lease 必须认识 panel | 不把 overlay HTTP 当 WS；不挂未签名 DMG |\\n| Impl | PWC | 类型宇宙是 `tray\\\\|summoner`；`__cmspark_surface` 保持二元；validate 不得把 `panel` 放上线；esbuild 是 tsx hoist；handshake 复本无 Origin | 不拆 message-router；可改 union |\\n| Skeptic | PWC | Origin 盖不住 tray vs summoner；`panel` 第三身份会 desync；E2 在 v=1 演戏；E4/E5/DMG 删除 | 只留：扩展 Origin 永不 stamp summoner + 声明 esbuild |\\n\\n**抽查（[inspected]）：**\\n\\n- `lifecycle.ts:1023-1024` omit/`non-summoner` → tray。\\n- `composer-lease.ts:118` stamp 二元 collapse；`:194` lease 只在 `tray\\\\|summoner` 检查 holder。\\n- `validate.ts:782` 客户端 surface 仅 omit/`tray`/`summoner`；`\\\"panel\\\"` 非法。\\n- `ws-client.ts:161` 扩展不发 surface；`companion-client.ts:612` 托盘恒发。\\n- `isAllowedWsOrigin` 仅 chrome-extension + `cmspark-tray://local`；local 可伪造 Origin，HMAC 才是门。\\n- `confirm-fanout.ts:25` / D5 `!== \\\"summoner\\\"`；`pickAuthenticatedClientWs` 看 Origin 不是 surface。\\n- `run-esbuild-bundle.mjs` 解析 `companion/node_modules/esbuild`；package.json 无直接依赖。\\n- `release.yml` 上传 zip + NSIS；DMG 仅 `make package-macos` 本机。\\n\\n## 2. 被推翻 / 降级\\n\\n- 「纯 Origin 盖章二分 tray/summoner」→ **假**（共用 `cmspark-tray://local`）。折成 **Origin 类 + 显式 surface**。\\n- omit-deny = 关未授权 RCE → **假**。HMAC 持有者仍可 `surface:\\\"tray\\\"`。Blast **T2**。\\n- E2 fail-closed omit / bump → **删**。omit=MIN 已测；consume-ok 保留为诚实（缺字段当 MIN）。\\n- E3 Release DMG / 签名 / engines 当主 DoD → **DMG 删**；engines `>=22` 廉价保留；**esbuild 直接依赖留下**。\\n- E4 当实现项 → **nit/盘点**。privacy_ack 伪造是 overlay-privacy-ack，**本票不修**。\\n- E5 当工作项 → **删**。NEVER 拆 message-router 留下。\\n- Skeptic「扩展自称 summoner 只是自残」→ **部分假**：`SUMMONER_ALLOW` 含 `composer.lease.claim`，扩展 Origin + summoner = overlay 租约盗窃。**保留 terminate**。\\n\\n## 3. 折法（进 spec）\\n\\n1. Handshake `wsAuth.surface` 三元：`panel | tray | summoner`。\\n2. `__cmspark_surface` **保持二元** `summoner | tray`（panel 握手 collapse 成 tray stamp = 现有 panel holder）。Fan-out / confirm 继续 `!== \\\"summoner\\\"`。`=== \\\"tray\\\"` 只用于 HUD 进程消息（`OVERLAY_SHELL_`）。\\n3. `chrome-extension://`：省略 → stamp `panel`；自称 `tray` → coerce `panel`；自称 `summoner` → **terminate**。\\n4. `cmspark-tray://local`：必须显式 `tray|summoner`；省略/`panel`/其它 → **terminate**。\\n5. 客户端 **不得**发 `surface:\\\"panel\\\"`（validate 继续拒）。扩展继续省略。\\n6. 不扩 `isAllowedWsOrigin`。不改 `SUMMONER_ALLOW`。不拆 `message-router.ts`（允许改 union + coerce 注释）。\\n7. E2：三处字面量锁步测；越界已拒；客户端读 auth.ok，缺字段 = MIN。不 bump。\\n8. E3：companion **直接** pin esbuild；engines `>=22`；**不**挂 Release DMG。\\n9. E4/E5：不实现。\",\n  \"steps\": [\n    {\n      \"step_id\": \"implementer-b1ed\",\n      \"step_number\": 1,\n      \"skill_id\": \"fallback-llm\",\n      \"intent\": \"implementer: red_team\",\n      \"input_query\": \"\",\n      \"output_as\": \"step_1_result\",\n      \"status\": \"pending\"\n    },\n    {\n      \"step_id\": \"red_team-b0f5\",\n      \"step_number\": 2,\n      \"skill_id\": \"fallback-llm\",\n      \"intent\": \"red_team: red_team\",\n      \"input_query\": \"\",\n      \"output_as\": \"step_2_result\",\n      \"status\": \"pending\"\n    },\n    {\n      \"step_id\": \"reviewer-381e\",\n      \"step_number\": 3,\n      \"skill_id\": \"fallback-llm\",\n      \"intent\": \"reviewer: red_team\",\n      \"input_query\": \"\",\n      \"output_as\": \"step_3_result\",\n      \"status\": \"pending\"\n    },\n    {\n      \"step_id\": \"tester-d45a\",\n      \"step_number\": 4,\n      \"skill_id\": \"fallback-llm\",\n      \"intent\": \"tester: red_team\",\n      \"input_query\": \"\",\n      \"output_as\": \"step_4_result\",\n      \"status\": \"pending\"\n    }\n  ],\n  \"detected_intents\": [\n    \"code_review\",\n    \"fix_bug\",\n    \"code_review\",\n    \"implement_feature\",\n    \"code_review\"\n  ],\n  \"reasoning\": \"Step 1: 'implementer' → fallback-llm (squad); Step 2: 'red_team' → fallback-llm (squad); Step 3: 'reviewer' → fallback-llm (squad); Step 4: 'tester' → fallback-llm (squad)\",\n  \"status\": \"pending\"\n}", "hookEventName": "UserPromptSubmit"}}

• I'm asked to do an independent dual re-review of the Batch E path. I need to spot-check the actual code files to confirm the synthesis's claims. Let me read the relevant files.

  Key claims to verify:
  1. `lifecycle.ts` handshake + `isAllowedWsOrigin` (lines ~1023-1024)
  2. `composer-lease.ts` stamp/lease if (lines 118, 194)
  3. `validate.ts` surface enum (line 782)
  4. `ws-client.ts` (line 161) / `companion-client.ts` (line 612) handshake
  5. `protocol.ts`
  6. `run-esbuild-bundle.mjs`
  7. `summoner-acl.ts` ALLOW

  Let me read these files. This is a review task — I could delegate spot-checking to an explore agent, but doing it myself with targeted reads is feasible. There are quite a few files. Let me delegate to an explore agent? The instructions say delegating keeps file contents out of context. But a reviewer needs to actually verify the claims. Let me do targeted greps/reads myself — that's efficient enough.

  Let me start with parallel reads/greps.

• I'll verify the synthesis's spot-check claims against the actual code before ruling on the path.

• composer-lease.ts is elsewhere. Find it.Now check composer-lease.ts stamp/lease, summoner-acl.ts ALLOW, run-esbuild-bundle.mjs, handshake verifyProof location in lifecycle.ts (lines ~1023), confirm-fanout, OVERLAY_SHELL_ relay, STT gate. Also check whether companion-client reads auth.ok protocol fields currently (line 515 area). And check whether the extension ws-client sends Origin header (chrome extensions WS — browser sets Origin automatically). Let me read composer-lease and summoner-acl and the handshake section of lifecycle.Find run-esbuild-bundle.mjs. Also need to check:
  - confirm-fanout.ts `!== "summoner"` 
  - OVERLAY_SHELL_ relay `auth.surface === "tray"` (lifecycle.ts:941 confirmed `auth?.surface === "tray"`)
  - STT gate: extension Origin always allowed; tray Origin only summoner — need to check current STT gating
  - message-router.ts session surface type
  - companion/package.json esbuild devDependency absence + engines
  - companion-client.ts auth.ok handling (does it read protocol fields? line 515 area)
  - The origin of handshake: where is the connection's origin stored? The plan says surfaceFromOrigin after verifyProof. Need to check that origin is accessible at handshake time (info.origin at verifyClient; need per-connection origin stored). Check whether st has origin stored.Good — `st.origin` is stored per-connection (line 835), so `surfaceFromOrigin` after verifyProof is feasible. Confirm-fanout.ts is elsewhere. Let me find it, plus check the STT gate, run-esbuild-bundle.mjs content, companion/package.json esbuild + engines, root package.json engines, message-router session surface union, and waitForExtensionPeer.Almost all synthesis claims verified. Remaining checks:

  1. STT gate: "扩展 Origin 总是允许；tray Origin 仅 summoner" — check lifecycle.ts:1385 area (origin class for voice.stt.*).
  2. waitForExtensionPeer — checks chrome-extension Origin.
  3. esbuild in lock at 0.28.1? Check companion/package-lock.json for esbuild version.
  4. Check the spec/synthesis files on disk match (they were embedded; a quick glance is fine but the embedded content is the SoT for review; the spot-check is on code).
  5. message-router session surface type at line 271-272: `surface?: "tray" | "summoner"` — spec says may add "panel" to union. OK.
  6. Root package.json engines — see line 4.
  7. CI node-version '22' — check workflow.

  Also one potential issue: the `panel` coerce for extension claiming `tray`. And E1-BINARY-STAMP: does the lease `if` at composer-lease.ts:194 `surface === "summoner" || surface === "tray"` matter if handshake surface becomes "panel"? If wsAuth.surface = "panel", then composer.lease.claim with surface "panel" skips the holder-surface mismatch check at :194! That's the hole the Security lane flagged: lease `if` only checks holder when surface is tray|summoner. If wsAuth.surface becomes "panel" and that surface is passed into the claim handler, a panel client could claim holder "overlay" without mismatch rejection?

  Wait — but composer.lease.claim is in SUMMONER_ALLOW, meaning only summoner surface is ACL-gated to a small set; tray surface is NOT gated (line 59: `if (surface !== "summoner") return ok`). So any non-summoner surface can send composer.lease.claim. The check at :194 ensures: if surface is tray, expected holder = "panel", so claiming holder "overlay" is rejected. If surface becomes "panel" (new third value), the `if (surface === "summoner" || surface === "tray")` would be false → no holder check → a "panel"-surface connection could claim holder "overlay" → overlay lease theft!

  But wait — where does the claim handler get `surface` from? Need to check. In lifecycle.ts:1092, `stampCmsparkSurface(msg, authState.surface)` stamps `__cmspark_surface` binary (summoner→summoner else tray). The claim handler at composer-lease.ts — what's the `surface` param passed at :194? Let me check the caller. If the caller uses the stamped `__cmspark_surface` (binary), then even with handshake surface "panel", the stamp is "tray" and the lease if works. If the caller passes raw wsAuth.surface ("panel"), there's a hole.

  Spec pin 4 says: `incomingHolderFromSurface` already maps non-summoner to "panel" holder. And pin 6: composer-lease.ts touched only if stamp policy broken; default binary stamp → lease if doesn't need relaxing for panel. This hinges on the claim path using the binary stamp. Let me verify the caller of handleComposerLeaseClaim — grep for where surface is passed.

• Key remaining check: whether the lease-claim path keys off the binary `__cmspark_surface` stamp or the raw handshake surface — that's the potential `panel` lease hole.

• Critical confirmation: `message-router.ts:1399` passes `stampedSurface` (from `stripCmsparkSurface`, the binary `__cmspark_surface` stamp) into `handleComposerLeaseFamily` — not the raw `wsAuth.surface`. So even if handshake surface becomes `"panel"`, the stamp is binary (`"tray"`), and the lease `if` at composer-lease.ts:194 sees `"tray"` → `incomingHolderFromSurface("tray")` = `"panel"` holder enforced. So the binary-stamp choice is safe: no lease hole, provided the stamp happens before routing (lifecycle.ts:1092 stamps before handleMessage). Confirmed at lifecycle.ts:1092 `stampCmsparkSurface(msg, authState.surface)`.

  But wait — one wrinkle: `assertSummonerAllowed(authState.surface, msg.type)` at lifecycle.ts:1077 uses raw surface. With "panel" → `surface !== "summoner"` → not gated → panel gets tray-level access. That's intended (panel = extension, previously stamped tray anyway).

  Also confirm-fanout `shouldReceiveConfirmRequest` — check line 25 quickly? It said `surface?: string` param; presumably `!== "summoner"`. Fine.

  Also OVERLAY_SHELL_ relay at lifecycle.ts:941 `auth?.surface === "tray"` — if tray Origin omit now terminates, tray surfaces still stamp "tray", panel connections will have surface "panel" and won't match `=== "tray"` — good, HUD relay only from real tray.

  One more check: E2 — companion-client auth.ok handler (line 515) does NOT currently read protocol fields. Spec pin 8 requires clients to read auth.ok protocol_version and close on mismatch, missing field = MIN. Currently it doesn't read at all — that's an implementation item, fine.

  Extension ws-client also needs to read auth.ok. Fine as implementation items.

  STT gate: lifecycle.ts:1385-1386 "origin class for voice.stt.*" passes origin. Spec pin 5: STT extension Origin always allowed; tray Origin only summoner. That's the current/preserved behavior.

  Remaining spot items: esbuild version in lock (0.28.1 claim), CI node-version '22', root engines value. Let me check quickly.

• All lease/stamp claims check out. Final spot-checks: esbuild lock version, CI node version, root engines, and the STT origin gate.

• All key claims verified. Also confirm esbuild hoisting via tsx: check that esbuild is present only transitively (tsx depends on esbuild). package-lock shows esbuild 0.28.1 at root node_modules — presumably via tsx. package.json has no direct esbuild dep. Confirmed.

  One more thing worth double-checking: the spec claim "message-router.ts 只许 session union 加 panel（若 wsAuth.surface 传入 handleMessage）" — line 1387 confirms `surface: wsAuth.get(ws)?.surface` is passed into handleMessage session. And message-router.ts:272 types it as `"tray" | "summoner"`. So adding "panel" to that union is needed. Confirmed.

  Also confirm-fanout.ts:25 — check the predicate quickly? It was referenced in synthesis as `!== "summoner"`. Minor; I read the signature. Let me just grep it one more time to be thorough. Actually I can infer from usage, but a quick read is cheap. Let me skip — actually the synthesis claims `confirm-fanout.ts:25` / D5. Let me verify quickly to be diligent. I'll do one read of lines 20-30.

  Also one subtlety for the verdict: pin 2 says extension claiming "summoner" → terminate AFTER verifyProof succeeds. And E1-OMIT-DENY-TRAY: old trays without surface will be terminated — breaking change for older tray versions, but spec says "升级一起走" (companion and tray ship together, same binary). Since tray is part of companion process, version skew is minimal. Acceptable.

  Potential nit: with omit-deny for tray Origin, any HMAC-holding local process that spoofs `cmspark-tray://local` Origin and omits surface gets terminated instead of silently tray — fine, they'd just send surface:"tray". This is mis-label hygiene, T2, consistent with calibration.

  Another nit: `validate.ts` keeps rejecting claimed "panel" — so extension cannot explicitly send surface:"panel"; extension omit → stamp panel. Consistent.

  Also: what about a chrome-extension Origin claiming "tray" → coerce panel. And tray Origin claiming anything but tray/summoner → validate.ts already rejects invalid values before... wait, validate rejects m.surface not in {undefined, tray, summoner}. So tray Origin + "panel" is rejected at validator with error message, not terminate. Spec says "tray Origin 省略 / panel / 其它 → terminate". Claimed "panel" fails validation → what happens currently on invalid handshake? Probably just an error response / ignored, connection stays unauthenticated and times out (there's a st.timer). Effectively unauthenticated — meets DoD "未 authenticated". Minor discrepancy: validator linger vs terminate, but pin 2 explicitly says terminate "不是 validator linger" only for extension+summoner. Fine.

  Edge: extension Origin with claimed surface:"panel" → validator rejects → not authenticated. DoD fine.

  Now the verdict. Check REJECT rules:
  1. Add overlay HTTP to WS Origin — spec explicitly forbids. ✓ no
  2. Change SUMMONER_ALLOW / overlay Allow/Deny — spec forbids. ✓
  3. Split message-router.ts — forbidden. ✓
  4. Introduce claimed wire surface:"panel" — spec keeps validate rejecting claimed "panel". ✓ (wire surface stays tray|summoner|omit; "panel" is only server-side stamp on wsAuth.surface)
  5. Treat omit-deny as unauth RCE — synthesis explicitly debunked, T2. ✓
  6. Extension+surface:"summoner" able to claim overlay lease — pin 2 terminates this. ✓

  Rule 3: binary `__cmspark_surface` — I verified the lease claim path uses stampedSurface (message-router.ts:1399), so a "panel" handshake surface collapses to "tray" stamp before the lease if → holder "overlay" claim rejected via LEASE_HOLDER_SURFACE_MISMATCH. No lease hole. Binary stamp is safe.

  Missing/BLOCK items: none that block. Nits:
  - E2-CONSUME-OK: companion-client.ts:515 currently ignores auth.ok protocol fields entirely; the spec adds a read-with-default-MIN. Honest, cheap. Nit: closing on mismatch when PROTOCOL_MAX=1 and server always sends 1 means this never fires — it's forward-compat scaffolding; acceptable as honesty, but don't oversell it as a gate.
  - Test red list: ws-auth-handshake replica has no Origin — new tests must set Origin headers; spec already notes this.
  - Back-compat: old extension (pre-#251) never sends surface and has chrome-extension Origin → stamped panel, fan-out `!== "summoner"` still includes it — no reconnect brick. Good.
  - Old tray without surface → terminate; acceptable since tray ships with companion (same binary), and CompanionClient already sends surface since... it's already in code (companion-client.ts:612). So any 0.5.x tray already sends. Fine.
  - `surfaceFromOrigin` naming — spec calls it that; implementation detail.
  - One genuine nit: pin 5 says STT "扩展 Origin 总是允许；tray Origin 仅 summoner" — that's preserving current behavior keyed off session.origin (lifecycle.ts:1386), not handshake surface; implementers must not "simplify" it to surface-only.
  - Another nit: engines `>=22` in companion/package.json while `@types/node` is ^20 — cosmetic mismatch, cheap to leave or bump; not blocking.
  - The lockstep test must read files as text (no cross-package import) — spec already says so.

  Verdict: APPROVE_WITH_NITS (nits are minor; nothing missing that blocks). Could be APPROVE. Given nits are trivial, I'd say APPROVE_WITH_NITS is honest since there are a couple of small clarifications for the implementer (validator-linger vs terminate for tray+claimed-panel; @types/node). Actually those are quite minor. I'll go APPROVE_WITH_NITS to record them.

  Write the review in the required format. Language: the prompt is English with Chinese embedded docs; the prompt header is English. I'll write in English with the required sections.

• Spot-checks complete. All load-bearing claims in the synthesis verified against the code. Here is the dual re-review.

  ## Verdict rationale

  The folded path survives independent spot-check. Every claim the synthesis marked `[inspected]` reproduces:

  - **The lease hole is real and the fold closes it correctly.** Today `lifecycle.ts:1024` stamps any non-`summoner` handshake as `tray`, so a chrome-extension Origin + `surface:"summoner"` would be ACL-gated as summoner *and* — because `SUMMONER_ALLOW` includes `composer.lease.claim` (`summoner-acl.ts:25`) — could claim `holder:"overlay"`. The terminate-on-extension-summoner pin is justified; the Skeptic "self-harm only" dismissal is indeed partially false.
  - **The binary `__cmspark_surface` choice is safe — I tried to BLOCK it per Rule 3 and could not.** The candidate hole: if `wsAuth.surface` becomes `"panel"`, the lease guard at `composer-lease.ts:194` (`surface === "summoner" || surface === "tray"`) would skip the holder check and let a panel-stamped client claim `holder:"overlay"`. But the claim path does not consume raw `wsAuth.surface`: `message-router.ts:1399` passes `stampedSurface` from `stripCmsparkSurface`, and `stampCmsparkSurface` (`composer-lease.ts:118`) collapses panel → `tray` before routing (`lifecycle.ts:1092` stamps before `handleMessage`). So a `"panel"` handshake still hits the lease `if` as `"tray"` → `incomingHolderFromSurface` → `"panel"` holder enforced. No hole; binary stamp stands.
  - **HMAC calibration is right.** `isAllowedWsOrigin` (`lifecycle.ts:202-215`) admits only `chrome-extension://` + `cmspark-tray://local`, and its own doc comment says a local process can spoof Origin — HMAC is the gate. Omit-deny is T2 mis-label hygiene among HMAC peers, not unauth RCE. Spec no longer claims otherwise.
  - **Feasibility of the file map confirmed:** per-connection `origin` is already stored on the auth state (`lifecycle.ts:835`), so `surfaceFromOrigin` after `verifyProof` (line 999) requires no plumbing invention. `message-router.ts:1387` already threads `wsAuth.surface` into the session, so the only router change needed is the union at `message-router.ts:272` — no split.
  - **E2/E3 grounded:** `PROTOCOL_VERSION = 1` (`protocol.ts:10`), lockstep literals at `ws-client.ts:161` and `companion-client.ts:611`; out-of-range rejection exists (`lifecycle.ts:1003-1020`). esbuild resolves at `0.28.1` in `companion/package-lock.json` with **no** direct dependency in `companion/package.json` (tsx hoist confirmed); `run-esbuild-bundle.mjs` spawns from `companion/node_modules`, no `npx`. CI is `node-version: '22'`; root + companion engines currently `>=20`.

  None of the six REJECT triggers fire: no overlay HTTP in WS Origin, `SUMMONER_ALLOW` untouched, no router split, claimed `"panel"` stays rejected at `validate.ts:782`, omit-deny not sold as RCE, extension-summoner lease theft closed by terminate.

  ## Confirmed pins

  - **E1-ORIGIN-CLASS / NO-SUMMONER-FROM-EXTENSION / OMIT-DENY-TRAY:** coherent and implementable; production `CompanionClient` already sends `surface: this.options.surface ?? "tray"` (`companion-client.ts:612`), so omit-deny bricks no in-version peer.
  - **E1-BINARY-STAMP:** verified safe against the one concrete lease hole I could construct (see rationale).
  - **E1-FANOUT-PREDICATES:** `!== "summoner"` predicates (`lifecycle.ts:1342,1349`; `confirm-fanout.ts:25`) automatically include `panel` — correct as written; `OVERLAY_SHELL_` relay gated on `=== "tray"` (`lifecycle.ts:941`) correctly excludes panel.
  - **E1-FILEMAP / E2-LOCKSTEP / E2-CONSUME-OK / E3-ESBUILD / E3-ENGINES:** all match the tree as inspected.
  - ** NEVER list:** consistent with code; nothing in the folded pins drifts into it.

  ## Missing / still BLOCK

  None.

  ## Nits

  1. **Tray Origin + claimed `"panel"` dies at the validator, not by terminate.** `validate.ts:782` rejects it pre-verify, so the connection lingers unauthenticated until the auth timer — spec pin 3 says "terminate." Behaviorally equivalent for DoD (never authenticated), but the implementer should either terminate explicitly or note the timer path; don't let a test assert `terminate` where validation silently wins.
  2. **E2-CONSUME-OK is forward-compat scaffolding, not a gate.** With `PROTOCOL_MAX = 1` and the server always sending 1, the client-side close-on-mismatch can never fire today. Fine as honesty; don't oversell it in the PR description. Also note `companion-client.ts:515` currently ignores `auth.ok` fields entirely — the "read with missing=MIN" behavior is new code, not a tweak.
  3. **STT pin is origin-keyed, not surface-keyed** (`lifecycle.ts:1385-1386` passes `origin` into the session). Pin 5 preserves this correctly, but an implementer "simplifying" STT to surface-only would regress it — worth one explicit comment in the diff.
  4. `engines.node: ">=22"` while companion keeps `@types/node: ^20` — cosmetic skew; leave or bump, don't let it sneak an unrelated type upgrade into this PR.
  5. Test red list is accurate: the no-Origin `ws-auth-handshake.test.ts` replica cannot carry E1 DoD alone; the new Origin×surface matrix must set real Origin headers.

  ## Recalibrated: implement now? YES only if APPROVE*

  YES — T2 handshake mis-label hygiene plus T1 lockstep/esbuild chores, scoped exactly to the file map, with the NEVER list intact. Nit 1 should be settled (terminate vs validator-linger) in the implementation, not deferred.

  VERDICT: APPROVE_WITH_NITS
