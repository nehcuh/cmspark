All tests pass (258/258). Now I have all the data needed for my final review.

---

## Review: UI Remaining R1–R4

### R1 — Cockpit Session Persist ✅
- `chrome.storage.session` key `cmspark.cockpitWindowId` with `persistWindowId`/`readPersistedWindowId`
- `reclaimCockpitWindowId()` scans `windows.getAll` + `isCockpitTabUrl`
- `ensureHydrated()` called in `openOrFocusCockpitImpl`, `focusCockpit`, `closeCockpit`
- `_resetCockpitWindowStateForTests` clears `hydrateInFlight` too
- Tests pass: path, size, session key, URL match (query/hash/undefined)

### R2 — Mode Pin + Confirm Queue ✅
- `ModeBadge`: `pinned`/`onTogglePin` props; `SET_MODE_PIN` dispatch in `App.tsx` Header; toast on toggle
- `MinimalConfirm`: queue badge `1/N`, tail tool names (capped at 4), Esc→deny, focus deny button on queue head change via `requestAnimationFrame`
- `respond()` wrapped in `useCallback` with `[request, activeThreadId, dispatch]` deps

### R3 — Secondary Panel Tokens + Dead Code ❌
- ✅ `ComputerTaskBar.tsx` deleted from disk; only a stale comment in `App.tsx:160` (no import)
- ✅ Many hex→token replacements correct: `tokens.accent`, `tokens.success`, `tokens.danger`, `tokens.warning` used as JS expressions
- **❌ BLOCKING — 7 string-literal bugs**: `tokens.accent` used as literal text inside CSS string values, producing **invalid CSS**. The browser will silently ignore these declarations — borders and spinners will not render:

| File | Line | Buggy expression |
|---|---|---|
| `AppsPanel.tsx` | 323 | `"2px solid tokens.accent"` |
| `AppsPanel.tsx` | 329 | `"2px solid tokens.accent"` |
| `AppsPanel.tsx` | 847 | `"1px dashed tokens.accent"` |
| `McpPanel.tsx` | 534 | `"1px dashed tokens.accent"` |
| `SkillCraftPanel.tsx` | 327 | `"1px solid tokens.accent"` |
| `SkillCraftPanel.tsx` | 349 | `"3px solid tokens.accent"` (spinner — will be invisible) |
| `SettingsSlideout.tsx` | 1083 | `"1px solid tokens.accent"` |

**Fix**: Each must use a template literal: `` `2px solid ${tokens.accent}` `` or string concatenation `"2px solid " + tokens.accent`.

### R4 — Empty Chips + FOUC + Discoverability ✅
- `SuggestionChips` component + `fillComposer()` → `cmspark:fill-composer` CustomEvent
- `InputArea` useEffect listens for `cmspark:fill-composer`, fills textarea, focuses and sets cursor position
- L2 header soft tint: `background: "#ecfdf5"` + `borderBottomColor: "#a7f3d0"` (intentionally pre-SafetyStrip)
- Empty copy mentions 底栏「更多」for packs/board

---

### Blocking Issues

1. **`chrome-extension/src/sidepanel/components/AppsPanel.tsx:323`** — `"2px solid tokens.accent"` is a string literal, not JS expression. Tab indicator border broken.
2. **`chrome-extension/src/sidepanel/components/AppsPanel.tsx:329`** — Same bug, duplicate.
3. **`chrome-extension/src/sidepanel/components/AppsPanel.tsx:847`** — `"1px dashed tokens.accent"` — Add button border broken.
4. **`chrome-extension/src/sidepanel/components/McpPanel.tsx:534`** — `"1px dashed tokens.accent"` — Add button border broken.
5. **`chrome-extension/src/sidepanel/components/SkillCraftPanel.tsx:327`** — `"1px solid tokens.accent"` — Secondary button border broken.
6. **`chrome-extension/src/sidepanel/components/SkillCraftPanel.tsx:349`** — `"3px solid tokens.accent"` — Loading spinner border-top invisible.
7. **`chrome-extension/src/sidepanel/components/SettingsSlideout.tsx:1083`** — `"1px solid tokens.accent"` — Test button border broken.

All 7 produce invalid CSS; borders/spinners silently degraded. These directly violate the R3 goal of migrating from Material hexes to tokens — the resulting CSS is strictly worse than the original `#4A90D9`.

VERDICT: REJECT
