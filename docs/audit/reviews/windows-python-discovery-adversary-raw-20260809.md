# Raw adversary outputs — windows-python-discovery

> Generated by workflow `windows-python-discovery-adversarial`

## Lane: platform

Recommended: D Full hybrid cascade

Platform lane prefers D (mirror S35 findUv hybrid for base Python). Managers are base-provider only for ensureIsolatedPythonEnv, not G. Unix may stay thinner (python3/python + homebrew paths). longPathFailureHint already covers deep venv/torch trees—keep. Severity high: Windows off-PATH installs false-negative experimental layer despite real Python on disk.

### Cascade order
1. 1.config_pin_absolute_pythonPath_if_set_and_valid
2. 2.existing_isolated_DATA_DIR_python-env_Scripts_or_bin
3. 3.findUv_S35_accelerator_only_for_venv_create
4. 4.managers_readonly_base_pyenv-win_conda_mamba_micromamba
5. 5.PATH_where_python_and_py_launcher_py_-0p_absolute
6. 6.wellknown_absolutes_python.org_Local_PF_WinGet_Scoop_Choco_Anaconda
7. 7.reject_WindowsApps_Store_stub_unless_real_backend
8. 8.version_filter_python3_min
9. 9.pin_absolute_path_for_all_execution
10. 10.missing_install_ux_winget_and_python.org_no_silent_install

### Confirmed problems
- **high** `P1-probe-path-only`: probePythonBin only spawns bare names and never probes well-known absolute Python layouts (unlike findUv). — companion/src/computer/python-runtime.ts:429-432 probePythonBin(bin) runCapture(bin,[-c,sys.executable]); contrast listWellKnownUvCandidates:189-240 and findUv:371-410 which pin absolute uv first.
- **high** `P2-resolve-no-managers-no-wellknown`: resolvePythonRuntime system and isolated-missing cascades only systemPythonPath + PATH python/py; no conda/pyenv-win/mamba, no python.org/WinGet/Anaconda absolute probes. — python-runtime.ts:454-519 cands win32 [systemPythonPath, python, py]; basePy python3|python|py only; resolution strings cite PATH only ('未在 PATH 中找到').
- **high** `P3-ensure-base-path-only`: ensureIsolatedPythonEnv without absolute uv cannot create venv from an off-PATH installed Python (common Windows PATH-incomplete Companion). — python-runtime.ts:586-621: uvBin absolute branch; else basePy = probePythonBin(python3|python|py) only; fail '本机没有可用的 Python' if PATH empty.
- **medium** `P4-no-version-filter`: No Python 3.x / min-version gate on discovered interpreters; any executable printing sys.executable is accepted. — probePythonBin:429-432 and validatePythonExecutable:749-776 only require -c sys.executable success; no version_info check in computer/ tree for runtime selection.
- **medium** `P5-no-py-0p-store-handling`: Windows py launcher is tried as bare 'py' only; no py -0p listing; no explicit Windows Store WindowsApps stub rejection. — resolvePythonRuntime:457,505,619 probePythonBin('py'); no py -0p / WindowsApps references in python-runtime.ts; Store path never allowlisted or denylisted.
- **medium** `P6-install-ux-thin`: Missing-Python UX is python.org + PATH tip only; no winget Python command; asymmetric vs platformized uvInstallHint. — model-state-messages.ts:129-135 python-missing; model-switch-logic.ts:132-138 same; uvInstallHint win32 winget at python-runtime.ts:336-339; no pythonInstallHint.
- **medium** `P7-lookup-path-uv-asymmetric`: processLocalLookupPath adds Roaming\Python\*\Scripts for finding uv but never discovers Local\Programs\Python\*\python.exe as a base interpreter. — python-runtime.ts:304-316 Python Scripts for PATH enrichment; no Local\\Programs\\Python or Program Files\\Python candidates for python.exe discovery anywhere in file.
- **medium** `P8-tests-gap-python-discovery`: computer-python-runtime tests lock findUv Scheme D but do not lock Python well-known/manager/Store/version invariants. — companion/tests/computer-python-runtime.test.ts imports findUv/listWellKnownUvCandidates/ensureIsolatedPythonEnv; no listWellKnownPython* / resolvePythonRuntime well-known fixtures.

### Rejected
- ~~A Copy/docs only~~ — Same class as pre-S35 uv false negatives: install text cannot find off-PATH python.org/WinGet/conda bases. Accept only as install_ux layer inside D.
- ~~B Well-known absolute probes only~~ — Misses PATH-only installs, py launcher multi-version, and manager-rooted pythons; incomplete alone (mirror S35 C-only reject).
- ~~C Env-manager cascade only~~ — Misses default Windows user installs (python.org Local\Programs, WinGet, Scoop shims) which dominate non-data-science machines.
- ~~E Bundle CPython with CMspark~~ — Supply-chain, package size, OS signing, update ownership; S35 SoT non-goal for uv; worse for full CPython+torch surface.
- ~~F Silent winget/choco install of Python~~ — Hard constraint: no silent software install / no permanent PATH/registry mutation without explicit user action.
- ~~G Deep-integrate conda envs as first-class alt to isolated python-env~~ — Breaks isolated vs system product semantics and ADR-020 Surface tooling scope; larger blast radius than using conda python only as base for CMspark venv. Detect-as-base under D is enough.

### Locks
- LOCK: W2-class: after discovery, store and spawn absolute pythonPath only; never canonicalize bare 'python'/'py' when absolute known (sys.executable or realpath).
- LOCK: Preserve pythonMode isolated vs system; isolated execution must not silently fall back to system PATH python for download/infer (existing qwen-vl-session/download/runtime locks).
- LOCK: findUv S35 W1–W12 remain; do not reopen brew-on-Windows or re-litigate uv absolute pin.
- LOCK: Well-known Python probes: fixed allowlist + single-level readdir with package-id/prefix filters (WinGet Python.*, Python3* dirs); no whole-disk walk.
- LOCK: Reject or fail-closed Windows Store stub under %LOCALAPPDATA%\Microsoft\WindowsApps\python*.exe unless -c probe returns real non-Apps executable path.
- LOCK: Version filter: accept only Python 3.x meeting agreed minimum (open Q: 3.10 vs 3.11 for torch); reject 2.x and too-old 3.x with clear message.
- LOCK: Managers: read-only discover base interpreter for ensureIsolatedPythonEnv; do not conda create, do not set CONDA_DEFAULT_ENV as product runtime, do not deep-integrate G.
- LOCK: PATH enrichment for lookup only (spawn env); never write User/Machine PATH or registry permanently.
- LOCK: No computer → mcp/transport import inversion; keep discovery in python-runtime (or shared util under computer).
- LOCK: Windows Scripts\python.exe vs Unix bin/python3 for isolated root stays lock-step.
- LOCK: validatePythonExecutable continues to require absolute path for user pick; extend probe with version filter.
- LOCK: Never silently pip-install into system Python without explicit system mode + user intent.
- MUST NOT: Must not silently run winget/choco/installer to install Python (scheme F).
- MUST NOT: Must not mutate Machine/User PATH or registry as discovery side-effect.
- MUST NOT: Must not return bare 'python' as canonical pythonPath when absolute is known.
- MUST NOT: Must not scan arbitrary drives or user home recursively for python.exe.
- MUST NOT: Must not treat conda env as replacement for DATA_DIR/python-env isolated product mode (G).
- MUST NOT: Must not regress findUv absolute pin or win32 uvInstallHint (no brew-only).
- MUST NOT: Must not execute untrusted relative paths as interpreters.
- MUST NOT: Must not auto-approve Store redirector as success without real interpreter proof.

### Tests
- stripped PATH + fixture Local\\Programs\\Python\\Python312\\python.exe → resolve/ensure base ok + absolute path
- stripped PATH + fixture WinGet Packages Python.Python.3.*\\python.exe (prefix-scoped) → ok; unrelated package ignored
- WindowsApps\\python.exe stub fails closed (no ok for Store redirector)
- py -0p style listing (mocked runCapture) prefers highest eligible 3.x absolute path over bare py
- conda/miniconda/micromamba well-known python.exe used only as base for venv create mock; isolated pythonPath remains DATA_DIR/python-env Scripts\\python.exe after ensure
- findUv W2 still: ok ⇒ absolute; path !== 'uv'; no mcp/transport import
- ensureIsolated without uv but with absolute base fixture succeeds; without base fails with install-oriented error
- version filter rejects fixture reporting Python 2.7 / below min 3.x
- system mode with config absolute pythonPath wins over PATH noise
- install hint strings: win32 includes winget Python and/or python.org; never brew install python as sole Windows guidance

### Install UX
When no usable base Python: block with clear title + next steps. Primary copy-paste: winget install -e --id Python.Python.3.12 (or current 3.x). Secondary: python.org Windows installer with 'Add python.exe to PATH'. Tertiary: optional uv via existing winget astral-sh.uv then Settings '创建独立环境'. Escape: pick_python_path file picker (absolute validate). Never auto-run installers; never write PATH/registry; prompt restart Companion after user install.

---

## Lane: security

Recommended: D full-hybrid-cascade

Extend S35 Scheme D from uv to all Python interpreters: well-known absolute and manager base roots before PATH, absolute pin after discovery (sys.executable/realpath), reject WindowsApps/Store stubs, create only CMspark isolated venv under DATA_DIR, never run conda/pyenv activate scripts or silent winget/choco. G as first-class runtime is vetoed; detecting conda/pyenv base python.exe absolute paths as base for venv is allowed inside D.

### Cascade order
1. config.computer.pythonPath absolute+validatePythonExecutable (system pin / user pick)
2. isolated DATA_DIR/python-env python[.exe] if exists+probe
3. findUv S35 absolute pin (venv create accelerator only)
4. well-known absolute base interpreters (python.org/Local Programs/Program Files/Anaconda roots; exclude WindowsApps)
5. env-manager base roots only (pyenv-win versions/*; conda/mamba root python.exe; optional single-level envs/*) via exists+bounded readdir — no activate scripts
6. process-local enriched PATH where/which python|py|python3 → pin absolute via sys.executable; reject relative and Store stubs
7. py -3 discovery helper only if yields absolute executable; never store bare py
8. if any validated base: ensureIsolatedPythonEnv (uv absolute preferred else absolute base -m venv + pip allowlist)
9. else missing-Python UX (winget + python.org + pick_python_path); no silent install

### Confirmed problems
- **high** `P1`: Python discovery spawns bare names (python/py/python3) with no well-known absolute cascade — PATH hijack on first spawn — companion/src/computer/python-runtime.ts:429-433 probePythonBin; 454-458 system cands; 501-505 isolated basePy; 617-619 ensureIsolatedPythonEnv fallback
- **high** `P2`: No WindowsApps/Microsoft Store python.exe stub rejection; Store aliases can fake discovery or open Store UX — validatePythonExecutable at python-runtime.ts:749-777 only absolute+exists+probePythonBin; zero WindowsApps denylist in computer/
- **medium** `P3`: No env-manager detection (pyenv-win/conda/mamba) for base interpreter when PATH is empty — python-runtime.ts resolvePythonRuntime/ensureIsolatedPythonEnv have no conda/pyenv paths; only listWellKnownUvCandidates for uv (189-241)
- **medium** `P4`: No well-known absolute Python install path probes (python.org/WinGet/Anaconda layouts) — Unlike listWellKnownUvCandidates, probePythonBin and resolvePythonRuntime never join Program Files or LocalAppData Programs\Python
- **low** `P5`: Missing-Python UX lacks winget and is weaker than product install-guidance intent — companion/src/computer/model-state-messages.ts:129-136 python-missing detail only python.org+PATH; no winget
- **medium** `P6`: No unit invariants for absolute-pin of Python (only uv covered) — regression risk for bare python canonical — companion/tests/computer-python-runtime.test.ts covers findUv bare-uv reject; grep shows no validatePythonExecutable/Store stub cases
- **low** `P7`: qwen-vl-preflight still contains bare-name findPython path parallel to resolvePythonRuntime — companion/src/computer/qwen-vl-preflight.ts:133-147 findPython cands [venvPy,'python','py']; runQwenVlPreflight uses resolvePythonRuntime at 368-371

### Rejected
- ~~A copy/docs only~~ — Does not close PATH hijack or Store stub execution surface; false negatives remain when PATH incomplete
- ~~B well-known absolute probes only~~ — Misses PATH-only legitimate installs and manager layouts; incomplete vs product cascade
- ~~C env-manager cascade only~~ — Incomplete without PATH/well-known; encourages activate-script integration which security vetoes
- ~~E bundle CPython~~ — Supply-chain/signing/size blast radius; needs packaging ADR; not required if D lands
- ~~F silent auto winget/choco install~~ — Hard veto: privilege escalation / unattended software install without explicit user action
- ~~G deep-integrate conda as first-class Qwen runtime~~ — Running activate.bat/conda shell hooks executes untrusted scripts; mutates env; blurs isolated vs system product semantics; optional base python.exe detect inside D is enough

### Locks
- LOCK: After discovery, every executed interpreter path is absolute (realpath preferred); never return bare 'python'/'py'/'python3' as canonical when absolute known
- LOCK: Discovery order for execution candidates: absolute well-known and manager base roots before PATH where/which (W11 class, same as findUv)
- LOCK: Reject Microsoft Store App Execution Alias paths under %LOCALAPPDATA%\Microsoft\WindowsApps\ for python*.exe
- LOCK: validatePythonExecutable: absolute required + exists + probe -c + basename allowlist (python/python3/python.exe/python3.x.exe) + Store denylist
- LOCK: Env managers: detect/use only absolute base python.exe roots; never source activate / conda.bat / micromamba shell init
- LOCK: Bounded scans only (single-level readdir, fixed prefixes); never recursive full-disk or whole WinGet Packages without package-id filter
- LOCK: Do not permanently mutate Machine/User PATH or registry
- LOCK: Do not silent-install Python or uv; install remains user-confirmed UX
- LOCK: Preserve isolated vs system modes; never silent pip into system Python without explicit user intent (install_deps system → commands only)
- LOCK: Do not merge user-env PATH/PYTHONPATH/PYTHONHOME into python-runtime discovery/spawn (user-env.ts denylist + S35 W10)
- LOCK: Do not regress findUv S35 absolute pin / WinGet package-id scope / no brew-on-win32
- LOCK: No computer → mcp/transport import inversion
- LOCK: ensureIsolatedPythonEnv: uvBin and basePy must be absolute after discovery before venv/pip spawn
- MUST NOT: Execute relative python paths or treat relative where hits as success
- MUST NOT: Scan entire disks or unbounded directory trees for python.exe
- MUST NOT: Run untrusted conda/pyenv/mamba activate or profile scripts
- MUST NOT: Auto winget/choco/scoop install of Python without explicit user action
- MUST NOT: Use user conda env as silent default substitute for DATA_DIR/python-env isolated mode
- MUST NOT: Return ok with path 'python' / 'py' after successful absolute discovery
- MUST NOT: Accept WindowsApps Store stub as usable base for venv or inference
- MUST NOT: Silently write PATH or App Paths registry
- MUST NOT: Re-open brew-on-Windows for uv or make uv required for canDownload/canEnable
- MUST NOT: Import mcp/transport from computer python discovery

### Tests
- resolvePythonRuntime/ensureIsolated: when pythonPath set, path.isAbsolute true and not basename-only bare names
- Fixture WindowsApps\python.exe exists and probes: must NOT count as ok basePy / system pythonPath
- Hostile PATH prepend with fake python.exe: well-known absolute real python wins when present (absolute-before-PATH)
- where/which relative hit 'python' rejected; only absolute hits proceed to pin
- ensureIsolatedPythonEnv spawns never use bare 'uv' or bare 'python' argv0 when absolute candidates known
- No test or code path writes process.env permanently to OS User/Machine PATH
- system install_deps still returns needsManual commands; does not auto-pip system
- Manager detection fixture: conda root python.exe used only as absolute base for venv; no activate.bat spawn
- validatePythonExecutable rejects empty, relative, non-file, and non-running binary
- findUv S35 tests remain green; no brew-only uvInstallHint on win32

### Install UX
When no validated Python base exists: show explicit multi-option guidance — (1) winget install -e --id Python.Python.3.12 (or current stable Python.Python.3.x), (2) python.org installer with Add python.exe to PATH checked, (3) optional Settings pick_python_path for absolute executable, (4) optional uv via winget astral-sh.uv for faster isolated env. Never silent auto-install. Primary post-base CTA remains create CMspark isolated env under DATA_DIR; system mode pip stays copy-paste only.

---

## Lane: product

Recommended: D full hybrid Python discovery cascade

Extend S35 Scheme D from uv to base Python: absolute well-known probes + process-local PATH/py lookup + optional manager bases as seeds for CMspark isolated venv only; progressive Chinese readiness (create-env vs install-Python); never silent install/PATH mutation; preserve isolated|system; do not deep-integrate conda as a peer runtime (reject G for P0).

### Cascade order
1. existing isolated DATA_DIR/python-env absolute bin if validates
2. findUv S35 absolute pin (optional accelerator only)
3. config/user-picked absolute pythonPath (system override or ensure base)
4. PATH/py-launcher under process-local enriched lookup; pin sys.executable absolute
5. well-known Windows absolute layouts (python.org Program Files/LocalAppData, scoped WinGet, non-stub installs)
6. env-manager bases detect-only (conda/miniconda/mamba/pyenv-win) as isolated-venv seeds only
7. if any usable base or uv can provision: primary CTA 创建独立环境
8. if all fail: missing-Python install UX (winget + python.org + optional uv) + pick-path escape

### Confirmed problems
- **high** `P1`: Python discovery only probes bare PATH names via spawn; no well-known absolute install paths—false negatives for installed-off-PATH Windows Python. — companion/src/computer/python-runtime.ts:429-433 probePythonBin spawn-only; 455-458 system cands python/py only; 502-505 isolated-missing base python3/python/py only; contrast listWellKnownUvCandidates 189-241
- **high** `P2`: process-local PATH enrichment is applied to uv where/which only; Python probes use default process.env—GUI Companion PATH holes remain for python/py. — python-runtime.ts:385-390 lookupPath used only in findUv; probePythonBin 429-433 uses runCapture without enriched env
- **high** `P3`: No conda/pyenv-win/mamba detection as base interpreters; manager-only installs cannot create isolated env without manual pick_python_path. — grep companion/src/computer: no conda/pyenv/mamba probes; ensureIsolatedPythonEnv 617-621 only bare PATH names when uv absent
- **medium** `P4`: Progressive cascade UX collapses: resolution can say base exists for create-env, but readinessSummary/python-missing still push 'install Python 3' without winget or staged CTAs. — python-runtime.ts:514-518 resolution ladder; qwen-vl-preflight.ts:612 readinessSummary 未就绪：请先安装 Python 3; model-state-messages.ts:129-135 python-missing python.org only
- **medium** `P5`: Missing-Python install guidance is weaker than S35 uv platformization (no winget Python, brew-shaped Mac tip, no Store-stub guidance). — model-state-messages.ts:129-135; model-switch-logic.ts:132-138; uvInstallHint win32 winget at python-runtime.ts:336-339
- **low** `P6`: Dead local findPython in preflight still PATH-only; production uses resolvePythonRuntime—risk of future dual-truth wiring. — qwen-vl-preflight.ts:133-147 findPython defined; only definition site; runQwenVlPreflight uses resolvePythonRuntime at 368-371

### Rejected
- ~~A copy/docs only~~ — Improves install trust but leaves PATH/false-negative discovery intact—same sole-A rejection as S35 uv.
- ~~B well-known absolute paths only~~ — Misses env-manager bases and PATH-only py launcher; incomplete vs hybrid D.
- ~~C env-manager cascade only~~ — Misses default python.org layouts; risks UI manager overload without PATH/well-known coverage.
- ~~E bundle CPython with CMspark~~ — Supply-chain, size, signing, update surface—parallel to rejected uv bundling; overkill vs discovery.
- ~~F silent auto winget/choco install of Python~~ — Hard constraint: no silent software install without explicit user action; irreversible host change.
- ~~G deep-integrate conda as first-class alternative to isolated python-env~~ — Breaks isolated|system product model, torch/CUDA pollution risk, large blast radius; managers as seed for isolated venv is enough for P0.

### Locks
- LOCK: Absolute path pin after discovery; never bare 'python' as canonical when absolute known
- LOCK: Preserve isolated vs system semantics; isolated never silent-fallback to system for inference
- LOCK: uv optional accelerator only; never sole canDownload/canEnable gate when a usable interpreter exists
- LOCK: No permanent Machine/User PATH or registry mutation
- LOCK: No silent pip into system Python; system deps stay explicit/manual commands
- LOCK: Do not regress findUv S35 locks or brew-on-Windows
- LOCK: Manager detections seed CMspark isolated venv only—not a third runtime mode
- LOCK: One primary CTA per readiness state; Chinese copy progressive create-env vs install-Python
- LOCK: Platformized missing-Python install hints (win32 winget+python.org; never brew-only on win32)
- LOCK: Windows Store/alias stubs fail closed on version/-c probe
- LOCK: Allowlisted probes only; no whole-disk scan
- LOCK: ADR-020: Surface tooling only; no new Autonomy axis
- MUST NOT: Silent auto-install of Python or package managers
- MUST NOT: Bundle CPython inside CMspark package
- MUST NOT: First-class 'run Qwen inside user conda env' product mode
- MUST NOT: Overwhelm Settings with conda/pyenv/mamba peer mode choosers
- MUST NOT: computer → mcp/transport layer inversion imports
- MUST NOT: Execute untrusted relative interpreter paths
- MUST NOT: Re-litigate findUv absolute pin or brew-as-Windows-primary

### Tests
- Off-PATH well-known python.exe fixture is discovered and pinned absolute
- ok discovery never returns bare 'python'/'py' as canonical path
- ensureIsolatedPythonEnv with absolute base creates only under DATA_DIR/python-env
- system install_deps never auto-runs pip (needsManual + commands)
- win32 missing-Python copy includes winget and/or python.org and excludes brew-only
- findUv S35 unit suite remains green
- Stub/alias binary that fails -c is not treated as usable base
- Isolated missing + usable base ⇒ resolution/CTA create-env, not install-Python-only

### Install UX
When fully missing: primary CTA copy-paste winget install -e --id Python.Python.3.12 (or current stable); secondary python.org installer with 勾选 Add to PATH + 重启 Companion; tertiary optional winget astral-sh.uv then 创建独立环境; escape 选择 Python 可执行文件…; warn against Windows Store placeholder python; NEVER silent winget/choco/auto-install from Companion.

---

## Lane: compat

Recommended: D full hybrid cascade

Compat severity overall: high (Windows false-negatives block experimental layer setup, but pick_python_path + manual install mitigate). Prefer D over G: managers are base suppliers for CMspark isolated venv, not a third product mode. Reuse UvDiscoveryDeps injection surface; extend listWellKnown* / processLocalLookupPath carefully without computer→mcp import. Surface fields: keep uvPath/uvAvailable; add absolute pythonPath when known; optional discovery resolution detail for managers; installCommands/install hints when fully missing. FAIL CLOSED without file evidence — claims above cite paths.

### Cascade order
1. config computer.pythonPath absolute+validate (system pin / manual pick)
2. isolated DATA_DIR/python-env bin if exists → absolute pin (isolated run)
3. findUv S35 absolute (optional accelerator only)
4. global PATH + Windows py launcher → pin sys.executable absolute
5. env-manager base detect allowlisted roots (conda/miniconda/mamba/micromamba/pyenv-win) for create capability
6. well-known absolute Windows python.exe probes (bounded readdir; no full disk)
7. user-triggered ensure_python_env: any usable base → isolated venv (uv absolute preferred else base -m venv); never mutate system PATH
8. fully missing → install UX (winget Python + python.org PATH tip + optional uv); never silent install

### Confirmed problems
- **high** `P1`: probePythonBin only spawns a given name/path; there is no well-known absolute Python candidate list analogous to listWellKnownUvCandidates. — companion/src/computer/python-runtime.ts:429-433 (probePythonBin = runCapture only); well-known absolute probes exist only for uv at :189-240.
- **high** `P2`: resolvePythonRuntime system mode is config path + bare PATH names (python/py); no managers, no absolute install-layout probes. — companion/src/computer/python-runtime.ts:454-480 candidates = systemPythonPath then win32 ["python","py"] else ["python3","python"].
- **high** `P3`: When isolated venv is missing, base-Python detection is PATH bare names only; managers and well-known installs are invisible for create-env UX. — companion/src/computer/python-runtime.ts:501-518 basePy = probePythonBin(python3|python|py); ensureIsolatedPythonEnv non-uv branch :616-621 same triad only.
- **medium** `P4`: No conda/pyenv-win/mamba/micromamba discovery or capability surface in computer Python runtime. — rg conda|pyenv|mamba under companion/src/computer → zero matches; only uv well-known layouts.
- **high** `P5`: System-mode download/runtime candidate list also stays bare python/py, so off-PATH system Python still fails even after user picks mode=system without picker. — companion/src/computer/qwen-vl-download.ts:228-236 pyCandidates system = sysPath + ["python","py"] on win32.
- **medium** `P6`: python-missing / install copy is incomplete vs product intent: python.org PATH tip only; no winget Python guidance; no structured multi-installer cascade. — companion/src/computer/model-state-messages.ts:129-135; preflight missing path pushes create-env/uv only :435-447; uvInstallHint win32 is winget for uv only :336-339.
- **low** `P7`: Dead preflight findPython remains PATH-only and is not wired; any future reuse would reintroduce the same false-negative class. — companion/src/computer/qwen-vl-preflight.ts:133-147 define findPython; runQwenVlPreflight uses resolvePythonRuntime :368-371; rg findPython( only that definition.
- **medium** `P8`: S35 findUv injection + absolute-pin + bounded readdir patterns are not yet applied to Python discovery; expanding cascade without them risks test and security regressions. — findUv + UvDiscoveryDeps :72-86,:371-410; tests companion/tests/computer-python-runtime.test.ts:186-470 cover uv only; no Python well-known/manager fixtures.

### Rejected
- ~~A copy/docs only~~ — Does not fix false negatives when Python is installed off Companion PATH; same failure mode that forced S35 findUv beyond brew-only copy.
- ~~B well-known absolute Python paths only~~ — Misses legitimate PATH/py-launcher and manager base installs; incomplete alone (parallel to rejecting absolute-only for uv).
- ~~C env-manager cascade only~~ — Misses python.org/WinGet/PATH installs; managers alone invite accidental first-class conda runtime without isolated semantics.
- ~~E bundle CPython with CMspark~~ — Supply-chain, size, signing, and update surface; S35 explicitly rejected bundling uv for the same reasons.
- ~~F silent auto winget/choco install of Python~~ — Violates explicit-user-action constraint; permanent host mutation; security veto.
- ~~G deep-integrate conda envs as first-class alternative to isolated python-env~~ — Collides with product modes isolated vs system; large blast radius (activation scripts, multi-env selection UI, dep pollution); managers should supply base for DATA_DIR/python-env only unless a separate ADR elevates Autonomy/Compose.

### Locks
- LOCK: Reuse findUv Scheme D: absolute pin after discovery; never return bare 'python'/'py'/'python3' as canonical when absolute sys.executable or well-known path is known (W2-class).
- LOCK: Keep pythonMode isolated | system semantics; isolated execution/download must not silently use PATH/system python (B3 + resolvePythonRuntime omit pythonPath until venv exists).
- LOCK: uv remains optional accelerator; missing uv must not block canDownload/canEnable when isolated env + deps are ready (W9).
- LOCK: ensureIsolatedPythonEnv: uv argv0 only if absolute; non-absolute uv falls through (T2 tests stay green).
- LOCK: Do not import mcp/transport from computer/python-runtime (W6 source lock).
- LOCK: Do not merge ADR-019 user-env PATH/PYTHONPATH/PYTHONHOME into python-runtime discovery or spawn env for probes (W10/G10).
- LOCK: ADR-020: Surface tooling only — discovery/UX fields, no new Autonomy axis or silent host install arm.
- LOCK: Never silently mutate Machine/User PATH or registry; lookup PATH enrichment is process-local only (W5).
- LOCK: system install_deps must not auto-pip into global Python; needsManual + commands only.
- LOCK: Bounded FS probes only (exists/stat + single-level readdir with prefix filters); no full-disk walk; reject relative untrusted paths.
- LOCK: darwin/linux keep working; win32 is primary cascade depth; unix may stay thinner PATH + existing absolute patterns.
- LOCK: Test injection deps (existsSync/readdirSync/statSync/realpathSync/runCapture/platform/env/homedir) required for offline fixtures — mirror UvDiscoveryDeps.
- LOCK: Do not re-open brew-on-Windows for uv or Python install hints; win32 never brew-only.
- LOCK: Config/pick absolute pythonPath remains escape hatch via validatePythonExecutable.
- MUST NOT: Re-litigate or regress findUv absolute pin / WinGet package-id filter / bare-uv ban tests.
- MUST NOT: Return ok discovery with path equal to bare 'python', 'py', or 'python3'.
- MUST NOT: Silently install Python or uv without explicit user action.
- MUST NOT: Silently pip-install into system Python without user intent (terminal commands or confirm path only).
- MUST NOT: Treat user conda env as default Qwen runtime instead of CMspark isolated python-env without product mode change.
- MUST NOT: Scan entire disks or unbounded WinGet/Anaconda trees.
- MUST NOT: Expand user-env denylist keys into discovery merge.
- MUST NOT: computer → mcp/transport layer inversion.
- MUST NOT: Change isolated download empty-candidate fail-closed behavior to PATH fallback.
- MUST NOT: Ship Windows Store alias/stub python without version/executable validation.
- MUST NOT: Auto-create isolated venv on every preflight without user ensure_python_env action (probe yes; mutate no).
- MUST NOT: Claim manager/path support without file:line evidence and injection tests.

### Tests
- Existing computer-python-runtime findUv suite (stripped PATH fixtures, WinGet package-id, bare uv reject, absolute argv0 ensureIsolated) remains 100% green.
- New: stripped PATH + fixture absolute well-known python.exe → resolve/system pin absolute; not bare 'python'.
- New: manager-root fixture with base python.exe → reported as usable base for ensureIsolated; not auto set as isolated pythonPath until venv created.
- New: ensureIsolated without uv uses absolute base path as argv0 for -m venv when discovery found absolute base.
- New: when no base and no uv → ensure fails closed with install_ux-oriented error; no registry/PATH writes in runCapture mock.
- New: WindowsApps or non-executable stub candidate fails validate/version probe and is skipped.
- W6: python-runtime source still has no mcp/transport import.
- buildInstallCommands still prefers absolute uvPath quoting; platform inject win32 vs darwin.
- system install_deps still returns needsManual without spawning pip when mode=system.
- isolated resolve never includes system pythonPath when isolatedExists=false.
- uvAvailable false still allows non-uv install command generation (W9).
- Injection: platform=win32 fixtures must pass on linux/darwin CI hosts via path.win32 + deps (same pattern as findUv tests).

### Install UX
When no usable base and no isolated env: win32 show BOTH `winget install -e --id Python.Python.3.12` (or current stable winget id) AND python.org installer with Add python.exe to PATH + restart Companion; keep optional uvInstallHint (already winget for uv). Offer Settings pick_python_path and create-independent-env after install. Never auto-run winget/choco/silent installer. Darwin: brew install python3; Linux: distro python3 + venv packages. Do not brew-only on Windows.

---

