Independent review complete. Patch verified non-stale against `git diff --stat`. Inspected the four tracked files, the lockstep `host-skylight.swift`, the rebuilt binary, and ran the suite.

**Findings**

**COMP-2 (executor.ts:620-625)** — carve-out is correct. `FORCE_INTERACTIVE_DANGEROUS = {"computer.danger_detected", "computer.experimental_suggestion"}`; `forceInteractive = dangerous.some(...)`; the trust branch is gated by `!forceInteractive`. Verified reL2 callers at executor.ts:924 (danger_detected) and :958 (experimental_suggestion) pass those exact codes. Budget/uncross/foreground_yielded callers (lines 752, 984, 1381) remain trust-eligible.

**COMP-2 tests (test:1268, :1292)** — authentic, not rubber-stamp. Both call `trust.grant("sess-1","win.app.test")` BEFORE the action, then assert `confirm.captured.length === 1`, `dangerousApis === [...]`, `injector.clicks.length === 0`, and the right error code. Without the carve-out these would fail (trust would suppress and `confirm.captured` would be 0). Verified by running the suite: both new tests ✔; 4 prior session-trust tests still ✔.

**COMP-1 (host.swift:563-616, 980-1006)** — math is correct. Traced the full data flow:
- screenshot sets `client.x = pos.x - fx` (AX-minus-CGWindow-frame)
- companion computes `pointClient.x = img.x - shot.client.x`
- host sets `clientOrigin = pos` (when AX matched) and `screenX = clientOrigin.x + px = pos.x + img.x - (pos.x - fx) = fx + img.x` ✓

Symmetric-fail case also resolves to `fx + img.x` because `clientOrigin` defaults to `(fx, fy)` when `bestWin == nil`. host-skylight.swift:633-680 + :1051-1083 is lockstep. `screenX/screenY` added to ok JSON; `x/y` retained as client coords for Windows parity.

**Binary rebuild** — `/Users/huchen/Projects/cmspark/companion/dist/cmspark-host` SHA256 = `29ee68d6…`, matches the implementer's claim (file mtime today).

**Test totals** — 1603 ✔, 7 ✖ (all pre-existing: `isUserWritablePath`, `apps.add lolbin`, `pruneOldLogs`/log-rotation, NTFS case-insensitive). None touch P0-C.

**Scope** — server.ts, occlusion, Stop/abort, packaging, grant path all untouched (verified by `git diff --stat`). Scroll/drag Darwin still skip client→screen (documented residual, out of scope).

---

**Nits (non-blocking)**

1. `executor.ts:620` — `FORCE_INTERACTIVE_DANGEROUS` Set is re-allocated on every reL2 call. Hoist to module scope for clarity (perf impact negligible — reL2 fires ~4× per task max).
2. `host.swift:608-611` — comment "client (0,0) in screen space is therefore AX position (when AX matched)" is accurate but cryptic; the relationship between screenshot-client offset, image pixel space, and AX/CGWindow frames took several re-reads to confirm. A one-line "screenX = fx + img.x when AX symmetric" invariant note would help future readers.
3. `host.swift:1049-1054` (scroll) — still posts at cursor position without client→screen mapping. Documented residual, but worth a `// P0-C residual: scroll still uses raw cursor pos` comment so it's not mistaken for an oversight.
4. `host-skylight.swift` — file is untracked (`??` in git status). Should be staged together with `host.swift` in the eventual commit so the lockstep pairing survives `git mv`/history.

VERDICT: APPROVE_WITH_NITS
