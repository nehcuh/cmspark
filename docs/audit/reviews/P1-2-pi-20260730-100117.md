All 101 tests pass, 0 fail. Now let me do the final ADR-020 capability checklist cross-check against the code:

**ADR-020 Capability Checklist — line-by-line verification:**

| Check | Result | Evidence |
|-------|--------|----------|
| **1. Axes fit** | ✅ PASS | Surface L1 (navigate domain confirm dialog) + Composition (mcp-server). No "中层 Agent" language. |
| **2. Pack-first** | ✅ PASS | No new Side Panel chrome, no Pack alternative bypassed. |
| **3. Confirm dialects** | ✅ PASS | Same `security.confirmation.request/response` family — no new gate type. |
| **4. Trust monotonicity** | ✅ PASS | OriginWs binding is strictly MORE restrictive. God-mode does not bypass MCP/tool confirms; `forceMcpConfirm`/`forceMetaConfirm` still fire. |
| **5. originWs** | ✅ PASS | All three targeted call sites bind `{ originWs: ws }`: navigate at L1831, MCP tool at L3933, MCP meta at L4115. |
| **6. No new runtime** | ✅ PASS | Pure parameter addition on existing `request()` calls. |
| **7. Experimental layers** | ✅ PASS | Not applicable; no TinyClick/model locators involved. |

**Call-site verification (live code):**
- `server.ts:1831` — `{ originWs: ws }` on navigate URL L2 ✅
- `server.ts:3933` — `{ originWs: ws }` on MCP tool confirm ✅
- `server.ts:4115` — `{ originWs: ws }` on MCP meta confirm ✅
- `server.ts:1447` — evaluate/shell conditional bind **untouched** ✅
- `server.ts:1479-1483` — tray `respond()` privileged path **untouched** ✅
- `security-confirmation.ts:411-415` — `respondFrom` originWs enforcement ✅
- `security-confirmation.ts:533` — `rejectAll` originWs filtering ✅
- `security-confirmation.ts:490-510` — `respond()` bypasses origin (privileged tray) ✅

**Test coverage:**
- Unit: `[P1-2] navigate originWs` — rogue mismatch + origin resolve + privileged `respond()` intact ✅
- Unit: `[P1-2] mcp-style tool originWs` — rogue mismatch + origin resolve + privileged `respond()` for MCP meta intact ✅
- Integration: navigate `respondFrom(rogue)` origin_mismatch then origin approve ✅
- Integration: navigate `handleSecurityConfirmationResponse(non-origin)` leaves pending intact ✅
- Integration: MCP tool `respondFrom(rogue)` origin_mismatch then `respondFrom(origin)` approve ✅
- All 101 tests pass (verified independently) ✅
- Inventory `P1-2` status → FIXED ✅

No blocking issues found. The adversarial note about "no MCP-meta multi-peer integration test" is non-blocking: MCP meta uses the identical `request(... { originWs: ws })` → `respondFrom` code path as MCP tool, and the unit test covers MCP-meta privileged path + MCP-tool integration covers the originWs mechanism end-to-end. A dedicated MCP-meta integration test would add no marginal safety signal.

VERDICT: APPROVE
