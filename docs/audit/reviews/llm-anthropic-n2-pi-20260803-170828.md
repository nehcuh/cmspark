All checks complete. Let me summarize my findings.

## Verification results

**Claims verified against real code:**
- ✅ `createProvider` (provider.ts:117-127) switches purely on `llmConfig.protocol ?? "openai"` — no base_url auto-detect (L3)
- ✅ `computeMaxTokens` (anthropic-convert.ts:74-78) matches M4 exactly: `min(8192, max(256, floor(cw/8)))`, deterministic incl. invalid-window fallback
- ✅ `sanitizeToolCallId` (anthropic-convert.ts:83-92) → `^[a-zA-Z0-9_-]+$`, applied to both `tool_use` and `tool_result` so pairing stays aligned; cross-protocol resume tests pass
- ✅ No `@anthropic-ai/sdk` — not in package.json, not imported; pure `fetch` + hand-rolled SSE (L4)
- ✅ L7 enforced in AnthropicProvider via `buildRequestHeaders` → `assertHeaderPolicy` (anthropic.ts:60,80) — throws `HeaderPolicyError` **before** `fetch`; test asserts `fetched === false`
- ✅ 52/52 tests pass (reproduced), both `tsc -p tsconfig.test.json` and `tsc --noEmit` clean
- ✅ M6 thinking→reasoning, M7 reasoning_content drop, contiguous tool_result merge all present and tested
- ✅ adapter.ts untouched — `createProvider` has zero callers yet (Node3 boundary honored)

**Edge-case stress (SSE loop):** I ran the parser against CRLF + chunk-split mid-field, multi-line `data:`, trailing data without final blank line, out-of-order blocks, and error events — all behave correctly. Stream loop is **not** broken; `done` emitted exactly once; abort propagates `AbortError`.

## Nits (non-blocking)

1. **anthropic.ts:243-251 (message_start) + 228-242 (message_delta)** — usage is emitted as **two partial events** (input tokens on `message_start`, output on `message_delta`), so the canonical stream never carries one complete final usage; a "take last usage event" consumer would see `total=3` instead of `13` in my repro. Node3 adapter must merge; consider a fixture test pinning the semantics.
2. **openai.ts:30-40** — `OpenAIProvider` silently ignores `config.extra_headers` and `auth_style: "x-api-key"`, though headers.ts (buildRequestHeaders) documents honoring them for openai. Since `createProvider` is not yet wired, this is a latent gap that will surface in Node3 — the config schema promises behavior the openai path can't deliver.
3. **anthropic.ts:169-174** — inbound SSE `tool_use` id is passed through raw (not sanitized). First-party ids are valid, but a relay could push an illegal id into the canonical event/thread; rebuild sanitizes both sides consistently so pairing holds, but canonical ids aren't guaranteed valid.
4. **anthropic-convert.ts:107-109** — comment says "prefer `" "` for schema validity" but code returns `{type:"text", text:""}`; comment/code mismatch (cosmetic).
5. **anthropic-convert.ts:139-142** — `resolveAnthropicMessagesUrl` mangles base URLs containing query strings (appends `/v1/messages` after the `?`), producing a malformed URL.
6. **anthropic.ts:106-110 (`mapAnthropicCompleteResponse`)** — `tool_use` blocks in a non-stream response are silently dropped (empty content). Acceptable per P0 (tools off the complete path) but silent.
7. **Test gaps** — no tests for non-200 HTTP → `AnthropicHttpError`, SSE `error` event throw, or usage-merge semantics.

None of the REJECT conditions trigger: stream loop works, ids sanitized/deterministic, L7 enforced pre-fetch in the anthropic provider.

VERDICT: APPROVE_WITH_NITS
