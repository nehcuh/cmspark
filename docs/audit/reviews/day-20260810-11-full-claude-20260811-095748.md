I've completed the independent senior code review. Here's my verdict.

## Findings

### Part 1 — Multi-adv deep (C1–C16) verified at tip `cd3aa9c`

| ID | Status | Evidence |
|----|--------|----------|
| C1 | OK | `unattended-grant.ts:149-203,210-216` — `captureCruiseSnapshot` writes durable file; `reconcileUnattendedCruiseOnBoot` wired at `ws/lifecycle.ts:413-442`; `shouldRestoreCruiseOnDisarm` gates on had_grant OR had_snapshot OR clear_cruise (bare disarm no clobber) |
| C2-C3 | OK | `SafetyStrip.tsx:91-118,212-217` abortWhileArmed label + `CockpitApp.tsx:213-217` 值守中 banner |
| C4 | OK | `l2-admission.ts:758` `forceConfirm = (capabilityForceConfirm \|\| hostComputerGated) && !userFullAutonomy`; `autopilot-tier.ts:177-182` matrix shows 仍确认 under default 值守 |
| C5 | OK | `pack-engine.ts:1616-1626` server-side `isValidSecurityArmPhrase` gate; `PacksPanel.tsx:36-39,521-523` client-side |
| C6 | OK | `thread-manager.ts:854-862` `isToolAllowed` re-enforces `WORKER_HARD_DENY` for `agent_role==="worker"` regardless of whitelist |
| C7 | OK | `server.ts:560-565` `normalizeShellCwd` BEFORE `runMultiAgentToolPregate` + `runL2ToolAdmission` |
| C8 | OK | `server.ts:566-572` `normalizeNetsecPorts` BEFORE L2 admission |
| C9 | OK | `ws-router-validator-lockstep.test.js` 3/3 pass |
| C12 | OK | No false-green OR — three-flag cruise is sole waiver |
| C13-C15 | OK | `docs/mcp.md` `require_grant=true` default; `docs/computer-use-user-guide.md` + `host-and-apps.md` AppsPanel toggle; `adr/021` windowLevel=hard residual added |

### Part 2 — C10 god-file split integrity

1. **No circular imports**: confirmed — no extracted module under `ws/`, `tool/`, `security/`, `orchestrator/`, `mcp/` imports `server.ts`.
2. **createToolExecutor order** preserved (`server.ts:463-821`): normalize → tool.start → pregate → cookie → browser_download → L2 → URL → image → companion/MCP → forward.
3. **Security algebra moved not rewritten**: L2 (`tool/l2-admission.ts:153-1500`), cookie/URL, image, browser_download, MCP critical all match origin/main semantics. `image-fetch-admission.ts:193` still lacks `{ originWs: ws }` — but this matches pre-existing origin/main behavior (`server.ts:2622` before split); not a regression.
4. **Public re-exports**: `startServer`, `handleSecurityConfirmationResponse`, `pendingToolCalls`, `enhanceMcpError`, `validateWsMessage`, `forwardToolToExtension` etc. all re-exported from `server.ts`.
5. **bind\* runtime**: `bindMcpDispatchFromServerLocals` + `bindToolForwardFromServerLocals` eager-called at `server.ts:915-916`; companion-dispatch bound in `initServices` + `seedThreadManagerForTests`.

### Part 3 — Tests (re-run, all green)

- security-gates: **63/63**
- ws-router-validator-lockstep: 3/3 · computer-unattended-grant: 30/30 · worker-hard-deny-runtime: 3/3
- orchestrator-tool-pregate: 9/9 · ws-tool-forward: 7/7 · image-fetch-admission: 7/7 · url-cookie-admission: 13/13 · ws-origin + healthz: 5/5
- `npx tsc -p tsconfig.test.json`: clean

### ADR-020 capability declaration
Surface/Compose/Autonomy/Trust/Channel declaration IS present in the implementer prompt.

### Nits (non-blocking)
- `image-fetch-admission.ts:193` `securityConfirmations.request(...)` lacks `{ originWs: ws }` — pre-existing on origin/main; not introduced by this PR. Worth a future hardening pass but explicitly out of scope per "MCP/navigate historically weak — do not regress further".
- `tabUrlCache` colocation deferred — explicitly allowed by verdict guidance.

No security algebra regression, no missing bind, executor order preserved, all C1–C16 confirmed at tip.

VERDICT: APPROVE
