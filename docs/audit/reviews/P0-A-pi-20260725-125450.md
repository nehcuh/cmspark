All 22 tests pass (14 chrome-extension, 8 companion). Now I have the full picture. Here's my review.

---

## External Review — P0-A (SEC-1, SRV-1, XC-Integration-1)

### 1. SEC-1: Selector Injection — **PASS**

**`chrome-extension/src/background/selector-js-literal.ts`**
- `JSON.stringify(selector)` is the correct universal fix. It handles backslash, single/double quotes, newlines, U+2028/2029 — the full set the old `replace(/'/g, "\\'")` missed.

**`chrome-extension/src/background/browser-bridge.ts`** (lines 609, 971, 996)
- All three `Runtime.evaluate` paths now use `selectorJsLiteral(...)` as a function argument to `.querySelector(...)`.
- `getPageHTML` keeps `document.querySelector('html')` prefix → `scriptingExecute` `bodyHtmlExpr` special-case at line 193 still matches.
- No remaining `replace(/'/g, "\\'")` in the file.

**Tests (6/6)**: Backslash+quote breakout, statement-injection payload, newlines, and structural comparison against the weak-escape form — all real security properties, not tautologies.

### 2. SRV-1: Config Fanout — **PASS**

**`companion/src/server.ts` lines 3273–3291**
- Old `for (const client of clients)` + `READYSTATE.OPEN` only → replaced with `broadcastToClients(...)` which uses `wss.clients` + `wsAuth.get(client)?.authenticated === true`. The auth gate is correct and reused (same function already guards MCP status + computer previews per X3).

**`companion/src/server.ts` `redactConfigForBroadcast` (line 2738+)**
- Correctly masks `llm.api_key`, `vision.api_key`, and `mcp.servers.*.env` + `mcp.servers.*.headers` values.
- **Preserves env/header key names** — so the Settings UI can still list configured env var / header names. Verified in test.
- Non-secret fields (command, url, transport, args, enabled, trust_level) are preserved.
- Non-mutating (confirmed by immutability test).
- Line 3291 `mcpManager.applyConfig(updatedConfig.mcp)` receives the **unredacted** original — secrets needed for process spawn.

**Tests (8/8)**: Redaction masking, key preservation, immutability, edge cases (no servers, empty servers), and the integration auth-gate test confirms `config.updated` reaches only authenticated clients.

### 3. XC-Integration-1: Confirmation Forward — **PASS**

**`chrome-extension/src/background/security-confirmation-payload.ts`**
- Forwards `add_to_whitelist` (array), `nonce_response` (string), `add_to_thread_whitelist` (boolean true only), `stop_thread` (boolean true only).
- Coerces `approved` to `=== true` — rejects string `"true"` and numeric `1`.
- Omits fields when value doesn't match type gate.

**Companion consumer** (`server.ts` lines 1446–1502): Already reads `msg.add_to_thread_whitelist` (line 1482) and `msg.nonce_response` (line 1502).

**Tests (8/8)**: Field forwarding, type coercion, omission, and combined payload.

### 4. Scope Boundary — **PASS**

- No P0-B/C/D files modified. Only the 10 in-scope files (`5 modified + 5 new helpers/tests`).
- `memory/session.md` is the only noise — session notes, not code.

---

### Nits (non-blocking)

1. **`selectorJsLiteral` is a thin JSON.stringify wrapper** (`selector-js-literal.ts:10`). It adds a call-site hop for clarity/testability. Not a problem, but a comment noting "no custom escaping needed — JSON.stringify is complete" would help future maintainers fight the urge to re-invent escaping.

2. **`redactConfigForBroadcast` does a shallow copy of `mcp` when `servers` is missing** (line ~2778: `redacted.mcp = { ...config.mcp }`). The earlier `...config` spread already copied `mcp`, so this is redundant. Harmless, but a minor code smell — won't cause bugs.

3. **`stop_thread` no consumer in companion** (confirmed: zero grep hits). Design doc explicitly acknowledges: "stop_thread is forwarded but unused on companion today (UI already chat.aborts)." Forward-compatible pass — nothing to fix now, but the companion handler comment at line 1446 could note this gap for the next person.

4. **Integration test SRV-1 case sends pre-redacted data** (`computer-broadcast-auth.test.ts:119` — `"***"` values hardcoded). It correctly tests the **auth gate**, not the redaction logic. Acceptable since `config-broadcast-redact.test.ts` covers redaction exhaustively. A comment linking the two tests would be nice.

---

**VERDICT: APPROVE_WITH_NITS**
