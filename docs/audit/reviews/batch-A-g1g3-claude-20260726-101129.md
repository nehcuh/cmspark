I've completed my inspection. Let me synthesize findings.

---

## Review summary — Batch A (G1–G3)

**Verified claims (✓):**

- **G1 posted/verified** (`companion/src/computer/executor.ts:1449-1459, 1583-1585`): `posted=true` for any inject event; `verified=false` for type/key, `verified=true` for click only when `crossverified===true`. Aggregates `posted_steps` / `verified_steps` exposed to LLM via `server.ts:2467-2472`. Honest `note` field warns when verified < posted.
- **TYPE_NO_EFFECT** (`companion/src/computer/executor.ts:1262-1296`): 1 re-focus retry via `forceForeground`, storm-guard at `priorFails >= 2`, code added to `types.ts:177`. Per-task `typeNoEffectCounts` Map scoped to a single `runComputerTask` invocation.
- **G2 explicit opt-in** (`companion/src/computer/session-trust.ts:181-211, server.ts:636-644`): `hostComputerTrustSkip` requires `optIn && keyOk && trusted && corpusOk && budgetOk && actionsOk && !experimentalFlag`. `respondFrom` (`security-confirmation.ts:336-338`) only honors `addToSessionTrust` when approved AND `relevantApps.length > 0` — WS injection on a non-`host_computer` dialog is dropped. Sticky OR semantics across re-grants verified by test.
- **G3 trust key** (`session-trust.ts:65-77`, `server.ts:618-624, 1036-1043, 1396-1407`): `resolveComputerTrustKey` prefers `thread:<id>`; `trustKeyAllowsInitialSkip` enforces `thread:` prefix. `__thread_id` injected at `adapter.ts:645`. WS-reconnect across same thread preserves trust; no thread → no initial skip.
- **Q4 exact-string corpus** (`session-trust.ts:333-352`): `corpus.add(t)` / `corpus.has(t)` — no NFKC. ✓
- Tests run clean: `computer-session-trust-g1` 5/5, `computer-executor` 102/102, `computer-session-trust*` 72/72, `computer-self-ui` 5/5. `tsc --noEmit` rc=0.

**Non-blocking nits:**

1. **Diff scope drift**: patch covers ~20 files; Batch A scope is ~6. Unrelated changes (Swift host rebuild + `cuWindowInfoDict`/`CUBox` race fix, `darwin-adapters.rethrowDarwinExecError` JSON recovery, `coords.maybeAutoscaleImageToClient` WeChat fix, `config.ts` bundle-id list, `tool-schemas.ts` `cmd/meta/command` whitelist) ship alongside. None break Batch A but the review surface is broader than the impl memo claims.
2. **Dead `experimentalFlag` check** (`server.ts:629-633`): `TOOL_ARG_SCHEMAS.host_computer` uses `.strict()` per action variant (tool-schemas.ts:120-150) — schema validation rejects unknown keys, so `(a as any).experimental === true` can never be true at the L2 gate. The actual experimental surface (TinyClick) is an internal locate-chain annotation (`locate-chain.ts:624`) gated mid-task via `PROMPT_ALWAYS_TAGS` / `computer.experimental_suggestion`. Net safety preserved, but the spec-locked Q3 gate "无 experimental 标志" is structurally inert on the initial-L2 path.
3. **Misleading comment** (`server.ts:617`): "strip inject-only param" — no strip occurs. `__thread_id` flows into `finalParams` (and thus into the security-token binding via `issueTokenFor`, which is actually a useful binding property). Comment should say "thread_id is included in token binding" instead.
4. **Missing test coverage**: `chrome-extension/tests/security-confirmation-forward.test.ts` was not extended for `add_to_session_trust` strict-boolean forwarding (existing tests cover thread_whitelist + stop_thread + nonce). No end-to-end test asserting the second-task-in-thread-with-opt-in path produces `hostComputerTrustSkip=true`. No NFKC-vs-exact corpus regression test.
5. **Tool description drift** (`tool-definitions.ts:549`): still says "input injection is NEVER thread-trusted — every task asks". Slightly inaccurate post-G2 (opt-in same-class skip exists). Defensible as conservative LLM-facing framing but worth tightening.
6. **`actions.length` cap asymmetry**: schema caps actions at 50 (`tool-schemas.ts:152`), so `maxActionsSeen` max is 50. Approving one 50-action task grants skip eligibility to any future ≤50-action task in the thread. Acceptable per Q3=B lock but a meaningful blast radius; consider documenting.

No blocking issues found. Core safety invariants (per-task L2, explicit opt-in for initial-skip, thread-key requirement, exact-string corpus, budget/actions monotonic gates, self-UI recovery preserving PROMPT_ALWAYS for non-self foreign FG, origin-bound WS confirmations) all hold.

VERDICT: APPROVE_WITH_NITS
