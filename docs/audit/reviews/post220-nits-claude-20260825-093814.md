All verification complete. Here is my independent review.

---

# Dual re-review (Claude) — post-#220 residual nits (`1d16b0e..9deff00`)

## Machine `[executed]`

- Frozen patch `post220-nits-diff-20260825-092457.patch` SHA256 = `26252380…74b51` — **matches declared**, byte-identical to `git diff 1d16b0e..9deff00 -- companion`. The 093814 file is the same diff with a context header — not stale.
- `npx tsc --noEmit -p tsconfig.json` → **clean**.
- Mandated 8-file battery (run-queues, tool-batch-heal, adapter-steer-overflow, message-router-nextrun-drain, tool-persistence-redact, history, summoner-overlay, overlay-session) → **167/167 pass**.

## Independent fold verification `[inspected]` — live source, not lane hearsay

| Fold | Live evidence |
|------|---------------|
| S-A1 nextRun `{text, clientMessageId?}` | `run-queues.ts:16-21,47-56,58-64`; `convertLeftoverSteerToNextRun` keeps first present id (`:75`); occupied enqueue `message-router.ts:441-450`; drain echo `:337-338` via `followUpCreateFromQueue` spread (`:277`) |
| S-A2 scoped heal skip | `tool-batch-heal.ts:182-190` — `blockUntil` walk + `now.slice(asstNow+1, blockUntil)`, not global `now.some` |
| S-A3 no steer wipe | `adapter.ts:43` imports only `convertLeftoverSteerToNextRun, takeSteer`; grep: no `dropSteer`/`enqueueNextRun` in adapter (only mid-run `takeSteer` at `:903`); helper `run-queues.ts:71-80` never calls `dropSteer` |
| S-B1 pause/trash before take | `message-router.ts:310-329` (not-found/trashed/paused) all precede `takeNextRun` at `:335`; no `await` between |
| S-B2 lease/conductor pre-check | `message-router.ts:331-334` before take |
| S-B3 upload ack invariant | `message-router.ts:1177-1181` — push any truthy drain frame, always `file.uploaded`; no `return drainedAfterUpload` anywhere; abort pickup also pushes (`:1274`); regen gate-error pushes (`:1524-1525`) |
| S-C1 | repo-wide grep: `setSummonerThreadId` — zero production refs (test + audit docs only) |
| S-C2 bind live-gate | `menu-bar-agent.ts:914` conjunctive `overlaySessionIsLive(token)`; token snapshotted at submit start (`:866`), not re-read after await |
| S-C2 reclaim | `menu-bar-agent.ts:700-706` `claimOverlayIfLive` with bound token; contract `overlay-session.ts:122-143` holds (pre-await gate, post-await keep, CAS-only unwind on stale, `releaseAll` on close) |
| S-D1/D2/D3 | regex + `redactSensitiveLeaf` + `redactSensitiveKeysDeep` byte-identical across `tool-persistence-redact.ts:35,41-77` and `history/store.ts:49,57-93`; cookie `rest` deep-redacted (`:96-98`); cookie params branch (`:205-214`) |

## ADR-020 checklist

Declaration present and accurate: Surface L0, no L2-classes, no Compose, Autonomy = queue plumbing only, Trust = strictly tighter redaction (monotone in the safe direction — no loosening anywhere), Channel = composer-lease/session-generation occupancy — **not** Allow/Deny. No new tools/gates/UI, no confirm families, no `originWs`/`securityConfirmations.request` changes, no new runtime, no P1 watchlist surface touched. Overlay-as-confirm refuted. T2 confirmed; no T3 trigger.

## Non-blocking nits (confirmed, consistent with lanes)

1. **S-A3 test pin gap** — the unit test enqueues the concurrent steer after the helper returns, so a `dropSteer`-on-full mutation inside `convertLeftoverSteerToNextRun` stays green; production is clean (grep pin + helper code).
2. **S-A1 first-vs-last** — single-id unit pin; production `.find` (first present) matches mid-run steer rule (`adapter.ts:910`).
3. **Trash half of S-B1 unpinned in-tree** — code holds (`message-router.ts:314-321`); no test dies if only the `trashed_at` pre-check is deleted.
4. **N-B2** — overlay-reject upload test doesn't uniquely pin "never `return drainedAfterUpload`" vs the old gate-push branch; source fold holds.
5. **N-B4** — `MULTI_AGENT_LLM_CAP` still gated after `takeNextRun` (await-import race window); rare, pre-existing.
6. **N-C** — menu-bar S-C pins are grep-based; `bindSummonerThread` token default `currentOverlaySession()` footgun remains (no caller omits token); reclaim omits `onStaleClaim` (repair-miss, not steal).
7. **N-D** — history `redactCookieParams` extra keys passthrough; history generic tools skip the key scanner; object-bag secrets (`{Authorization:{value:…}}`) leak on both stores; "Mirrors history/store.ts" / "verbatim" comments over-sell.
8. Regen still `return drainedAfterRegen` for a hypothetical non-gate truthy successor (latent only; current successor contract is `null`).

No lane BLOCK is live; no over-claiming found — every claimed fold is real at the cited file:line. Out-of-slice items (blanket `value`, M3, N1 idle flash, N9) not counted per rules.

VERDICT: APPROVE_WITH_NITS
