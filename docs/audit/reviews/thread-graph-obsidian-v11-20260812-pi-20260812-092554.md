All checks complete. Summary of my independent verification:

**Blocker fixes confirmed in code (not just synthesis):**
- **C1** — `layoutSignature` (sorted ids + degs + normalized edges incl. `score.toFixed(3)`) gates reseed (`ThreadGraphApp.tsx:212-232, 245-275`). Same-signature focus changes early-return after refreshing radii only; x/y/pin preserved; real topology changes preserve surviving-node positions via `prev` map.
- **C2** — fit runs on `settled || exhausted` (ticks ≥ 320), guarded by `fittedRef` (`:319-325`).
- **C3** — `onPointerLeave` clears hover only; capture on pointerdown + `endDrag` on up/cancel with try/catch release (`:434-477, 560-588`).
- **C4** — screen-constant `pad = HIT_PAD / scale` (`:400-417`).
- **C5** — sticky pin only on real drag: `n.pinned = drag.moved` (`:443-447`), matching spec §3.4.
- **localStorage only on explicit toggle** — `persistPanelPref` called only from toolbar/✕; node-click/`openThread` use transient `setPanelOpen(true)` (`:70, 116-122, 296-302, 454`). Draw rAF effect deps `[]` with refs → no remount on focus.

**ADR-020 checklist:** declaration present and consistent; Surface L0 UI-only, no L2 classes/Compose/Autonomy/Trust/Channel changes; no new tools/gates/confirmations/`request(` (no originWs surface), no new runtime, no P1-watchlist touch. Pack-first N/A (existing-feature polish, no new scenario). Pass.

**Machine evidence:** `npm test` → **628 pass / 0 fail** (re-ran myself); `tsc --noEmit` strict clean; no unstaged drift in in-scope files.

**Non-blocking nits** (the `scoped.diff` artifact is the stale round-1 snapshot — do not treat it as the shipped code):

1. The unstaged `companion/src/host-use/darwin/host-integrity.ts` SHA-pin change is inside the review patch file — commit only the staged set, never `git commit -a`, so the pin swap doesn't ride along.
2. `hoverId` state is set but never read in JSX (only mirrored to `hoverIdRef`) → redundant re-render per hover change (`ThreadGraphApp.tsx:96`).
3. Deep-link/`focus_id` load sets focus but doesn't transiently open the panel, so focus detail can be hidden behind a closed panel on load.
4. Resize doesn't re-fit; graph can sit off-center after window resize.
5. Spec §3.1 (left rail, light theme) stale vs §6 dark floating chrome — doc debt.
6. Empty state still lacks the §3.1「为未标注提取要点」CTA.
7. Canvas a11y (no role/fallback, keyboard nav); toolbar wrap can overlap float panel (`top: 64` fixed).
8. No canvas interaction integration tests.

None of these are merge-blockers for a T2 L0 polish ship.

VERDICT: APPROVE_WITH_NITS
