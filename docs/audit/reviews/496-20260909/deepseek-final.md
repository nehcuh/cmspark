No P0 findings.

P1 — The suppression gate at `companion/src/server.ts:571` operates on `finalParams` *after* `contextReadParams` has run (`server.ts:467`). `contextReadParams` strips raw JSON `__site_context_tab_id` and restores it only from validated `invokeOpts.siteContextTabId`. There is no intervening code path between line 467 and line 571 that re-introduces a model-supplied marker: `__outbound_*` stripping, security-token removal, tabId normalization, and shell/netsec normalization do not touch this key, and `evidenceScope` only adds `__thread_id`. Therefore a spoofed JSON `__site_context_tab_id` cannot reach the line 571 check unless the server invoked the executor with a validated option—which is precisely the internal metadata case the patch intends to suppress. The test `"ordinary list_tabs stays visible, even with spoofed internal params or id"` (site-context-tool-events.test.ts) exercises the real executor with spoofed JSON params and confirms a `tool.start` is emitted; this is the actual execution evidence, not simulated stub behavior. The suggestion to branch on `invokeOpts?.siteContextTabId` instead would be a re-implementation that ignores the stripping behavior and the tool-name binding, and would need a second recomputation of validity. The claim of a narrow exploitable window is unsupported by the source. This concern is **retracted**.

P2 — `handleToolResult` accepts a result for the internal `site-context-*` id only if `originWs` matches the peer in `pendingToolCalls` for that id. During internal metadata discovery, `resolveBrowserSiteTarget` goes through `createToolExecutor(ws)` with the chat-connection `ws`; the forward path then dispatches via `forwardL1OrUnavailable`/`forwardToolToExtension`, which binds `originWs` to the *actuator* peer returned by `pickAuthenticatedClientWs`. A malicious local peer cannot match that `originWs` unless it already occupies the authenticated actuator slot. No changed callsite was introduced here; the internal read still goes through the existing forward machinery. The claim that a mismatched peer result is accepted is not supported by the `handleToolResult` code, and no changed line creates it. This concern is **retracted**.

P2 — The UI replay is a browser test, not Windows physical verification, and the user explicitly reserved real Windows verification for themselves. The compiled-server substitution result in the browser machine check shows the base server still fails and the patched server passes across three repeats, which is concrete evidence for the replay-level observable outcome. The scope mismatch is accurately disclosed in the test and does not constitute a defect. This is an observation, not a finding; no retained concern.

The existing `stopped:false` semantics are untouched (`useWebSocket.ts:519-545` applies `shouldClearBusyOnChatAborted`; the patch changes only the `tool.start` emission site, not the aborted-ack logic).

VERDICT: APPROVE