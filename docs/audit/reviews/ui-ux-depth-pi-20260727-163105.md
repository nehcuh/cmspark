Now I have all the evidence needed. Let me write the review.

---

# External Dual Review — UI/UX Design Conclusions

## Strengths

1. **C8 correctly rejected.** Hiding Skills/Packs/Knowledge/MCP/Apps behind only `/`+settings directly reverses locked D5 and §4. Code confirms `contextBarTabsForLevel` exposes them as permanent tabs. The per-claim verdict JSON's REJECT on C8 is sound.

2. **C1 and C6 confirmed with strong evidence.** Dual color systems (`#2563eb` vs `#4A90D9`) coexist across 13 source files (~70+ Material hexes; I verified: `ChatView.tsx`, `App.tsx`, `BottomBar.tsx`, `ComputerTaskBar.tsx`, `MinimalConfirm.tsx`, `McpPanel.tsx`, `AppsPanel.tsx`, `SettingsSlideout.tsx`, `KnowledgeSubPanel.tsx`, `SkillCraftPanel.tsx`, `ThreadList.tsx`, `McpServerForm.tsx`, `NotebooklmImporterPanel.tsx`). The `DESIGN.md` Components Quick Reference still documents `#4A90D9`/`#F44336`/`#4CAF50`/`#FF9800` for Message Bubbles, Buttons, Input, Tool Call Cards, and Connection Status — in direct conflict with `tokens.ts` (`#2563eb`/`#dc2626`/`#16a34a`). The ontology is correct: `CapabilityLevel` (`chat|browser|computer`) × `SurfaceLayout` (`panel|cockpit`) is implemented in types and `mode-controller.ts`.

3. **Phase order in final conclusions is locked and correct.** `final_conclusions[8]` restores the approved sequence: P0 mode awareness + ContextBar cut; P1 Cockpit + confirm split; P2 tokens/polish. This corrects C10's proposed visual-unify-first inversion.

4. **C9 confirmed.** `tokens.ts` defines quiet-professional soft neutrals + single blue accent `#2563eb`. Zero purple palette in `sidepanel/`. Dark tokens scoped to L2 SafetyStrip/Cockpit as per D6. The residual `#4A90D9` hardcodes are token-inconsistency debt (C10 scope), not a competing brand direction.

5. **Competitor amendments (C2-C5) are appropriate.** None of Gemini, Kimi, Claude, or Sider/Monica get ranked as sole template over locked D5/§4. Each is reduced to a lesson or pattern source subordinate to CMspark's own product law. This prevents the competitor claims from being misused to justify C7/C8 errors.

6. **Missing claims section is honest.** FleetStrip always-mounting (idle「舰队」consumes permanent chrome), 320px vertical budget math, multi-confirm storm handling, a11y, i18n/density, and board tab position are all flagged as unaddressed.

## Blocking Issues

**None.** The synthesis correctly rejects C8 (the only claim that would break locked product law), amends C2-C5/C7/C10-C12 to fit approved D5/D10'/§4/§6, and preserves the locked three-mode ontology. The P0 actions in `p0_actions` are actionable and aligned with locked decisions.

No ontology error, no security UX regression, no wrong file:line claim.

## Nits

1. **C2 "not brand purple" framing is weak analysis, not blocking.** The Gemini claim reduces to "density lesson, not purple" — this is poetry, not a design rule. The amendment is correct in outcome but a stronger operational rule would simply state: *"Soft neutral surfaces, conversation as primary scroll region, page/host context as chip/strip when relevant, secondary power via `/`/settings. Do not copy Google-branded chrome."*

2. **FleetStrip absent from §4 vertical order is underemphasized in final conclusions.** `FleetStrip.tsx` always mounts (idle「舰队」button even when zero workers — line 46-53). `App.tsx:160` renders it unconditionally. The `missing_claims` section notes it, but `final_conclusions[2]` doesn't mention it, and `p0_actions[5]` only gates it. The vertical budget math (Header + optional ContextStrip + SafetyStrip + always-FleetStrip + ChatStream + ContextBar + Composer) deserves explicit acknowledgment in conclusions — it's contributing to the IA landfill that C1 correctly diagnoses.

3. **Header L1 tint `#f5f9ff` vs DESIGN `#eef4ff` vs tokens `#dbeafe`.** `App.tsx:667` hardcodes `#f5f9ff` for browser header tint. DESIGN.md specifies `#eef4ff`. `tokens.ts` has `modeBrowserBg: "#dbeafe"` and `accentSoft: "#dbeafe"`. Three different blues for one header tint. `p0_actions[4]` correctly schedules this for later, but it's worth flagging as a concrete example of the token dual-system problem.

4. **Board tab in all three `contextBarTabsForLevel` (L0/L1/L2) but absent from §4 ContextBar tables.** `mode-controller.ts:130-136` includes `"board"` in all three level arrays. §4 only lists Skills·Know·Hist (L0), Tabs·Skills (L1), Minimal/hidden (L2). `p0_actions[0]` addresses removing packs/board but doesn't explicitly resolve board's permanent-vs-`/` question.

5. **L2 ContextBar in code shows `["tabs", "apps", "mcp", "board"]` — 4 entries — while §4 says "Minimal/hidden" and D5 caps at 2–3.** The `p0_actions[0]` change to `L2: []` or single optional entry would fix this, but the current code discrepancy should be called out as stronger evidence for the P0 ContextBar cut.

## Missing Angles

The `missing_claims` JSON is thorough, but two additional angles deserve mention:

1. **MinimalConfirm risk colors still use Material hexes (`#FFC107`/`#FF9800`/`#F44336`) — but this is inside a dark SafetyStrip context.** The `MinimalConfirm.tsx:9-11` `riskColor()` function uses `#FFC107`/`#FF9800`/`#F44336` — these are the **old Material hexes** from DESIGN.md, not `tokens.ts` values (`#d97706`/`#dc2626`). Given this is **inside the dark SafetyStrip**, using the lighter Material yellows/oranges on a `linear-gradient(#3b1f1f, #2a1515)` background actually creates a contrast problem. This should be on the P2 token-unify radar.

2. **`tokens.ts` defines `darkWarning`/`darkWarningBg` implicitly but no explicit token exists.** The dark surface tokens include `darkDanger`, `darkDangerBg`, `darkLive`, but no `darkWarning` or `darkWarningSoft`. The SafetyStrip and MinimalConfirm components end up hardcoding `#fcd34d`/`#fde68a` for warning states. This token gap should be filled in P2.

## Recommended Final Stance

The synthesis is **safe to drive P0**. C8 is correctly rejected. All other claims are either confirmed or amended to fit locked three-mode product law. The P0 actions are concrete, verifiable, and aligned with the approved §4 IA, D5, D10', and D13. Token/visual unification is correctly deferred to P2.

VERDICT: APPROVE
