All checks complete. Here's my re-review summary.

## Re-review: slice 6 IMPLEMENTATION (HEAD bbfd687 vs origin/main)

### Spot-checks (all verified against repo, not just the patch)

1. **IDF scope** ✓ — `matchSkills` uses `idfFromDocs` + `tfidfVec` (`skill-engine.ts:574-579`); `tokensToVec` remains in `obsidian/vault-index.ts:52,72` and `threads/related.ts:77`. Docs (CLAUDE.md A5, GOAL.md G17) state the split honestly.
2. **Slash pin door** ✓ — `pinSlashSkill` matches leading `^\/(\S+)` case-insensitive from `rest.message` (`skill-engine.ts:126-143`); wired before `resolveSkillIdsForThread` in chat.create (574), chat.upload (1149), chat.regenerate (1531). `skill.activate` handler writes only `active_skill_ids`, never `skill_selection_mode`. Manual resolve returns active-only, no matchSkills union (`skill-engine.ts:621-626`).
3. **H1 persist hydration (fold bbfd687)** ✓ — `thread-manager.ts` both `get()` (757-765) and `update()` (865-871) seed `run_progress` from `runtime_context_budget.handoff.open_todos` when empty; test locks hydrate + no-clobber.
4. **applyToolResult** ✓ — exact `item.tool === tool`, seed|user only, never model_draft, no text substring, success-only (run-progress.ts:117-130).
5. **Toggle ACL** ✓ — `thread.run_progress.toggle` absent from `SUMMONER_ALLOW` (`summoner-acl.ts:14-42`) and `SUMMONER_WEB_DISPATCH_ALLOW`; lifecycle.ts:1060 gate denies summoner; tray allowed. Not in `thread.update` allowlist.
6. **UI copy/mount** ✓ — `本轮步骤` / `草稿`, no `进行中` anywhere in ChatView/RunProgress; EmptyState untouched (mount is a sibling above it); no BoardPanel import.

### Tests — I ran them myself
- `run-progress.test.js` 20/20, `slash-skill-pin.test.js` 2/2, `skills.test.js` 109/109, `files.test.js` 74/74, `summoner-acl.test.js` 12/12, `run-progress-ui.test.ts` 4/4 — all pass on HEAD bbfd687. tsc clean both packages.

### ADR-020
Declaration present in plan r2 (Surface/L2-classes/Compose/Autonomy/Trust/Channel). No 中层 Agent framing, no new runtime, no new confirm family, no originWs-relevant `request()` added. Trust monotonicity holds: overlay cannot toggle and cannot flip mode. No T1 bake-off claim (GOAL.md claims only G17 matching; plan explicitly defers T1). Product REJECT fold confirmed folded (H1 persist hydration present + tested).

### Nits (non-blocking)
1. **Evidence tick is inert in v1.** Seeds carry no `tool` field and no id-binding is wired at the adapter hook, so `applyToolResult` can never match a seeded item — the adapter tick path (`adapter.ts:1305-1323`) is currently dead code (correct machinery, becomes live with v2 tool-carrying ingest). DESIGN.md's "H1 seed + tool_result / click" slightly overstates the working surface; only click/userToggle actually ticks today.
2. **No permanent clear.** `update()` re-seeds `run_progress` whenever the list is empty, including after an explicit `run_progress: null` clear, if handoff still holds todos; `get()` also performs a disk write (`saveIndex`) on a read path when seeding. Matches fold intent but leaves no escape hatch.

VERDICT: APPROVE_WITH_NITS
