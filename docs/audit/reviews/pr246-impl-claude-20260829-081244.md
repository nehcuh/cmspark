Review complete. All findings verified against current code on the branch, with both full test suites executed.

---

# Re-review: PR #246 (#245 A+B) — independent inspection

## REJECT sweep — all clear `[executed/inspected]`

| Condition | Finding |
|---|---|
| `SUMMONER_ALLOW` rollback | **Not rolled back.** `mcp.toggle_server` still present (summoner-acl.ts:43); freeze tests preserved (summoner-acl.test.ts:67, workbench-compose.test.ts:38-51 assert ALLOW *contains* it). Only additions were `meeting.*` methods — explicitly in the spec's overlay-MAY list (§94). |
| hide → `abortThreadChat(lease.thread_id)` | **Not present.** Hide → `invokeOnShellClosed` (summoner-web.ts:446-450) → tray `handleSummonerClosed` → `summonerClient.releaseAllOverlayComposerLeases()` → router `composer.lease.release_overlay` aborts via `abortLlmLoopsForPanel(session.panelId)` **before** lease release (message-router.ts:1393-1395). Owner stamp at chat.create:572 keys loops to the summoner WS connection. Behavioral test (composer-lease.test.ts:585) proves panel-owned Operate on the lease thread survives while the overlay-owned loop dies. |
| Raw href concat / `unsafe-inline` | **Fixed.** `overlay-md.ts:34` escapes `"`→`&quot;`/`'`→`&#39;` in URL before wrap; `https?://` enforced (regex + re-test), so `javascript:` can't pass. CSP is `script-src 'nonce-<random>'` with no script `unsafe-inline` (summoner-web.ts:616-625); exactly one `<script>` tag, nonce-stamped per request; template interpolations are all trusted constants. |
| Truncated save overwrites tail | **Can't.** UI renders truncated body read-only and omits body from the update (knowledge-save.ts); server has two independent gates (handlers/knowledge.ts:52-58 on `current.truncated`; skill-engine.ts on on-disk > wire cap — explicitly not a length compare, larger replacements also rejected). Tests assert disk bytes unchanged. |
| Anonymous `/tmp` estop | **Gone.** Socket/flag/nonce under DATA_DIR (`CMSPARK_DATA_DIR` ∥ Swift `cmsparkDataDir()` — lockstep); 0600 + uid checks; nonce greeting with `timingSafeEqual`. CONNECT alone no longer arms; pre-bound `/tmp` and pre-bound DATA_DIR-without-nonce both fail-closed (tests B2-1/B2-2/wrong-nonce). Swift Aqua launcher and CLI derive flag/nonce from the socket path — no `/tmp` literal remains. `sun_path` 104 checked. |
| Overlay Allow/Deny | None — `openConfirm` remains notice+Chrome only; test asserts zero Allow/Deny chrome. |
| #228 profile expanded | Not touched (renter changes are copy honesty only). |

## DoD verification `[executed]`

- **A3-1/1b**: `surface` on `ChatCreateParams` comes only from lifecycle-stamped `stampedSurface` (client field ignored — tested; validate.ts unchanged — tested). Full-catalog deny (`isSummonerNativeExecutorDenied` = catalog ∪ `mcp__|host_|workspace_|acp_|mcp_` prefixes) at both offer and exec; hallucinated `list_tabs` with `whitelist=null` hard-denied; `list_tabs` auto-retry disabled on summoner.
- **A3-2**: `/api/mcp/toggle` → 404; `SUMMONER_WEB_DISPATCH_ALLOW` dropped toggle (kept `mcp.list`); tray `dispatchSummonerWeb` rejects toggle/add pre-WS; Swift `mcpRowClicked`/`mcpAddClicked` are no-ops, rail stays hidden.
- **A3-3/4**: `pack.apply`/`skill.activate`/`skill.deactivate`/`knowledge.set_active` gated to the overlay lease thread; `skill.activate` writes no `skill_selection_mode`.
- **A3-5**: copy is 「要操作网页，请打开侧栏」— no overlay-operates-page promise.
- **A2-3**: SSE-only close (incl. past launch grace) walks the same closed path; STT/meeting aborted on close.
- **Tests**: full companion suite **3574 pass / 0 fail** (10 platform skips), chrome-extension **835 pass / 0 fail**, `tsc -p tsconfig.test.json` clean.

## Nits (all in the pre-accepted class)

1. **Swift binaries not re-hashed after A3**: `dfda5381` edited `SummonerOverlay.swift` without updating `SWIFT_TRAY_SHA256` (last set in `597a5827`); `cmspark-host` estop changes also need rebuild. Launcher self-rebuilds on hash mismatch, and the source-level no-ops mean the stale binary only matters if the old binary still dispatched toggle — which the dead tray/HTTP paths already block.
2. Hidden HTML MCP rail still POSTs `/api/mcp/toggle` → 404 (summoner-web.ts:2414).
3. Extra deny: `use_skill` is catalog-listed so it's denied on summoner — overlay binds skills via `skill.activate` WS, so harmless fail-closed.
4. Minor: a few A2/A3 assertions are source-grep style; the load-bearing abort semantics are behavioral.
5. Observation: branch carries two pre-A/B feature commits (meeting workbench, card default-expand) beyond pin 13's three-commit shape — in-spec surface, fully tested, but wider than the batch-A commit plan.

VERDICT: APPROVE_WITH_NITS
