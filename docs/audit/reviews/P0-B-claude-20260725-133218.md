I've completed the review. Here's my assessment:

## Review summary

**Verified against the 8 acceptance criteria — all met:**

1. **chat.abort flips computerTaskAbort** — `server.ts:3477-3479` calls `flipAllComputerTaskAborts()` before falling through to handleMessage. Real integration test (`computer-task-abort.test.ts:133-153`) asserts the silent flip + no ack. ✅
2. **chat.token/done/error/aborted gated by thread_id** — `useWebSocket.ts:170, 176, 203, 239` all call `shouldApplyStreamEvent`. Pure helper unit-tested in `stream-thread-gate.test.ts`. ✅
3. **streamingRef cleared on thread switch** — `useWebSocket.ts:141-143` adds `useEffect` on `state.activeThreadId`. ✅
4. **shouldStop multi-tool rollback** — `adapter.ts:903-913` calls `deleteMessagesFrom(savedAssistantId)`. Verified via `m10-shouldstop` test asserting disk state (1 user message only, no orphans). ✅
5. **inter-tool abort rolls back** — `adapter.ts:567-571` throws `AbortError` (was quiet break), caught by outer try/catch at `adapter.ts:919-923` which calls `deleteMessagesFrom`. ✅
6. **rebuildMessagesFromHistory skips orphan tool rows** — `adapter.ts:151-208` tracks `openToolCallIds`. Three adapter tests cover orphan-after-strip, lone orphan, and fully-paired. ✅
7. **No chat.done after terminal chat.error** — `adapter.ts:907-912` returns without `sendToExtension({type:"chat.done"})`. Wire assertion in m10-shouldstop test confirms 0 chat.done events. ✅
8. **UI chat.error clears streamingRef** — `useWebSocket.ts:240-241` sets `streamingRef.current = ""` and dispatches `SET_STREAMING` clear before ADD_MESSAGE. ✅

**Tests run green:** all 24 companion tests + 5 stream-thread-gate tests pass.

## Non-blocking nits

- **`server.ts:226-250` docstring misalignment** — The original WP2 (§E.6) docstring for `handleComputerTaskAbort` is now orphaned above `flipAllComputerTaskAborts`. `handleComputerTaskAbort` lost its attached docstring. Cosmetic — reattach the original docstring to `handleComputerTaskAbort`.
- **WS dispatch seam for `chat.abort → flipAllComputerTaskAborts` untested at integration level** — Tests call `flipAllComputerTaskAborts()` directly, not via the actual `msg.type === "chat.abort"` WS path in `server.ts:3477-3479`. The silent-flip-before-fallthrough wiring relies on visual inspection only.
- **`streamingRef` clear on thread switch untested at integration level** — `useWebSocket.ts:141-143` useEffect has no direct integration test; only the pure helper is unit-tested.
- **`chat.aborted`/`chat.error` use `activeThreadRef.current` directly for ADD_MESSAGE thread_id** — `chat.done` uses `doneThreadId = msg.thread_id || activeThreadRef.current`. Functionally equivalent under the gate (gate ensures msg.thread_id matches active or is empty), but stylistically inconsistent.
- **`tool.start`/`tool.result`/`tool.vision_*` still ungated by thread_id** — Explicitly deferred per plan; `tool.start` would inject a tool placeholder into the wrong thread's UI if a tool is running on a non-active thread. Track as follow-up.

No blocking defects. All in-scope fixes are correct, tests assert real disk + wire behavior, no security regressions identified.

VERDICT: APPROVE_WITH_NITS
