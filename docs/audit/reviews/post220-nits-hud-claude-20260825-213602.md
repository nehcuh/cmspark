All security gates verified:

**BLAST T2**: Overlay WS cannot mcp.add / knowledge.import / config.set or elevate trust. `pack.apply` strips `allowTrust`, `workspace_path`, `force_takeover`, `confirmation_phrase`. HTML `mcp.toggle` rides tray `companionClient`. `evaluate` result_summary fully redacted even under 200 chars. COM0/LPT0 reserved. DSA PEM covered. Junction/symlink skip in `skill-engine` + `skill-install`. Knowledge import ACL denies on summoner. Skills use `on:!on` (toggle), knowledge uses `ids:next` (append/remove). `CHAT_MARKED_OPTIONS` single-sourced.

**Machine**: PASS (companion `tsc --noEmit`; 819/819 extension tests; all history/distill/doc-identity/summoner tests green)

**Adversary synthesis**: All four lanes APPROVE_WITH_NITS. Non-blocking nits documented (pack.apply naming, fallback path, timing edge case, history error column, pin full behavior).

VERDICT: APPROVE_WITH_NITS
