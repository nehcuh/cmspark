All spot-checks complete. Here is my independent review.

---

# Dual re-review — Batch F path (#253) · independent

**Baseline verified:** HEAD `4d41f92f` = spec baseline. All six spot-check targets read in full.

## F1 — `bindingPayloadFor` default throw

**Confirmed against source.** `default: return ""` at security-policy.ts:147-148. All 13 `L2_GATE_TOOLS` (l2-admission.ts:63-81) plus `host_app`/`host_cli`/`host_computer` have cases; every production `issueTokenFor`/`validateTokenFor` call site is case-keyed, and every test literal (19 usages audited) hits a cased name — **the default is unreachable today, and flipping it to throw breaks zero existing tests**. The folded framing is technically correct: HMAC binds `toolName` (security-policy.ts:217 `toolOk`), so cross-tool empty-ticket replay fails; the real exposure of `default: ""` is **same-tool param-swap** — a future gate name without a case yields a param-agnostic ticket. Rewriting the product sentence as a lockstep footgun is justified, per Rule 2.

The spec's sequencing demand is load-bearing and correct: today `issueTokenFor` fires at l2-admission.ts:1661 *inside* the gate block but *after* the confirm dialog — an unknown name would get a user confirmation and then a raw throw propagating out of the IIFE. Pre-confirmation binding + `{success:false,error}` containment fixes both. `evaluate` empty-code keeps its case (line 48-49), untouched — matches spec.

## F2 — tab.navigated Origin gate

**Vulnerability confirmed.** lifecycle.ts:1329-1332 applies `applyTabNavigated` with no origin check; the handler's own comment (1325-1327) names tabUrlCache "the evaluate auto-approve trust anchor". Any authenticated tray/summoner peer (Origin `cmspark-tray://local`) can poison it today.

**The path's formulation is right.** Batch E handshake-surface.ts:26-30 derives `surface: "panel"` *only* from the `chrome-extension://…` Origin class — so "Origin matches chrome-extension" ≡ "panel surface", and panel is explicitly preserved as the sole legitimate sender. The adversary lane proposing "ignore panel" was correctly overruled by the fold. `wsAuth.get(ws)?.origin` is in scope (lifecycle.ts:296), `applyTabNavigated` signature untouched, `isAllowedWsOrigin` (203-215) untouched, tray → silent `return` is correct for fire-and-forget. Residual (accepted by Batch E, not new): a ws_secret holder can spoof the Origin header — Origin is labeling, the HMAC pairing is the gate.

## F3 — SW user_gesture honesty

**Confirmed.** Three unconditional `user_gesture: true` stamps at background/index.ts:945/959/969 relay `knowledge.update/export/delete`. Companion enforces strict `=== true` (validate.ts:1117-1145). Every existing runtime sender already carries the flag honestly (KnowledgeSubPanel.tsx:96/141/633/758; knowledge-save.ts:32) — so `message.user_gesture === true` forwarding keeps all golden paths green while the SW stops attesting gestures it cannot observe. `"true"`/`1`/missing → false matches the validator's strictness exactly.

## F4 — dual /0 gate

**Confirmed.** modules.ts:121-122 accepts `bits=0`; scope.ts:27 allows it and :32 builds `mask=0`, so `isTargetAllowed` matches **any** IPv4 under `0.0.0.0/0` (verified arithmetic: `ipInt & 0 === 0 === network`). `*` already rejected (modules.ts:116). Dual gate is correctly placed — config.json is hand-editable, so runtime `parseCidrV4` must reject independently of settings validation. `bits < 8` keeps `10.0.0.0/8` per DoD; after rejection, a `/0` rule falls through to exact-host compare and never matches → deny, the safe direction. All three DoD values check out against the actual functions.

## F5 — known-keys strip on copy

**Confirmed.** dispatch.ts:165-168 reads `actingTid` from `params.__thread_id`; :328 passes the **same object verbatim** to `manager.callTool` — internal thread IDs leak to external MCP servers today. Copy-then-delete of `__thread_id`/`_thread_id`/`/^__cmspark_/` preserves any MCP-declared `__meta` (no `/^__/` blanket strip); outbound executor path (`__outbound_mcp` re-apply, server.ts:511-519) untouched.

## Rails (Rule 1 REJECT triggers) — all clear

| Trigger | Path does it? |
|---|---|
| Ignore handshake `panel` / drop real tab.navigated | No — gates *on* panel's Origin class |
| Change SUMMONER_ALLOW (ws/summoner-acl.ts:14) | No — NEVER list, no file overlap |
| Add overlay HTTP to WS Origin | No — `isAllowedWsOrigin` explicitly frozen |
| Strip `/^__/` breaking MCP `__meta` | No — known keys only |
| Split message-router.ts | No — six-file map, no router |
| Expand #228 | No — §1.2 refuses new `L2_GATE_TOOLS` entries |

## Nits (non-blocking)

1. **F1:** spec doesn't pin the containment shape — implementer should cover *both* the pre-dialog check and the post-approve `issueTokenFor` at :1661 (belt-and-suspenders); either alone satisfies the DoD as written.
2. **F5:** the L2 confirm preview (dispatch.ts:272 `safeJsonStringify(params)`) still embeds `__thread_id` — cosmetic (user's own panel), but the stripped copy could serve the preview too.
3. **F2:** a debug-level log on dropped tray-origin `tab.navigated` would aid future stale-cache diagnosis; silent return remains spec-correct.
4. **F4:** settings-page mirror deferred as nit — agreed; runtime gate denies regardless.

The path is implementable as written, every fix maps to a verified code-level defect, DoD entries are testable against real functions, and no NEVER/KEEP rail is crossed.

VERDICT: APPROVE_WITH_NITS
