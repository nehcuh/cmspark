# P1 Security Batch Fix Report — P1-3

- both_external_approve: true
- build_ok: true
- rounds: 1
- design: {"acceptance":["Approved evaluate executes byte-identical code bound by security_token / shown at L2","No post-approval sanitizeText rewrite of evaluate source","Unapproved path never bare-runs evaluate","detectDangerousApis remains advisory only (risk_pattern_matches)","evaluate success payload omits threats_removed for approved runs","get_page_text/HTML sanitizer behavior unchanged","extension unit tests green; no companion token-binding change required"],"batch":"P1-3","capability_declaration":"Surface: L1 | L2-classes: (none) | Compose: none | Autonomy: single | Trust: L2 (evaluate confirm + security_token bind; post-approval code integrity) | Channel: community","files":["chrome-extension/src/background/browser-bridge.ts","chrome-extension/src/background/evaluate-code-policy.ts","chrome-extension/tests/evaluate-code-integrity.test.ts","docs/audit/p1-security-open-items-2026-07-29.md"],"plan":"Option A only: in chrome-extension browser-bridge.evaluate(), if security_token present execute String(params.code) as-is with advisory detectDangerousApis only (no sanitizeText mutation); if token missing/empty return success:false and never bare-run. Companion already issues token bound to original code via issueTokenFor/bindingPayloadFor — no companion bind change. Leave get_page_text mutative sanitize and page-sanitizer patterns alone. Optional pure resolveEvaluateExecution helper for unit tests. Bug: browser-bridge.ts:1075-1087 sanitizeText after companion server.ts:1716-1720 token bind + security-policy.ts:49 original code.","risks":"Paths that forwarded evaluate without token will hard-fail (companion currently always re-issues after L2/auto-approve). Dropping evaluate threats_removed may surprise LLM consumers of that field. Extension trusts token presence not HMAC (companion is sole issuer; WS auth assumed). Accidental FILTERED rewrite was never a real control — real gate remains L2+token.","tests":["token present + code containing jailbreak/ignore-previous-instructions: execution body equals original (not [FILTERED:...])","security_token missing: evaluate refuses; safeEvaluate/CDP not invoked","security_token empty string: same refuse as missing","token present + clean code: identity preserve; detectDangerousApis still advisory on original","page-sanitizer + get_page_text mutative path regression: still rewrites page text only","optional companion integration: tool.execute params.code equals approved fixture with injection phrase"]}
- last_impl: {"batch":"P1-3","diff_stat":"4 files changed, 22 insertions(+), 11 deletions(-) (+2 untracked: evaluate-code-policy.ts, evaluate-code-integrity.test.ts)","done":true,"files_changed":["chrome-extension/src/background/evaluate-code-policy.ts","chrome-extension/src/background/browser-bridge.ts","chrome-extension/tests/evaluate-code-integrity.test.ts","chrome-extension/tests/node-shims.d.ts","chrome-extension/tsconfig.test.json","docs/audit/p1-security-open-items-2026-07-29.md"],"notes":"Extension trusts token presence not HMAC (companion sole issuer). Paths that forwarded evaluate without token hard-fail by design; companion always re-issues after L2/auto-approve.","summary":"Option A: evaluate with security_token executes original code byte-identical (no post-approval sanitizeText); missing/empty token refuses without bare-run; detectDangerousApis advisory only; threats_removed omitted on success; companion bind unchanged; inventory marked FIXED.","tests_added":["chrome-extension/tests/evaluate-code-integrity.test.ts"],"tests_run":"npm --prefix chrome-extension test → 292 pass / 0 fail"}
- last_adv: {"blocking":[],"pass":true,"summary":"P1-3 Option A holds: evaluate with non-empty security_token executes original params.code byte-identical (no post-approval sanitizeText); missing/empty/whitespace token refuses without bare-run; detectDangerousApis advisory only; companion L2 still binds original code via issueTokenFor and forwards unchanged finalParams; page-sanitizer mutative path left on get_page_text only. No blocking bypass found on the integrity contract. | P1-3 Option A holds: token path executes original code without sanitizeText; missing/empty token refuses without bare-run; detectDangerousApis advisory only; page-sanitizer path unchanged. Tests assert rewrite-triggering fixtures stay byte-identical under token and refuse without token — no false-green mocks. Companion bind left on original code. No blocking findings. | "}
- last_ext: {"both_approve":true,"claude_path":"/Users/huchen/Projects/cmspark/docs/audit/reviews/P1-3-claude-20260730-132859.md","claude_verdict":"APPROVE","exit_code":0,"pi_path":"/Users/huchen/Projects/cmspark/docs/audit/reviews/P1-3-pi-20260730-132859.md","pi_verdict":"APPROVE","raw_tail":"===== CLAUDE TAIL =====\nAll checks complete. Verifying my findings:\n\n**Diff scope (verified):** 4 modified files + 2 new files. Patch file `P1-3-diff-20260730-132859.patch` matches working tree (`git diff --stat` identical).\n\n**Bug existence (verified):** `git show fd2d4a1:chrome-extension/src/background/browser-bridge.ts` lines 1070-1087 shows the original `sanitizeText(params.code)` AFTER companion token bind — silent rewrite confirmed.\n\n**Fix correctness (verified):**\n- `evaluate-code-policy.ts:35-65` — pure helper. Token present (non-empty after `.trim()`) → identity preserve `String(params.code ?? \"\")`; missing/null/empty/whitespace → refuse with `allowed: false`. No CDP, no side effects. Testable.\n- `browser-bridge.ts:1078-1107` — single dispatch entry (`case \"evaluate\"` at line 102). When `decision.allowed === false`, returns `{ success: false, error }` before `safeEvaluate`. Never bare-runs.\n- `detectDangerousApis` import correctly removed from `browser-bridge.ts` (only referenced in comment at line 1083).\n- `threats_removed` dropped from evaluate success payload; `risk_pattern_matches` preserved (empty → `undefined`).\n- `get_page_text` (line 614) and `get_page_html` (line 652) sanitizer paths unchanged — P1-3 scope respected.\n\n**Companion contract (verified unchanged):**\n- `security-policy.ts:46-49` — `bindingPayloadFor(\"evaluate\", params)` returns `String(params?.code || \"\")` → token HMAC binds original code.\n- `server.ts:469-483` — strips any LLM-provided `security_token` before L2 gate (no LLM token forgery).\n- `server.ts:1716-1720` — issues fresh token via `issueTokenFor` after L2 confirm OR auto-approve.\n- `server.ts:1721-1735` — replay branch validates HMAC binds `finalParams.code`.\n- Every legitimate evaluate reaching extension carries a valid token bound to the original code.\n\n**Tests (verified real, not false-green):**\n- 9/9 pass in new file; 292/0 full suite.\n- Line 86-103 (`would-be FILTERED phrase preserved`) constructs a true-negative guard: it first proves `sanitizer.sanitizeText(raw)` *does* rewrite the fixture (`threatsRemoved.length > 0` AND `notStrictEqual`), then asserts the policy keeps the original. This proves the test fixture is actually rewrite-triggering.\n- Missing/empty/whitespace/null token branches each covered.\n- Page-sanitizer regression test confirms P1-3 didn't disable mutative sanitize for page content.\n\n**ADR-020 checklist:**\n- Capability declaration present in implementer prompt (Surface: L1, Compose: none, Autonomy: single, Trust: L2, Channel: community). ✓\n- Axes fit: Trust-axis integrity fix on L1 Surface. No new runtime/pack/confirm family. ✓\n- Trust monotonicity: TIGHTENED — previously missing-token path could still bare-run; now refuses. god-mode/auto-approve still go through companion L2 gate (line 1716-1720). ✓\n- No new originWs regression (P1-2 untouched). ✓\n\n**Nits (non-blocking):**\n- `threats_removed` removed from evaluate success payload. No companion schema references it (grep found zero matches in `companion/src`), and no tool definition promised it — but LLM consumers that previously observed the field will stop seeing it. Implementer's design summary explicitly accepts this; acceptable.\n- Untracked scratch files (`.grok/workflows/p1-security-batch-fix.rhai`, `scratch/_decode_git_obj.mjs`) are in the working tree but outside this diff — ensure they aren't accidentally swept into the commit.\n\nFix fully addresses inventory §P1-3. Tests are real (true-negative guard included). No new holes. Trust monotonicity holds. Companion token binding unchanged and correct.\n\nVERDICT: APPROVE\n===== PI TAIL =====\n| 5 | **originWs** — no new `securityConfirmations.request` calls | ✓ |\n| 6 | **No new runtime** — pure helper function `resolveEvaluateExecution`; no second agent framework | ✓ |\n| 7 | **Experimental layers** — N/A | ✓ |\n\n## Implementation Verification\n\n**`evaluate-code-policy.ts`** — `resolveEvaluateExecution`:\n- `undefined`/`null` token → `allowed: false` ✓\n- Empty `\"\"` / whitespace-only `\"   \"` token → `allowed: false` ✓\n- Non-empty token → `allowed: true` with `String(code)` byte-identical, `detectDangerousApis` advisory only ✓\n- No `sanitizeText` call anywhere in the approved path ✓\n\n**`browser-bridge.ts` evaluate()** (lines 1075–1107):\n- Calls `resolveEvaluateExecution(params)` as first decision point ✓\n- Early-return `{ success: false, error: decision.error }` on refusal ✓\n- `codeToExecute = decision.code` — unmutated original ✓\n- `risk_pattern_matches` from advisory `detectDangerousApis` ✓\n- `threats_removed` field **omitted** from success payload ✓\n- `get_page_text` (line 614) still uses `this.sanitizer.sanitizeText(rawText)` — page-sanitizer path unchanged ✓\n- `get_page_html` (line ~652) still has `threats_removed` — unchanged ✓\n- `detectDangerousApis` import cleanly moved to `evaluate-code-policy.ts`; no stale import ✓\n\n**Companion side** (unchanged, verified):\n- `bindingPayloadFor(\"evaluate\")` = `String(params?.code || \"\")` — binds original code ✓\n- `issueTokenFor` uses `bindingPayloadFor` — issuance/validation cannot diverge ✓\n- Replay/stale token path validates via `validateToken(token, \"evaluate\", evalCode)` ✓\n\n**Tests** — `evaluate-code-integrity.test.ts`:\n- 8 policy tests + 2 page-sanitizer regression tests\n- Proves sanitizeText *would* rewrite the injection fixture, but policy preserves original ✓\n- Tests missing, empty, whitespace, null token refusal ✓\n- Tests advisory `risk_pattern_matches` on original code for `fetch` ✓\n- Page-sanitizer regression: still rewrites page text, still strips scripts ✓\n- Test run: **292 pass / 0 fail** ✓\n\n**Inventory** — `p1-security-open-items-2026-07-29.md`: P1-3 marked `FIXED (2026-07-30, Option A)` with correct contract description ✓\n\nNo blocking issues. No nits.\n\nVERDICT: APPROVE","verdict_json":"/Users/huchen/Projects/cmspark/docs/audit/reviews/P1-3-verdict-20260730-132859.json"}
- build: {"commands":["git diff --stat; git status","npm --prefix chrome-extension test"],"log":"git: branch fix/diagnosis-P1-3; modified chrome-extension browser-bridge + test config/shims + docs audit; untracked evaluate-code-policy.ts + evaluate-code-integrity.test.ts + review artifacts. Companion not in diff. Extension tests: tsc -p tsconfig.test.json OK; 292 pass / 0 fail / ~9.4s. Companion tests skipped (no companion changes).","ok":true}
