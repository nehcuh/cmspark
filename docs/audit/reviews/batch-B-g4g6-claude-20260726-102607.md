## Review summary — Batch B (G4 / G5 / G6)

Inspected real code at `companion/src/host-use/darwin/notes-verify.ts`, `companion/src/server.ts` (host_read :1976-1998, host_write :2157-2197), `companion/src/llm/adapter.ts` (:295-314, :638-643), `companion/tests/notes-verify.test.ts`, and the impl doc. Patch file matches live `git status` (not stale).

### Claims verified

**G4 — Notes create verify** (`evaluateNotesCreateVerify` at `notes-verify.ts:25-53`, wired at `server.ts:2162-2180`)
- Re-reads via `adapter.listReadTargets("note", { limit: 100 })` — confirmed the method exists on `DarwinHostAdapter` (`host-use/darwin/adapter.ts:180`) and returns encoded TargetIds comparable to `result.target_id` (both produced through `validateTargetId` + `encodeRawTargetId`).
- Fail-closed paths covered: missing target_id, empty body, empty list.
- Suffix matcher (`note-<id>$`) handles account-segment drift. Tests cover all four branches.

**G5 — Mail read verify** (`evaluateMailReadVerify` at `notes-verify.ts:56-69`, wired at `server.ts:1981-1994`)
- All four structured fields (sender/subject/date_received/body_preview) must be non-empty strings.
- `summary` line only emitted when `verified===true` — prevents the LLM from parroting a fabricated sender/subject on a failed read.

**G6 — Rule 12** (`adapter.ts:308-314`)
- "Prefer SEMANTIC tools over coordinate host_computer" — explicit.
- "Aggregate ALL same-app actions in ONE host_computer call" — matches LOCKED Q14=B.
- "posted=true,verified=false — NEVER say 已发送/已完成 unless verified===true or verified_steps covers the write" — matches Q7=B and the posted≠verified discipline.

### Verification
- `npx tsx --test tests/notes-verify.test.ts` → 6/6 pass.
- `npx tsx --test tests/computer-session-trust-g1.test.ts` → 8/8 pass (adjacent G1/G2/Q3 plumbing used by the same call sites).
- `npx tsc --noEmit` → green.

### Non-blocking nits

1. **`server.ts:2184-2186` — over-claim for non-create write paths.** The `else` branch sets `verified = true` for `move`/`update`/`delete` without an independent read-back. Comment acknowledges this ("no read-back yet") and impl doc lists it as residual. Acceptable for Finder move (no clean read-back), but the Rule 12 wording "Only claim 'note created' when verified===true" is Notes-specific so it doesn't leak into Move claims. Still, a `verify_note` explaining the assumption would be cleaner.

2. **`server.ts:1503-1508 / 1593-1598 / 1641-1646` — dead `thread_id` fallback.** The `__thread_id` is always injected by `adapter.ts:642`, so the `else if typeof thread_id === "string"` branch is unreachable from the chat path. Defense-in-depth: either drop it or assert it matches `__thread_id` when both are present (the LLM could pass a forged `thread_id` in `params`; today it cannot unlock ungranted trust because `hasExplicitOptIn` still gates, but tightening removes the surface).

3. **`executor.ts:1445-1448` — `posted = true` is unconditional per step.** Read-only actions (`wait`/`screenshot`/`describe`) don't post inject events but still report `posted=true`. Minor semantic drift from "posted = inject event was posted". Doesn't affect LLM behavior (`verified_steps` gates success claims), but the step-level contract is slightly fuzzy.

4. **`server.ts:1499-1509 / 1593-1599 / 1641-1646` — trust-key resolution duplicated 3×.** Could be a single helper. Not blocking.

VERDICT: APPROVE_WITH_NITS
