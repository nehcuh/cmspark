All checks complete. Here's my verification summary:

## B1 — error banner clobbered by ENGINE_END: **FIXED**

`session-reducer.ts:175-180`: `ENGINE_END` when `phase==="error"` → `resetToIdle` preserving `banner` + `errorCode` (no clobber with 「未识别到内容」). A second defensive branch (non-`no-speech`/`empty`/`timeout` errorCode while stopping) also re-maps and preserves the §6.6 message.

Ran `npx tsx --test tests/voice-session-reducer.test.ts`: 9/9 pass, including both claimed regression tests:
- `not-allowed → error banner survives ENGINE_END (real Web Speech order)` — banner stays 「无法使用麦克风…」
- `network error → end keeps network banner` — banner stays 「语音识别需要网络…」

## B2 — bootstrap on non-granted, begin() only on granted: **FIXED**

`useVoiceInput.ts:146-162` (verified against working tree):
- `permissions.query` → `granted` → `begin()`
- `prompt` or `denied` → `onNeedPermissionBootstrap()` (opens `voice-permission.html` via `chrome.tabs.create`)
- `denied` additionally dispatches `ENGINE_ERROR not-allowed` (§6.6 copy)

Matches SoT §7.1 exactly.

## Claimed nits (verified)
- `USER_TOGGLE_STOP` while `stopping` forces idle — reducer + passing test ✓
- Send disabled while `voice.listening` — `App.tsx:409` `&& !voice.listening` ✓
- `draftTextAfterEnd` — grep confirms removed ✓

## No new ship-blockers
- `voice-permission.html` is emitted by the plasmo build (`.plasmo/tabs/voice-permission.html`) — bootstrap URL resolves ✓
- Full suite: 466 tests pass, 0 fail; `tsc -p tsconfig.test.json` clean ✓
- Prefs live in `chrome.storage.local` only (no Companion `config.json` writes); `tabs` permission present in manifest ✓

## Nits (non-blocking)
- `session-reducer.ts` ENGINE_END second branch (`state.errorCode` while `phase==="stopping"`) is effectively dead — `ENGINE_ERROR` transitions all hard codes straight to `phase:"error"`, which the first branch already handles. Harmless defense.
- `mapSpeechError("timeout")` says 「文字已保留在输入框」 but the `ENGINE_ERROR` branch clears `finals`. Unreachable in practice (the hook uses the `TIMEOUT` event, which preserves finals, and browsers don't emit a `timeout` error code) — cosmetic only.

VERDICT: APPROVE_WITH_NITS
