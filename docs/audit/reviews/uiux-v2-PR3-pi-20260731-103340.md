## PR3 Review — FocusBand State Machine (§4.3)

### Files changed (7 modified + 3 new)

| File | Change |
|------|--------|
| `App.tsx` | Replaced 3 individual strip renders (`SafetyStrip`, `ContextStrip`, `FleetStrip`) with single `<FocusBand>` |
| `FocusBand.tsx` **(new)** | Priority slot machine: Confirm > L2+急停 > Fleet > L1 Context; ≤80px |
| `focus-band-priority.ts` **(new)** | Pure resolver + `fleetStripShouldShow` (pending-free) |
| `focus-band-priority.test.ts` **(new)** | 12 tests covering all priority combos + fleet rules |
| `ContextStrip.tsx` | Added `compact` + `secondary` variants for FocusBand integration |
| `SafetyStrip.tsx` | Added `compact` variant (56px max) + internal confirm fallback |
| `MinimalConfirm.tsx` | Added `compact` variant (40–56px, single-row actions + 停止 + 确认台) |
| `FleetStrip.tsx` | Added `focusBand` mode; removed `pending>0` from visibility via `fleetStripShouldShow` |
| `DESIGN.md` | Updated FocusBand/FleetStrip descriptions |
| `tsconfig.test.json` | Added `focus-band-priority.ts` to test paths |

---

### Spec compliance — §4.3 hard rules

| Rule | Status |
|------|--------|
| **P0 Confirm wins primary** | ✅ `resolveFocusBandSlot`: `hasPendingConfirm` → `primary: "confirm"` |
| **P1 L2+急停** — 急停 never buried | ✅ When confirm primary + L2 running: `secondaryAbort: true` → `<AbortSecondaryLine>` renders 急停 button in 24px secondary row |
| **P2 Fleet** — no pending gate | ✅ `fleetStripShouldShow` signature has no `pending` param; `FleetStrip` no longer checks `pending > 0` |
| **P3 L1 Context yields to all above** | ✅ Lowest non-empty priority; yields to confirm (gets `secondaryContext` instead when browser + confirm) |
| **Max ≤80px** | ✅ `FOCUS_BAND_MAX_PX = 80`; primary ≤56px + secondary ≤24px |
| **No dual full-height stack** | ✅ Single slot renders exactly one primary row |

### ADR-020 fidelity

- **Surface axis** preserved: L0/L1/L2 labels unchanged; FocusBand is chrome only
- **Composition axis** not touched: no new composition concepts
- **Autonomy axis**: Fleet routes to Cockpit in focusBand mode (correct — §4.3 "expand → Autonomy panel / Cockpit")
- **No "中层 Agent"** — not in scope

### Cross-cutting checks

| Check | Result |
|-------|--------|
| Material hexes (`#4CAF50`, `#F44336`, etc.) in diff | **None found** |
| BottomBar deleted | **No** — still present, PR5 scope preserved |
| Offline `alert()` changes | **None** — PR1 scope preserved |
| DESIGN.md updated | ✅ Consistent |
| Tests pass? | `focus-band-priority.test.ts` covers all priority levels + fleet visibility (12 tests) |

### Nit (non-blocking)

**`SafetyStrip.tsx:140`** — Compact mode contains `{hasConfirm && <MinimalConfirm compact />}`. This branch is unreachable in the FocusBand path because the priority machine always resolves `primary="confirm"` before reaching `l2_safety` when `hasPendingConfirm` is true. The fallback is harmless but adds dead code. Consider removing or annotating as `/* unreachable in FocusBand — defensive fallback for standalone SafetyStrip usage */`.

---

VERDICT: APPROVE_WITH_NITS
