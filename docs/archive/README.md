# docs/archive

Historical / process documents only — **not** product norms.

Canonical specs live under [`docs/adr/`](../adr/), live guides under [`docs/README.md`](../README.md), and working locks under [`docs/decisions/`](../decisions/) / [`docs/superpowers/`](../superpowers/).

Layout: `archive/YYYY-MM/{proposals,roadmaps,rfcs,audits}/`. Always use `git mv` (no first-pass `git rm`).
