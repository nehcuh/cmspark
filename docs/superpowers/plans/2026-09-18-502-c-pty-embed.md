# 502-C ACP 挂现有 PTY（#432 P1）Implementation Plan

> GitHub: #502 · Spec §4 · ADR-025 · #432 spec P1 · Claude `out-claude.md`
> Blast: **T2** · darwin · 默认仍关 `embedded_terminal.enabled`

**Goal:** 编程接力可把 agent 的交互 stdio 挂到 **已有** xterm.js + node-pty 全页 tab，侧栏只放入口。ACP JSON-RPC 时间线/permission/diff **并存**。不嵌 Alacritty，不侧栏跑 TUI。

**Architecture:**
- `spawnPtySession` 增加可选 `file` + `args`（默认仍 `$SHELL -l`）。
- Mode C 新选项 `embed: true`：不 `osascript` 外跳；在 L2 `terminal.open` 通过后 `spawnPtySession` 用 agent argv + 既有 task 文件。
- UI：CodingAgentPanel「在本插件打开终端」→ 打开 `tabs/embedded-terminal.html` 并发 `terminal.open`（`user_gesture:true`）。ACP `acp.ui_start` **不**自动 embed。
- `terminal_busy`：诚实失败，不杀用户已开的裸 shell。
- 非 darwin：`unsupported` 文案不变。

**NEVER:** Alacritty 嵌 MV3；侧栏 TUI；自动 spawn；新监听口；free shell；用 Computer Use 操作 TUI；cruise 跳过 terminal.open L2；Win/Linux 首发承诺。

---

### Task 1: PTY argv（TDD）

**Files:**
- Modify: `companion/src/pty/session.ts` `spawnPtySession`
- Modify: `companion/src/pty/handler.ts` 若需转发 argv
- Create/extend: `companion/tests/pty-session.test.ts`（已有则改）

`spawnPtySession({ file, args })`：测 mock spawn 收到的 file/args。默认路径仍 `["-l"]`。argv 必须是绝对 file + string[]，拒绝空 file。

- [ ] Commit `feat(pty): optional argv for agent embed (#502 C)`

---

### Task 2: Mode C embed 分支

**Files:**
- Modify: `companion/src/acp/open-local-terminal.ts`
- Modify: `companion/src/acp/manager.ts` — **不要**在 session start 自动 embed
- Tests: mock spawnPtySession vs osascript

`openLocalTerminalForAgent({ ..., embed: true })`：
- darwin + `embedded_terminal.enabled===true` 才 embed，否则返回诚实 `{ ok:false, detail:"embedded_terminal_disabled" }` 不外跳撒谎
- embed 成功不调用 Terminal.app
- embed 失败（terminal_busy / unsupported）返回 ok:false，**不静默改外跳**（避免用户以为开了插件终端其实跳到 Alacritty）

- [ ] Commit `feat(acp): Mode C embed into existing PTY (#502 C)`

---

### Task 3: 侧栏入口

**Files:**
- Modify: `chrome-extension/src/sidepanel/components/CodingAgentPanel.tsx`
- 打开全页 tab 的既有方式（图谱/embedded-terminal 先例）

按钮「在本插件打开终端」：
- 仅当 `embedded_terminal.enabled` 且 darwin 显示
- click → `chrome.tabs.create` runtime URL `tabs/embedded-terminal.html` + `terminal.open` user_gesture
- 侧栏仍只显示 ACP 时间线 + 该入口
- 源码锁：无 Alacritty 嵌入；无侧栏 xterm

- [ ] Commit `feat(sidepanel): open embedded PTY from coding panel (#502 C)`

---

### Task 4: 验收

- [ ] 默认关：无入口
- [ ] embed 不自动随 ACP start
- [ ] terminal_busy 诚实
- [ ] 非 darwin unsupported
- [ ] ACP permission / apply_diff 仍走 L2，不解析 ANSI
- [ ] 无新端口
- [ ] 写 `C-DONE.md`

P2 断线重连 / agent_write 不在本 PR。
