# 现状诊断 — 产品 0.5.3 / post #227

> **日期**: 2026-08-27  
> **HEAD（写本文时）**: `a56fa0a` Merge #227；随后 lockstep 到 **0.5.3**  
> **性质**: T0 状态快照（不改行为）  
> **形态 SoT**: [2026-08-26-product-form-deepening-design.md](./2026-08-26-product-form-deepening-design.md) §11  
> **知识 SoT**: [2026-08-26-knowledge-crud-honesty-design.md](./2026-08-26-knowledge-crud-honesty-design.md)  
> **前序快照**: [2026-08-26-post-226-status.md](./2026-08-26-post-226-status.md)（切片 1–3 + 知识 Wave 3）

仓库在 0.5.2（Windows NSIS）之上合入知识诚实与形态切片 1–3、5、6。**0.5.3 不是召唤器/租手「做完」里程碑。** T1：CMspark 臂已完成；Playwright 干净 profile 打不开门户（`ERR_EMPTY_RESPONSE`）。L7 PASS 带 nit（[#228](https://github.com/nehcuh/cmspark/issues/228)）。禁止扩 profile。

```text
Surface:      Capture=召唤器 ; Operate=侧栏或后台 CDP ; Confirm=确认台/托盘 ; 租手=Outbound MCP
Compose:      知识 Wave 3 已落地 ; 技能匹配 IDF（仅 skills） ; 租手钥匙 CLI + L8 fan-out 已落地
Trust:        overlay 永不 Allow/Deny ; grant cmg_ ≠ ws_secret ; T1 已跑（PW 未到登录墙）
Channel:      community ; outbound 非 default-on ; require_grant 默认 true
Blast:        本文 T0 ；T1 = T0 过程（扩 profile = T4） ; 召唤器 P2 = T2 ; 残留分项见 #230
```

---

## 0. 产品句（未变）

**家 = 已登录 Chrome + 硬闸。** 侧栏是人盯着 Chrome 时的 Operate 面。人在别的 Agent 里时，Operate 是后台 CDP，确认台才弹出。

四面：Capture 召唤器 → Operate 侧栏或后台 CDP → Confirm 确认台/托盘 → 租手 Outbound MCP。

---

## 1. 已合上的（相对 0.5.2）

| 批 | 用户能看见的 | 闸门 / PR |
|----|----------------|-----------|
| 知识 Wave 0–3 | 点开正文、确认保存、下载 `.md`、related≤3 芯片。图谱本季不做 | #222 / #223 / #226 |
| 切片 1–3 | `cmspark-agent outbound-grant` 签发 `cmg_`；首次外泄走确认台；overlay 无 Allow/Deny；「展开对话 / 打开浏览器 / 打开确认台」；MCP 轨藏而不删 | #226 |
| 切片 5 | 侧栏空态 CompanionMark + 22px 招呼 + 句子邀请 + 作曲区 | 已在 main（#226 前） |
| 切片 6 | 技能匹配走 TF-IDF（knowledge related / Obsidian 仍 TF）；聊天列 L0「本轮步骤」 | #227 |

---

## 2. 本季还没做（GitHub 追踪 · 顺序锁）

**需求设计必须先有 Issue。** 下表不再只活在 spec 文件里。

| 优先级 | Issue | 工作 | Blast | 用户能看见的完成 | 未完成时禁止假装 |
|--------|-------|------|-------|------------------|------------------|
| **P0** | **[#228](https://github.com/nehcuh/cmspark/issues/228)** | T1 真人 bake-off（切片 4） | T0 过程 | **已记分**：CMspark Y / PW N（打不开门户）。关票仍 **禁止扩 profile** | 把 nit 说成「撞上 SSO 登录页」；扩 cookies/evaluate/L2 |
| **P2** | **[#229](https://github.com/nehcuh/cmspark/issues/229)** | 召唤器只修快与淡 | T2 | 不完成 WorkBuddy 五轨。HUD 导入/MCP 添加继续藏 | 展开工作台；第六轨当产品 |
| **冻结/残留** | **[#230](https://github.com/nehcuh/cmspark/issues/230)** | F-S-10 · overlay-acl-rollback · grant-cli 未知 flag · RunProgress 勾选 | 分项 T1–T3 | 见票内清单 | 用 overlay 管 MCP 掩盖 F-S-10；宣称 tool_result 已自动勾 |

记分卡：[outbound-mcp-p0d-bakeoff-checklist.md](../plans/2026-08-04-outbound-mcp-p0d-bakeoff-checklist.md) — **T1–T3 仍待真人**。

**2026-08-26 预检 BLOCK（写入 #228，勿绕）**：live Companion 曾无 `outbound-grant`；盘上 `require_grant=false` 且 `auto_approve_dangerous=true`。**禁止**不经用户确认改 `~/.cmspark-agent/config.json`。需要操作员提供 SSO / 已登录 URL。

切片 6 v1 诚实缺口（#230）：H1 seed 行常无 `tool`，`applyToolResult` inert；勾选基本只能点。

---

## 3. 明确不做 / 冻结

- 知识图谱、双链、Project、默认 embedding
- 第二只 Chrome 扩展、侧栏 IDE、用户面把租手叫成 Handoff
- Overlay Allow/Deny；`knowledge.get` 进召唤器
- Win/Linux 原生托盘确认（必须打开 Chrome 确认台）
- GOAL G10 SSO 自动复用、G11 Type B、G14 历史重放：长期 deferred

正交旧债（已有票，不是本季主线）：[#71](https://github.com/nehcuh/cmspark/issues/71) CU 视觉 · [#70](https://github.com/nehcuh/cmspark/issues/70) Apps P2 · [#69](https://github.com/nehcuh/cmspark/issues/69) host-use Darwin Phase 2。

---

## 4. 下一场从哪开始

单人项目下一刀：**[#229](https://github.com/nehcuh/cmspark/issues/229) 召唤器只修快与淡**。T1 已记分（#228）。残留 [#230](https://github.com/nehcuh/cmspark/issues/230) 不挡，也不许「顺便」扩 overlay ACL。

T1 失败 → 收窄默认 profile，不删租手，也不为了赢 bake-off 把 cookies / evaluate / L2 / shell 放进默认 profile。
