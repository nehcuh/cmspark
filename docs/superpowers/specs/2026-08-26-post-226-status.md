# 现状诊断 — post #226

> **日期**: 2026-08-26  
> **HEAD**: `bf03318` `docs(status): post-#226 diagnosis` on `origin/main`  
> **性质**: T0 状态快照（不改行为）  
> **形态 SoT**: [2026-08-26-product-form-deepening-design.md](./2026-08-26-product-form-deepening-design.md) §11  
> **知识 SoT**: [2026-08-26-knowledge-crud-honesty-design.md](./2026-08-26-knowledge-crud-honesty-design.md)

仓库干净。开着的 PR 为零。版本仍是 **0.5.2**（Windows NSIS），不是召唤器/租手里程碑。

> **2026-08-27**：切片 6 已合 #227；产品切到 **0.5.3**。活状态改看 [2026-08-27-post-227-status.md](./2026-08-27-post-227-status.md)。余项 GitHub：[#228](https://github.com/nehcuh/cmspark/issues/228) T1 · [#229](https://github.com/nehcuh/cmspark/issues/229) 召唤器 P2 · [#230](https://github.com/nehcuh/cmspark/issues/230) 残留。

```text
Surface:      Capture=召唤器 ; Operate=侧栏或后台 CDP ; Confirm=确认台/托盘 ; 租手=Outbound MCP
Compose:      知识 Wave 3 已落地 ; 租手钥匙 CLI + L8 fan-out 已落地
Trust:        overlay 永不 Allow/Deny ; grant cmg_ ≠ ws_secret ; T1 真人未跑
Channel:      community ; outbound 非 default-on ; require_grant 默认 true
Blast:        本文件 T0 ；下一刀切片 4 = T0 过程 ；切片 5–6 = T2
```

---

## 0. 产品句（未变）

**家 = 已登录 Chrome + 硬闸。** 侧栏是人盯着 Chrome 时的 Operate 面。人在别的 Agent 里时，Operate 是后台 CDP，确认台才弹出。

四面：Capture 召唤器 → Operate 侧栏或后台 CDP → Confirm 确认台/托盘 → 租手 Outbound MCP。

---

## 1. 刚合上的（#226）

| 批 | 用户能看见的 | 闸门 |
|----|----------------|------|
| 知识 Wave 3 | 点开正文、确认保存、下载 `.md`、related≤3 芯片。图谱本季不做 | 设计+实现 dual 均 AWN |
| 切片 1–3 | `cmspark-agent outbound-grant` 签发 `cmg_`；首次外泄走确认台；overlay 无 Allow/Deny；「展开对话 / 打开浏览器 / 打开确认台」；MCP 轨藏而不删 | 四路对抗 + Claude/Pi 均 AWN；PR CI 与 main CI 全绿 |

实现 Dual：`product-form-slices-123-impl-verdict-20260826-185622`。知识 Dual：`knowledge-crud-honesty-impl-verdict-20260826-122915`。

合门后本地残枝/stash/未跟踪评审 patch 已清；`origin` 只剩 `main`。

---

## 2. 本季还没做（顺序锁）

切片 1–3 的**代码** DoD 已绿。匹配 IDF / RunProgress **不得**在 1–3 绿之前开工——现在可以排后面的刀。

| 优先级 | 工作 | Blast | 用户能看见的完成 | 未完成时禁止假装 |
|--------|------|-------|------------------|------------------|
| **P0 · 切片 4** | T1 真人 bake-off | T0 过程 | 已登录任务记分卡有结果。门的是工具面宽窄，不门「租手算不算形态」 | T1 没跑就扩 profile；对外声称护城河；T1 失败就删租手。**2026-08-26 预检 BLOCK**：live Companion 无 `outbound-grant`；盘上 `require_grant=false` 且 `auto_approve_dangerous=true` |
| **P1 · 切片 5** | 侧栏空态 / 作曲区看山 | T2 | **已在 main**（`EmptyState` = CompanionMark + 22px 招呼 + 句子邀请 + 作曲区；computer 空态「打开确认台」）。不重开 | 变成 Chrome Gemini；新视觉世界压过 stream |
| **P1 · 切片 6** | 匹配诚实（TS IDF）+ RunProgress | T2 | IDF port 进现有 TS；清单 = 聊天列 L0；勾选绑 `tool_result` 或人手势 | 宣称语义匹配已修好；Mission Board 当 todo；模型自勾 |
| **P2 召唤器** | 只修快与淡 | T2 | 不完成 WorkBuddy 五轨。HUD 导入/MCP 添加继续藏 | 展开工作台；第六轨当产品 |

记分卡：[outbound-mcp-p0d-bakeoff-checklist.md](../plans/2026-08-04-outbound-mcp-p0d-bakeoff-checklist.md) — **T1–T3 仍待真人**。

五分钟租手对**真人**仍未验收：Companion 在跑、扩展已配对、一把 `cmg_`、Codex/Grok 调 `cmspark__list_tabs`、非白名单站弹出确认台。代码路径在；人还没走过一遍。

#226 手测清单也还空着：重载 companion+扩展 → 打开知识 → 保存 → 下载；overlay 无 Allow/Deny；带 `--allow-page-export` 的钥匙第一次外泄等确认台。

---

## 3. 明确不做 / 冻结

- 知识图谱、双链、Project、默认 embedding
- 第二只 Chrome 扩展、侧栏 IDE、用户面把租手叫成 Handoff
- Overlay Allow/Deny；`knowledge.get` 进召唤器
- Win/Linux 原生托盘确认（必须打开 Chrome 确认台）
- `overlay-acl-rollback`（`mcp.toggle_server` / `skill.activate` 先冻结）
- **F-S-10 残留**（trusted/cruise 的 `mcp__*` 仍可能不弹确认）：本季不修完；禁止用 overlay 管 MCP 来掩盖
- GOAL G10 SSO 自动复用、G11 Type B、G14 历史重放：长期 deferred

---

## 4. 旧债（不是本季主线）

GitHub 仍开着三张 7 月的票，和当前押注正交：

- #71 坐标化 Computer Use / 本地视觉
- #70 Apps 页签 P2
- #69 host-use Darwin Phase 2

代码 nit（不挡下一切片）：`grant-cli` 对未知 flag 仍静默忽略。

---

## 5. 下一场从哪开始

单人项目的正确顺序：**先做切片 4（真人 T1）**，再开切片 5。

T1 失败 → 收窄默认 profile，不删租手，也不为了赢 bake-off 把 cookies / evaluate / L2 / shell 放进默认 profile。

切片 5 才是「侧栏打开像看山」那一刀。别回头做图谱，也别把召唤器做成工作台。
