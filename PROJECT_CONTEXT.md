# Project Context

## Session Handoff

<!-- handoff:start -->
### 2026-08-25 ~10:05 (S79 · #221 MERGED · session-end)
- **Workspace**: `main` `ac0a3be` == `origin/main`。无打开 PR。
- **Ship**：**#221 squash** post-#220 nits（nextRun cmid、drain 先闸再 take、heal skip 限定块、overlay 显式 token、passwd/object 袋 + history 调用点 redact）。
- **闸门**：#220 合后四路独立对抗 AWN → 折 nits → 再四路+Claude/Pi AWN → 折 dual 残留 → CI 绿 squash。
- **Next（可选）**：真机托盘「召唤器（实验）」；M3 pack.apply 测 / N1 idle / N9 length 仍开放。勿全局 redact `value`。
- **Do not**：`isAllowedWsOrigin` 加 loopback；overlay Allow/Deny；Electron；`git add .`；提交 `pr218-remainder-nits-diff-*.patch`。

### 2026-08-24 ~17:23 (S78 · #219 MERGED · session-end)
- **当时**：`main` `daf8bc9` = #219 overlay L0 + C-thin。**已过时** — tip 现为 `ac0a3be` #221。
- **仍有效**：HTML 不直连 WS；C-thin 开窗 hex token；Mac NSPanel 冻增长。
<!-- handoff:end -->
