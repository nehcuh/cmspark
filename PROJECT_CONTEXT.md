# Project Context

## Session Handoff

<!-- handoff:start -->
### 2026-08-24 ~17:23 (S78 · #219 MERGED · session-end)
- **Workspace**: `main` `daf8bc9` == `origin/main`。无打开 PR。
- **Ship**：**#219 squash** steer/nextRun UI + overlay L0 hub + C-thin HTML 召唤壳（loopback+token、SSE、Chromium `--app`）。Mac NSPanel 冻增长。
- **闸门**：T2 对抗→Pi AWN；SSE r1 REJECT 已折；用户「CI 绿再合」。
- **Next（可选）**：真机托盘「召唤器（实验）」；不要做原生 WKWebView 除非另开。
- **Do not**：`isAllowedWsOrigin` 加 loopback；overlay Allow/Deny；Electron；`git add .`；把 `pr218-remainder-nits-diff-*.patch` 提交。

### 2026-08-23 (S77 · overlay 独立分支 · session-end)
- **当时**：`feat/os-agent-shell` 勿合 main。**已过时** — overlay 经 #219 已在 main。
- **Main tip then**: `fc18725` #213 site-op-memory。
<!-- handoff:end -->
